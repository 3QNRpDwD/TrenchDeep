# Preserved comparison engine

Exact source snapshot of commit `65b4d40`. Only the Cargo package name is changed. This source is retained for numerical and performance comparisons; removal requires the user's explicit instruction. `BASELINE.json` records original source hashes. The main crate must load this package only through its optional `legacyBenchmark` feature.
