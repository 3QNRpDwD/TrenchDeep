name: Rust feature matrix

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: default
            args: "--release"
          - name: no-features
            args: "--release --no-default-features"
          - name: backward
            args: "--release --features enableBackward"
          - name: visualization
            args: "--release --features enableVisualization"
          - name: legacy
            args: "--release --features legacyBenchmark"
          - name: all-features
            args: "--release --all-features"
    name: test (${{ matrix.name }})
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Check
        run: cargo check --lib ${{ matrix.args }}
      - name: Test
        run: cargo test ${{ matrix.args }}
