// Generated by build.mjs; repository inputs are read only.
globalThis.PROJECT_DATA = {"schemaVersion":1,"generatedAt":"2026-09-13T07:41:02.004Z","fingerprint":"264271c4867a8820651d01687a430237a1a074d8d49b2c0e1f8f7915eb231236","inventory":[{"path":".github/workflows/ci.yml","bytes":907},{"path":".gitignore","bytes":898},{"path":"benches/context_diffusion.rs","bytes":15320,"preview":"//! Product route benchmark. Run via scripts/benchmark_context_diffusion.py.\n//! No test includes, copied models, or replay Trainer implementations.\n#[path = \"support/counting_allocator.rs\"]\nmod allocation;\n#[cfg(feature = \"benchmarkAlloc\")]\n#[global_allocator]\nstatic ALLOCATOR: allocation::CountingAllocator = allocation::CountingAllocator;\n\nuse serde::Serialize;\nuse std::{cell::RefCell, rc::Rc, time::Instant};\nuse trench_deep::{\n    nn::{Diffusion, DiffusionScheduler, Unet},\n    optimizer::{Adam, Optimizer},\n    trainer::*,\n    *,\n};\n\n#[derive(Serialize)]\nstruct Measurement {\n    phase: &'static str,\n    seconds: f64,\n    memory: allocation::Snapshot,\n    start_live_bytes: usize,\n    peak_extra_bytes: usize,\n}\nfn measure\u003cT>(phase: &'static str, f: impl FnOnce() -> MlResult\u003cT>) -> MlResult\u003c(T, Measurement)> {\n    let before = allocation::begin();\n    let start = Instant::now();\n    let value = f()?;\n    let seconds = start.elapsed().as_secs_f64();\n    let memory = allocation::delta(before);\n    Ok((\n        value,\n        Measurement {\n            phase,\n            seconds,\n            memory,\n            start_live_bytes: before.live_bytes,\n            peak_extra_bytes: memory.peak_live_bytes.saturating_sub(before.live_bytes),\n        },\n    ))\n}\nfn context(route: ExecutionRoute) -> MlResult\u003cExecutionContext> {\n    ExecutionContext::builder()\n        .initialization_seed(7)\n        .route(route)\n        .build()\n}\nfn model(ctx: &ExecutionContext) -> MlResult\u003cDiffusion> {\n    Diffusion::new(\n        ctx,\n        Unet::new(ctx, 1, 8, &[1, 2], 4, &[])?,\n        DiffusionScheduler::linear(10, 1e-4, 0.02)?,\n        11,\n    )\n}\nfn optimizer(ctx: &ExecutionContext, model: &Diffusion) -> MlResult\u003cAdam> {\n    let mut adam = Adam::new(ctx, 1e-3, 0.9, 0.999, 1e-8)?;\n    adam.register_all(&model.parameters())?;\n    Ok(adam)\n}\nfn noise_values(t: usize) -> Vec\u003cf32> {\n    (0..128)\n        .map(|i| ((i + 137 * t) as f32 * 0.13).cos())\n        .collect()"},{"path":"benches/p1_compare.rs","bytes":441,"preview":"#[path = \"support/legacy_diffusion.rs\"]\nmod diffusion;\n#[path = \"support/legacy_operation.rs\"]\nmod operation;\n#[path = \"support/legacy_trainer.rs\"]\nmod trainer;\nfn main() -> Result\u003c(), Box\u003cdyn std::error::Error>> {\n    let result = serde_json::json!({\"operation\":operation::run_case(100)?,\"unet\":diffusion::run_case(30)?,\"trainer\":trainer::run_case(30)?});\n    println!(\"{}\", serde_json::to_string_pretty(&result)?);\n    Ok(())\n}\n"},{"path":"benches/support/counting_allocator.rs","bytes":3746,"preview":"//! Requested allocation bytes, not RSS. Counters never allocate or log.\nuse serde::Serialize;\n#[cfg(feature = \"benchmarkAlloc\")]\nuse std::{\n    alloc::{GlobalAlloc, Layout, System},\n    sync::atomic::{AtomicUsize, Ordering::Relaxed},\n};\n\n#[derive(Clone, Copy, Default, Serialize)]\npub struct Snapshot {\n    pub allocations: usize,\n    pub reallocations: usize,\n    pub deallocations: usize,\n    pub requested_bytes: usize,\n    pub live_bytes: usize,\n    pub peak_live_bytes: usize,\n}\n#[cfg(feature = \"benchmarkAlloc\")]\nstatic ALLOC: AtomicUsize = AtomicUsize::new(0);\n#[cfg(feature = \"benchmarkAlloc\")]\nstatic REALLOC: AtomicUsize = AtomicUsize::new(0);\n#[cfg(feature = \"benchmarkAlloc\")]\nstatic DEALLOC: AtomicUsize = AtomicUsize::new(0);\n#[cfg(feature = \"benchmarkAlloc\")]\nstatic REQUESTED: AtomicUsize = AtomicUsize::new(0);\n#[cfg(feature = \"benchmarkAlloc\")]\nstatic LIVE: AtomicUsize = AtomicUsize::new(0);\n#[cfg(feature = \"benchmarkAlloc\")]\nstatic PEAK: AtomicUsize = AtomicUsize::new(0);\n\n#[cfg(feature = \"benchmarkAlloc\")]\npub struct CountingAllocator;\n#[cfg(feature = \"benchmarkAlloc\")]\nfn acquired(size: usize) {\n    REQUESTED.fetch_add(size, Relaxed);\n    let live = LIVE.fetch_add(size, Relaxed) + size;\n    PEAK.fetch_max(live, Relaxed);\n}\n#[cfg(feature = \"benchmarkAlloc\")]\nunsafe impl GlobalAlloc for CountingAllocator {\n    unsafe fn alloc(&self, layout: Layout) -> *mut u8 {\n        // SAFETY: forward the caller's layout unchanged to System.\n        let ptr = unsafe { System.alloc(layout) };\n        if !ptr.is_null() {\n            ALLOC.fetch_add(1, Relaxed);\n            acquired(layout.size());\n        }\n        ptr\n    }\n    unsafe fn alloc_zeroed(&self, layout: Layout) -> *mut u8 {\n        // SAFETY: same allocator/layout contract as alloc.\n        let ptr = unsafe { System.alloc_zeroed(layout) };\n        if !ptr.is_null() {\n            ALLOC.fetch_add(1, Relaxed);\n            acquired(layout.size());\n        }\n        ptr\n    }\n    unsafe fn dealloc(&self, ptr: *mut u8, layout: Layout) {\n        // SAFETY: ptr and layout are the original allocation supplied by caller.\n        unsafe { System.dealloc(ptr, layout) };\n        DEALLOC.fetch_add(1, Relaxed);\n        LIVE.fetch_sub(layout.size(), Relaxed);\n    }\n    unsafe fn realloc(&self, ptr: *mut u8, layout: Layout, size: usize) -> *mut u8 {"},{"path":"benches/support/legacy_diffusion.rs","bytes":6285,"preview":"#![cfg(all(\n    feature = \"legacyBenchmark\",\n    feature = \"builtinStorage\",\n    feature = \"builtinKernels\"\n))]\nuse old::nn::{Layer as OldLayer, Parameter as OldParameter};\nuse old::tensor::{AutogradFunction, TensorBase, operators::Function};\nuse trench_deep::legacy as old;\nuse trench_deep::{nn::Unet, trainer::TrainableModel, *};\n\nfn close(a: &[f32], b: &[f32], label: &str) {\n    assert_eq!(a.len(), b.len(), \"{label}\");\n    for (i, (&a, &b)) in a.iter().zip(b).enumerate() {\n        assert!(\n            a.is_finite()\n                && b.is_finite()\n                && (a - b).abs() \u003c= 1e-3f32.max(1e-3 * a.abs().max(b.abs())),\n            \"{label}[{i}]: {a} vs {b}\"\n        );\n    }\n}\n\npub fn run_case(repetitions: usize) -> Result\u003cserde_json::Value, Box\u003cdyn std::error::Error>> {\n    if repetitions == 0 {\n        return Err(\"benchmark requires at least one iteration\".into());\n    }\n    use old::optimizer::Optimizer as OldOptimizer;\n    use std::time::Instant;\n    use trench_deep::optimizer::Optimizer;\n    let initialization = Instant::now();\n    let ctx = ExecutionContext::new();\n    let model = Unet::new(&ctx, 1, 2, &[1, 2], 1, &[0, 1])?;\n    let public_model_initialization_ms = initialization.elapsed().as_secs_f64() * 1000.0;\n    let legacy_initialization = Instant::now();\n    let mut baseline =\n        old::comparison::unet::Unet::new(2, None, None, &[1, 2], 1, 1, &[true, true])?;\n    let legacy_model_initialization_ms = legacy_initialization.elapsed().as_secs_f64() * 1000.0;\n    let parameters = model.parameters();\n    let legacy_parameters = baseline.params();\n    assert_eq!(parameters.len(), legacy_parameters.len());\n    for (i, (p, q)) in parameters.iter().zip(&legacy_parameters).enumerate() {\n        let shape = p.tensor().shape()?;\n        assert_eq!(shape, q.tensor().shape(), \"parameter {i}\");\n        let values = (0..p.tensor().numel()?)\n            .map(|j| {\n                if shape.len() == 1 {\n                    0.1 + (j % 3) as f32 * 0.01\n                } else {\n                    ((i * 13 + j * 7) % 19) as f32 * 0.003 - 0.027\n                }\n            })\n            .collect::\u003cVec\u003c_>>();\n        ctx.replace_parameter(\n            p.variable(),\n            TensorBuffer::from_vec(values.clone(), &shape)?,\n        )?;\n        q.tensor()\n            .replace(old::tensor::GlobalTensor::from_vec(values, &shape)?);\n    }\n    drop(legacy_parameters);\n    let values = (0..16).map(|i| i as f32 * 0.03).collect::\u003cVec\u003c_>>();\n    let x = ctx.input(values.clone(), &[1, 1, 4, 4])?;\n    let t = ctx.tensor(vec![0.5], &[1, 1])?;\n    let ox = old::nn::Variable::new(old::tensor::Tensor::from_vec(values, &[1, 1, 4, 4])?);\n    let ot = old::nn::Variable::new(old::tensor::Tensor::from_vec(vec![0.5], &[1, 1])?);"},{"path":"benches/support/legacy_operation.rs","bytes":3100,"preview":"#![cfg(all(\n    feature = \"legacyBenchmark\",\n    feature = \"builtinStorage\",\n    feature = \"builtinKernels\"\n))]\nuse old::tensor::{TensorBase, operators::Function};\nuse trench_deep::{ExecutionContext, legacy as old};\n\npub fn run_case(iterations: usize) -> Result\u003cserde_json::Value, Box\u003cdyn std::error::Error>> {\n    use std::{hint::black_box, time::Instant};\n    if iterations == 0 {\n        return Err(\"at least one iteration is required\".into());\n    }\n    let initialization = Instant::now();\n    let ctx = ExecutionContext::new();\n    let values = (0..256).map(|i| (i % 17) as f32 * 0.01).collect::\u003cVec\u003c_>>();\n    let x = ctx.tensor(values.clone(), &[16, 16])?;\n    let y = ctx.tensor(values.clone(), &[16, 16])?;\n    let public_model_initialization_ms = initialization.elapsed().as_secs_f64() * 1000.0;\n    let legacy_initialization = Instant::now();\n    let ox = old::tensor::Tensor::from_vec(values.clone(), &[16, 16])?;\n    let oy = old::tensor::Tensor::from_vec(values, &[16, 16])?;\n    let operation = old::tensor::operators::Matmul::new()?;\n    let legacy_model_initialization_ms = legacy_initialization.elapsed().as_secs_f64() * 1000.0;\n    let initialization_ms = initialization.elapsed().as_secs_f64() * 1000.0;\n    let actual = x.matmul(&y)?.to_vec()?;\n    let expected = operation\n        .forward(&[&ox, &oy])?\n        .into_iter()\n        .next()\n        .ok_or(\"missing output\")?;\n    for (a, b) in actual.iter().zip(expected.data()) {\n        assert!((*a - *b).abs() \u003c= 1e-3f32.max(1e-3 * a.abs().max(b.abs())));\n    }\n    let mut public = Vec::new();\n    let mut legacy = Vec::new();\n    for i in 0..=iterations {\n        let start = Instant::now();\n        let result = black_box(x.matmul(black_box(&y))?);\n        let elapsed = start.elapsed().as_secs_f64() * 1e6;\n        drop(result);\n        let start = Instant::now();\n        let result = black_box(operation.forward(black_box(&[&ox, &oy]))?);\n        let old_elapsed = start.elapsed().as_secs_f64() * 1e6;\n        drop(result);\n        if i != 0 {\n            public.push(elapsed);\n            legacy.push(old_elapsed);\n        }\n    }\n    let summary = |mut values: Vec\u003cf64>| {\n        values.sort_by(f64::total_cmp);\n        let n = values.len();\n        let median = values[n / 2];\n        serde_json::json!({\"median_us\":median,\"p95_us\":values[((n as f64*0.95).ceil() as usize)-1],\"operations_per_second\":1e6/median,\"samples_us\":values})\n    };\n    let (legacy_storage_handles, legacy_graph_nodes) = old::comparison::statistics()?;\n    Ok(\n        serde_json::json!({\"legacy_storage_handles\":legacy_storage_handles,\"legacy_graph_nodes\":legacy_graph_nodes,\"case\":\"matmul_16x16_forward\",\"iterations\":iterations,\"initialization_ms\":initialization_ms,\"public_model_initialization_ms\":public_model_initialization_ms,\"legacy_model_initialization_ms\":legacy_model_initialization_ms,\"public\":summary(public),\"legacy\":summary(legacy),\"parity\":\"passed\",\"public_storage_handles\":ctx.graph_stats()?.tensors,\"public_graph_nodes\":ctx.graph_stats()?.graph_nodes}),\n    )\n}\n"},{"path":"benches/support/legacy_trainer.rs","bytes":5204,"preview":"#![cfg(all(\n    feature = \"legacyBenchmark\",\n    feature = \"builtinStorage\",\n    feature = \"builtinKernels\"\n))]\nuse old::{\n    nn::Parameter as OldParameter, optimizer::Optimizer as OldOptimizer, tensor::TensorBase,\n    trainer::TrainableModel as OldModel,\n};\nuse trench_deep::legacy as old;\nuse trench_deep::{\n    nn::LinearRegression,\n    optimizer::{Optimizer, SGD},\n    trainer::*,\n    *,\n};\n\nfn close(a: f32, b: f32) {\n    assert!(\n        a.is_finite() && b.is_finite() && (a - b).abs() \u003c= 1e-3f32.max(1e-3 * a.abs().max(b.abs())),\n        \"{a} versus {b}\"\n    );\n}\n\npub fn run_case(iterations: usize) -> Result\u003cserde_json::Value, Box\u003cdyn std::error::Error>> {\n    use std::time::Instant;\n    if iterations == 0 {\n        return Err(\"at least one iteration is required\".into());\n    }\n    let initialization = Instant::now();\n    let ctx = ExecutionContext::new();\n    let mut model = LinearRegression::new(&ctx, 2, 1)?;\n    let public_model_initialization_ms = initialization.elapsed().as_secs_f64() * 1000.0;\n    let legacy_initialization = Instant::now();\n    let mut baseline = old::comparison::linear::LinearRegression::build_model(2, 1)?;\n    let legacy_model_initialization_ms = legacy_initialization.elapsed().as_secs_f64() * 1000.0;\n    for (p, q) in model.parameters().iter().zip(baseline.params()) {\n        let shape = p.tensor().shape()?;\n        let values = vec![0.1; p.tensor().numel()?];\n        ctx.replace_parameter(\n            p.variable(),\n            TensorBuffer::from_vec(values.clone(), &shape)?,\n        )?;\n        q.tensor()\n            .replace(old::tensor::GlobalTensor::from_vec(values, &shape)?);\n    }\n    let mut samples = Vec::new();\n    let mut old_samples = Vec::new();\n    for i in 0..8 {\n        let x = vec![i as f32 / 8.0, 1.0 - i as f32 / 8.0];\n        let y = vec![x[0] * 0.3 + x[1] * 0.7];\n        samples.push(SupervisedSample::new(\n            ctx.tensor(x.clone(), &[2])?,\n            ctx.tensor(y.clone(), &[1])?,\n        ));\n        old_samples.push(old::trainer::SupervisedSample::new(\n            old::tensor::Tensor::from_vec(x, &[2])?,\n            old::tensor::Tensor::from_vec(y, &[1])?,\n        ));\n    }\n    let mut loader = DataLoader::builder(InMemoryDataset::new(samples)?)\n        .collator(SupervisedStackCollator::new())\n        .batch_size(2)\n        .shuffle(false)\n        .build()?;"},{"path":"Cargo.lock","bytes":22103},{"path":"Cargo.toml","bytes":2523},{"path":"commit","bytes":45},{"path":"cuda/kernels.cu","bytes":2384},{"path":"data/t10k-images-idx3-ubyte","bytes":7840016},{"path":"data/t10k-images.idx3-ubyte","bytes":7840016},{"path":"data/t10k-labels-idx1-ubyte","bytes":10008},{"path":"data/t10k-labels.idx1-ubyte","bytes":10008},{"path":"data/train-images-idx3-ubyte","bytes":47040016},{"path":"data/train-images.idx3-ubyte","bytes":47040016},{"path":"data/train-labels-idx1-ubyte","bytes":60008},{"path":"data/train-labels.idx1-ubyte","bytes":60008},{"path":"docs/benchmarks/p1-windows-cpu.json","bytes":7961},{"path":"docs/benchmarks/p1-windows-cpu.metadata.json","bytes":19584},{"path":"docs/DATA_LOADING.md","bytes":3317},{"path":"docs/p1/B_DYNAMIC_BASELINE_20260913.json","bytes":403814},{"path":"docs/p1/B_DYNAMIC_BASELINE_20260913.md","bytes":8985},{"path":"docs/p1/P1_API.md","bytes":3821},{"path":"docs/p1/P1_DEPENDENCY_AUDIT.md","bytes":1870},{"path":"docs/p1/P1_HANDOFF.md","bytes":11738},{"path":"docs/p1/P1_LEGACY_OPERATIONS.md","bytes":7827},{"path":"docs/p1/P1_REVISED_PLAN.md","bytes":9319},{"path":"docs/p1/P1_SNAPSHOT_REFERENCE_AUDIT.md","bytes":5512},{"path":"docs/p1/P1_STATIC_TRANSITION_PLAN.md","bytes":20434},{"path":"docs/p1/P1_STATUS.md","bytes":13543},{"path":"docs/p1/P1_UNUSED_FILES_AUDIT.md","bytes":8476},{"path":"docs/p1/S0_PREPARED_REPLAY.md","bytes":3247},{"path":"docs/TRAINER_API_OPTIMIZATION.md","bytes":1446},{"path":"docs/TRAINER_NEXT_PHASES.md","bytes":21861},{"path":"docs/TRAINER_PROGRESS_REPORT.md","bytes":16834},{"path":"docs/TRAINER_REMEDIATION_PLAN.md","bytes":12802},{"path":"docs/TRAINING_VISUALIZATION.md","bytes":2914},{"path":"examples/replay_reference_diffusion.rs","bytes":7099,"preview":"//! Replay a captured three-step baseline without linking or executing legacy code.\nuse serde::{Deserialize, Serialize};\n#[path = \"support/reference_parameters.rs\"]\npub mod mapping;\nuse trench_deep::{\n    nn::{Diffusion, DiffusionScheduler, Unet},\n    optimizer::{Adam, Optimizer},\n    trainer::TrainableModel,\n    *,\n};\n\n#[derive(Serialize, Deserialize)]\npub struct Weight {\n    #[serde(flatten)]\n    pub descriptor: mapping::Descriptor,\n    pub values: Vec\u003cf32>,\n}\n#[derive(Serialize, Deserialize)]\npub struct Step {\n    pub timestep: usize,\n    pub noise: Vec\u003cf32>,\n    pub prediction: Vec\u003cf32>,\n    pub loss: Vec\u003cf32>,\n    pub gradients: std::collections::BTreeMap\u003cString, Vec\u003cf32>>,\n    pub updated: std::collections::BTreeMap\u003cString, Vec\u003cf32>>,\n}\n#[derive(Serialize, Deserialize)]\npub struct Fixture {\n    pub version: u32,\n    pub corrections: serde_json::Value,\n    pub initial: Vec\u003cWeight>,\n    pub steps: Vec\u003cStep>,\n}\n\nfn close(a: &[f32], b: &[f32], label: &str) -> Result\u003c(), Box\u003cdyn std::error::Error>> {\n    if a.len() != b.len() {\n        return Err(format!(\"{label}: length mismatch\").into());\n    }\n    for (i, (&a, &b)) in a.iter().zip(b).enumerate() {\n        if !a.is_finite()\n            || !b.is_finite()\n            || (a - b).abs() > 1e-3f32.max(1e-3 * a.abs().max(b.abs()))\n        {\n            return Err(format!(\"{label}[{i}]: {a} != {b}\").into());\n        }\n    }\n    Ok(())\n}\n\npub fn replay(fixture: &Fixture) -> Result\u003c(), Box\u003cdyn std::error::Error>> {\n    if fixture.version != 2 || fixture.steps.len() != 3 {\n        return Err(\"expected version 2 named reference DDPM fixture with exactly three steps; regenerate older fixtures\".into());\n    }\n    let expected: serde_json::Value =\n        serde_json::from_str(include_str!(\"../src/native_provenance/CORRECTIONS.json\"))?;\n    if fixture.corrections != expected {\n        return Err(\"baseline correction provenance mismatch\".into());\n    }\n    // Reject malformed later steps before performing any training work.\n    for (i, step) in fixture.steps.iter().enumerate() {\n        if step.timestep >= 10 {\n            return Err(format!(\"step {i}: timestep must be less than 10\").into());\n        }\n        if step.noise.len() != 128 || step.prediction.len() != 128 || step.loss.len() != 1 {\n            return Err(format!(\"step {i}: reference tensor length mismatch\").into());"},{"path":"examples/support/reference_parameters.rs","bytes":2455,"preview":"use std::collections::BTreeMap;\n\n#[derive(Debug, Clone, PartialEq, Eq, serde::Serialize, serde::Deserialize)]\npub struct Descriptor {\n    pub name: String,\n    pub shape: Vec\u003cusize>,\n    /// Lexicographically first structural path referring to the same parameter.\n    pub shared_with: String,\n}\n\npub fn describe\u003cI: Eq>(entries: Vec\u003c(String, Vec\u003cusize>, I)>) -> Result\u003cVec\u003cDescriptor>, String> {\n    let mut sorted = BTreeMap::new();\n    for (name, shape, identity) in entries {\n        if name.is_empty() || sorted.insert(name.clone(), (shape, identity)).is_some() {\n            return Err(format!(\"empty or duplicate parameter name: {name}\"));\n        }\n    }\n    let mut result = Vec::new();\n    for (name, (shape, identity)) in &sorted {\n        let (canonical, (canonical_shape, _)) =\n            sorted.iter().find(|(_, (_, id))| id == identity).unwrap();\n        if canonical_shape != shape {\n            return Err(format!(\"shared parameter shape mismatch: {name}\"));\n        }\n        result.push(Descriptor {\n            name: name.clone(),\n            shape: shape.clone(),\n            shared_with: canonical.clone(),\n        });\n    }\n    Ok(result)\n}\n\npub fn validate(actual: &[Descriptor], expected: &[Descriptor]) -> Result\u003c(), String> {\n    fn index(items: &[Descriptor]) -> Result\u003cBTreeMap\u003c&str, &Descriptor>, String> {\n        let mut result = BTreeMap::new();\n        for item in items {\n            if item.name.is_empty() || result.insert(item.name.as_str(), item).is_some() {\n                return Err(format!(\"empty or duplicate parameter name: {}\", item.name));\n            }\n        }\n        for item in items {\n            let canonical = result\n                .get(item.shared_with.as_str())\n                .ok_or_else(|| format!(\"missing shared parameter: {}\", item.name))?;\n            if canonical.shared_with != canonical.name\n                || canonical.shape != item.shape\n                || canonical.name > item.name\n            {\n                return Err(format!(\"invalid shared parameter: {}\", item.name));\n            }\n        }\n        Ok(result)\n    }\n    let actual = index(actual)?;\n    let expected = index(expected)?;\n    if actual.keys().ne(expected.keys()) {\n        return Err(\"parameter name set mismatch\".into());\n    }\n    for (name, actual) in actual {\n        if actual != expected[name] {\n            return Err(format!(\"parameter shape/sharing mismatch: {name}\"));\n        }\n    }\n    Ok(())"},{"path":"graph/goldstein.dot","bytes":21673},{"path":"graph/goldstein.png","bytes":1266720},{"path":"graph/rosenbrock.dot","bytes":6176},{"path":"graph/rosenbrock.png","bytes":769916},{"path":"graph/rosenbrock.svg","bytes":19245},{"path":"graph/twolayer_refactored.svg","bytes":27471},{"path":"graph/twolayer.dot","bytes":5956},{"path":"graph/twolayer.png","bytes":152797},{"path":"graph/twolayer.svg","bytes":1248333},{"path":"LICENSE","bytes":11523},{"path":"logs/test_run.log.2026-09-03-04","bytes":34644},{"path":"logs/test_run.log.2026-09-06-04","bytes":7395},{"path":"logs/test_run.log.2026-09-09-09","bytes":25995966},{"path":"logs/test_run.log.2026-09-10-00","bytes":42824466},{"path":"logs/test_run.log.2026-09-10-01","bytes":16721049},{"path":"logs/test_run.log.2026-09-10-08","bytes":2012},{"path":"logs/test_run.log.2026-09-10-09","bytes":303746},{"path":"logs/test_run.log.2026-09-11-00","bytes":25593406},{"path":"logs/test_run.log.2026-09-13-06","bytes":7761532},{"path":"README.md","bytes":1432},{"path":"scripts/benchmark_context_diffusion.py","bytes":8417},{"path":"scripts/verify_legacy.py","bytes":1942},{"path":"src/backend/cpu/compute.rs","bytes":10870,"preview":"use super::parallel::ParallelExecutor;\nuse crate::{MlResult, TensorError};\n\nconst PARALLEL_THRESHOLD: usize = 1024;\n\n#[derive(Debug)]\npub struct CpuCompute {\n    parallel: ParallelExecutor,\n}\n\nimpl CpuCompute {\n    pub fn new() -> Self {\n        CpuCompute {\n            parallel: ParallelExecutor::new(),\n        }\n    }\n\n    // Optimized vector operations with bounds checking\n    fn check_dimensions(&self, a: &[f32], b: &[f32]) -> Option\u003cusize> {\n        if a.len() != b.len() {\n            return None;\n        }\n        Some(a.len())\n    }\n\n    // Optimized binary operations using chunks\n    pub fn add(&self, a: &[f32], b: &[f32]) -> MlResult\u003cVec\u003cf32>> {\n        Ok(if let Some(len) = self.check_dimensions(a, b) {\n            if len \u003c PARALLEL_THRESHOLD {\n                // Use existing sequential implementation for small arrays\n                let mut result = Vec::with_capacity(len);\n                for (x, y) in a.chunks(4).zip(b.chunks(4)) {\n                    result.extend(x.iter().zip(y.iter()).map(|(a, b)| a + b));\n                }\n                result\n            } else {\n                // Use parallel implementation for large arrays\n                self.parallel\n                    .execute_binary(a, b, PARALLEL_THRESHOLD, |x, y| {\n                        x.iter().zip(y.iter()).map(|(a, b)| a + b).collect()\n                    })?\n            }\n        } else {\n            return Err(TensorError::InvalidOperation {\n                op: \"cpu_binary\",\n                reason: \"length mismatch\".into(),\n            }\n            .into());\n        })\n    }\n\n    pub fn multiply(&self, a: &[f32], b: &[f32]) -> MlResult\u003cVec\u003cf32>> {\n        Ok(if let Some(len) = self.check_dimensions(a, b) {\n            if len \u003c PARALLEL_THRESHOLD {\n                // Use existing sequential implementation\n                let mut result = Vec::with_capacity(len);\n                let chunks = len / 8;\n                let remainder = len % 8;\n\n                for i in 0..chunks {\n                    let idx = i * 8;\n                    result.extend_from_slice(&[\n                        a[idx] * b[idx],\n                        a[idx + 1] * b[idx + 1],\n                        a[idx + 2] * b[idx + 2],"},{"path":"src/backend/cpu/core.rs","bytes":1512,"preview":"use super::CpuCompute;\nuse crate::backend::DeviceType;\n\n#[derive(Debug)]\npub struct CpuCore;\n\nimpl CpuCore {\n    pub fn new() -> Self {\n        CpuCore\n    }\n\n    pub fn device_type(&self) -> DeviceType {\n        DeviceType::Cpu\n    }\n\n    pub fn calc_device_flops(&self) -> f64 {\n        let size = 1024;\n        let a = vec![1.0; size * size];\n        let b = vec![2.0; size * size];\n        let compute = CpuCompute::new();\n        let start = std::time::Instant::now();\n        std::hint::black_box(compute.matmul(&a, &b, size, size, size));\n        let duration = start.elapsed();\n        // Calculate FLOPS:\n        // For matrix multiplication of (n x n) matrices:\n        // Each element requires n multiplications and n-1 additions\n        // Total operations = n * n * (2n - 1)\n        let operations = size as u64 * size as u64 * (2 * size as u64 - 1);\n        let flops = (operations as f64) / duration.as_secs_f64();\n\n        flops\n    }\n}\n\n#[cfg(test)]\nmod tests {\n\n    fn pretty_flops(flops: f64) -> String {\n        if flops >= 1_000_000_000_000.0 {\n            format!(\"{:.2} Tflops/s\", flops / 1_000_000_000_000.0)\n        } else if flops >= 1_000_000_000.0 {\n            format!(\"{:.2} Gflops/s\", flops / 1_000_000_000.0)\n        } else if flops >= 1_000_000.0 {\n            format!(\"{:.2} Mflops/s\", flops / 1_000_000.0)\n        } else if flops >= 1_000.0 {\n            format!(\"{:.2} Kflops/s\", flops / 1_000.0)\n        } else {\n            format!(\"{:.2} flops/s\", flops)\n        }\n    }\n}\n"},{"path":"src/backend/cpu/legacy_compute.rs","bytes":10848,"preview":"use super::parallel::ParallelExecutor;\n\nconst PARALLEL_THRESHOLD: usize = 1024;\n\n#[derive(Debug)]\npub struct CpuCompute {\n    parallel: ParallelExecutor,\n}\n\nimpl CpuCompute {\n    pub fn new() -> Self {\n        CpuCompute {\n            parallel: ParallelExecutor::new(),\n        }\n    }\n\n    // Optimized vector operations with bounds checking\n    fn check_dimensions(&self, a: &[f32], b: &[f32]) -> Option\u003cusize> {\n        if a.len() != b.len() {\n            return None;\n        }\n        Some(a.len())\n    }\n\n    // Optimized binary operations using chunks\n    pub fn add(&self, a: &[f32], b: &[f32]) -> Vec\u003cf32> {\n        if let Some(len) = self.check_dimensions(a, b) {\n            if len \u003c PARALLEL_THRESHOLD {\n                // Use existing sequential implementation for small arrays\n                let mut result = Vec::with_capacity(len);\n                for (x, y) in a.chunks(4).zip(b.chunks(4)) {\n                    result.extend(x.iter().zip(y.iter()).map(|(a, b)| a + b));\n                }\n                result\n            } else {\n                // Use parallel implementation for large arrays\n                self.parallel\n                    .execute_binary(a, b, PARALLEL_THRESHOLD, |x, y| {\n                        x.iter().zip(y.iter()).map(|(a, b)| a + b).collect()\n                    })\n            }\n        } else {\n            Vec::new()\n        }\n    }\n\n    pub fn multiply(&self, a: &[f32], b: &[f32]) -> Vec\u003cf32> {\n        if let Some(len) = self.check_dimensions(a, b) {\n            if len \u003c PARALLEL_THRESHOLD {\n                // Use existing sequential implementation\n                let mut result = Vec::with_capacity(len);\n                let chunks = len / 8;\n                let remainder = len % 8;\n\n                for i in 0..chunks {\n                    let idx = i * 8;\n                    result.extend_from_slice(&[\n                        a[idx] * b[idx],\n                        a[idx + 1] * b[idx + 1],\n                        a[idx + 2] * b[idx + 2],\n                        a[idx + 3] * b[idx + 3],\n                        a[idx + 4] * b[idx + 4],\n                        a[idx + 5] * b[idx + 5],\n                        a[idx + 6] * b[idx + 6],\n                        a[idx + 7] * b[idx + 7],"},{"path":"src/backend/cpu/legacy_core.rs","bytes":1953,"preview":"use crate::legacy::backend::DeviceType;\nuse crate::legacy::tensor::operators::Function;\nuse crate::legacy::tensor::operators::Matmul;\nuse crate::legacy::tensor::{Tensor, TensorBase};\n\n#[derive(Debug)]\npub struct CpuCore;\n\nimpl CpuCore {\n    pub fn new() -> Self {\n        CpuCore\n    }\n\n    pub fn device_type(&self) -> DeviceType {\n        DeviceType::Cpu\n    }\n\n    pub fn calc_device_flops(&self) -> f64 {\n        let matmul = Matmul::new().unwrap();\n        // Create two large tensors for benchmarking\n        let size = 1024;\n        let elements = size * size;\n\n        let a = Tensor::from_vec(vec![1.0; elements], &[size, size].to_vec()).unwrap();\n        let b = Tensor::from_vec(vec![2.0; elements], &[size, size].to_vec()).unwrap();\n\n        // Measure matrix multiplication time (more compute intensive than addition)\n        let start = std::time::Instant::now();\n        let _c = matmul.forward(&[&a, &b]).unwrap();\n        let duration = start.elapsed();\n\n        // Calculate FLOPS:\n        // For matrix multiplication of (n x n) matrices:\n        // Each element requires n multiplications and n-1 additions\n        // Total operations = n * n * (2n - 1)\n        let operations = size as u64 * size as u64 * (2 * size as u64 - 1);\n        let flops = (operations as f64) / duration.as_secs_f64();\n\n        flops\n    }\n}\n\n#[cfg(test)]\nmod tests {\n\nfn pretty_flops(flops: f64) -> String {\n        if flops >= 1_000_000_000_000.0 {\n            format!(\"{:.2} Tflops/s\", flops / 1_000_000_000_000.0)\n        } else if flops >= 1_000_000_000.0 {\n            format!(\"{:.2} Gflops/s\", flops / 1_000_000_000.0)\n        } else if flops >= 1_000_000.0 {\n            format!(\"{:.2} Mflops/s\", flops / 1_000_000.0)\n        } else if flops >= 1_000.0 {\n            format!(\"{:.2} Kflops/s\", flops / 1_000.0)\n        } else {\n            format!(\"{:.2} flops/s\", flops)\n        }\n    }\n}\n"},{"path":"src/backend/cpu/legacy_mod.rs","bytes":3759,"preview":"pub use compute::CpuCompute;\npub use core::CpuCore;\n\nuse crate::legacy::backend::feature::{\n    DeviceFeatures, CPU_FEATURE_AVX, CPU_FEATURE_AVX2, CPU_FEATURE_AVX512F,\n};\nuse crate::legacy::backend::{Backend, Device, DeviceType};\nuse crate::legacy::MlResult;\n\n#[path = \"legacy_compute.rs\"]\nmod compute;\n#[path = \"legacy_core.rs\"]\nmod core;\n#[path = \"legacy_parallel.rs\"]\nmod parallel;\n\n#[derive(Debug)]\npub struct CpuBackend {\n    core: CpuCore,\n    compute: CpuCompute,\n}\n\nimpl Device for CpuBackend {\n    fn new() -> MlResult\u003cSelf> {\n        Ok(CpuBackend {\n            core: CpuCore::new(),\n            compute: CpuCompute::new(),\n        })\n    }\n\n    fn device_type(&self) -> DeviceType {\n        DeviceType::Cpu\n    }\n\n    fn get_features(&self) -> DeviceFeatures {\n        let mut features = DeviceFeatures::new();\n\n        #[cfg(target_arch = \"x86_64\")]\n        {\n            features.add_feature(\n                CPU_FEATURE_AVX,\n                is_x86_feature_detected!(\"avx\"),\n                Some(\"Advanced Vector Extensions\".to_string()),\n            );\n\n            features.add_feature(\n                CPU_FEATURE_AVX2,\n                is_x86_feature_detected!(\"avx2\"),\n                Some(\"Advanced Vector Extensions 2\".to_string()),\n            );\n\n            features.add_feature(\n                CPU_FEATURE_AVX512F,\n                is_x86_feature_detected!(\"avx512f\"),\n                Some(\"AVX-512 Foundation\".to_string()),\n            );\n        }\n\n        features\n    }\n}\n\nimpl Backend for CpuBackend {\n    fn device(&self) -> DeviceType {\n        self.core.device_type()"},{"path":"src/backend/cpu/legacy_parallel.rs","bytes":3613,"preview":"use std::sync::Arc;\nuse std::thread;\n\n#[derive(Debug)]\npub struct ParallelExecutor {\n    thread_count: usize,\n}\n\nimpl ParallelExecutor {\n    pub fn new() -> Self {\n        let thread_count = thread::available_parallelism()\n            .map(|n| n.get())\n            .unwrap_or(1);\n\n        ParallelExecutor { thread_count }\n    }\n\n    pub fn execute\u003cT, F>(&self, data: &[T], chunk_size: usize, f: F) -> Vec\u003cT>\n    where\n        T: Send + Sync + Copy + 'static,\n        F: Fn(&[T]) -> Vec\u003cT> + Send + Sync + Clone + 'static,\n    {\n        if data.len() \u003c= chunk_size {\n            return f(data);\n        }\n\n        let data = Arc::new(data.to_vec());\n        let total_elements = data.len();\n        let elements_per_thread = (total_elements + self.thread_count - 1) / self.thread_count;\n        let mut handles = Vec::with_capacity(self.thread_count);\n\n        for thread_idx in 0..self.thread_count {\n            let start = thread_idx * elements_per_thread;\n            if start >= total_elements {\n                break;\n            }\n\n            let end = (start + elements_per_thread).min(total_elements);\n            let thread_data = Arc::clone(&data);\n            let thread_f = f.clone();\n\n            handles.push(thread::spawn(move || thread_f(&thread_data[start..end])));\n        }\n\n        handles\n            .into_iter()\n            .flat_map(|h| h.join().unwrap())\n            .collect()\n    }\n\n    pub fn execute_binary\u003cT, F>(&self, a: &[T], b: &[T], chunk_size: usize, f: F) -> Vec\u003cT>\n    where\n        T: Send + Sync + Copy + 'static,\n        F: Fn(&[T], &[T]) -> Vec\u003cT> + Send + Sync + Clone + 'static,\n    {\n        if a.len() \u003c= chunk_size {\n            return f(a, b);\n        }\n\n        let a = Arc::new(a.to_vec());\n        let b = Arc::new(b.to_vec());\n        let total_elements = a.len();\n        let elements_per_thread = (total_elements + self.thread_count - 1) / self.thread_count;\n        let mut handles = Vec::with_capacity(self.thread_count);\n"},{"path":"src/backend/cpu/mod.rs","bytes":2563,"preview":"use compute::CpuCompute;\nuse core::CpuCore;\n\nuse crate::MlResult;\nuse crate::backend::feature::{\n    CPU_FEATURE_AVX, CPU_FEATURE_AVX2, CPU_FEATURE_AVX512F, DeviceFeatures,\n};\nuse crate::backend::{Backend, Device, DeviceType};\n\nmod compute;\nmod core;\npub(crate) mod operations;\nmod parallel;\n\n#[derive(Debug)]\npub struct CpuBackend {\n    core: CpuCore,\n    compute: CpuCompute,\n}\n\nimpl Default for CpuBackend {\n    fn default() -> Self {\n        Self {\n            core: CpuCore::new(),\n            compute: CpuCompute::new(),\n        }\n    }\n}\n\nimpl Device for CpuBackend {\n    fn new() -> MlResult\u003cSelf> {\n        Ok(CpuBackend {\n            core: CpuCore::new(),\n            compute: CpuCompute::new(),\n        })\n    }\n\n    fn device_type(&self) -> DeviceType {\n        DeviceType::Cpu\n    }\n\n    fn get_features(&self) -> DeviceFeatures {\n        let mut features = DeviceFeatures::new();\n\n        #[cfg(target_arch = \"x86_64\")]\n        {\n            features.add_feature(\n                CPU_FEATURE_AVX,\n                is_x86_feature_detected!(\"avx\"),\n                Some(\"Advanced Vector Extensions\".to_string()),\n            );\n\n            features.add_feature(\n                CPU_FEATURE_AVX2,\n                is_x86_feature_detected!(\"avx2\"),\n                Some(\"Advanced Vector Extensions 2\".to_string()),\n            );\n\n            features.add_feature(\n                CPU_FEATURE_AVX512F,\n                is_x86_feature_detected!(\"avx512f\"),\n                Some(\"AVX-512 Foundation\".to_string()),\n            );\n        }\n"},{"path":"src/backend/cpu/operations/forward.rs","bytes":13376,"preview":"use super::*;\n\nuse crate::backend::CpuBackend;\nimpl OperationProvider for CpuBackend {\n    fn supports_prepared_replay(&self) -> bool { true }\n    fn execute(\n        &self,\n        operation: &Operation,\n        inputs: &[TensorView\u003c'_>],\n    ) -> MlResult\u003cOperationOutput> {\n        if inputs.is_empty() || operation.input_count().is_some_and(|n| n != inputs.len()) {\n            return Err(TensorError::InvalidOperation {\n                op: operation.name(),\n                reason: \"invalid input count\".into(),\n            }\n            .into());\n        }\n        let values = inputs\n            .iter()\n            .map(|x| (*x).to_owned())\n            .collect::\u003cMlResult\u003cVec\u003c_>>>()?;\n        let x = &values[0];\n        let mut saved = Vec::new();\n        let (output, backward) = match operation {\n            Operation::Add | Operation::Sub | Operation::Mul | Operation::Div => {\n                let y = &values[1];\n                let shape = broadcast_shape(&x.shape, &y.shape).ok_or_else(|| {\n                    TensorError::InvalidOperation {\n                        op: operation.name(),\n                        reason: \"incompatible broadcast shapes\".into(),\n                    }\n                })?;\n                let a = broadcast_data(x, &shape)?;\n                let b = broadcast_data(y, &shape)?;\n                let data = match operation {\n                    Operation::Add => self.compute.add(&a, &b)?,\n                    Operation::Sub => self.compute.sub(&a, &b),\n                    Operation::Mul => self.compute.multiply(&a, &b)?,\n                    _ => self.compute.div(&a, &b),\n                };\n                let backward = match operation {\n                    Operation::Add => BuiltinBackward::Add,\n                    Operation::Sub => BuiltinBackward::Sub,\n                    Operation::Mul => BuiltinBackward::Mul,\n                    _ => BuiltinBackward::Div,\n                };\n                (TensorBuffer::from_vec(data, &shape)?, backward)\n            }\n            Operation::Neg => (tensor_map(x, |x| -x)?, BuiltinBackward::Neg),\n            Operation::Square => (tensor_map(x, |x| x * x)?, BuiltinBackward::Square),\n            Operation::Exp => (\n                TensorBuffer::from_vec(self.compute.exp(&x.data), &x.shape)?,\n                BuiltinBackward::Exp,\n            ),\n            Operation::Log => (\n                TensorBuffer::from_vec(self.compute.log(&x.data), &x.shape)?,\n                BuiltinBackward::Log,\n            ),\n            Operation::Sqrt => (\n                TensorBuffer::from_vec(self.compute.sqrt(&x.data), &x.shape)?,\n                BuiltinBackward::Sqrt,\n            ),\n            Operation::Pow(p) => (\n                TensorBuffer::from_vec(self.compute.pow(&x.data, *p), &x.shape)?,\n                BuiltinBackward::Pow(*p),"},{"path":"src/backend/cpu/operations/functional.rs","bytes":16900,"preview":"use super::*;\n\npub(crate) fn topk_forward_data(\n    input: TensorView\u003c'_>,\n    k: usize,\n    sorted: bool,\n) -> MlResult\u003c(Vec\u003cf32>, Vec\u003cf32>, Vec\u003cusize>)> {\n    let rank = input.shape.len();\n    if rank == 0 {\n        return Err(TensorError::InvalidOperation {\n            op: \"topk\",\n            reason: \"input must have at least one dimension\".into(),\n        }\n        .into());\n    }\n    let width = input.shape[rank - 1];\n    if k == 0 || k > width {\n        return Err(TensorError::InvalidOperation {\n            op: \"topk\",\n            reason: format!(\"k must be in 1..={width}, got {k}\"),\n        }\n        .into());\n    }\n    let compare = |(left_index, left): &(usize, f32), (right_index, right): &(usize, f32)| {\n        right.total_cmp(left).then(left_index.cmp(right_index))\n    };\n    let mut values = Vec::with_capacity(input.data.len() / width * k);\n    let mut indices = Vec::with_capacity(values.capacity());\n    for row in input.data.chunks_exact(width) {\n        let mut pairs: Vec\u003c_> = row.iter().copied().enumerate().collect();\n        if k \u003c width {\n            pairs.select_nth_unstable_by(k, compare);\n            pairs.truncate(k);\n        }\n        if sorted {\n            pairs.sort_unstable_by(compare);\n        } else {\n            pairs.sort_unstable_by_key(|(index, _)| *index);\n        }\n        values.extend(pairs.iter().map(|(_, value)| *value));\n        indices.extend(pairs.iter().map(|(index, _)| *index as f32));\n    }\n    let mut shape = input.shape.to_vec();\n    shape[rank - 1] = k;\n    Ok((values, indices, shape))\n}\n\npub(crate) fn matmax_forward_data(\n    input: TensorView\u003c'_>,\n    axis: Option\u003cisize>,\n    keepdim: bool,\n) -> MlResult\u003c(Vec\u003cf32>, Vec\u003cf32>, Vec\u003cusize>)> {\n    if input.data.is_empty() {\n        return Err(TensorError::EmptyTensor.into());\n    }\n    let Some(requested_axis) = axis else {\n        let (index, maximum) = input\n            .data\n            .iter()\n            .copied()\n            .enumerate()\n            .max_by(|(left_index, left), (right_index, right)| {\n                left.total_cmp(right).then(right_index.cmp(left_index))\n            })\n            .ok_or(TensorError::EmptyTensor)?;"},{"path":"src/backend/cpu/operations/kernels.rs","bytes":27849,"preview":"use super::*;\n\npub(crate) fn group_norm_spec(\n    input: &[usize],\n    gamma: &[usize],\n    beta: &[usize],\n    groups: usize,\n    epsilon: f32,\n) -> MlResult\u003c(usize, usize, usize, usize, usize, usize)> {\n    let channels = input.get(1).copied().unwrap_or_default();\n    let group_size = if groups == 0 { 0 } else { channels / groups };\n    let elements_per_group = input\n        .get(2)\n        .and_then(|height| height.checked_mul(*input.get(3)?))\n        .and_then(|spatial| spatial.checked_mul(group_size));\n    if input.len() != 4\n        || groups == 0\n        || channels == 0\n        || channels % groups != 0\n        || gamma != [channels]\n        || beta != [channels]\n        || !epsilon.is_finite()\n        || epsilon \u003c= 0.0\n        || elements_per_group.is_none_or(|elements| elements == 0)\n    {\n        return Err(TensorError::InvalidOperation {\n            op: \"group_norm\",\n            reason: format!(\n                \"expected input [N,C,H,W], gamma/beta [C], C divisible by non-zero groups, and finite positive epsilon; got input={input:?}, gamma={gamma:?}, beta={beta:?}, groups={groups}, epsilon={epsilon}\"\n            ),\n        }\n        .into());\n    }\n    Ok((\n        input[0],\n        channels,\n        input[2],\n        input[3],\n        group_size,\n        elements_per_group.unwrap_or_default(),\n    ))\n}\n\npub(crate) fn group_norm_forward_data(\n    input: &TensorBuffer,\n    gamma: &TensorBuffer,\n    beta: &TensorBuffer,\n    groups: usize,\n    epsilon: f32,\n) -> MlResult\u003c(TensorBuffer, Vec\u003cTensorBuffer>)> {\n    let (n, c, h, w, channels_per_group, elements_per_group) =\n        group_norm_spec(&input.shape, &gamma.shape, &beta.shape, groups, epsilon)?;\n    let mut output = vec![0.0; input.data.len()];\n    let mut normalized = vec![0.0; input.data.len()];\n    let mut means = vec![0.0; n * groups];\n    let mut variances = vec![0.0; n * groups];\n    for batch in 0..n {\n        for group in 0..groups {\n            let channel_start = group * channels_per_group;\n            let statistic_index = batch * groups + group;\n            let mut sum = 0.0;\n            for channel in channel_start..channel_start + channels_per_group {\n                let base = (batch * c + channel) * h * w;\n                sum += input.data[base..base + h * w].iter().sum::\u003cf32>();\n            }"},{"path":"src/backend/cpu/operations/mod.rs","bytes":19107,"preview":"use crate::contracts::*;\nuse crate::{AutogradError, LossError, MlResult, TensorError};\nmod functional;\nmod kernels;\npub(crate) use functional::*;\npub(crate) use kernels::*;\n#[derive(Debug, Clone)]\nenum BuiltinBackward {\n    Add,\n    Sub,\n    Mul,\n    Div,\n    Neg,\n    Square,\n    Exp,\n    Log,\n    Sqrt,\n    Pow(f32),\n    Sin,\n    Cos,\n    ApproxSin {\n        threshold: f32,\n    },\n    ApproxCos {\n        threshold: f32,\n    },\n    Tanh,\n    Sigmoid,\n    Silu,\n    Relu,\n    Abs,\n    Softmax {\n        axis: usize,\n    },\n    Reshape,\n    Transpose(Vec\u003cusize>),\n    Concat {\n        axis: usize,\n        sizes: Vec\u003cusize>,\n    },\n    Sum,\n    Matmul,\n    Conv2d {\n        stride: (usize, usize),\n        padding: (usize, usize),\n    },\n    MaxPool2d {\n        kernel: (usize, usize),\n        stride: (usize, usize),\n    },\n    AvgPool2d {\n        kernel: (usize, usize),\n        stride: (usize, usize),\n    },\n    NearestUpsample2d {\n        scale: (usize, usize),\n    },\n    GroupNorm {\n        groups: usize,\n        epsilon: f32,\n    },\n    Loss {\n        kind: LossKind,\n        reduction: Reduction,\n    },"},{"path":"src/backend/cpu/parallel.rs","bytes":4641,"preview":"use crate::{MlResult, TensorError};\nuse std::sync::Arc;\nuse std::thread;\n\n#[derive(Debug)]\npub struct ParallelExecutor {\n    thread_count: usize,\n}\n\nimpl ParallelExecutor {\n    pub fn new() -> Self {\n        let thread_count = thread::available_parallelism()\n            .map(|n| n.get())\n            .unwrap_or(1);\n\n        ParallelExecutor { thread_count }\n    }\n\n    pub fn execute\u003cT, F>(&self, data: &[T], chunk_size: usize, f: F) -> MlResult\u003cVec\u003cT>>\n    where\n        T: Send + Sync + Copy + 'static,\n        F: Fn(&[T]) -> Vec\u003cT> + Send + Sync + Clone + 'static,\n    {\n        if data.len() \u003c= chunk_size {\n            return Ok(f(data));\n        }\n\n        let data = Arc::new(data.to_vec());\n        let total_elements = data.len();\n        let elements_per_thread = (total_elements + self.thread_count - 1) / self.thread_count;\n        let mut handles = Vec::with_capacity(self.thread_count);\n\n        for thread_idx in 0..self.thread_count {\n            let start = thread_idx * elements_per_thread;\n            if start >= total_elements {\n                break;\n            }\n\n            let end = (start + elements_per_thread).min(total_elements);\n            let thread_data = Arc::clone(&data);\n            let thread_f = f.clone();\n\n            handles.push(thread::spawn(move || thread_f(&thread_data[start..end])));\n        }\n\n        let chunks = handles\n            .into_iter()\n            .map(|h| {\n                h.join().map_err(|_| {\n                    TensorError::InvalidOperation {\n                        op: \"cpu_parallel\",\n                        reason: \"worker thread failed\".into(),\n                    }\n                    .into()\n                })\n            })\n            .collect::\u003cVec\u003cMlResult\u003cVec\u003cT>>>>();\n        Ok(chunks\n            .into_iter()\n            .collect::\u003cMlResult\u003cVec\u003c_>>>()?\n            .into_iter()\n            .flatten()\n            .collect())\n    }\n"},{"path":"src/backend/cpu/storage.rs","bytes":2322,"preview":"use crate::contracts::*;\nuse crate::{ContextError, MlResult};\nuse std::{cell::RefCell, collections::HashMap, rc::Rc};\n\n#[derive(Debug, Default)]\npub struct CpuTensorStore {\n    buffers: HashMap\u003cTensorId, Rc\u003cRefCell\u003cTensorBuffer>>>,\n}\nimpl TensorStore for CpuTensorStore {\n    fn insert(&mut self, id: TensorId, buffer: TensorBuffer) -> MlResult\u003c()> {\n        if self.buffers.contains_key(&id) {\n            return Err(crate::TensorError::InvalidOperation {\n                op: \"insert\",\n                reason: \"duplicate tensor ID\".into(),\n            }\n            .into());\n        }\n        self.buffers.insert(id, Rc::new(RefCell::new(buffer)));\n        Ok(())\n    }\n    fn alias(&mut self, id: TensorId, source: TensorId) -> MlResult\u003c()> {\n        if self.buffers.contains_key(&id) {\n            return Err(crate::TensorError::InvalidOperation {\n                op: \"alias\",\n                reason: \"duplicate tensor ID\".into(),\n            }\n            .into());\n        }\n        let buffer = self\n            .buffers\n            .get(&source)\n            .ok_or(ContextError::UnknownTensor(source))?\n            .clone();\n        self.buffers.insert(id, buffer);\n        Ok(())\n    }\n    fn with_view(\n        &self,\n        id: TensorId,\n        visitor: &mut dyn FnMut(TensorView\u003c'_>) -> MlResult\u003c()>,\n    ) -> MlResult\u003c()> {\n        let buffer = self\n            .buffers\n            .get(&id)\n            .ok_or(ContextError::UnknownTensor(id))?;\n        let buffer = buffer\n            .try_borrow()\n            .map_err(|_| ContextError::BorrowConflict)?;\n        visitor(buffer.view())\n    }\n    fn replace(&mut self, id: TensorId, buffer: TensorBuffer) -> MlResult\u003c()> {\n        let old = self\n            .buffers\n            .get(&id)\n            .ok_or(ContextError::UnknownTensor(id))?;\n        let mut old = old\n            .try_borrow_mut()\n            .map_err(|_| ContextError::BorrowConflict)?;\n        if old.shape() != buffer.shape() {\n            return Err(crate::TensorError::InvalidShape {\n                expected: old.shape().to_vec(),\n                got: buffer.shape().to_vec(),\n            }\n            .into());\n        }"},{"path":"src/backend/device.rs","bytes":502,"preview":"use crate::MlResult;\nuse crate::backend::feature::DeviceFeatures;\nuse std::fmt::{Display, Formatter};\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]\npub enum DeviceType {\n    Cpu,\n    Cuda,\n}\n\nimpl Display for DeviceType {\n    fn fmt(&self, f: &mut Formatter\u003c'_>) -> std::fmt::Result {\n        write!(f, \"{:?}\", self)\n    }\n}\n\npub trait Device {\n    fn new() -> MlResult\u003cSelf>\n    where\n        Self: Sized;\n    fn device_type(&self) -> DeviceType;\n    fn get_features(&self) -> DeviceFeatures;\n}\n"},{"path":"src/backend/feature.rs","bytes":1088,"preview":"#[derive(Debug, Clone, PartialEq)]\npub struct DeviceFeature {\n    pub name: String,\n    pub supported: bool,\n    pub description: Option\u003cString>,\n}\n\n#[derive(Debug, Clone, Default)]\npub struct DeviceFeatures {\n    features: Vec\u003cDeviceFeature>,\n}\n\nimpl DeviceFeatures {\n    pub fn new() -> Self {\n        Self {\n            features: Vec::new(),\n        }\n    }\n\n    pub fn add_feature(&mut self, name: &str, supported: bool, description: Option\u003cString>) {\n        self.features.push(DeviceFeature {\n            name: name.to_string(),\n            supported,\n            description,\n        });\n    }\n\n    pub fn is_supported(&self, feature_name: &str) -> bool {\n        self.features\n            .iter()\n            .find(|f| f.name == feature_name)\n            .map_or(false, |f| f.supported)\n    }\n}\n\n// CPU Features\npub const CPU_FEATURE_AVX: &str = \"avx\";\npub const CPU_FEATURE_AVX2: &str = \"avx2\";\npub const CPU_FEATURE_AVX512F: &str = \"avx512f\";\npub const CPU_FEATURE_SSE4_1: &str = \"sse4.1\";\npub const CPU_FEATURE_SSE4_2: &str = \"sse4.2\";\n"},{"path":"src/backend/legacy_device.rs","bytes":3173,"preview":"use std::collections::HashSet;\nuse std::fmt::{Display, Formatter};\nuse std::sync::Mutex;\nuse std::sync::Once;\n\nuse crate::legacy::backend::feature::*;\nuse crate::legacy::MlResult;\n\nstatic INIT: Once = Once::new();\nstatic mut GLOBAL_DEVICE_MANAGER: Option\u003cDeviceManager> = None;\nstatic mut DEFAULT_DEVICE: Option\u003cMutex\u003cDeviceType>> = None;\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]\npub enum DeviceType {\n    Cpu,\n    Cuda\n}\n\nimpl Display for DeviceType {\n    fn fmt(&self, f: &mut Formatter\u003c'_>) -> std::fmt::Result {\n        write!(f, \"{:?}\", self)\n    }\n}\n\npub struct DeviceManager {\n    available_devices: HashSet\u003cDeviceType>,\n}\n\nimpl Default for DeviceManager {\n    fn default() -> Self {\n        Self::new()\n    }\n}\n\nimpl DeviceManager {\n    pub fn new() -> Self {\n        let mut available_devices = HashSet::new();\n        available_devices.insert(DeviceType::Cpu);\n        println!(\"Available devices: {:?}\", available_devices);\n        Self { available_devices }\n    }\n\n    pub fn available_devices(&self) -> &HashSet\u003cDeviceType> {\n        &self.available_devices\n    }\n\n    pub fn global() -> &'static DeviceManager {\n        #[warn(static_mut_refs)]\n        unsafe {\n            INIT.call_once(|| {\n                GLOBAL_DEVICE_MANAGER = Some(DeviceManager::new());\n\n                // Initialize default device\n                let _manager = GLOBAL_DEVICE_MANAGER.as_ref().unwrap();\n\n                // Select default device based on priority and availability\n                let device_type = {\n                    {\n                        DeviceType::Cpu\n                    }\n                };\n\n                DEFAULT_DEVICE = Some(Mutex::new(device_type));\n                println!(\"Default device set to: {:?}\", device_type);\n            });"},{"path":"src/backend/legacy_mod.rs","bytes":1182,"preview":"use std::fmt::{Debug, Display, Formatter};\n\npub use cpu::CpuBackend;\npub use device::{Device, DeviceType};\n\nuse crate::legacy::MlResult;\n\n#[path = \"legacy_device.rs\"]\nmod device;\n#[path = \"feature.rs\"]\nmod feature;\n#[path = \"cpu/legacy_mod.rs\"]\nmod cpu;\n\npub trait Backend: Debug + Send + Sync {\n    fn device(&self) -> DeviceType;\n    fn calc_device_flops(&self) -> f64;\n    fn add(&self, a: &[f32], b: &[f32]) -> Vec\u003cf32>;\n    fn multiply(&self, a: &[f32], b: &[f32]) -> Vec\u003cf32>;\n    fn matmul(&self, a: &[f32], b: &[f32], m: usize, n: usize, k: usize) -> Vec\u003cf32>;\n    fn div(&self, a: &[f32], b: &[f32]) -> Vec\u003cf32>;\n    fn sub(&self, a: &[f32], b: &[f32]) -> Vec\u003cf32>;\n    fn exp(&self, a: &[f32]) -> Vec\u003cf32>;\n    fn log(&self, a: &[f32]) -> Vec\u003cf32>;\n    fn pow(&self, a: &[f32], power: f32) -> Vec\u003cf32>;\n    fn sqrt(&self, a: &[f32]) -> Vec\u003cf32>;\n    fn sum(&self, a: &[f32]) -> f32;\n    fn mean(&self, a: &[f32]) -> f32;\n    fn execute_compute(&self, _dimensions: [u32; 3]) -> MlResult\u003c()>;\n}\n\n#[derive(thiserror::Error, Debug)]\npub enum BackendError {\n    #[error(\"{0}\")]\n    CpuError(String),\n    #[error(\"{0}\")]\n    Other(String),\n}"},{"path":"src/backend/mod.rs","bytes":948,"preview":"use std::fmt::Debug;\n\n#[cfg(feature = \"builtinKernels\")]\npub use cpu::CpuBackend;\n#[cfg(all(test, feature = \"builtinKernels\"))]\npub(crate) use cpu::operations::{\n    approx_cos_value, approx_sin_value, conv2d_forward_data, group_norm_forward_data,\n    max_pool2d_forward_data, nearest_upsample2d_forward_data,\n};\n#[cfg(feature = \"builtinStorage\")]\n#[path = \"cpu/storage.rs\"]\nmod storage;\npub use device::{Device, DeviceType};\n#[cfg(feature = \"builtinStorage\")]\npub use storage::CpuTensorStore;\n\n#[cfg(feature = \"builtinKernels\")]\nmod cpu;\nmod device;\nmod feature;\n\n/// Device metadata plus the same fallible operation contract used by the runtime.\npub trait Backend: crate::contracts::OperationProvider + Debug + Send + Sync {\n    fn device(&self) -> DeviceType;\n    fn calc_device_flops(&self) -> f64;\n}\n\n#[derive(thiserror::Error, Debug)]\npub enum BackendError {\n    #[error(\"{0}\")]\n    CpuError(String),\n    #[error(\"{0}\")]\n    Other(String),\n}\n"},{"path":"src/comparison.rs","bytes":4372,"preview":"//! Visibility bridge to unchanged baseline model implementations.\npub use crate::legacy::nn::models::diffusion::Diffusion as ReferenceDiffusion;\nuse crate::legacy::{MlResult,var_with_label};\nuse crate::legacy::nn::{Layer,Linear,Model,Parameter,Sequential,Variable,Conv2D,GroupNorm,activation::{SiLU,SoftmaxOp}};\nuse crate::legacy::tensor::{AutogradFunction,GlobalFunction,GlobalTensor,Tensor,TensorBase,operators::{Add,Function,Matmul,Concat,Cos,Mul,NearestUpsample2d,ReshapeOp,Sin,Transpose}};\nuse std::fmt::Debug;\nuse tracing::info;\npub use crate::legacy::nn::models::diffusion::encoder;\npub use crate::legacy::nn::models::diffusion::embedding;\npub use crate::legacy::nn::models::diffusion::unet;\npub use crate::legacy::nn::models::diffusion::scheduler;\nuse encoder::SinusoidalPE;\nuse embedding::TimeEmbeddingMLP;\npub use crate::legacy::nn::models::linear;\nuse crate::legacy::loss::CrossEntropyLoss;\npub use crate::legacy::nn::models::mlp;\npub use crate::legacy::nn::models::autoregressive;\npub use crate::legacy::nn::models::semi_supervised;\npub use crate::legacy::nn::models::reinforcement;\npub fn clear_graph() { crate::legacy::tensor::ComputationGraph::reset_graph(); }\n\n/// Read the original DDPM's random choices after its unmodified forward call.\n/// The source image identifies q_sample's signal multiplier; the loss identifies\n/// the target noise. This does not reseed or modify the original computation.\npub fn diffusion_draw(\n    model: &ReferenceDiffusion,\n    image: &Variable,\n    loss: &Variable,\n) -> MlResult\u003c(usize, GlobalTensor\u003cf32>)> {\n    crate::legacy::tensor::COMPUTATION_GRAPH.with(|graph| {\n        let graph = graph.lock().map_err(|_| crate::legacy::MlError::StringError(\"legacy graph lock poisoned\".into()))?;\n        let mut signal_ids = Vec::new();\n        let mut noise_id = None;\n        graph.visit_nodes(|node| {\n            if node.operation == Some(\"Mul\") && node.inputs.first() == Some(&image.node_id()) && node.inputs.len() == 2 {\n                signal_ids.push(node.inputs[1]);\n            }\n            if node.id == loss.node_id() { noise_id = node.inputs.first().copied(); }\n        });\n        if signal_ids.len() != 1 {\n            return Err(crate::legacy::MlError::StringError(\"ambiguous DDPM signal multiplier\".into()));\n        }\n        let mut timestep = None;\n        let mut noise = None;\n        graph.visit_nodes(|node| {\n            if node.id == signal_ids[0] && node.tensor.data().len() == 1 {\n                let value = node.tensor.data()[0];\n                timestep = model.scheduler.sqrt_alpha_bars.iter().position(|v| v.to_bits() == value.to_bits());\n            }\n            if Some(node.id) == noise_id {\n                noise = Some(GlobalTensor::from_vec(node.tensor.data().to_vec(), node.tensor.shape()));\n            }\n        });\n        Ok((\n            timestep.ok_or_else(|| crate::legacy::MlError::StringError(\"DDPM timestep not found\".into()))?,\n            noise.ok_or_else(|| crate::legacy::MlError::StringError(\"DDPM noise not found\".into()))??,\n        ))\n    })\n}\n/// Read-only counters; no baseline algorithm or lifetime policy is changed.\npub fn statistics()->MlResult\u003c(usize,usize)> {\n    let tensors=crate::legacy::tensor::TENSOR_STORAGE.with(|storage|storage.try_borrow().map(|storage|storage.len()).map_err(|_|crate::legacy::MlError::StringError(\"legacy storage borrow conflict\".into())))?;\n    let nodes=crate::legacy::tensor::COMPUTATION_GRAPH.with(|graph|graph.lock().map(|graph|graph.node_map.len()).map_err(|_|crate::legacy::MlError::StringError(\"legacy graph lock poisoned\".into())))?;\n    Ok((tensors,nodes))\n}"},{"path":"src/contracts/errors.rs","bytes":6091,"preview":"use std::path::PathBuf;\nuse thiserror::Error;\n#[derive(Error, Debug, Clone)]\npub enum TensorError {\n    #[error(\"Invalid shape: expected {:?}, got {:?}\", expected, got)]\n    InvalidShape {\n        expected: Vec\u003cusize>,\n        got: Vec\u003cusize>,\n    },\n    #[error(\"Invalid data length: expected {}, got {}\", expected, got)]\n    InvalidDataLength { expected: usize, got: usize },\n    #[error(\"Invalid operation '{}': {}\", op, reason)]\n    InvalidOperation { op: &'static str, reason: String },\n    #[error(\"Invalid axis {} for tensor with shape {:?}\", axis, shape)]\n    InvalidAxis { axis: usize, shape: Vec\u003cusize> },\n    #[error(\n        \"Invalid dimensions for matrix multiplication: left shape {:?}, right shape {:?}\",\n        left_shape,\n        right_shape\n    )]\n    MatrixMultiplicationError {\n        left_shape: Vec\u003cusize>,\n        right_shape: Vec\u003cusize>,\n    },\n    #[error(\"InvalidInputCount: expected {:?}, got {:?}\", expected, got)]\n    InvalidInputCount { expected: i32, got: usize },\n    #[error(\"Empty tensor\")]\n    EmptyTensor,\n    #[error(\"Expected a scalar tensor, got shape {shape:?}\")]\n    NotScalar { shape: Vec\u003cusize> },\n    #[error(\"Invalid tensor index {indices:?} for shape {shape:?}\")]\n    InvalidIndex {\n        indices: Vec\u003cusize>,\n        shape: Vec\u003cusize>,\n    },\n}\n\n#[derive(Error, Debug, Clone, PartialEq, Eq)]\npub enum ContextError {\n    #[error(\"The execution context has already been dropped\")]\n    Dropped,\n    #[error(\"Tensors belong to different execution contexts\")]\n    Mismatch,\n    #[error(\"Tensor node {0:?} is not present in this execution context\")]\n    UnknownTensor(super::TensorId),\n    #[error(\"The execution context is already borrowed\")]\n    BorrowConflict,\n    #[error(\"A training scope cannot start while another scope or graph is active\")]\n    ActiveGraphConflict,\n}\n\n#[derive(Error, Debug, Clone, PartialEq, Eq)]\npub enum AutogradError {\n    #[error(\"backward() requires a scalar output, got shape {0:?}\")]\n    OutputNotScalar(Vec\u003cusize>),\n    #[error(\"Gradient shape mismatch: expected {expected:?}, got {got:?}\")]\n    GradientShapeMismatch {\n        expected: Vec\u003cusize>,\n        got: Vec\u003cusize>,\n    },\n    #[error(\"Operator '{0}' does not support backward\")]\n    BackwardNotSupported(String),\n    #[error(\"The computation graph for node {0:?} has already been freed\")]\n    GraphAlreadyFreed(super::TensorId),\n    #[error(\"Computation node {0:?} was not found\")]"},{"path":"src/contracts/mod.rs","bytes":9060,"preview":"//! Implementation-independent contracts for the single-threaded runtime.\n//!\n//! Providers own their implementation details. Buffers are validated at the\n//! execution boundary; views must not escape the callback that lends them.\nuse std::fmt::Debug;\nuse std::rc::Rc;\nmod errors;\npub use errors::*;\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord)]\npub struct TensorId(pub(crate) u64);\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord)]\npub struct ParameterId(pub(crate) u64);\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]\npub struct ContextId(pub(crate) u64);\n\n#[derive(Debug, Clone, PartialEq)]\npub struct TensorBuffer {\n    pub(crate) data: Vec\u003cf32>,\n    pub(crate) shape: Vec\u003cusize>,\n}\nimpl TensorBuffer {\n    pub fn from_vec(data: Vec\u003cf32>, shape: &[usize]) -> MlResult\u003cSelf> {\n        let expected = shape\n            .iter()\n            .try_fold(1usize, |n, &d| n.checked_mul(d))\n            .ok_or_else(|| TensorError::InvalidOperation {\n                op: \"buffer\",\n                reason: \"shape size overflow\".into(),\n            })?;\n        if data.len() != expected {\n            return Err(TensorError::InvalidDataLength {\n                expected,\n                got: data.len(),\n            }\n            .into());\n        }\n        Ok(Self {\n            data,\n            shape: shape.to_vec(),\n        })\n    }\n    pub fn data(&self) -> &[f32] {\n        &self.data\n    }\n    pub fn shape(&self) -> &[usize] {\n        &self.shape\n    }\n    pub fn numel(&self) -> usize {\n        self.data.len()\n    }\n    pub fn view(&self) -> TensorView\u003c'_> {\n        TensorView {\n            data: &self.data,\n            shape: &self.shape,\n        }\n    }\n    pub fn into_vec(self) -> Vec\u003cf32> {\n        self.data\n    }\n}\n\n#[derive(Debug, Clone, Copy)]\npub struct TensorView\u003c'a> {\n    pub(crate) data: &'a [f32],"},{"path":"src/legacy.rs","bytes":14401,"preview":"//! Native graph implementation, built in the same crate as P1.\n#[path = \"backend/legacy_mod.rs\"]\npub mod backend;\n#[path = \"loss/mod.rs\"]\npub mod loss;\n#[path = \"nn/legacy_mod.rs\"]\npub mod nn;\n#[path = \"optimizer/legacy_mod.rs\"]\npub mod optimizer;\n#[path = \"tensor/mod.rs\"]\npub mod tensor;\n#[path = \"trainer/legacy_mod.rs\"]\npub mod trainer;\npub use loss::Reduction;\n#[cfg(feature = \"enableVisualization\")]\n#[path = \"visualization/legacy_mod.rs\"]\npub mod visualization;\n#[path = \"tests/mod.rs\"]\npub mod tests;\n\nuse crate::legacy::backend::BackendError;\nuse crate::legacy::loss::LossError;\nuse crate::legacy::optimizer::OptimError;\nuse thiserror::Error;\n\n#[derive(Error, Debug, Clone)]\npub enum TensorError {\n    #[error(\"Invalid shape: expected {:?}, got {:?}\", expected, got)]\n    InvalidShape {\n        expected: Vec\u003cusize>,\n        got: Vec\u003cusize>,\n    },\n    #[error(\"Invalid data length: expected {}, got {}\", expected, got)]\n    InvalidDataLength { expected: usize, got: usize },\n    #[error(\"Invalid operation '{}': {}\", op, reason)]\n    InvalidOperation { op: &'static str, reason: String },\n    #[error(\"Invalid axis {} for tensor with shape {:?}\", axis, shape)]\n    InvalidAxis { axis: usize, shape: Vec\u003cusize> },\n    #[error(\n        \"Invalid dimensions for matrix multiplication: left shape {:?}, right shape {:?}\",\n        left_shape,\n        right_shape\n    )]\n    MatrixMultiplicationError {\n        left_shape: Vec\u003cusize>,\n        right_shape: Vec\u003cusize>,\n    },\n    #[error(\"InvalidInputCount: expected {:?}, got {:?}\", expected, got)]\n    InvalidInputCount { expected: i32, got: usize },\n    #[error(\"Empty tensor\")]\n    EmptyTensor,\n    #[error(\"Expected a scalar tensor, got shape {shape:?}\")]\n    NotScalar { shape: Vec\u003cusize> },\n    #[error(\"Invalid tensor index {indices:?} for shape {shape:?}\")]\n    InvalidIndex {\n        indices: Vec\u003cusize>,\n        shape: Vec\u003cusize>,\n    },\n}\n\n#[derive(Error, Debug, Clone, PartialEq, Eq)]\npub enum ContextError {\n    #[error(\"The execution context has already been dropped\")]\n    Dropped,\n    #[error(\"Tensors belong to different execution contexts\")]"},{"path":"src/lib.rs","bytes":809,"preview":"//! Explicit, composable tensor and training runtime.\npub mod backend;\npub mod contracts;\npub mod runtime;\npub use contracts::{\n    AutogradError, BackwardOp, ContextError, ContextId, CustomOp, DataError, LossError, MlError,\n    MlResult, OpOutput, OptimError, ParameterId, Reduction, TensorBuffer, TensorError, TensorId,\n    TensorView,\n};\npub use runtime::{\n    BackwardOptions, ExecutionContext, ExecutionContextBuilder, GraphStats, MaxResult, Parameter,\n    RequiresGrad, Tensor, TopKResult, Variable,\n    ExecutionRoute, RoutedContextBuilder,\n};\npub mod tensor {\n    pub use crate::{Tensor, TensorBuffer, TensorView};\n}\npub mod loss {\n    pub use crate::{LossError, Reduction};\n}\n#[cfg(feature = \"legacyBenchmark\")]\npub mod legacy;\npub mod nn;\npub mod optimizer;\npub mod trainer;\n\npub mod visualization;\n"},{"path":"src/loss/display.rs","bytes":499,"preview":"use super::*;\n\nimpl std::error::Error for LossError {}\n\nimpl Display for LossError {\n    fn fmt(&self, f: &mut Formatter\u003c'_>) -> std::fmt::Result {\n        match self {\n            LossError::InvalidShape { expected, got } => {\n                write!(f, \"Invalid shape: expected {:?}, got {:?}\", expected, got)\n            }\n            LossError::InvalidOperation { op, reason } => {\n                write!(f, \"Invalid operation: {} ({})\", op, reason)\n            }\n        }\n    }\n}"},{"path":"src/loss/function.rs","bytes":21058,"preview":"use super::*;\n\nimpl Function for MeanSquaredError {\n    fn new() -> MlResult\u003cGlobalFunction> {\n        register_operator!(MeanSquaredError)\n    }\n    fn forward(&self, targets: &[&dyn TensorBase]) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        let pred = targets[0];\n        let target = targets[1];\n\n        if pred.shape() != target.shape() {\n            return Err(LossError::InvalidShape {\n                expected: pred.shape().to_vec(),\n                got: target.shape().to_vec(),\n            }.into());\n        }\n\n        let n = pred.data().len() as f32;\n        let diff = pred.data().iter().zip(target.data().iter()).map(|(&p, &t)| p - t);\n        let squared_error = diff.map(|d| d * d).sum::\u003cf32>();\n\n        Ok(vec![scalar!(squared_error / n)])\n    }\n\n    #[cfg(all(feature = \"enableBackward\"))]\n    fn backward(&self, targets: &[&dyn TensorBase], grad: &dyn TensorBase) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        let pred = targets[0];\n        let target = targets[1];\n        let n = pred.data().len() as f32;\n        let grad_val = grad.data()[0];\n\n        let grad_pred_data: Vec\u003cf32> = pred.data()\n            .iter()\n            .zip(target.data().iter())\n            .map(|(&p, &t)| grad_val * 2.0 * (p - t) / n)\n            .collect();\n\n        let grad_target_data: Vec\u003cf32> = grad_pred_data.iter().map(|&g| -g).collect();\n\n        let grad_pred = GlobalTensor::from_vec(grad_pred_data, pred.shape())?;\n        let grad_target = GlobalTensor::from_vec(grad_target_data, target.shape())?;\n\n        Ok(vec![grad_pred, grad_target])\n    }\n\n    fn backend(&self) -> &Arc\u003cdyn Backend> {\n        &self.backend\n    }\n\n    fn node_id(&self) -> &NodeId {\n        &self.node_id\n    }\n}\n\n\nimpl Function for MeanAbsoluteError {\n    fn new() -> MlResult\u003cGlobalFunction> {\n        register_operator!(MeanAbsoluteError)\n    }\n    fn forward(&self, targets: &[&dyn TensorBase]) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        let pred = targets[0];\n        let target = targets[1];\n\n        if pred.shape() != target.shape() {\n            return Err(LossError::InvalidShape {"},{"path":"src/loss/mod.rs","bytes":2243,"preview":"#[path = \"display.rs\"]\nmod display;\n#[path = \"function.rs\"]\nmod function;\n\n/// Loss aggregation contract. Detailed high-rank and masking semantics are\n/// intentionally deferred to TODO(Phase-B1).\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub enum Reduction { Mean, Sum, None }\n\nimpl Default for Reduction {\n    fn default() -> Self {\n        Self::Mean\n    }\n}\n\n\nuse std::{\n    fmt::{Debug, Display, Formatter},\n    sync::Arc\n};\nuse crate::legacy::{\n    tensor::{\n        operators::{Function, Matmax, Sub},\n        AutogradFunction,\n        GlobalFunction,\n        NodeId,\n        Tensor,\n        TensorBase,\n        NODE_ID_GEN,\n        OPERATOR_STORAGE,\n        GlobalTensor\n    },\n    backend::{Backend, CpuBackend, Device},\n    register_operator,\n    scalar,\n    MlResult,\n    MlError,\n    TensorError::InvalidInputCount\n};\n\n// f32-representable probability protection, shared by forward and surrogate VJP.\nconst EPSILON: f32 = 1e-7;\n\n#[derive(Debug, Clone)]\npub enum LossError {\n    InvalidShape {\n        expected: Vec\u003cusize>,\n        got: Vec\u003cusize>,\n    },\n    InvalidOperation {\n        op: &'static str,\n        reason: String,\n    },\n}\n\npub struct MeanSquaredError {\n    backend: Arc\u003cdyn Backend>, node_id: NodeId,\n}\n\npub struct MeanAbsoluteError {\n    backend: Arc\u003cdyn Backend>, node_id: NodeId,\n}\n\npub struct HuberLoss {"},{"path":"src/native_provenance/BASELINE.json","bytes":14420},{"path":"src/native_provenance/CORRECTIONS.json","bytes":2393},{"path":"src/native_provenance/INTEGRATED.json","bytes":36692},{"path":"src/native_provenance/NUMERICAL_TRANSITION.json","bytes":1739},{"path":"src/native_provenance/README.md","bytes":1582},{"path":"src/nn/activation/identity.rs","bytes":763,"preview":"use super::*;\n\nimpl Function for IdentityOp {\n    fn new() -> MlResult\u003cGlobalFunction> {\n        register_operator!(IdentityOp)\n    }\n\n    fn forward(&self, targets: &[&dyn TensorBase]) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        let x = targets[0];\n        Ok(vec![GlobalTensor::from_vec(x.data().to_vec(), x.shape())?])\n    }\n\n    #[cfg(all(feature = \"enableBackward\"))]\n    fn backward(&self, _targets: &[&dyn TensorBase], grad: &dyn TensorBase) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        // dy/dx = 1, grad passes through unchanged\n        Ok(vec![GlobalTensor::from_vec(grad.data().to_vec(), grad.shape())?])\n    }\n\n    fn backend(&self) -> &Arc\u003cdyn Backend> { &self.backend }\n\n    fn node_id(&self) -> &NodeId { &self.node_id }\n}\n"},{"path":"src/nn/activation/mod.rs","bytes":2257,"preview":"#[path = \"sigmoid.rs\"]\npub mod sigmoid;\n#[path = \"tanh.rs\"]\npub mod tanh;\n#[path = \"relu.rs\"]\npub mod relu;\n#[path = \"softmax.rs\"]\npub mod softmax;\n#[path = \"identity.rs\"]\npub mod identity;\n#[path = \"silu.rs\"]\nmod silu;\n\nuse super::*;\n\npub trait Activation: Layer {}\n\nimpl\u003cT: Layer> Activation for T {}\n\n/// 활성화 함수의 연산자 구조체를 생성합니다.\nmacro_rules! define_activation_op {\n    ($name:ident) => {\n        #[derive(Debug, Clone)]\n        pub struct $name {\n            backend: Arc\u003cdyn Backend>,\n            node_id: NodeId,\n        }\n    };\n}\n\n/// 활성화 레이어의 struct 정의 + 생성자 + Layer impl 전체를 자동 생성합니다.\n/// 각 활성화 `.rs` 파일에는 `impl Function for $Op`(forward/backward)만 남습니다.\n///\n/// # 사용법\n/// ```ignore\n/// activation_layer!(SigmoidLayer, Sigmoid);\n/// ```\nmacro_rules! activation_layer {\n    ($layer:ident, $op:ident) => {\n        #[derive(Debug, Clone)]\n        pub struct $layer {\n            label: String,\n            operator: GlobalFunction,\n        }\n\n        impl $layer {\n            pub fn new(label: &str) -> MlResult\u003cSelf> {\n                Ok(Self { label: label.to_string(), operator: $op::new()? })\n            }\n        }\n\n        impl Layer for $layer {\n            #[cfg(all(feature = \"enableBackward\"))]\n            fn apply(&mut self, input: &Variable) -> MlResult\u003cVariable> {\n                self.operator.apply(&[input])\n            }\n\n            fn predict(&self, input: &dyn TensorBase) -> MlResult\u003cGlobalTensor\u003cf32>> {\n                Ok(self.operator.forward(&[input])?.remove(0))\n            }\n\n            fn params(&self) -> Vec\u003c&dyn Parameter> { vec![] }\n\n            fn label(&self) -> &str { &self.label }\n        }"},{"path":"src/nn/activation/relu.rs","bytes":1070,"preview":"use super::*;\n\nimpl Function for ReLUOp {\n    fn new() -> MlResult\u003cGlobalFunction> {\n        register_operator!(ReLUOp)\n    }\n\n    fn forward(&self, x: &[&dyn TensorBase]) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        let result = x[0].data().iter()\n            .map(|&val| if val > 0.0 { val } else { 0.0 })\n            .collect::\u003cVec\u003cf32>>();\n\n        Ok(vec![GlobalTensor::from_vec(result, x[0].shape())?])\n    }\n\n    #[cfg(all(feature = \"enableBackward\"))]\n    fn backward(&self, target: &[&dyn TensorBase], grad: &dyn TensorBase) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        let relu_output = target[0];\n\n        Ok(vec![\n            GlobalTensor::from_vec(\n                self.backend.multiply(\n                    &grad.data(),\n                    &relu_output.data().iter()\n                        .map(|&val| if val > 0.0 { 1.0 } else { 0.0 })\n                        .collect::\u003cVec\u003cf32>>()\n                ),\n                grad.shape()\n            )?\n        ])\n    }\n\n    fn node_id(&self) -> &NodeId { &self.node_id }\n}\n"},{"path":"src/nn/activation/sigmoid.rs","bytes":1754,"preview":"use super::*;\n\nimpl Function for SigmoidOp {\n    fn new() -> MlResult\u003cGlobalFunction> {\n        register_operator!(SigmoidOp)\n    }\n\n    fn forward(&self, targets: &[&dyn TensorBase]) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        let x = targets[0];\n        let ones = vec![1.0f32; x.data().len()];\n        Ok(vec![\n            GlobalTensor::from_vec(\n                self.backend.div(\n                    &ones,\n                    &self.backend.add(\n                        &ones,\n                        &self.backend.exp(\n                            &x.data().iter().map(|&v| -v).collect::\u003cVec\u003cf32>>()\n                        )\n                    )\n                ),\n                x.shape()\n            )?]\n        )\n    }\n\n    #[cfg(all(feature = \"enableBackward\"))]\n    fn backward(&self, targets: &[&dyn TensorBase], grad: &dyn TensorBase) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        let input_x = targets[0];\n        let neg_x_data = input_x.data().iter().map(|&v| -v).collect::\u003cVec\u003cf32>>();\n        let ones = vec![1.0f32; input_x.data().len()];\n        let exp_neg_x = self.backend.exp(&neg_x_data);\n        let one_plus_exp = self.backend.add(&ones, &exp_neg_x);\n        let sigmoid_output_data = self.backend.div(&ones, &one_plus_exp);\n\n        let derivative = self.backend.multiply(\n            &sigmoid_output_data,\n            &self.backend.sub(&ones, &sigmoid_output_data)\n        );\n\n        Ok(vec![\n            GlobalTensor::from_vec(\n                self.backend.multiply(&grad.data(), &derivative),\n                grad.shape()\n            )?\n        ])\n    }\n\n    fn backend(&self) -> &Arc\u003cdyn Backend> { &self.backend }\n    fn node_id(&self) -> &NodeId { &self.node_id }\n}\n"},{"path":"src/nn/activation/silu.rs","bytes":2025,"preview":"use super::*;\n\nimpl Function for SiLUOp {\n    fn new() -> MlResult\u003cGlobalFunction> {\n        register_operator!(SiLUOp)\n    }\n\n    fn forward(&self, targets: &[&dyn TensorBase]) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        let x = targets[0];\n        // Numerically stable sigmoid:\n        //   x >= 0 → σ = 1 / (1 + exp(-x))\n        //   x \u003c  0 → σ = exp(x) / (1 + exp(x))\n        let sigmoid: Vec\u003cf32> = x.data().iter().map(|&v| {\n            if v >= 0.0 {\n                1.0 / (1.0 + (-v).exp())\n            } else {\n                let e = v.exp();\n                e / (1.0 + e)\n            }\n        }).collect();\n        // SiLU(x) = x * σ(x)\n        Ok(vec![\n            GlobalTensor::from_vec(\n                self.backend.multiply(x.data(), &sigmoid),\n                x.shape()\n            )?\n        ])\n    }\n\n    #[cfg(all(feature = \"enableBackward\"))]\n    fn backward(&self, targets: &[&dyn TensorBase], grad: &dyn TensorBase) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        let x = targets[0];\n        // Numerically stable sigmoid (same as forward)\n        let sigmoid: Vec\u003cf32> = x.data().iter().map(|&v| {\n            if v >= 0.0 {\n                1.0 / (1.0 + (-v).exp())\n            } else {\n                let e = v.exp();\n                e / (1.0 + e)\n            }\n        }).collect();\n        // σ'(x) = σ(x) * (1 - σ(x))\n        let sigmoid_deriv: Vec\u003cf32> = sigmoid.iter().map(|&s| s * (1.0 - s)).collect();\n        // ∂SiLU/∂x = σ(x) + x * σ'(x)\n        let derivative = self.backend.add(\n            &sigmoid,\n            &self.backend.multiply(x.data(), &sigmoid_deriv)\n        );\n        // ∂L/∂x = grad * ∂SiLU/∂x\n        Ok(vec![\n            GlobalTensor::from_vec(\n                self.backend.multiply(grad.data(), &derivative),\n                grad.shape()\n            )?\n        ])\n    }\n\n    fn backend(&self) -> &Arc\u003cdyn Backend> { &self.backend }\n\n    fn node_id(&self) -> &NodeId { &self.node_id }\n}\n"},{"path":"src/nn/activation/softmax.rs","bytes":6276,"preview":"use super::*;\n\n/// axis 기준 row-wise softmax를 계산합니다.\n/// 음수 axis를 지원합니다 (axis=-1 → 마지막 축).\nfn softmax_along_axis(data: &[f32], shape: &[usize], axis: usize) -> Vec\u003cf32> {\n    let ndim = shape.len();\n    let axis_dim = shape[axis];\n    let outer_size: usize = shape[..axis].iter().product();\n    let inner_size: usize = shape[axis + 1..ndim].iter().product();\n\n    let mut out = vec![0.0f32; data.len()];\n\n    for outer in 0..outer_size {\n        for inner in 0..inner_size {\n            // axis 축을 따른 stride 기반 row 추출\n            let base = outer * axis_dim * inner_size + inner;\n\n            // max (수치 안정성)\n            let mut max_val = f32::NEG_INFINITY;\n            for k in 0..axis_dim {\n                let idx = base + k * inner_size;\n                if data[idx] > max_val { max_val = data[idx]; }\n            }\n\n            // exp + sum\n            let mut sum_exp = 0.0f32;\n            for k in 0..axis_dim {\n                let idx = base + k * inner_size;\n                let e = (data[idx] - max_val).exp();\n                out[idx] = e;\n                sum_exp += e;\n            }\n\n            // normalize\n            for k in 0..axis_dim {\n                let idx = base + k * inner_size;\n                out[idx] /= sum_exp;\n            }\n        }\n    }\n    out\n}\n\n/// axis 기준 softmax backward를 계산합니다.\n/// s = softmax output, g = upstream gradient\n/// ∂L/∂x_i = s_i * (g_i - dot(s, g))   (row 단위)\n#[cfg(feature = \"enableBackward\")]\nfn softmax_backward_along_axis(s: &[f32], g: &[f32], shape: &[usize], axis: usize) -> Vec\u003cf32> {\n    let ndim = shape.len();\n    let axis_dim = shape[axis];\n    let outer_size: usize = shape[..axis].iter().product();\n    let inner_size: usize = shape[axis + 1..ndim].iter().product();\n\n    let mut dx = vec![0.0f32; s.len()];\n\n    for outer in 0..outer_size {\n        for inner in 0..inner_size {\n            let base = outer * axis_dim * inner_size + inner;\n\n            // dot(s, g) for this row\n            let mut dot = 0.0f32;\n            for k in 0..axis_dim {\n                let idx = base + k * inner_size;\n                dot += s[idx] * g[idx];\n            }"},{"path":"src/nn/activation/tanh.rs","bytes":1286,"preview":"use super::*;\n\nimpl Function for TanhOp {\n    fn new() -> MlResult\u003cGlobalFunction> {\n        register_operator!(TanhOp)\n    }\n\n    fn forward(&self, targets: &[&dyn TensorBase]) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        let x = targets[0];\n\n        Ok(vec![\n            GlobalTensor::from_vec(\n                x.data().iter().map(|&value| value.tanh()).collect(),\n                x.shape()\n            )?\n        ])\n    }\n\n    #[cfg(all(feature = \"enableBackward\"))]\n    fn backward(&self, targets: &[&dyn TensorBase], grad: &dyn TensorBase) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        // The graph supplies the original input, so recompute y = tanh(x).\n        let outputs = self.forward(targets)?;\n        let tanh_output = &outputs[0];\n        let ones = vec![1.0f32; tanh_output.data().len()];\n\n        Ok(vec![\n            GlobalTensor::from_vec(\n                self.backend.multiply(\n                    &grad.data(),\n                    &self.backend.sub(\n                        &ones,\n                        &self.backend.multiply(&tanh_output.data(), &tanh_output.data())\n                    )\n                ),\n                grad.shape()\n            )?\n        ])\n    }\n\n    fn node_id(&self) -> &NodeId { &self.node_id }\n}\n"},{"path":"src/nn/checkpoint.rs","bytes":9479,"preview":"use crate::MlResult;\nuse serde::{Deserialize, Serialize};\nuse std::path::Path;\n\nconst MAGIC: &[u8; 8] = b\"TRNCHDP\\0\";\nconst FORMAT_VERSION: u32 = 1;\n\n// ── 직렬화 타입 ─────────────────────────────────────────────────────────────\n\n#[derive(Serialize, Deserialize, Debug, Clone)]\npub struct ModelState {\n    pub version: String,\n    pub layers: Vec\u003cLayerState>,\n}\n\n#[derive(Serialize, Deserialize, Debug, Clone)]\npub struct LayerState {\n    pub layer_type: String,\n    pub label: String,\n    pub config: serde_json::Value,\n    pub params: Vec\u003cParamState>,\n}\n\n#[derive(Serialize, Deserialize, Debug, Clone)]\npub struct ParamState {\n    pub name: String,\n    pub shape: Vec\u003cusize>,\n    /// JSON 포맷에서 실제 가중치 데이터. binary 포맷에서는 비어있음.\n    #[serde(default)]\n    pub data: Vec\u003cf32>,\n    /// binary(.tdw) 포맷 전용: blob 내 f32 시작 인덱스\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub blob_offset: Option\u003cu64>,\n    /// binary(.tdw) 포맷 전용: blob 내 f32 개수\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub blob_length: Option\u003cu64>,\n}\n\n// ── ModelState 저장/로드 ─────────────────────────────────────────────────────\n\nimpl ModelState {\n    pub fn new(layers: Vec\u003cLayerState>) -> Self {\n        Self {\n            version: FORMAT_VERSION.to_string(),\n            layers,\n        }\n    }\n\n    /// 확장자에 따라 JSON(.json) 또는 binary(.tdw)로 자동 저장\n    pub fn save(&self, path: &str) -> MlResult\u003c()> {\n        match Path::new(path).extension().and_then(|e| e.to_str()) {\n            Some(\"tdw\") => self.save_binary(path),\n            _ => self.save_json(path),\n        }\n    }\n\n    /// 확장자에 따라 JSON(.json) 또는 binary(.tdw)로 자동 로드\n    pub fn load(path: &str) -> MlResult\u003cSelf> {\n        match Path::new(path).extension().and_then(|e| e.to_str()) {\n            Some(\"tdw\") => Self::load_binary(path),\n            _ => Self::load_json(path),\n        }\n    }\n\n    // ── JSON ────────────────────────────────────────────────────────────────"},{"path":"src/nn/conv2d.rs","bytes":3664,"preview":"use super::*;\nuse crate::legacy::tensor::operators::Conv2dOp;\n\nlayer_params!(Conv2D, \"Conv2D\", [weight, bias], |s| serde_json::json!({\n    \"in_channels\":  s.in_channels,\n    \"out_channels\": s.out_channels,\n    \"kernel_h\": s.kernel_size.0, \"kernel_w\": s.kernel_size.1,\n    \"stride_h\": s.stride.0,      \"stride_w\": s.stride.1,\n    \"pad_h\":    s.padding.0,     \"pad_w\":    s.padding.1,\n}));\n\nimpl Conv2D {\n    /// 새로운 Conv 레이어를 생성.\n    ///\n    /// # Arguments\n    /// * `in_channels`  - 입력 채널 수\n    /// * `out_channels` - 출력 채널 수 (필터 수)\n    /// * `kernel_size`  - 커널 크기 (kH, kW)\n    /// * `stride`       - 스트라이드 (sH, sW)\n    /// * `padding`      - 패딩 크기 (pH, pW)\n    /// * `label`        - 레이어 레이블\n    pub fn new(\n        in_channels: usize,\n        out_channels: usize,\n        kernel_size: (usize, usize),\n        stride: (usize, usize),\n        padding: (usize, usize),\n        label: &str,\n    ) -> MlResult\u003cSelf> {\n        // Kaiming He 초기화 (fan_in = C_in * kH * kW)\n        let fan_in = in_channels * kernel_size.0 * kernel_size.1;\n        let k = 1.0 / (fan_in as f32).sqrt();\n        let w_size = out_channels * in_channels * kernel_size.0 * kernel_size.1;\n        let weight_data: Vec\u003cf32> = (0..w_size)\n            .map(|_| rand::random::\u003cf32>() * 2.0 * k - k)\n            .collect();\n        let weight_tensor = Tensor::from_vec(\n            weight_data,\n            &[out_channels, in_channels, kernel_size.0, kernel_size.1],\n        )?;\n        let bias_tensor = Tensor::from_vec(vec![0.0f32; out_channels], &[out_channels])?;\n\n        Ok(Self {\n            label: label.to_string(),\n            weight: var_weight!(weight_tensor),\n            bias: var_bias!(bias_tensor),\n            in_channels,\n            out_channels,\n            kernel_size,\n            stride,\n            padding,\n        })\n    }\n\n    fn stride_padding_scalars(&self) -> MlResult\u003c[GlobalTensor\u003cf32>; 4]> {\n        Ok([\n            GlobalTensor::from_vec(vec![self.stride.0  as f32], &[1, 1])?,\n            GlobalTensor::from_vec(vec![self.stride.1  as f32], &[1, 1])?,\n            GlobalTensor::from_vec(vec![self.padding.0 as f32], &[1, 1])?,\n            GlobalTensor::from_vec(vec![self.padding.1 as f32], &[1, 1])?,\n        ])\n    }\n}\n\nimpl Layer for Conv2D {"},{"path":"src/nn/diffusion_parameters.rs","bytes":3172,"preview":"use super::*;\n\nfn conv\u003c'a>(layer: &'a Conv2D, prefix: &str) -> Vec\u003c(String, &'a Parameter)> {\n    vec![\n        (format!(\"{prefix}.weight\"), layer.weight()),\n        (format!(\"{prefix}.bias\"), layer.bias()),\n    ]\n}\nfn linear\u003c'a>(layer: &'a Linear, prefix: &str) -> Vec\u003c(String, &'a Parameter)> {\n    vec![\n        (format!(\"{prefix}.weight\"), layer.weight()),\n        (format!(\"{prefix}.bias\"), layer.bias()),\n    ]\n}\nfn norm\u003c'a>(layer: &'a GroupNorm, prefix: &str) -> Vec\u003c(String, &'a Parameter)> {\n    vec![\n        (format!(\"{prefix}.gamma\"), layer.gamma()),\n        (format!(\"{prefix}.beta\"), layer.beta()),\n    ]\n}\nimpl Residual {\n    fn named_parameters(&self, prefix: &str) -> Vec\u003c(String, &Parameter)> {\n        let mut result = norm(&self.norm1, &format!(\"{prefix}.norm1\"));\n        result.extend(conv(&self.conv1, &format!(\"{prefix}.conv1\")));\n        result.extend(norm(&self.norm2, &format!(\"{prefix}.norm2\")));\n        result.extend(conv(&self.conv2, &format!(\"{prefix}.conv2\")));\n        result.extend(linear(&self.time, &format!(\"{prefix}.time\")));\n        if let Some(layer) = &self.skip {\n            result.extend(conv(layer, &format!(\"{prefix}.skip\")));\n        }\n        result\n    }\n}\nimpl Attention {\n    fn named_parameters(&self, prefix: &str) -> Vec\u003c(String, &Parameter)> {\n        let mut result = norm(&self.norm, &format!(\"{prefix}.norm\"));\n        for (name, layer) in [\n            (\"q\", &self.q),\n            (\"k\", &self.k),\n            (\"v\", &self.v),\n            (\"out\", &self.out),\n        ] {\n            result.extend(linear(layer, &format!(\"{prefix}.{name}\")));\n        }\n        result\n    }\n}\nimpl Stage {\n    fn named_parameters(&self, prefix: &str) -> Vec\u003c(String, &Parameter)> {\n        let mut result = self.first.named_parameters(&format!(\"{prefix}.first\"));\n        result.extend(self.second.named_parameters(&format!(\"{prefix}.second\")));\n        if let Some(layer) = &self.attention {\n            result.extend(layer.named_parameters(&format!(\"{prefix}.attention\")));\n        }\n        result.extend(conv(&self.resize, &format!(\"{prefix}.resize\")));\n        result\n    }\n}\nimpl Unet {\n    /// Structural parameter paths, independent of labels and enumeration order.\n    /// Repeated parameter identities are retained to expose weight sharing.\n    pub fn named_parameters(&self) -> Vec\u003c(String, &Parameter)> {\n        let mut result = conv(&self.initial, \"initial\");\n        result.extend(linear(&self.time1, \"time1\"));\n        result.extend(linear(&self.time2, \"time2\"));"},{"path":"src/nn/diffusion.rs","bytes":23362,"preview":"//! Time-conditioned residual U-Net using only the public tensor API.\n#[path = \"diffusion_parameters.rs\"]\nmod named_parameters;\nuse super::{Conv2D, GroupNorm, Layer, Linear};\nuse crate::trainer::{TrainableModel, UnsupervisedModel};\nuse crate::{ContextId, ExecutionContext, MlResult, Parameter, Tensor, TensorError, Variable};\nuse rand::{Rng, SeedableRng, rngs::StdRng};\n\nfn invalid(reason: &str) -> crate::MlError {\n    TensorError::InvalidOperation {\n        op: \"diffusion\",\n        reason: reason.into(),\n    }\n    .into()\n}\nfn conv(\n    ctx: &ExecutionContext,\n    input: usize,\n    output: usize,\n    kernel: usize,\n    stride: usize,\n    label: &str,\n) -> MlResult\u003cConv2D> {\n    Conv2D::new(\n        ctx,\n        input,\n        output,\n        (kernel, kernel),\n        (stride, stride),\n        (kernel / 2, kernel / 2),\n        label,\n    )\n}\n\n#[derive(Debug)]\nstruct Residual {\n    norm1: GroupNorm,\n    conv1: Conv2D,\n    norm2: GroupNorm,\n    conv2: Conv2D,\n    skip: Option\u003cConv2D>,\n    time: Linear,\n    channels: usize,\n}\nimpl Residual {\n    fn new(\n        ctx: &ExecutionContext,\n        input: usize,\n        output: usize,\n        groups: usize,\n        time: usize,\n    ) -> MlResult\u003cSelf> {\n        Ok(Self {\n            norm1: GroupNorm::new(ctx, groups, input, 1e-5, \"norm1\")?,\n            conv1: conv(ctx, input, output, 3, 1, \"conv1\")?,\n            norm2: GroupNorm::new(ctx, groups, output, 1e-5, \"norm2\")?,\n            conv2: conv(ctx, output, output, 3, 1, \"conv2\")?,\n            skip: if input != output {\n                Some(conv(ctx, input, output, 1, 1, \"skip\")?)\n            } else {\n                None\n            },\n            time: Linear::new(ctx, time, output, \"time\")?,\n            channels: output,\n        })"},{"path":"src/nn/group_norm.rs","bytes":2810,"preview":"use super::*;\nuse crate::legacy::tensor::operators::GroupNormOp;\n\nlayer_params!(GroupNorm, \"GroupNorm\", [gamma, beta], |s| serde_json::json!({\n    \"num_groups\":   s.num_groups,\n    \"num_channels\": s.num_channels,\n    \"eps\":          s.eps,\n}));\n\nimpl GroupNorm {\n    /// 새로운 GroupNorm 레이어를 생성합니다.\n    ///\n    /// # Arguments\n    /// * `num_groups`   - 채널을 나눌 그룹 수. `num_channels % num_groups == 0` 필요\n    /// * `num_channels` - 입력 채널 수 C\n    /// * `eps`          - 수치 안정성을 위한 소수 (기본값: 1e-5)\n    /// * `label`        - 레이어 레이블\n    pub fn new(\n        num_groups: usize,\n        num_channels: usize,\n        eps: f32,\n        label: &str,\n    ) -> MlResult\u003cSelf> {\n        if num_channels % num_groups != 0 {\n            return Err(MlError::StringError(format!(\n                \"GroupNorm: num_channels({}) % num_groups({}) != 0\",\n                num_channels, num_groups\n            )));\n        }\n\n        // γ = 1, β = 0 초기화\n        let gamma_tensor = Tensor::from_vec(vec![1.0f32; num_channels], &[num_channels])?;\n        let beta_tensor  = Tensor::from_vec(vec![0.0f32; num_channels], &[num_channels])?;\n\n        Ok(Self {\n            label: label.to_string(),\n            gamma: var_weight!(gamma_tensor),\n            beta:  var_bias!(beta_tensor),\n            num_groups,\n            num_channels,\n            eps,\n        })\n    }\n\n    fn param_scalars(&self) -> MlResult\u003c[GlobalTensor\u003cf32>; 2]> {\n        Ok([\n            GlobalTensor::from_vec(vec![self.num_groups as f32], &[1, 1])?,\n            GlobalTensor::from_vec(vec![self.eps],               &[1, 1])?,\n        ])\n    }\n}\n\nimpl Layer for GroupNorm {\n    #[cfg(all(feature = \"enableBackward\"))]\n    fn apply(&mut self, input: &Variable) -> MlResult\u003cVariable> {\n        let g_var   = Variable::new(Tensor::from_vec(vec![self.num_groups as f32], &[1, 1])?);\n        let eps_var = Variable::new(Tensor::from_vec(vec![self.eps],               &[1, 1])?);\n\n        let mut op = GroupNormOp::new()?;\n        op.apply_with_saved(&[input, &self.gamma, &self.beta, &g_var, &eps_var])\n    }\n\n    fn predict(&self, input: &dyn TensorBase) -> MlResult\u003cGlobalTensor\u003cf32>> {\n        let [g, e] = self.param_scalars()?;\n        let op = GroupNormOp::new()?;"},{"path":"src/nn/layers.rs","bytes":25342,"preview":"use crate::{ContextId, ExecutionContext, MlError, MlResult, Tensor, TensorError, Variable};\nuse crate::{Parameter, TensorBuffer};\n\nuse super::checkpoint::{find_param, validate_shape};\nuse super::{LayerState, ModelState, ParamState};\n\npub trait Layer: std::fmt::Debug {\n    fn forward(&self, input: &Variable) -> MlResult\u003cVariable>;\n    /// Compatibility entry point during the public API migration.\n    fn apply(&self, input: &Variable) -> MlResult\u003cVariable> {\n        self.validate_input(input.tensor())?;\n        self.forward(input)\n    }\n\n    fn predict(&self, input: &Tensor) -> MlResult\u003cTensor> {\n        self.validate_input(input)?;\n        input.execution_context()?.no_grad(|| {\n            let variable = input.as_variable()?;\n            let output = self.forward(&variable)?;\n            self.validate_input(output.tensor())?;\n            Ok(output.tensor().clone())\n        })\n    }\n\n    fn validate_input(&self, input: &Tensor) -> MlResult\u003c()> {\n        if input.context_id() != self.context_id() {\n            return Err(crate::ContextError::Mismatch.into());\n        }\n        input.numel()?;\n        for parameter in self.parameters() {\n            if parameter.context_id() != self.context_id() {\n                return Err(crate::ContextError::Mismatch.into());\n            }\n            parameter.tensor().numel()?;\n        }\n        Ok(())\n    }\n    fn parameters(&self) -> Vec\u003c&Parameter>;\n    fn context_id(&self) -> ContextId;\n    fn label(&self) -> &str;\n\n    fn save_state(&self) -> MlResult\u003cLayerState> {\n        if !self.parameters().is_empty() {\n            return Err(crate::MlError::UnsupportedCapability {\n                module: \"layer\",\n                capability: \"checkpoint save\",\n                operation: \"save_state\",\n            });\n        }\n        Ok(LayerState {\n            layer_type: std::any::type_name::\u003cSelf>()\n                .split(\"::\")\n                .last()\n                .unwrap_or(\"Layer\")\n                .trim_start_matches(\"Context\")\n                .to_string(),\n            label: self.label().to_string(),\n            config: serde_json::Value::Null,\n            params: Vec::new(),\n        })\n    }\n\n    fn load_state(&mut self, state: &LayerState) -> MlResult\u003c()> {\n        if !self.parameters().is_empty() || !state.params.is_empty() {\n            return Err(crate::MlError::UnsupportedCapability {"},{"path":"src/nn/legacy_checkpoint.rs","bytes":9817,"preview":"use serde::{Deserialize, Serialize};\nuse std::path::Path;\nuse crate::legacy::MlResult;\n\nconst MAGIC: &[u8; 8] = b\"TRNCHDP\\0\";\nconst FORMAT_VERSION: u32 = 1;\n\n// ── 직렬화 타입 ─────────────────────────────────────────────────────────────\n\n#[derive(Serialize, Deserialize, Debug, Clone)]\npub struct ModelState {\n    pub version: String,\n    pub layers: Vec\u003cLayerState>,\n}\n\n#[derive(Serialize, Deserialize, Debug, Clone)]\npub struct LayerState {\n    pub layer_type: String,\n    pub label: String,\n    pub config: serde_json::Value,\n    pub params: Vec\u003cParamState>,\n}\n\n#[derive(Serialize, Deserialize, Debug, Clone)]\npub struct ParamState {\n    pub name: String,\n    pub shape: Vec\u003cusize>,\n    /// JSON 포맷에서 실제 가중치 데이터. binary 포맷에서는 비어있음.\n    #[serde(default)]\n    pub data: Vec\u003cf32>,\n    /// binary(.tdw) 포맷 전용: blob 내 f32 시작 인덱스\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub blob_offset: Option\u003cu64>,\n    /// binary(.tdw) 포맷 전용: blob 내 f32 개수\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub blob_length: Option\u003cu64>,\n}\n\n// ── ModelState 저장/로드 ─────────────────────────────────────────────────────\n\nimpl ModelState {\n    pub fn new(layers: Vec\u003cLayerState>) -> Self {\n        Self {\n            version: FORMAT_VERSION.to_string(),\n            layers,\n        }\n    }\n\n    /// 확장자에 따라 JSON(.json) 또는 binary(.tdw)로 자동 저장\n    pub fn save(&self, path: &str) -> MlResult\u003c()> {\n        match Path::new(path).extension().and_then(|e| e.to_str()) {\n            Some(\"tdw\") => self.save_binary(path),\n            _           => self.save_json(path),\n        }\n    }\n\n    /// 확장자에 따라 JSON(.json) 또는 binary(.tdw)로 자동 로드\n    pub fn load(path: &str) -> MlResult\u003cSelf> {\n        match Path::new(path).extension().and_then(|e| e.to_str()) {\n            Some(\"tdw\") => Self::load_binary(path),\n            _           => Self::load_json(path),\n        }\n    }\n\n    // ── JSON ────────────────────────────────────────────────────────────────"},{"path":"src/nn/legacy_mod.rs","bytes":17677,"preview":"/// 파라미터가 있는 레이어의 체크포인트(save/load) 보일러플레이트를 자동 생성합니다.\n///\n/// # 사용법\n/// ```ignore\n/// layer_params!(Linear, \"Linear\", [weight, bias], |s| serde_json::json!({\n///     \"in_features\": s.weight.tensor().shape()[0],\n///     \"out_features\": s.weight.tensor().shape()[1],\n/// }));\n/// ```\n///\n/// `|s|` 는 `self`를 바인딩하는 식별자입니다 (매크로 위생 규칙 때문에 `self` 직접 사용 불가).\n///\n/// 생성되는 메서드: `_params()`, `_save_state()`, `_load_state()`\n/// Layer impl에서 위임하여 사용:\n/// ```ignore\n/// impl Layer for Linear {\n///     fn params(&self) -> Vec\u003c&dyn Parameter> { self._params() }\n///     fn save_state(&self) -> LayerState { self._save_state() }\n///     fn load_state(&mut self, state: &LayerState) -> MlResult\u003c()> { self._load_state(state) }\n/// }\n/// ```\nmacro_rules! layer_params {\n    ($layer:ty, $type_name:expr, [$($param:ident),+], |$s:ident| $config:expr) => {\n        impl $layer {\n            fn _params(&self) -> Vec\u003c&dyn Parameter> {\n                vec![$(&self.$param),+]\n            }\n\n            fn _save_state(&self) -> LayerState {\n                let $s = self;\n                LayerState {\n                    layer_type: $type_name.to_string(),\n                    label: $s.label.clone(),\n                    config: $config,\n                    params: vec![$(\n                        {\n                            let t = $s.$param.tensor();\n                            ParamState {\n                                name: stringify!($param).to_string(),\n                                shape: t.shape().to_vec(),\n                                data: t.data().to_vec(),\n                                blob_offset: None,\n                                blob_length: None,\n                            }\n                        },\n                    )+],\n                }\n            }\n\n            fn _load_state(&mut self, state: &LayerState) -> MlResult\u003c()> {\n                if state.layer_type != $type_name {\n                    return Err(MlError::StringError(format!(\n                        \"레이어 타입 불일치: 파일='{}', 현재='{}'\", state.layer_type, $type_name\n                    )));\n                }\n                $(\n                    let p = crate::legacy::nn::checkpoint::find_param(&state.params, stringify!($param))?;\n                    crate::legacy::nn::checkpoint::validate_shape(p, self.$param.tensor().shape())?;\n                    self.$param.tensor().replace(GlobalTensor::from_vec(p.data.clone(), &p.shape)?);\n                )+\n                Ok(())\n            }\n        }\n    };\n}"},{"path":"src/nn/linear.rs","bytes":2413,"preview":"use super::*;\n\nlayer_params!(Linear, \"Linear\", [weight, bias], |s| serde_json::json!({\n    \"in_features\":  s.weight.tensor().shape()[0],\n    \"out_features\": s.weight.tensor().shape()[1],\n}));\n\nimpl Linear {\n    /// 새로운 Linear 레이어를 생성합니다.\n    ///\n    /// # Arguments\n    /// * `in_features` - 입력 텐서의 특성(피처) 수\n    /// * `out_features` - 출력 텐서의 특성(피처) 수\n    /// * `label` - 시각화 및 디버깅을 위한 레이어의 이름\n    pub fn new(in_features: usize, out_features: usize, label: &str) -> MlResult\u003cSelf> {\n        // 가중치(W) 초기화: (in_features, out_features) 형태\n        // Kaiming He 초기화와 유사하게 표준편차를 조절하여 가중치를 초기화합니다.\n        let k = 1.0 / (in_features as f32).sqrt();\n        let weight_data: Vec\u003cf32> = (0..in_features * out_features)\n            .map(|_| rand::random::\u003cf32>() * 2.0 * k - k)\n            .collect();\n        let weight_tensor = Tensor::from_vec(weight_data, &[in_features, out_features])?;\n\n        // 편향(b) 초기화: (out_features) 형태\n        let bias_data = vec![0.0; out_features];\n        let bias_tensor = Tensor::from_vec(bias_data, &[out_features])?;\n\n        Ok(Self {\n            label: label.to_string(),\n            weight: var_weight!(weight_tensor),\n            bias: var_bias!(bias_tensor),\n        })\n    }\n}\n\n\nimpl Layer for Linear {\n    #[cfg(all(feature = \"enableBackward\"))]\n    fn apply(&mut self, input: &Variable) -> MlResult\u003cVariable> {\n        let mut matmul = Matmul::new()?;\n        // y = xW + b\n        let x = matmul.apply(&[input, &self.weight])?;\n        Ok(&x + &self.bias)\n    }\n\n    fn predict(&self, input: &dyn TensorBase) -> MlResult\u003cGlobalTensor\u003cf32>> {\n        let matmul = Matmul::new()?;\n        let add = Add::new()?;\n\n        let weight_tensor = self.weight.tensor();\n        let bias_tensor = self.bias.tensor();\n\n        let x = matmul.forward(&[input, weight_tensor])?.remove(0);\n        let output = add.forward(&[&x, bias_tensor])?.remove(0);\n\n        Ok(output)\n    }\n\n    fn params(&self) -> Vec\u003c&dyn Parameter> { self._params() }\n    fn label(&self) -> &str { &self.label }\n    fn save_state(&self) -> LayerState { self._save_state() }\n    fn load_state(&mut self, state: &LayerState) -> MlResult\u003c()> { self._load_state(state) }\n}\n"},{"path":"src/nn/mod.rs","bytes":330,"preview":"//! Layers use the public tensor contract and one forward implementation.\npub mod checkpoint;\nmod layers;\npub mod pilots;\npub use crate::{Parameter, Variable};\npub use checkpoint::{LayerState, ModelState, ParamState};\npub use layers::*;\npub use pilots::*;\n\nmod diffusion;\npub use diffusion::{Diffusion, DiffusionScheduler, Unet};\n"},{"path":"src/nn/native_models/autoregressive/mod.rs","bytes":6158,"preview":"//! 자기회귀학습 파일럿: **Bigram 언어모델**.\n//!\n//! 이 모듈은 Phase 1 에서 도입된 `AutoregressiveTrainer` + `AutoregressiveModel`\n//! 인터페이스가 end-to-end 로 동작함을 최소 예제로 증명한다.\n//!\n//! ## 구조\n//!\n//! - 어휘 크기 `V`, 시퀀스 길이 `L+1` (입력 `L` 토큰 + 타깃 `L` 토큰)\n//! - 파라미터: `W: [V, V]` 단일 전이 행렬(bigram transition logits)\n//! - 입력은 한 시퀀스당 `[L+1, V]` one-hot 텐서 하나로 패킹된다:\n//!   - `rows[0..L]`  → 입력 토큰\n//!   - `rows[1..L+1]` → 타깃 토큰 (shift-by-one)\n//! - 모델이 내부에서 입력/타깃을 분리한 뒤, 입력 leaf Variable 로부터\n//!   `logits = input @ W` 를 계산하고 `SoftmaxCrossEntropyLoss` 로 손실을 구한다.\n//!\n//! 이 수준의 모델은 *실제 LM* 이라기보다는 **AR 트레이너의 제어 흐름을\n//! 검증하는 도구**이다. 실 LM 은 임베딩 + RNN/Transformer 로 구현된다.\n\nuse super::*;\n\nuse crate::legacy::{\n    MlResult,\n    loss::SoftmaxCrossEntropyLoss,\n    nn::Variable,\n    tensor::{\n        GlobalFunction, GlobalTensor, Tensor, TensorBase,\n        operators::{Function, Matmul},\n    },\n    var_with_label,\n};\n\n// ────────────────────────────────────────────────────────────────────────────\n// BigramLM\n// ────────────────────────────────────────────────────────────────────────────\n\n/// Bigram 토이 언어모델.\npub struct BigramLM {\n    pub w: Variable,\n    loss_fn: GlobalFunction,\n    vocab: usize,\n}\n\nimpl BigramLM {\n    /// `vocab` 크기의 전이 행렬을 작은 무작위 값으로 초기화하여 구성한다.\n    pub fn new(vocab: usize) -> MlResult\u003cSelf> {\n        let loss_fn = SoftmaxCrossEntropyLoss::new()?;\n        let w_data: Vec\u003cf32> = (0..vocab * vocab)\n            .map(|_| (rand::random::\u003cf32>() - 0.5) * 0.2)\n            .collect();\n        let w = var_with_label!(Tensor::from_vec(w_data, &[vocab, vocab])?, \"bigram_w\");\n        Ok(Self { w, loss_fn, vocab })\n    }\n\n    /// 한 토큰 위치의 one-hot row 를 `[1, V]` Variable 로 만든다.\n    fn row_var(&self, data: &[f32], row: usize) -> MlResult\u003cVariable> {\n        let v = self.vocab;\n        let slice: Vec\u003cf32> = data[row * v..(row + 1) * v].to_vec();\n        Ok(Variable::new(Tensor::from_vec(slice, &[1, v])?))\n    }\n}\n\n// ────────────────────────────────────────────────────────────────────────────\n// AutoregressiveModel impl\n// ────────────────────────────────────────────────────────────────────────────\n"},{"path":"src/nn/native_models/diffusion/decoder.rs","bytes":113,"preview":"// TODO(LatentDiffusion): VAE 디코더 구현 예정. 삭제 금지.\n#[allow(dead_code)]\npub struct Decoder;\n"},{"path":"src/nn/native_models/diffusion/embedding.rs","bytes":7777,"preview":"use super::*;\n\n// ╔═══════════════════════════════════════════════════════════════════════════╗\n// ║  TimeEmbeddingMLP — 타임스텝 임베딩 모듈                                     ║\n// ║                                                                           ║\n// ║  DDPM 에서 U-Net 은 \"지금 몇 번째 노이즈 제거 단계인지\"를 알아야                ║\n// ║  각 단계에 맞는 노이즈를 예측할 수 있음.                                       ║\n// ║                                                                           ║\n// ║  ── 변환 과정 ──                                                           ║\n// ║                                                                           ║\n// ║  t (스칼라)                                                                ║\n// ║    ↓  SinusoidalPE                                                        ║\n// ║  [sin(ω₁t), sin(ω₂t), ..., cos(ω₁t), cos(ω₂t), ...]  ∈ ℝ^dim              ║\n// ║    ↓  Linear(dim → t_emb_dim)                                             ║\n// ║    ↓  SiLU (비선형성)                                                      ║\n// ║    ↓  Linear(t_emb_dim → t_emb_dim)                                       ║\n// ║  t_emb ∈ ℝ^{t_emb_dim}                                                    ║\n// ║                                                                           ║\n// ║  ── 왜 MLP 가 필요한가? ──                                                  ║\n// ║                                                                           ║\n// ║  SinusoidalPE 만으로는 고정된 주파수 패턴만 제공함.                            ║  \n// ║  MLP 를 거치면 모델이 학습 과정에서 각 ResNetBlock 에 최적화된                  ║\n// ║  시간 표현을 스스로 학습할 수 있음.                                           ║\n// ║                                                                           ║\n// ║  ── Shape 흐름 ──                                                          ║\n// ║                                                                           ║\n// ║  t [N, 1] → SinusoidalPE → [N, dim]                                       ║\n// ║           → Linear        → [N, t_emb_dim]                                ║\n// ║           → SiLU          → [N, t_emb_dim]                                ║\n// ║           → Linear        → [N, t_emb_dim]                                ║\n// ║                                                                           ║\n// ║  관례: t_emb_dim = dim × 4 (DDPM 논문)                                     ║\n// ║  dim 대비 4배 넓은 표현 공간을 사용하여 시간 정보의 표현력을 높임.               ║\n// ╚═══════════════════════════════════════════════════════════════════════════╝\n\n/// 타임스텝 임베딩 MLP.\n///\n/// `SinusoidalPE → Linear → SiLU → Linear` 파이프라인으로\n/// 스칼라 타임스텝을 풍부한 벡터 표현으로 변환함.\n///\n/// ## 사용처\n///\n/// U-Net 내부에서 각 ResNetBlock 에 시간 정보를 주입할 때 사용:\n///\n/// ```text\n/// t_emb = TimeEmbeddingMLP(t)\n/// h = ResNetBlock(x, t_emb)   ← h + MLP(t_emb)  형태로 주입\n/// ```\n#[derive(Debug)]\npub struct TimeEmbeddingMLP {\n    /// SinusoidalPE 의 출력 차원 (= MLP 입력 차원)\n    dim: usize,\n    /// MLP 출력 차원 (= t_emb_dim, 보통 dim × 4)\n    t_emb_dim: usize,\n    /// 내부 파이프라인: SinusoidalPE → Linear → SiLU → Linear\n    mlp: Sequential,\n}\n\nimpl TimeEmbeddingMLP {\n    /// TimeEmbeddingMLP 생성.\n    ///\n    /// ## 파라미터\n    ///\n    /// * `dim`       - SinusoidalPE 출력 차원 (U-Net 의 기본 채널 수와 동일)\n    /// * `t_emb_dim` - MLP 출력 차원 (보통 `dim × 4`)"},{"path":"src/nn/native_models/diffusion/encoder.rs","bytes":2477,"preview":"use super::*;\n\npub const MAX_PERIOD: f32 = 10000.0;\n\n// TODO(LatentDiffusion): VAE 인코더 구현 예정. 삭제 금지.\n#[allow(dead_code)]\npub struct Encoder;\n\n/// Sinusoidal Positional Embedding.\n///\n/// 입력 shape: `[B, 1]` (timestep 스칼라 배치).\n/// 출력 shape: `[B, dim]` (sin half + cos half concat).\n#[derive(Debug)]\npub struct SinusoidalPE {\n    inv_freq: Variable,\n    concat_axis: Variable, // Concat 연산자가 요구하는 axis 스칼라 ([1,1], value=1.0 = 마지막 축)\n    label: String,\n}\n\nimpl SinusoidalPE {\n    pub fn new(dim: usize, label: &str) -> MlResult\u003cSelf> {\n        let half_dim = dim / 2;\n        let data: Vec\u003cf32> = (0..half_dim)\n            .map(|i| (MAX_PERIOD).powf(2.0 * i as f32 / dim as f32).recip())\n            .collect();\n        Ok(SinusoidalPE {\n            inv_freq: Variable::new(Tensor::from_vec(data, &[1, half_dim])?),\n            concat_axis: Variable::new(Tensor::from_vec(vec![1.0], &[1, 1])?),\n            label: label.to_string(),\n        })\n    }\n}\n\nimpl Layer for SinusoidalPE {\n    #[cfg(all(feature = \"enableBackward\"))]\n    fn apply(&mut self, input: &Variable) -> MlResult\u003cVariable> {\n        let mut sin = Sin::new()?;\n        let mut cos = Cos::new()?;\n        let mut matmul = Matmul::new()?;\n        let mut concat = Concat::new()?;\n\n        // (B, 1) · (1, half) → (B, half)\n        let args = matmul.apply(&[input, &self.inv_freq])?;\n        let sin_features = sin.apply(&[&args])?;\n        let cos_features = cos.apply(&[&args])?;\n\n        // axis=1 로 concat → (B, dim)\n        concat.apply(&[&sin_features, &cos_features, &self.concat_axis])\n    }\n\n    fn predict(&self, input: &dyn TensorBase) -> MlResult\u003cGlobalTensor\u003cf32>> {\n        let sin = Sin::new()?;\n        let cos = Cos::new()?;\n        let matmul = Matmul::new()?;\n        let concat = Concat::new()?;\n\n        let args = matmul.forward(&[input, self.inv_freq.tensor()])?;\n        let sin_features = sin.forward(&[&args[0]])?;\n        let cos_features = cos.forward(&[&args[0]])?;\n\n        Ok(concat\n            .forward(&[&sin_features[0], &cos_features[0], self.concat_axis.tensor()])?\n            .remove(0))\n    }\n"},{"path":"src/nn/native_models/diffusion/mod.rs","bytes":16090,"preview":"#[path = \"encoder.rs\"]\npub mod encoder;\n#[path = \"decoder.rs\"]\nmod decoder;\n#[path = \"unet.rs\"]\npub mod unet;\n#[path = \"scheduler.rs\"]\npub mod scheduler;\n#[path = \"embedding.rs\"]\npub mod embedding;\n\nuse super::*; // info, MlResult, Layer, Linear, Sequential, ... (from model/mod.rs)\n\n// diffusion 하위 모듈 전용 import\nuse std::fmt::Debug;\nuse crate::legacy::{\n    nn::{Conv2D, GroupNorm, activation::{SiLU, SoftmaxOp}},\n    tensor::operators::{Concat, Cos, Mul, NearestUpsample2d, ReshapeOp, Sin, Transpose},\n};\nuse crate::legacy::loss::MeanSquaredError;\nuse crate::legacy::nn::models::diffusion::unet::Unet;\nuse self::embedding::TimeEmbeddingMLP;\nuse self::encoder::SinusoidalPE;\nuse self::scheduler::DDPMScheduler;\n// NOTE: `Decoder`, `Encoder`, `Scheduler` (wrapper) 스텁은 LatentDiffusion 구현 시\n//        사용될 예정이므로 제거하지 않는다. 현재는 dead import 방지를 위해 제외.\n\n// ╔═══════════════════════════════════════════════════════════════════════════╗\n// ║                     DDPM — Diffusion 모델                               ║\n// ║                                                                         ║\n// ║  \"Denoising Diffusion Probabilistic Models\" (Ho et al., 2020)           ║\n// ║                                                                         ║\n// ║  ── 전체 알고리즘 개요 ──                                                ║\n// ║                                                                         ║\n// ║  【학습 (Algorithm 1)】                                                  ║\n// ║    repeat:                                                              ║\n// ║      1. x₀ ~ q(x₀)              데이터에서 이미지 샘플링                ║\n// ║      2. t ~ Uniform({1,...,T})   랜덤 타임스텝                          ║\n// ║      3. ε ~ N(0, I)             가우시안 노이즈 생성                    ║\n// ║      4. x_t = √ᾱ_t·x₀ + √(1-ᾱ_t)·ε    forward process               ║\n// ║      5. L = ‖ε - ε_θ(x_t, t)‖²  U-Net 의 노이즈 예측과 실제 비교     ║\n// ║      6. ∇_θ L → optimizer step   gradient descent                      ║\n// ║    until converged                                                      ║\n// ║                                                                         ║\n// ║  【샘플링 (Algorithm 2)】                                                ║\n// ║    1. x_T ~ N(0, I)             순수 노이즈에서 시작                    ║\n// ║    2. for t = T, T-1, ..., 1:                                           ║\n// ║         z ~ N(0, I) if t > 1, else z = 0                               ║\n// ║         x_{t-1} = 1/√α_t · (x_t - β_t/√(1-ᾱ_t) · ε_θ(x_t,t))       ║\n// ║                   + √β̃_t · z                                           ║\n// ║    3. return x₀                  생성된 이미지!                         ║\n// ╚═══════════════════════════════════════════════════════════════════════════╝\n\n/// DDPM 디퓨전 모델.\n///\n/// 세 가지 컴포넌트로 구성\n/// - `unet`:      노이즈 예측 네트워크 ε_θ(x_t, t)\n/// - `scheduler`: noise schedule (β, ᾱ) 및 forward/reverse process\n/// - `loss`:      MSE loss ‖ε - ε_θ‖²\npub struct Diffusion {\n    pub unet: Unet,\n    pub scheduler: DDPMScheduler,\n    /// MSE loss 연산자 (Arc\u003cdyn Function>)\n    pub loss: GlobalFunction,\n    /// 입력 이미지 shape [C, H, W] (batch 제외)"},{"path":"src/nn/native_models/diffusion/scheduler.rs","bytes":8794,"preview":"use super::*;\n\n/// DDPM noise schedule 및 forward/reverse diffusion process.\n///\n/// Ho et al., \"Denoising Diffusion Probabilistic Models\" (2020) 기반.\n///\n/// ## 핵심 사전계산 값\n/// - `betas`:      β_1 … β_T  (linear schedule)\n/// - `alphas`:     α_t = 1 - β_t\n/// - `alpha_bars`: ᾱ_t = ∏_{s=1}^{t} α_s\n/// - `sqrt_alpha_bars`:         √ᾱ_t\n/// - `sqrt_one_minus_alpha_bars`: √(1 - ᾱ_t)\n/// - `posterior_variance`:      β̃_t = β_t · (1 - ᾱ_{t-1}) / (1 - ᾱ_t)\npub struct DDPMScheduler {\n    pub timesteps: usize,\n    pub betas: Vec\u003cf32>,\n    pub alphas: Vec\u003cf32>,\n    pub alpha_bars: Vec\u003cf32>,\n    pub sqrt_alpha_bars: Vec\u003cf32>,\n    pub sqrt_one_minus_alpha_bars: Vec\u003cf32>,\n    pub sqrt_recip_alphas: Vec\u003cf32>,\n    pub posterior_variance: Vec\u003cf32>,\n}\n\nimpl DDPMScheduler {\n    /// Linear β-schedule: β 가 `beta_start` 에서 `beta_end` 까지 선형 증가.\n    ///\n    /// DDPM 논문 기본값: beta_start=1e-4, beta_end=0.02, timesteps=1000.\n    pub fn linear_schedule(timesteps: usize, beta_start: f32, beta_end: f32) -> Self {\n        let betas: Vec\u003cf32> = (0..timesteps)\n            .map(|i| beta_start + (beta_end - beta_start) * i as f32 / (timesteps - 1).max(1) as f32)\n            .collect();\n\n        Self::from_betas(timesteps, betas)\n    }\n\n    /// Cosine β-schedule (Nichol & Dhariwal, 2021).\n    ///\n    /// ᾱ_t = f(t) / f(0), f(t) = cos²((t/T + s) / (1+s) · π/2)\n    pub fn cosine_schedule(timesteps: usize, s: f32) -> Self {\n        let f = |t: f32| -> f32 {\n            ((t / timesteps as f32 + s) / (1.0 + s) * std::f32::consts::FRAC_PI_2).cos().powi(2)\n        };\n        let f0 = f(0.0);\n\n        let mut alpha_bars = Vec::with_capacity(timesteps);\n        for t in 0..timesteps {\n            alpha_bars.push((f((t + 1) as f32) / f0).clamp(1e-4, 1.0));\n        }\n\n        // α_bar → β 역산: β_t = 1 - ᾱ_t / ᾱ_{t-1}\n        let mut betas = Vec::with_capacity(timesteps);\n        for t in 0..timesteps {\n            let alpha_bar_prev = if t == 0 { 1.0 } else { alpha_bars[t - 1] };\n            betas.push((1.0 - alpha_bars[t] / alpha_bar_prev).clamp(0.0, 0.999));\n        }\n\n        Self::from_betas(timesteps, betas)\n    }\n\n    /// β 벡터로부터 모든 사전계산 값을 도출한다.\n    fn from_betas(timesteps: usize, betas: Vec\u003cf32>) -> Self {\n        let alphas: Vec\u003cf32> = betas.iter().map(|&b| 1.0 - b).collect();\n\n        // ᾱ_t = cumulative product of α"},{"path":"src/nn/native_models/diffusion/unet.rs","bytes":62446,"preview":"use super::*;\n\n// ╔═══════════════════════════════════════════════════════════════════════════╗\n// ║                          U-Net for DDPM                                 ║\n// ║                                                                         ║\n// ║  DDPM (Ho et al., 2020) 의 노이즈 예측 네트워크 ε_θ(x_t, t).           ║\n// ║                                                                         ║\n// ║  ── 핵심 아이디어 ──                                                     ║\n// ║  디퓨전 모델은 깨끗한 이미지 x_0 에 점진적으로 가우시안 노이즈를 추가하여 ║\n// ║  x_T ≈ N(0, I) 를 만드는 forward process 와, 그 역방향으로 노이즈를      ║\n// ║  제거하여 이미지를 복원하는 reverse process 로 구성됨.                ║\n// ║                                                                         ║\n// ║  U-Net 의 역할은 reverse process 에서 \"현재 시점 t 의 이미지 x_t 에     ║\n// ║  어떤 노이즈가 섞여 있는지\"를 예측하는 것:                          ║\n// ║                                                                         ║\n// ║      ε_θ(x_t, t) ≈ ε   (실제로 추가된 노이즈)                           ║\n// ║                                                                         ║\n// ║  학습 목표:  L = E[‖ε - ε_θ(x_t, t)‖²]   (단순 MSE)                   ║\n// ║                                                                         ║\n// ║  ── U-Net 구조 ──                                                        ║\n// ║                                                                         ║\n// ║  입력 x_t ──→ init_conv ──→ [Down₁ → Down₂ → ... → Downₙ]             ║\n// ║                                   ↓ skip connections ↓                  ║\n// ║                               MidBlock (bottleneck)                     ║\n// ║                                   ↓ skip concat  ↓                     ║\n// ║              [Upₙ → ... → Up₂ → Up₁] ──→ final_conv ──→ ε_θ           ║\n// ║                                                                         ║\n// ║  time embedding t ──→ SinusoidalPE → MLP → 각 ResNetBlock에 주입        ║\n// ║                                                                         ║\n// ║  \"skip connection\" 이 U-Net의 핵심 특징.                           ║\n// ║  Down path 에서 생성된 feature map을 Up path 의 대응되는 해상도에        ║\n// ║  채널 방향으로 concat 하여, 고해상도 세부 정보를 보존.              ║\n// ╚═══════════════════════════════════════════════════════════════════════════╝\n\n// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n//  1. 구조체 정의 (Struct Definitions)\n// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n\n/// ── DownBlock ──────────────────────────────────────────────────────────────\n///\n/// U-Net 인코더(down path)의 한 단계.\n///\n/// ```text\n///  입력 [N, C_in, H, W]\n///   ├── ResNet₁(C_in → C_out) + t_emb 주입\n///   ├── ResNet₂(C_out → C_out) + t_emb 주입\n///   ├── (optional) SelfAttention(C_out)\n///   └── Downsample: stride-2 Conv2D → [N, C_out, H/2, W/2]\n/// ```\n///\n/// **왜 downsample 이 필요한가?**\n/// 공간 해상도를 점진적으로 줄이면서 채널 수를 늘려,\n/// 네트워크가 점점 더 추상적인 (global) 특징을 학습하게함.\n/// 이게 바로 \"coarse-to-fine\" 전략임\n#[derive(Debug)]\nstruct DownBlock {\n    resnet1:    ResNetBlock,\n    resnet2:    ResNetBlock,\n    attn:       Option\u003cSelfAttentionBlock>,\n    downsample: Conv2D,  // stride=2 conv → 공간 해상도 1/2\n}\n\n/// ── MidBlock (Bottleneck) ──────────────────────────────────────────────────\n///\n/// U-Net 의 가장 깊은 지점. 가장 낮은 해상도에서 전역적(global) 정보를 처리."},{"path":"src/nn/native_models/linear/linear.rs","bytes":3231,"preview":"use super::*;\n\nimpl LinearRegression {\n    pub fn build_model(n_input: usize, n_output: usize) -> MlResult\u003cLinearRegression> {\n        let activation = IdentityOp::new()?;\n        let loss_function = crate::legacy::loss::MeanSquaredError::new()?;\n\n        info!(\"Network Structure: {}(Input) -> {}(Output)\", n_input, n_output);\n        info!(\"Activation Functions: {} (Output)\", activation.name());\n\n        let sr = LinearRegression::new(&[n_input, n_output], activation, loss_function);\n        info!(\"linear regression model created successfully.\");\n        Ok(sr)\n    }\n\n    pub fn new(\n        layer_parms: &[usize],\n        activation: GlobalFunction,\n        loss_function: GlobalFunction,\n    ) -> Self {\n        let n_input = layer_parms[0];\n        let n_output = layer_parms[1];\n        // He 초기화 또는 Xavier 초기화와 같은 더 나은 가중치 초기화 방법을 고려할 수 있음\n        let w1_data: Vec\u003cf32> = (0..n_input * n_output)\n            .map(|_| (rand::random::\u003cf32>() - 0.5) * 0.5)\n            .collect();\n        let w1 = var_with_label!(\n            Tensor::from_vec(w1_data, &[n_input, n_output]).unwrap(),\n            \"weight_1\"\n        );\n\n        // bias: [n_output] — Add의 broadcasting으로 [1, n_output] + [n_output] 지원\n        let b1_data: Vec\u003cf32> = vec![0.0; n_output];\n        let b1 = var_with_label!(\n            Tensor::from_vec(b1_data, &[n_output]).unwrap(),\n            \"bias_1\"\n        );\n\n        Self { w1, b1, activation, loss_function }\n    }\n}\n\n\nimpl Model for LinearRegression {\n    #[cfg(feature = \"enableBackward\")]\n    fn apply(&mut self, x: &Variable) -> MlResult\u003cVariable> {\n        let mut matmul = Matmul::new()?;\n        // x [1, n_input] × w1 [n_input, n_output] = [1, n_output]\n        let linear_out = &matmul.apply(&[x, &self.w1])? + &self.b1;\n        self.activation.apply(&[&linear_out])\n    }\n\n    fn predict(&mut self, x: &dyn TensorBase) -> MlResult\u003cGlobalTensor\u003cf32>> {\n        let matmul = Matmul::new()?;\n        let add = Add::new()?;\n\n        let uh1_pre = matmul.forward(&[x, self.w1.tensor()])?.remove(0);\n        let uh1 = add.forward(&[&uh1_pre, self.b1.tensor()])?.remove(0);\n        let ah1 = self.activation.forward(&[&uh1])?.remove(0);\n\n        Ok(ah1)\n    }\n\n}\n"},{"path":"src/nn/native_models/linear/mod.rs","bytes":698,"preview":"use super::*;\n\nuse crate::legacy::nn::activation::IdentityOp;\n\n#[path = \"linear.rs\"]\npub mod linear;\n\npub struct LinearRegression {\n    pub w1: Variable,\n    pub b1: Variable,\n    activation: GlobalFunction,\n    loss_function: GlobalFunction,\n}\n\nimpl std::fmt::Debug for LinearRegression {\n    fn fmt(&self, f: &mut std::fmt::Formatter\u003c'_>) -> std::fmt::Result {\n        writeln!(f, \"LinearRegression {{\")?;\n        writeln!(f, \"  w1 = {:?}\", self.w1)?;\n        writeln!(f, \"  b1 = {:?}\", self.b1)?;\n        writeln!(f, \"  activation = {}\", self.activation.name())?;\n        writeln!(f, \"  loss_function = {}\", self.loss_function.name())?;\n        writeln!(f, \"}}\")\n    }\n}\n"},{"path":"src/nn/native_models/mlp/mlp.rs","bytes":2516,"preview":"use super::*;\n\nimpl MLP {\n    pub fn build_model(n_input: usize, n_hidden: usize, n_output: usize) -> MlResult\u003cMLP> {\n        let loss_function = CrossEntropyLoss::new()?;\n        info!(\"Network Structure: {}(Input) -> {}(Hidden) -> {}(Output)\", n_input, n_hidden, n_output);\n        let mlp = MLP::new(\n            &[n_input, n_hidden, n_output],\n            Box::new(crate::legacy::nn::activation::Sigmoid::new(\"hidden_act\")?),\n            Box::new(crate::legacy::nn::activation::Softmax::new(\"output_act\")?),\n            loss_function,\n        )?;\n        info!(\"MLP model created successfully.\");\n        Ok(mlp)\n    }\n\n    pub fn new(\n        layer_params: &[usize], // [n_input, n_hidden, n_output]\n        hidden_act: Box\u003cdyn Layer>,\n        output_act: Box\u003cdyn Layer>,\n        loss_function: GlobalFunction,\n    ) -> MlResult\u003cSelf> {\n        let layer = Sequential::from(vec![\n            Box::new(crate::legacy::nn::Linear::new(layer_params[0], layer_params[1], \"linear1\")?),\n            hidden_act,\n            Box::new(crate::legacy::nn::Linear::new(layer_params[1], layer_params[2], \"linear2\")?),\n            output_act,\n        ], \"MLP\");\n        Ok(Self { layer, loss_function })\n    }\n}\n\nimpl Model for MLP {\n    #[cfg(feature = \"enableBackward\")]\n    fn apply(&mut self, x: &Variable) -> MlResult\u003cVariable> {\n        self.layer.apply(x)\n    }\n\n    fn predict(&mut self, x: &dyn TensorBase) -> MlResult\u003cGlobalTensor\u003cf32>> {\n        self.layer.predict(x)\n    }\n\n}\n\n#[cfg(feature = \"enableBackward\")]\nimpl crate::legacy::trainer::SupervisedModel for MLP {\n    fn forward_loss(\n        &mut self,\n        x: &Variable,\n        t: &Variable,\n    ) -> MlResult\u003c(Variable, Variable)> {\n        let y = self.layer.apply(x)?;\n        let loss = self.loss_function.apply_with_label(&[&y, t], \"loss\")?;\n        Ok((y, loss))\n    }\n\n    fn predict_raw(\n        &mut self,\n        x: &dyn TensorBase,\n    ) -> MlResult\u003cGlobalTensor\u003cf32>> {\n        self.layer.predict(x)\n    }\n}\n\nimpl crate::legacy::trainer::TrainableModel for MLP {"},{"path":"src/nn/native_models/mlp/mod.rs","bytes":443,"preview":"use super::*;\n\n#[path = \"mlp.rs\"]\npub mod mlp;\n\npub struct MLP {\n    pub layer: Sequential,\n    loss_function: GlobalFunction,\n}\n\nimpl std::fmt::Debug for MLP {\n    fn fmt(&self, f: &mut std::fmt::Formatter\u003c'_>) -> std::fmt::Result {\n        writeln!(f, \"MLP {{\")?;\n        writeln!(f, \"  layer = {:?}\", self.layer)?;\n        writeln!(f, \"  loss_function = {}\", self.loss_function.name())?;\n        writeln!(f, \"}}\")\n    }\n}\n"},{"path":"src/nn/native_models/mod.rs","bytes":1242,"preview":"use crate::legacy::MlResult;\nuse tracing::info;\n\n// ── model 하위 모듈 공통 import ──────────────────────────────────────────────\nuse crate::legacy::{\n    loss::{CrossEntropyLoss, SoftmaxCrossEntropyLoss},\n    nn::{\n        activation::{Sigmoid, Softmax},\n        Layer,\n        Linear,\n        Model,\n        Parameter,\n        Sequential,\n        Variable,\n    },\n    tensor::{\n        AutogradFunction,\n        GlobalFunction,\n        GlobalTensor,\n        operators::{Add, Function, Matmul},\n        Tensor,\n        TensorBase,\n    },\n    var_with_label,\n};\n\n#[path = \"linear/mod.rs\"]\npub mod linear;\n#[path = \"nonlinear/mod.rs\"]\npub mod nonlinear;\n#[path = \"mlp/mod.rs\"]\npub mod mlp;\n#[path = \"diffusion/mod.rs\"]\npub mod diffusion;\n#[path = \"transformer/mod.rs\"]\npub mod transformer;\n#[path = \"semi_supervised/mod.rs\"]\npub mod semi_supervised;\n#[path = \"reinforcement/mod.rs\"]\npub mod reinforcement;\n#[path = \"autoregressive/mod.rs\"]\npub mod autoregressive;\n\npub use self::linear::LinearRegression;\npub use self::nonlinear::{LogisticRegression, SoftmaxRegression};\npub use self::mlp::MLP;\n"},{"path":"src/nn/native_models/nonlinear/logistic.rs","bytes":2829,"preview":"use super::*;\n\nimpl LogisticRegression {\n    pub fn build_model(n_input: usize, n_output: usize) -> MlResult\u003cLogisticRegression> {\n        let activation = SigmoidOp::new()?;\n        let loss_function = crate::legacy::loss::MeanSquaredError::new()?;\n\n        info!(\"Network Structure: {}(Input) -> {}(Output)\", n_input, n_output);\n        info!(\"Activation Functions: {} (Output)\", activation.name());\n\n        let sr = LogisticRegression::new(&[n_input, n_output], activation, loss_function);\n        info!(\"logistic regression model created successfully.\");\n        Ok(sr)\n    }\n\n    pub fn new(\n        layer_parms: &[usize],\n        activation: GlobalFunction,\n        loss_function: GlobalFunction,\n    ) -> Self {\n        let n_input = layer_parms[0];\n        let n_output = layer_parms[1];\n        let w1_data: Vec\u003cf32> = (0..n_input * n_output)\n            .map(|_| (rand::random::\u003cf32>() - 0.5) * 0.5)\n            .collect();\n        let w1 = var_with_label!(\n            Tensor::from_vec(w1_data, &[n_input, n_output]).unwrap(),\n            \"weight_1\"\n        );\n\n        let b1_data: Vec\u003cf32> = vec![0.0; n_output];\n        let b1 = var_with_label!(\n            Tensor::from_vec(b1_data, &[n_output]).unwrap(),\n            \"bias_1\"\n        );\n\n        Self { w1, b1, activation, loss_function }\n    }\n}\n\n\nimpl Model for LogisticRegression {\n    #[cfg(feature = \"enableBackward\")]\n    fn apply(&mut self, x: &Variable) -> MlResult\u003cVariable> {\n        let mut matmul = Matmul::new()?;\n        let linear_out = &matmul.apply(&[x, &self.w1])? + &self.b1;\n        self.activation.apply(&[&linear_out])\n    }\n\n    fn predict(&mut self, x: &dyn TensorBase) -> MlResult\u003cGlobalTensor\u003cf32>> {\n        let matmul = Matmul::new()?;\n        let add = Add::new()?;\n\n        let uh1_pre = matmul.forward(&[x, self.w1.tensor()])?.remove(0);\n        let uh1 = add.forward(&[&uh1_pre, self.b1.tensor()])?.remove(0);\n        let ah1 = self.activation.forward(&[&uh1])?.remove(0);\n\n        Ok(ah1)\n    }\n\n}\n\n#[cfg(feature = \"enableBackward\")]\nimpl crate::legacy::trainer::SupervisedModel for LogisticRegression {\n    fn forward_loss("},{"path":"src/nn/native_models/nonlinear/mod.rs","bytes":1356,"preview":"use super::*;\n\nuse crate::legacy::nn::activation::{SoftmaxOp, SigmoidOp};\n\n#[path = \"logistic.rs\"]\npub mod logistic;\n#[path = \"softmax.rs\"]\npub mod softmax;\n\npub struct LogisticRegression {\n    pub w1: Variable,\n    pub b1: Variable,\n    activation: GlobalFunction,\n    loss_function: GlobalFunction,\n}\n\npub struct SoftmaxRegression {\n    pub w1: Variable,\n    pub b1: Variable,\n    activation: GlobalFunction,\n    loss_function: GlobalFunction,\n}\n\nimpl std::fmt::Debug for LogisticRegression {\n    fn fmt(&self, f: &mut std::fmt::Formatter\u003c'_>) -> std::fmt::Result {\n        writeln!(f, \"LogisticRegression {{\")?;\n        writeln!(f, \"  w1 = {:?}\", self.w1)?;\n        writeln!(f, \"  b1 = {:?}\", self.b1)?;\n        writeln!(f, \"  activation = {}\", self.activation.name())?;\n        writeln!(f, \"  loss_function = {}\", self.loss_function.name())?;\n        writeln!(f, \"}}\")\n    }\n}\n\nimpl std::fmt::Debug for SoftmaxRegression {\n    fn fmt(&self, f: &mut std::fmt::Formatter\u003c'_>) -> std::fmt::Result {\n        writeln!(f, \"SoftmaxRegression {{\")?;\n        writeln!(f, \"  w1 = {:?}\", self.w1)?;\n        writeln!(f, \"  b1 = {:?}\", self.b1)?;\n        writeln!(f, \"  activation = {}\", self.activation.name())?;\n        writeln!(f, \"  loss_function = {}\", self.loss_function.name())?;\n        writeln!(f, \"}}\")\n    }\n}\n"},{"path":"src/nn/native_models/nonlinear/softmax.rs","bytes":2783,"preview":"use super::*;\n\nimpl SoftmaxRegression {\n    pub fn build_model(n_input: usize, n_output: usize) -> MlResult\u003cSoftmaxRegression> {\n        let activation = SoftmaxOp::new()?;\n        let loss_function = SoftmaxCrossEntropyLoss::new()?;\n\n        info!(\"Network Structure: {}(Input) -> {}(Output)\", n_input, n_output);\n        info!(\"Activation Functions: {} (Output)\", activation.name());\n\n        let sr = SoftmaxRegression::new(&[n_input, n_output], activation, loss_function);\n        info!(\"softmax regression model created successfully.\");\n        Ok(sr)\n    }\n\n    pub fn new(\n        layer_parms: &[usize],\n        activation: GlobalFunction,     \n        loss_function: GlobalFunction,\n    ) -> Self {\n        let n_input = layer_parms[0];\n        let n_output = layer_parms[1];\n        let w1_data: Vec\u003cf32> = (0..n_input * n_output)\n            .map(|_| (rand::random::\u003cf32>() - 0.5) * 0.5)\n            .collect();\n        let w1 = var_with_label!(\n            Tensor::from_vec(w1_data, &[n_input, n_output]).unwrap(),\n            \"weight_1\"\n        );\n\n        let b1_data: Vec\u003cf32> = vec![0.0; n_output];\n        let b1 = var_with_label!(\n            Tensor::from_vec(b1_data, &[n_output]).unwrap(),\n            \"bias_1\"\n        );\n\n        Self { w1, b1, activation, loss_function }\n    }\n}\n\n\nimpl Model for SoftmaxRegression {\n    #[cfg(feature = \"enableBackward\")]\n    fn apply(&mut self, x: &Variable) -> MlResult\u003cVariable> {\n        let mut matmul = Matmul::new()?;\n        let uh1_pre = matmul.apply(&[x, &self.w1])?;\n        Ok(&uh1_pre + &self.b1)\n    }\n\n    fn predict(&mut self, x: &dyn TensorBase) -> MlResult\u003cGlobalTensor\u003cf32>> {\n        let matmul = Matmul::new()?;\n        let add = Add::new()?;\n\n        let uh1_pre = matmul.forward(&[x, self.w1.tensor()])?.remove(0);\n        let uh1 = add.forward(&[&uh1_pre, self.b1.tensor()])?.remove(0);\n        let ah1 = self.activation.forward(&[&uh1])?.remove(0);\n\n        Ok(ah1)\n    }\n\n}\n\n#[cfg(feature = \"enableBackward\")]\nimpl crate::legacy::trainer::SupervisedModel for SoftmaxRegression {\n    fn forward_loss("},{"path":"src/nn/native_models/reinforcement/mod.rs","bytes":5206,"preview":"//! 강화학습 파일럿: **2-armed stochastic bandit** + **선형 정책**.\n//!\n//! P5 의 `RLTrainer` + `Environment` + `RLModel` 인터페이스가 end-to-end 로\n//! 동작함을 최소 예제로 증명한다.\n//!\n//! ## 환경: `TwoArmedBandit`\n//!\n//! - 상태: 고정 [1.0] (contextless)\n//! - 행동 0: 평균 보상 `0.2`\n//! - 행동 1: 평균 보상 `0.8`\n//! - 보상: 평균 ± 잡음 (`uniform(-0.1, 0.1)`)\n//! - 에피소드 길이: 1 스텝\n//!\n//! 최적 정책은 `action=1` 을 항상 선택. 학습 후 정책이 그쪽으로 치우치는지 확인.\n//!\n//! ## 정책: `LinearPolicy`\n//!\n//! `logits = Wx + b`, shape `[1] → [2]`.\n//! Softmax 는 트레이너가 롤아웃 단계에서 자체 수행.\n\nuse super::*;\n\nuse crate::legacy::{\n    nn::Variable,\n    tensor::{\n        operators::{Add, Function, Matmul},\n        AutogradFunction,\n        GlobalTensor,\n        Tensor,\n        TensorBase,\n    },\n    trainer::{Environment, StepResult},\n    var_with_label,\n    MlResult,\n};\n\n// ────────────────────────────────────────────────────────────────────────────\n// TwoArmedBandit\n// ────────────────────────────────────────────────────────────────────────────\n\n/// 2-arm 확률적 밴딧 환경. 최적 행동은 `1` (평균 보상 0.8).\npub struct TwoArmedBandit {\n    pub mean_rewards: [f32; 2],\n    pub noise_scale:  f32,\n}\n\nimpl Default for TwoArmedBandit {\n    fn default() -> Self {\n        Self { mean_rewards: [0.2, 0.8], noise_scale: 0.1 }\n    }\n}\n\nimpl Environment for TwoArmedBandit {\n    fn reset(&mut self) -> MlResult\u003cTensor> {\n        Tensor::from_vec(vec![1.0], &[1, 1])\n    }\n\n    fn step(&mut self, action: usize) -> MlResult\u003cStepResult> {\n        let base  = self.mean_rewards.get(action).copied().unwrap_or(0.0);\n        let noise = (rand::random::\u003cf32>() - 0.5) * 2.0 * self.noise_scale;\n        Ok(StepResult {\n            next_observation: Tensor::from_vec(vec![1.0], &[1, 1])?,\n            reward:           base + noise,\n            done:             true, // 1-step bandit\n        })"},{"path":"src/nn/native_models/semi_supervised/mod.rs","bytes":6389,"preview":"//! 반지도학습 파일럿: **Pi-model 스타일 이진 분류기**.\n//!\n//! 이 모듈은 P4 에서 도입된 `SemiSupervisedTrainer` + `SemiSupervisedModel`\n//! 인터페이스가 end-to-end 로 동작함을 최소 예제로 증명한다.\n//!\n//! ## 구조\n//!\n//! - 단층 선형 모델: `y = Wx + b`, `(n_input=2) → (n_output=2)` 이진 분류\n//! - 지도 손실: `SoftmaxCrossEntropyLoss(y_l, t_l)`\n//! - 일관성 손실: `MSE(f(x_u + ε₁), f(x_u + ε₂))` — Pi-model (Laine & Aila, 2017)\n//! - 총 손실: `sup + λ · con` (λ 는 트레이너의 `ConsistencyRamp` 로 결정)\n//!\n//! 실제 논문은 dropout/augmentation 기반 stochastic forward pass 를 쓰지만,\n//! 여기서는 **입력 가우시안 노이즈** 로 대체해 의존성을 최소화했다.\n\nuse super::*;\n\nuse crate::legacy::{\n    loss::{MeanSquaredError, SoftmaxCrossEntropyLoss},\n    nn::Variable,\n    tensor::{\n        operators::{Mul, Function},\n        GlobalFunction,\n        GlobalTensor,\n        Tensor,\n        TensorBase,\n    },\n    var_with_label,\n    MlResult,\n};\n\n// ────────────────────────────────────────────────────────────────────────────\n// PiToyClassifier\n// ────────────────────────────────────────────────────────────────────────────\n\n/// 반지도학습 파일럿용 장난감 이진 분류기.\npub struct PiToyClassifier {\n    pub w1: Variable,\n    pub b1: Variable,\n    sup_loss: GlobalFunction,\n    con_loss: GlobalFunction,\n    noise_scale: f32,\n}\n\nimpl PiToyClassifier {\n    /// `(n_input → n_output)` 선형층 + Pi-model 일관성 손실용 MSE 를 구성.\n    pub fn new(n_input: usize, n_output: usize, noise_scale: f32) -> MlResult\u003cSelf> {\n        let sup_loss = SoftmaxCrossEntropyLoss::new()?;\n        let con_loss = MeanSquaredError::new()?;\n\n        let w1_data: Vec\u003cf32> = (0..n_input * n_output)\n            .map(|_| (rand::random::\u003cf32>() - 0.5) * 0.5)\n            .collect();\n        let w1 = var_with_label!(\n            Tensor::from_vec(w1_data, &[n_input, n_output])?,\n            \"pi_weight\"\n        );\n        let b1_data: Vec\u003cf32> = vec![0.0; n_output];\n        let b1 = var_with_label!(\n            Tensor::from_vec(b1_data, &[n_output])?,\n            \"pi_bias\"\n        );\n\n        Ok(Self { w1, b1, sup_loss, con_loss, noise_scale })\n    }"},{"path":"src/nn/native_models/transformer/mod.rs","bytes":37,"preview":"use super::*;\n\npub struct GPT2 {}\n"},{"path":"src/nn/parameter.rs","bytes":15135,"preview":"use super::*;\nuse crate::legacy::tensor::GlobalTensor;\n\n// Variable in-place operator overloading (no graph registration, for parameter updates)\nimpl std::ops::SubAssign\u003cGlobalTensor\u003cf32>> for Variable {\n    fn sub_assign(&mut self, rhs: GlobalTensor\u003cf32>) {\n        Sub::new()\n            .unwrap()\n            .assign_forward(&[self.tensor(), &rhs], self.node_id())\n            .unwrap();\n    }\n}\n\nimpl std::ops::AddAssign\u003cGlobalTensor\u003cf32>> for Variable {\n    fn add_assign(&mut self, rhs: GlobalTensor\u003cf32>) {\n        Add::new()\n            .unwrap()\n            .assign_forward(&[self.tensor(), &rhs], self.node_id())\n            .unwrap();\n    }\n}\n\nimpl Parameter for Variable {\n    fn new(tensor: Tensor) -> Self {\n        Variable {\n            #[cfg(feature = \"enableVisualization\")]\n            label: None,\n            #[cfg(feature = \"enableVisualization\")]\n            node_type: None,\n            tensor,\n            requires_grad: false.into(),\n            //  zeros_like() → new_empty()\n            //   zeros_like(): shape 크기만큼 Vec\u003cf32> 힙 할당 + TENSOR_STORAGE insert\n            //   new_empty() : data=[], shape=[] — capacity=0, 힙 할당 없음\n            grad: Tensor::new_empty(),\n        }\n    }\n\n    fn node_id(&self) -> NodeId {\n        self.tensor.id()\n    }\n\n    fn tensor(&self) -> &Tensor {\n        &self.tensor\n    }\n    fn is_retain_grad(&self) -> bool {\n        self.requires_grad.get()\n    }\n\n    fn retain_grad(&self) {\n        // retain_grad는 학습 전 명시적으로 grad 버퍼를 미리 할당할 때 사용.\n        // zeros로 초기화해 버퍼를 확보하되, dirty는 건드리지 않음.\n        //   - dirty=false 상태에서 버퍼가 비어있지 않으면 accumulate_grad가\n        //     in-place 덧셈 경로를 타므로 동작상 문제 없다.\n        //   - clear_grad는 dirty=true일 때만 제로화하므로,\n        //     retain_grad만 호출하고 backward를 돌리지 않으면 clear_grad는 no-op.\n        self.requires_grad.replace(true);\n        self.grad.replace(GlobalTensor::zeros(self.tensor.shape()));\n    }\n\n    fn grad(&self) -> &Tensor {\n        &self.grad\n    }\n\n    #[cfg(feature = \"enableBackward\")]"},{"path":"src/nn/pilots/autoregressive.rs","bytes":3195,"preview":"//! Explicit-context bigram language-model pilot.\n\nuse crate::loss::Reduction;\nuse crate::nn::Parameter;\nuse crate::trainer::{AutoregressiveModel, TrainableModel};\nuse crate::{ContextId, ExecutionContext, MlError, MlResult, Variable};\n\n#[derive(Debug)]\npub struct BigramLm {\n    context: ExecutionContext,\n    weight: Parameter,\n    vocab: usize,\n}\n\nimpl BigramLm {\n    pub fn new(context: &ExecutionContext, vocab: usize) -> MlResult\u003cSelf> {\n        if vocab == 0 {\n            return Err(MlError::StringError(\"vocabulary must not be empty\".into()));\n        }\n        let values = context.initialization_uniform(\n            vocab\n                .checked_mul(vocab)\n                .ok_or_else(|| MlError::StringError(\"vocabulary dimension overflow\".into()))?,\n            0.1,\n        )?;\n        Ok(Self {\n            context: context.clone(),\n            weight: context.parameter(values, &[vocab, vocab])?,\n            vocab,\n        })\n    }\n\n    pub fn weight(&self) -> &Parameter {\n        &self.weight\n    }\n}\n\nimpl TrainableModel for BigramLm {\n    fn context_id(&self) -> ContextId {\n        self.context.id()\n    }\n    fn parameters(&self) -> Vec\u003c&Parameter> {\n        vec![&self.weight]\n    }\n}\n\nimpl AutoregressiveModel for BigramLm {\n    fn forward_loss(&mut self, sequence: &Variable) -> MlResult\u003c(Variable, Variable, usize)> {\n        let shape = sequence.tensor().shape()?;\n        let (batch, length) = match shape.as_slice() {\n            [length, vocab] if *vocab == self.vocab => (1, *length),\n            [batch, length, vocab] if *vocab == self.vocab => (*batch, *length),\n            _ => {\n                return Err(MlError::StringError(\n                    \"bigram input must have shape [sequence, vocab] or [batch, sequence, vocab]\"\n                        .into(),\n                ));\n            }\n        };\n        if batch == 0 || length \u003c 2 {\n            return Err(MlError::StringError(\n                \"bigram input requires a non-empty batch and sequence length >= 2\".into(),\n            ));\n        }\n        let data = sequence.tensor().to_vec()?;"},{"path":"src/nn/pilots/reinforcement.rs","bytes":3357,"preview":"//! Explicit-context two-armed-bandit and linear-policy pilot.\n\nuse crate::nn::{Layer, Linear, Parameter};\nuse crate::tensor::TensorBuffer;\nuse crate::trainer::{Environment, RLModel, StepResult, TrainableModel};\nuse crate::{ContextId, ExecutionContext, MlResult, Tensor, Variable};\nuse rand::{Rng, SeedableRng, rngs::StdRng};\n\npub struct TwoArmedBandit {\n    pub mean_rewards: [f32; 2],\n    pub noise_scale: f32,\n    rng: StdRng,\n}\n\nimpl Default for TwoArmedBandit {\n    fn default() -> Self {\n        Self {\n            mean_rewards: [0.2, 0.8],\n            noise_scale: 0.1,\n            rng: StdRng::seed_from_u64(0),\n        }\n    }\n}\nimpl TwoArmedBandit {\n    pub fn with_seed(mut self, seed: u64) -> Self {\n        self.rng = StdRng::seed_from_u64(seed);\n        self\n    }\n}\n\nimpl Environment for TwoArmedBandit {\n    fn reset(&mut self) -> MlResult\u003cTensorBuffer> {\n        TensorBuffer::from_vec(vec![1.0], &[1, 1])\n    }\n\n    fn step(&mut self, action: usize) -> MlResult\u003cStepResult> {\n        let mean = self.mean_rewards.get(action).copied().ok_or_else(|| {\n            crate::TensorError::InvalidOperation {\n                op: \"bandit\",\n                reason: \"invalid action\".into(),\n            }\n        })?;\n        let reward = mean + (self.rng.random::\u003cf32>() - 0.5) * 2.0 * self.noise_scale;\n        Ok(StepResult {\n            next_observation: TensorBuffer::from_vec(vec![1.0], &[1, 1])?,\n            reward,\n            done: true,\n        })\n    }\n\n    fn num_actions(&self) -> usize {\n        2\n    }\n    fn observation_shape(&self) -> Vec\u003cusize> {\n        vec![1, 1]\n    }\n}\n\n#[derive(Debug)]\npub struct LinearPolicy {\n    context: ExecutionContext,\n    linear: Linear,\n}\n\nimpl LinearPolicy {"},{"path":"src/nn/pilots/semi_supervised.rs","bytes":4042,"preview":"//! Explicit-context Pi-model style semi-supervised pilot.\n\nuse crate::loss::Reduction;\nuse crate::nn::{Layer, Linear, Parameter};\nuse crate::trainer::{SemiSupervisedModel, TrainableModel};\nuse crate::{ContextId, ExecutionContext, MlResult, Tensor, Variable};\n\n#[derive(Debug)]\npub struct PiClassifier {\n    context: ExecutionContext,\n    linear: Linear,\n    noise_scale: f32,\n}\n\nimpl PiClassifier {\n    pub fn new(\n        context: &ExecutionContext,\n        inputs: usize,\n        outputs: usize,\n        noise_scale: f32,\n    ) -> MlResult\u003cSelf> {\n        if !noise_scale.is_finite() || noise_scale \u003c 0.0 {\n            return Err(crate::TensorError::InvalidOperation {\n                op: \"pi_model\",\n                reason: \"noise_scale must be finite and non-negative\".into(),\n            }\n            .into());\n        }\n        Ok(Self {\n            context: context.clone(),\n            linear: Linear::new(context, inputs, outputs, \"pi_linear\")?,\n            noise_scale,\n        })\n    }\n\n    fn noisy(&self, input: &Variable) -> MlResult\u003cVariable> {\n        let shape = input.tensor().shape()?;\n        let noise = self\n            .context\n            .model_uniform(input.tensor().numel()?, self.noise_scale)?;\n        let noise = self.context.input(noise, &shape)?;\n        input.add(noise.tensor())\n    }\n\n    pub fn predict(&self, input: &Tensor) -> MlResult\u003cTensor> {\n        self.linear.predict(input)\n    }\n}\n\nimpl TrainableModel for PiClassifier {\n    fn context_id(&self) -> ContextId {\n        self.context.id()\n    }\n    fn parameters(&self) -> Vec\u003c&Parameter> {\n        self.linear.parameters()\n    }\n}\n\nimpl SemiSupervisedModel for PiClassifier {\n    fn forward_loss(\n        &mut self,\n        labeled_input: &Variable,\n        labeled_target: &Tensor,\n        unlabeled_input: &Variable,\n        lambda: f32,"},{"path":"src/nn/pilots.rs","bytes":3471,"preview":"//! Small explicit-context models used as P1 migration and E2E benchmark pilots.\n\nmod autoregressive;\nmod reinforcement;\nmod semi_supervised;\n\npub use autoregressive::BigramLm;\npub use reinforcement::{LinearPolicy, TwoArmedBandit};\npub use semi_supervised::PiClassifier;\n\nuse crate::loss::Reduction;\nuse crate::trainer::{SupervisedModel, TrainableModel};\nuse crate::{ContextId, ExecutionContext, MlResult, Tensor, Variable};\n\nuse super::{Activation, ActivationKind, Layer, Linear, Parameter, Sequential};\n\n#[derive(Debug)]\npub struct LinearRegression {\n    context: ExecutionContext,\n    layer: Linear,\n}\n\nimpl LinearRegression {\n    pub fn new(context: &ExecutionContext, inputs: usize, outputs: usize) -> MlResult\u003cSelf> {\n        Ok(Self {\n            context: context.clone(),\n            layer: Linear::new(context, inputs, outputs, \"linear\")?,\n        })\n    }\n\n    pub fn predict(&self, input: &Tensor) -> MlResult\u003cTensor> {\n        self.layer.predict(input)\n    }\n\n    pub fn layer(&self) -> &Linear {\n        &self.layer\n    }\n}\n\nimpl TrainableModel for LinearRegression {\n    fn context_id(&self) -> ContextId {\n        self.context.id()\n    }\n    fn parameters(&self) -> Vec\u003c&Parameter> {\n        self.layer.parameters()\n    }\n}\n\nimpl SupervisedModel for LinearRegression {\n    fn forward_loss(\n        &mut self,\n        input: &Variable,\n        target: &Tensor,\n    ) -> MlResult\u003c(Variable, Variable)> {\n        let prediction = self.layer.apply(input)?;\n        let loss = prediction.mse_loss(target, Reduction::Mean)?;\n        Ok((prediction, loss))\n    }\n}\n\n#[derive(Debug)]\npub struct Mlp {\n    context: ExecutionContext,\n    network: Sequential,\n}"},{"path":"src/nn/pooling.rs","bytes":3718,"preview":"use super::*;\nuse crate::legacy::tensor::operators::{MaxPool2d, AvgPool2d};\n\nimpl Pooling {\n    /// MaxPool2d 레이어를 생성.\n    pub fn new_max(\n        kernel_size: (usize, usize),\n        stride: (usize, usize),\n        label: &str,\n    ) -> Self {\n        Self {\n            label: label.to_string(),\n            kernel_size,\n            stride,\n            mode: PoolingMode::Max,\n        }\n    }\n\n    /// AvgPool2d 레이어를 생성.\n    pub fn new_avg(\n        kernel_size: (usize, usize),\n        stride: (usize, usize),\n        label: &str,\n    ) -> Self {\n        Self {\n            label: label.to_string(),\n            kernel_size,\n            stride,\n            mode: PoolingMode::Average,\n        }\n    }\n\n    fn kernel_stride_scalars(&self) -> MlResult\u003c[GlobalTensor\u003cf32>; 4]> {\n        Ok([\n            GlobalTensor::from_vec(vec![self.kernel_size.0 as f32], &[1, 1])?,\n            GlobalTensor::from_vec(vec![self.kernel_size.1 as f32], &[1, 1])?,\n            GlobalTensor::from_vec(vec![self.stride.0     as f32], &[1, 1])?,\n            GlobalTensor::from_vec(vec![self.stride.1     as f32], &[1, 1])?,\n        ])\n    }\n}\n\nimpl Layer for Pooling {\n    #[cfg(all(feature = \"enableBackward\"))]\n    fn apply(&mut self, input: &Variable) -> MlResult\u003cVariable> {\n        let kh = Variable::new(Tensor::from_vec(vec![self.kernel_size.0 as f32], &[1, 1])?);\n        let kw = Variable::new(Tensor::from_vec(vec![self.kernel_size.1 as f32], &[1, 1])?);\n        let sh = Variable::new(Tensor::from_vec(vec![self.stride.0      as f32], &[1, 1])?);\n        let sw = Variable::new(Tensor::from_vec(vec![self.stride.1      as f32], &[1, 1])?);\n\n        match self.mode {\n            PoolingMode::Max => {\n                // forward()[0]=Y, forward()[1]=mask\n                // apply_with_saved: mask를 saved tensor로 보존 →\n                // backward targets = [input, kH, kW, sh, sw, mask]\n                let mut op = MaxPool2d::new()?;\n                op.apply_with_saved(&[input, &kh, &kw, &sh, &sw])\n            }\n            PoolingMode::Average => {\n                let mut op = AvgPool2d::new()?;\n                op.apply(&[input, &kh, &kw, &sh, &sw])\n            }\n        }\n    }\n"},{"path":"src/nn/reshape.rs","bytes":3764,"preview":"use crate::legacy::tensor::operators::ReshapeOp;\nuse super::*;\n\nimpl Reshape {\n    /// `target_shape`에 `-1`을 하나만 사용하면 해당 차원을 자동 추론합니다.\n    ///\n    /// # 예시\n    /// ```ignore\n    /// // [N, C, H, W] → [N, H*W, C]  (batch 차원 유지)\n    /// Reshape::new(&[0, -1, 0], \"flatten_spatial\")?;\n    /// // 0 = 입력의 해당 차원을 그대로 사용 (런타임에 결정)\n    ///\n    /// // 고정 shape\n    /// Reshape::new(&[4, 256], \"fixed_reshape\")?;\n    /// ```\n    pub fn new(target_shape: &[isize], label: &str) -> MlResult\u003cSelf> {\n        let neg_count = target_shape.iter().filter(|&&d| d \u003c 0).count();\n        if neg_count > 1 {\n            return Err(MlError::TensorError(TensorError::InvalidShape {\n                expected: vec![],\n                got: target_shape.iter().map(|&d| d as usize).collect(),\n            }));\n        }\n        Ok(Self {\n            label: label.to_string(),\n            target_shape: target_shape.to_vec(),\n            operator: ReshapeOp::new()?,\n        })\n    }\n\n    /// 입력 shape를 기반으로 실제 target shape를 계산합니다.\n    /// `-1`은 나머지 원소 수로 추론하고, `0`은 입력의 해당 차원을 그대로 복사합니다.\n    fn resolve_shape(&self, input_shape: &[usize]) -> MlResult\u003cVec\u003cusize>> {\n        let total: usize = input_shape.iter().product();\n        let mut resolved: Vec\u003cusize> = Vec::with_capacity(self.target_shape.len());\n        let mut infer_idx: Option\u003cusize> = None;\n        let mut known_product: usize = 1;\n\n        for (i, &dim) in self.target_shape.iter().enumerate() {\n            if dim == 0 {\n                // 입력의 해당 차원을 그대로 사용\n                if i >= input_shape.len() {\n                    return Err(MlError::TensorError(TensorError::InvalidShape {\n                        expected: self.target_shape.iter().map(|&d| d as usize).collect(),\n                        got: input_shape.to_vec(),\n                    }));\n                }\n                resolved.push(input_shape[i]);\n                known_product *= input_shape[i];\n            } else if dim \u003c 0 {\n                // 추론 대상\n                infer_idx = Some(i);\n                resolved.push(0); // placeholder\n            } else {\n                resolved.push(dim as usize);\n                known_product *= dim as usize;\n            }\n        }\n\n        if let Some(idx) = infer_idx {\n            if known_product == 0 || total % known_product != 0 {\n                return Err(MlError::TensorError(TensorError::InvalidShape {\n                    expected: self.target_shape.iter().map(|&d| d as usize).collect(),\n                    got: input_shape.to_vec(),\n                }));"},{"path":"src/optimizer/adagrad.rs","bytes":2631,"preview":"use super::*;\n\n\nstruct AdaGradState {\n    weight_id: NodeId,\n    grad_id:   NodeId,\n    /// 누적 제곱 그래디언트\n    g_accum:   Vec\u003cf32>,\n}\n\n/// AdaGrad (Adaptive Gradient).\n///\n/// 업데이트 규칙:\n/// ```text\n/// G += grad²\n/// W = W - lr * grad / sqrt(G + eps)\n/// ```\n///\n/// 파라미터마다 학습률을 자동 조정한다.\n/// 단, G가 계속 누적되므로 학습이 장기화될수록 업데이트가 소멸할 수 있다.\npub struct AdaGrad {\n    lr:     f32,\n    eps:    f32,\n    params: Vec\u003cAdaGradState>,\n}\n\nimpl AdaGrad {\n    /// * `lr`  - 초기 학습률 (일반적으로 0.01)\n    /// * `eps` - 수치 안정성을 위한 소량값 (일반적으로 1e-8)\n    pub fn new(lr: f32, eps: f32) -> Self {\n        Self { lr, eps, params: vec![] }\n    }\n}\n\nimpl Optimizer for AdaGrad {\n    fn register(&mut self, param: &dyn Parameter) {\n        let size = TENSOR_STORAGE.with_borrow(|s| {\n            s.get(&param.node_id()).map(|w| w.data.len()).unwrap_or(0)\n        });\n        self.params.push(AdaGradState {\n            weight_id: param.node_id(),\n            grad_id:   param.grad().id(),\n            g_accum:   vec![0.0; size],\n        });\n    }\n\n    fn step(&mut self) -> MlResult\u003c()> {\n        let grad_ids: Vec\u003cNodeId> = self.params.iter().map(|e| e.grad_id).collect();\n        let grads = snapshot_grads(&grad_ids);\n\n        let lr = self.lr;\n        let eps = self.eps;\n\n        TENSOR_STORAGE.with_borrow_mut(|storage| {\n            for (e, grad) in self.params.iter_mut().zip(grads.iter()) {\n                if grad.is_empty() { continue; }\n\n                // G += grad²\n                e.g_accum.iter_mut()\n                    .zip(grad.iter())\n                    .for_each(|(g, &gi)| *g += gi * gi);\n\n                // W = W - lr * grad / sqrt(G + eps)\n                if let Some(w) = storage.get_mut(&e.weight_id) {\n                    w.data.iter_mut()"},{"path":"src/optimizer/adam.rs","bytes":3431,"preview":"use super::*;\n\npub(super) struct AdamState {\n    pub(super) weight_id: NodeId,\n    pub(super) grad_id:   NodeId,\n    /// 1차 모멘트 (편향 미보정)\n    pub(super) m: Vec\u003cf32>,\n    /// 2차 모멘트 (편향 미보정)\n    pub(super) v: Vec\u003cf32>,\n}\n\nimpl AdamState {\n    pub(super) fn new(weight_id: NodeId, grad_id: NodeId, size: usize) -> Self {\n        Self {\n            weight_id,\n            grad_id,\n            m: vec![0.0; size],\n            v: vec![0.0; size],\n        }\n    }\n}\n\n/// Adam (Adaptive Moment Estimation).\n///\n/// 업데이트 규칙:\n/// ```text\n/// m = β1 * m + (1 - β1) * grad\n/// v = β2 * v + (1 - β2) * grad²\n/// m̂ = m / (1 - β1^t)\n/// v̂ = v / (1 - β2^t)\n/// W = W - lr * m̂ / (sqrt(v̂) + ε)\n/// ```\npub struct Adam {\n    lr:    f32,\n    beta1: f32,\n    beta2: f32,\n    eps:   f32,\n    /// 현재 스텝 수 (bias correction에 사용)\n    t:     u32,\n    params: Vec\u003cAdamState>,\n}\n\nimpl Adam {\n    /// * `lr`    - 학습률 (일반적으로 1e-3)\n    /// * `beta1` - 1차 모멘트 감쇠 계수 (일반적으로 0.9)\n    /// * `beta2` - 2차 모멘트 감쇠 계수 (일반적으로 0.999)\n    /// * `eps`   - 수치 안정성 소량값 (일반적으로 1e-8)\n    pub fn new(lr: f32, beta1: f32, beta2: f32, eps: f32) -> Self {\n        Self { lr, beta1, beta2, eps, t: 0, params: vec![] }\n    }\n}\n\nimpl Optimizer for Adam {\n    fn register(&mut self, param: &dyn Parameter) {\n        let size = TENSOR_STORAGE.with_borrow(|s| {\n            s.get(&param.node_id()).map(|w| w.data.len()).unwrap_or(0)\n        });\n        self.params.push(AdamState::new(param.node_id(), param.grad().id(), size));\n    }\n\n    fn step(&mut self) -> MlResult\u003c()> {\n        self.t += 1;\n        let bc1 = 1.0 - self.beta1.powi(self.t as i32);\n        let bc2 = 1.0 - self.beta2.powi(self.t as i32);\n"},{"path":"src/optimizer/adamw.rs","bytes":3461,"preview":"use super::*;\n\n\n/// AdamW (Adam with Decoupled Weight Decay).\n///\n/// Adam에서 weight decay를 gradient에 포함시키는 대신 가중치에 직접 적용한다.\n/// 이 분리(decoupled)가 일반화 성능을 개선한다.\n///\n/// 업데이트 규칙:\n/// ```text\n/// m = β1 * m + (1 - β1) * grad\n/// v = β2 * v + (1 - β2) * grad²\n/// m̂ = m / (1 - β1^t)\n/// v̂ = v / (1 - β2^t)\n/// W = W - lr * (m̂ / (sqrt(v̂) + ε) + weight_decay * W)\n/// ```\npub struct AdamW {\n    lr:           f32,\n    beta1:        f32,\n    beta2:        f32,\n    eps:          f32,\n    weight_decay: f32,\n    t:            u32,\n    params:       Vec\u003ccrate::legacy::optimizer::adam::AdamState>,\n}\n\nimpl AdamW {\n    /// * `lr`           - 학습률 (일반적으로 1e-3)\n    /// * `beta1`        - 1차 모멘트 감쇠 계수 (일반적으로 0.9)\n    /// * `beta2`        - 2차 모멘트 감쇠 계수 (일반적으로 0.999)\n    /// * `eps`          - 수치 안정성 소량값 (일반적으로 1e-8)\n    /// * `weight_decay` - 가중치 감쇠 계수 (일반적으로 1e-2)\n    pub fn new(lr: f32, beta1: f32, beta2: f32, eps: f32, weight_decay: f32) -> Self {\n        Self { lr, beta1, beta2, eps, weight_decay, t: 0, params: vec![] }\n    }\n}\n\nimpl Optimizer for AdamW {\n    fn register(&mut self, param: &dyn Parameter) {\n        let size = TENSOR_STORAGE.with_borrow(|s| {\n            s.get(&param.node_id()).map(|w| w.data.len()).unwrap_or(0)\n        });\n        self.params.push(crate::legacy::optimizer::adam::AdamState::new(param.node_id(), param.grad().id(), size));\n    }\n\n    fn step(&mut self) -> MlResult\u003c()> {\n        self.t += 1;\n        let bc1 = 1.0 - self.beta1.powi(self.t as i32);\n        let bc2 = 1.0 - self.beta2.powi(self.t as i32);\n\n        let grad_ids: Vec\u003cNodeId> = self.params.iter().map(|e| e.grad_id).collect();\n        let grads = snapshot_grads(&grad_ids);\n\n        let lr           = self.lr;\n        let beta1        = self.beta1;\n        let beta2        = self.beta2;\n        let eps          = self.eps;\n        let weight_decay = self.weight_decay;\n\n        TENSOR_STORAGE.with_borrow_mut(|storage| {\n            for (e, grad) in self.params.iter_mut().zip(grads.iter()) {\n                if grad.is_empty() { continue; }\n\n                // m = β1*m + (1-β1)*grad,  v = β2*v + (1-β2)*grad²\n                for i in 0..e.m.len() {"},{"path":"src/optimizer/algorithms.rs","bytes":17068,"preview":"use crate::nn::Parameter;\nuse crate::tensor::TensorBuffer;\nuse crate::{ContextError, ContextId, ExecutionContext, MlResult};\n\nuse super::OptimError;\n\npub trait Optimizer {\n    fn register(&mut self, parameter: &Parameter) -> MlResult\u003c()>;\n    fn register_all(&mut self, parameters: &[&Parameter]) -> MlResult\u003c()> {\n        let mut seen = std::collections::HashSet::new();\n        for parameter in parameters {\n            if seen.insert(parameter.id()) {\n                self.register(parameter)?;\n            }\n        }\n        Ok(())\n    }\n    fn step(&mut self) -> MlResult\u003c()>;\n    fn zero_grad(&self) -> MlResult\u003c()>;\n    fn lr(&self) -> f32;\n    fn set_lr(&mut self, learning_rate: f32) -> MlResult\u003c()>;\n    fn registered_param_count(&self) -> usize;\n    fn registered_parameters(&self) -> Vec\u003c&Parameter>;\n    fn context_id(&self) -> ContextId;\n}\n\n#[derive(Clone, Copy, Debug)]\nenum Algorithm {\n    Sgd,\n    Momentum {\n        momentum: f32,\n    },\n    AdaGrad {\n        epsilon: f32,\n    },\n    RmsProp {\n        rho: f32,\n        epsilon: f32,\n    },\n    Adam {\n        beta1: f32,\n        beta2: f32,\n        epsilon: f32,\n        weight_decay: f32,\n    },\n}\n\n#[derive(Debug)]\nstruct ParameterState {\n    parameter: Parameter,\n    first: Vec\u003cf32>,\n    second: Vec\u003cf32>,\n}\n\n#[derive(Debug)]\nstruct OptimizerCore {\n    context: ExecutionContext,\n    learning_rate: f32,\n    algorithm: Algorithm,\n    step: u32,\n    parameters: Vec\u003cParameterState>,\n}\n\nfn positive(name: &'static str, value: f32) -> MlResult\u003cf32> {\n    if value.is_finite() && value > 0.0 {"},{"path":"src/optimizer/legacy_mod.rs","bytes":5398,"preview":"#[path = \"sgd.rs\"]\npub mod sgd;\n#[path = \"momentum.rs\"]\npub mod momentum;\n#[path = \"adagrad.rs\"]\npub mod adagrad;\n#[path = \"rmsprop.rs\"]\npub mod rmsprop;\n#[path = \"adam.rs\"]\npub mod adam;\n#[path = \"adamw.rs\"]\npub mod adamw;\n\npub use sgd::SGD;\npub use momentum::Momentum;\npub use adagrad::AdaGrad;\npub use rmsprop::RMSProp;\npub use adam::Adam;\npub use adamw::AdamW;\n\nuse crate::legacy::{\n    MlResult,\n    nn::Parameter,\n    tensor::{NodeId, TENSOR_STORAGE}\n};\nuse serde::{Deserialize, Serialize};\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]\npub enum OptimizerKind { Sgd, Momentum, AdaGrad, RmsProp, Adam, AdamW }\n\n#[derive(Debug, Clone, Serialize, Deserialize)]\npub struct ParameterOptimizerState {\n    pub shape: Vec\u003cusize>,\n    pub buffers: Vec\u003cVec\u003cf32>>,\n}\n\n#[derive(Debug, Clone, Serialize, Deserialize)]\npub struct OptimizerSnapshot {\n    pub version: u32,\n    pub kind: OptimizerKind,\n    pub lr: f32,\n    pub scalars: Vec\u003cf32>,\n    pub parameters: Vec\u003cParameterOptimizerState>,\n}\n\n#[derive(thiserror::Error, Debug)]\npub enum OptimError {\n    #[error(\"Gradient Error: {0}\")]\n    GradientError(String),\n    #[error(\"invalid optimizer hyperparameter '{name}': {reason}\")]\n    InvalidHyperparameter { name: &'static str, reason: String },\n    #[error(\"parameter {0:?} is already registered\")]\n    DuplicateParameter(NodeId),\n    #[error(\"optimizer parameters do not match the model: missing {missing:?}, extra {extra:?}\")]\n    ParameterSetMismatch { missing: Vec\u003cNodeId>, extra: Vec\u003cNodeId> },\n}\n\n/// 모든 옵티마이저가 구현해야 하는 공통 인터페이스.\n///\n/// # 사용 예시\n/// ```ignore\n/// let mut opt = SGD::new(0.01);\n/// opt.register(&model.w1);\n/// opt.register(&model.b1);\n///"},{"path":"src/optimizer/mod.rs","bytes":146,"preview":"//! Optimizers consume stable parameters, never graph IDs or storage internals.\nmod algorithms;\npub use crate::OptimError;\npub use algorithms::*;\n"},{"path":"src/optimizer/momentum.rs","bytes":2266,"preview":"use super::*;\n\nstruct MomentumState {\n    weight_id: NodeId,\n    grad_id:   NodeId,\n    velocity:  Vec\u003cf32>,\n}\n\n/// SGD with Momentum.\n///\n/// 업데이트 규칙:\n/// ```text\n/// v = momentum * v + grad\n/// W = W - lr * v\n/// ```\npub struct Momentum {\n    lr:       f32,\n    momentum: f32,\n    params:   Vec\u003cMomentumState>,\n}\n\nimpl Momentum {\n    /// * `lr`       - 학습률\n    /// * `momentum` - 모멘텀 계수 (일반적으로 0.9)\n    pub fn new(lr: f32, momentum: f32) -> Self {\n        Self { lr, momentum, params: vec![] }\n    }\n}\n\nimpl Optimizer for Momentum {\n    fn register(&mut self, param: &dyn Parameter) {\n        let size = TENSOR_STORAGE.with_borrow(|s| {\n            s.get(&param.node_id()).map(|w| w.data.len()).unwrap_or(0)\n        });\n        self.params.push(MomentumState {\n            weight_id: param.node_id(),\n            grad_id:   param.grad().id(),\n            velocity:  vec![0.0; size],\n        });\n    }\n\n    fn step(&mut self) -> MlResult\u003c()> {\n        let grad_ids: Vec\u003cNodeId> = self.params.iter().map(|e| e.grad_id).collect();\n        let grads = snapshot_grads(&grad_ids);\n\n        let momentum = self.momentum;\n        let lr = self.lr;\n\n        TENSOR_STORAGE.with_borrow_mut(|storage| {\n            for (e, grad) in self.params.iter_mut().zip(grads.iter()) {\n                if grad.is_empty() { continue; }\n\n                // v = momentum * v + grad\n                e.velocity.iter_mut()\n                    .zip(grad.iter())\n                    .for_each(|(v, &g)| *v = momentum * *v + g);\n\n                // W = W - lr * v\n                if let Some(w) = storage.get_mut(&e.weight_id) {\n                    w.data.iter_mut()\n                        .zip(e.velocity.iter())\n                        .for_each(|(p, &v)| *p -= lr * v);\n                }\n            }\n        });"},{"path":"src/optimizer/rmsprop.rs","bytes":2733,"preview":"use super::*;\n\nstruct RMSPropState {\n    weight_id: NodeId,\n    grad_id:   NodeId,\n    /// 지수이동평균 제곱 그래디언트\n    g_avg:     Vec\u003cf32>,\n}\n\n/// RMSProp (Root Mean Square Propagation).\n///\n/// 업데이트 규칙:\n/// ```text\n/// G = rho * G + (1 - rho) * grad²\n/// W = W - lr * grad / sqrt(G + eps)\n/// ```\n///\n/// AdaGrad의 G 누적 소멸 문제를 지수이동평균으로 해결한다.\npub struct RMSProp {\n    lr:     f32,\n    rho:    f32,\n    eps:    f32,\n    params: Vec\u003cRMSPropState>,\n}\n\nimpl RMSProp {\n    /// * `lr`  - 학습률 (일반적으로 1e-3)\n    /// * `rho` - 감쇠 계수 (일반적으로 0.9)\n    /// * `eps` - 수치 안정성 소량값 (일반적으로 1e-8)\n    pub fn new(lr: f32, rho: f32, eps: f32) -> Self {\n        Self { lr, rho, eps, params: vec![] }\n    }\n}\n\nimpl Optimizer for RMSProp {\n    fn register(&mut self, param: &dyn Parameter) {\n        let size = TENSOR_STORAGE.with_borrow(|s| {\n            s.get(&param.node_id()).map(|w| w.data.len()).unwrap_or(0)\n        });\n        self.params.push(RMSPropState {\n            weight_id: param.node_id(),\n            grad_id:   param.grad().id(),\n            g_avg:     vec![0.0; size],\n        });\n    }\n\n    fn step(&mut self) -> MlResult\u003c()> {\n        let grad_ids: Vec\u003cNodeId> = self.params.iter().map(|e| e.grad_id).collect();\n        let grads = snapshot_grads(&grad_ids);\n\n        let lr  = self.lr;\n        let rho = self.rho;\n        let eps = self.eps;\n\n        TENSOR_STORAGE.with_borrow_mut(|storage| {\n            for (e, grad) in self.params.iter_mut().zip(grads.iter()) {\n                if grad.is_empty() { continue; }\n\n                // G = rho * G + (1 - rho) * grad²\n                e.g_avg.iter_mut()\n                    .zip(grad.iter())\n                    .for_each(|(g, &gi)| *g = rho * *g + (1.0 - rho) * gi * gi);\n\n                // W = W - lr * grad / sqrt(G + eps)\n                if let Some(w) = storage.get_mut(&e.weight_id) {"},{"path":"src/optimizer/sgd.rs","bytes":1669,"preview":"use super::*;\n\n\nstruct ParamEntry {\n    weight_id: NodeId,\n    grad_id:   NodeId,\n}\n\n/// Stochastic Gradient Descent.\n///\n/// 업데이트 규칙: `W = W - lr * grad`\n///\n/// BGD/MiniBGD와 업데이트 규칙은 동일하며, 배치 구성은 학습 루프 측에서 담당한다.\npub struct SGD {\n    lr:     f32,\n    params: Vec\u003cParamEntry>,\n}\n\nimpl SGD {\n    pub fn new(lr: f32) -> Self {\n        Self { lr, params: vec![] }\n    }\n}\n\nimpl Optimizer for SGD {\n    fn register(&mut self, param: &dyn Parameter) {\n        self.params.push(ParamEntry {\n            weight_id: param.node_id(),\n            grad_id:   param.grad().id(),\n        });\n    }\n\n    fn step(&mut self) -> MlResult\u003c()> {\n        let grad_ids: Vec\u003cNodeId> = self.params.iter().map(|e| e.grad_id).collect();\n        let grads = snapshot_grads(&grad_ids);\n\n        TENSOR_STORAGE.with_borrow_mut(|storage| {\n            for (e, grad) in self.params.iter().zip(grads.iter()) {\n                if grad.is_empty() { continue; }\n                if let Some(w) = storage.get_mut(&e.weight_id) {\n                    w.data.iter_mut()\n                        .zip(grad.iter())\n                        .for_each(|(p, &g)| *p -= self.lr * g);\n                }\n            }\n        });\n        Ok(())\n    }\n\n    fn zero_grad(&self) -> MlResult\u003c()> {\n        let grad_ids: Vec\u003cNodeId> = self.params.iter().map(|e| e.grad_id).collect();\n        clear_grads(&grad_ids);\n        Ok(())\n    }\n\n    fn lr(&self) -> f32 { self.lr }\n    fn set_lr(&mut self, lr: f32) { self.lr = lr; }\n    fn registered_param_count(&self) -> usize { self.params.len() }\n}\n"},{"path":"src/runtime/backward.rs","bytes":5389,"preview":"use super::*;\nimpl ExecutionContext {\n    pub fn backward(&self, output: &Variable, options: BackwardOptions\u003c'_>) -> MlResult\u003c()> {\n        #[cfg(feature = \"debugging\")]\n        let _trace =\n            tracing::debug_span!(\"backward\",context=?self.id(),output=?output.tensor().id())\n                .entered();\n        self.backward_observed(output, options, |_| Ok(()))\n    }\n    pub(super) fn backward_observed(\n        &self,\n        output: &Variable,\n        options: BackwardOptions\u003c'_>,\n        observe: impl FnOnce(&State) -> MlResult\u003c()>,\n    ) -> MlResult\u003c()> {\n        self.validate(output.tensor())?;\n        #[cfg(feature = \"legacyBenchmark\")]\n        if self.route() == ExecutionRoute::Legacy {\n            return self.backward_legacy(output, options, observe);\n        }\n        let mut state = self\n            .inner\n            .state\n            .try_borrow_mut()\n            .map_err(|_| ContextError::BorrowConflict)?;\n        state.engine()?;\n        let root = output.tensor.id();\n        if !state.tracked.contains(&root) {\n            return Err(AutogradError::NodeNotFound(root).into());\n        }\n        if state.consumed.contains(&root) {\n            return Err(AutogradError::GraphAlreadyFreed(root).into());\n        }\n        let value = state.snapshot(root)?;\n        let seed = if let Some(gradient) = options.gradient {\n            if gradient.context_id() != self.id {\n                return Err(ContextError::Mismatch.into());\n            }\n            let grad = state.snapshot(gradient.id())?;\n            if grad.shape != value.shape {\n                return Err(AutogradError::GradientShapeMismatch {\n                    expected: value.shape,\n                    got: grad.shape,\n                }\n                .into());\n            }\n            grad\n        } else {\n            if value.data.len() != 1 {\n                return Err(AutogradError::OutputNotScalar(value.shape).into());\n            }\n            TensorBuffer::from_vec(vec![1.0], &value.shape)?\n        };\n        let order = state.engine()?.order(root)?;\n        let mut seen = HashSet::from([root]);\n        let mut records = Vec::new();\n        for &id in &order {\n            if !seen.insert(id) && records.iter().any(|n: &GradientRecord| n.output == id) {\n                return Err(AutogradError::CycleDetected.into());\n            }\n            let record = state\n                .engine()?\n                .get(id)\n                .ok_or(AutogradError::NodeNotFound(id))?;\n            seen.extend(record.inputs.iter().copied());"},{"path":"src/runtime/builtin_autograd.rs","bytes":2019,"preview":"use crate::contracts::*;\nuse crate::{AutogradError, MlResult};\nuse std::collections::{HashMap, HashSet};\n\n#[derive(Debug, Default)]\npub struct ReverseMode {\n    nodes: HashMap\u003cTensorId, GradientRecord>,\n}\nimpl AutogradEngine for ReverseMode {\n    fn record(&mut self, record: GradientRecord) -> MlResult\u003c()> {\n        if self.nodes.contains_key(&record.output) {\n            return Err(crate::TensorError::InvalidOperation {\n                op: \"record\",\n                reason: \"duplicate graph output\".into(),\n            }\n            .into());\n        }\n        self.nodes.insert(record.output, record);\n        Ok(())\n    }\n    fn get(&self, output: TensorId) -> Option\u003cGradientRecord> {\n        self.nodes.get(&output).cloned()\n    }\n    fn remove(&mut self, output: TensorId) -> MlResult\u003cOption\u003cGradientRecord>> {\n        Ok(self.nodes.remove(&output))\n    }\n    fn nodes(&self) -> Vec\u003cTensorId> {\n        self.nodes.keys().copied().collect()\n    }\n    fn order(&self, output: TensorId) -> MlResult\u003cVec\u003cTensorId>> {\n        // Iterative DFS also supports deep user graphs without overflowing the stack.\n        let mut stack = vec![(output, false)];\n        let mut visiting = HashSet::new();\n        let mut finished = HashSet::new();\n        let mut order = Vec::new();\n        while let Some((id, exiting)) = stack.pop() {\n            if finished.contains(&id) {\n                continue;\n            }\n            let Some(node) = self.nodes.get(&id) else {\n                finished.insert(id);\n                continue;\n            };\n            if exiting {\n                visiting.remove(&id);\n                finished.insert(id);\n                order.push(id);\n                continue;\n            }\n            if !visiting.insert(id) {\n                return Err(AutogradError::CycleDetected.into());\n            }\n            stack.push((id, true));\n            for &input in node.inputs.iter().rev() {\n                stack.push((input, false));\n            }\n        }\n        Ok(order)\n    }\n}\n"},{"path":"src/runtime/dispatch.rs","bytes":7235,"preview":"use super::*;\nimpl ExecutionContext {\n    fn tracking(&self, inputs: &[&Tensor]) -> MlResult\u003cbool> {\n        for input in inputs {\n            self.validate(input)?;\n        }\n        let state = self\n            .inner\n            .state\n            .try_borrow()\n            .map_err(|_| ContextError::BorrowConflict)?;\n        let tracked =\n            self.inner.no_grad.get() == 0 && inputs.iter().any(|x| state.tracked.contains(&x.id()));\n        if tracked {\n            state.engine()?;\n        }\n        Ok(tracked)\n    }\n    fn with_inputs\u003cR>(\n        &self,\n        inputs: &[&Tensor],\n        compute: impl FnOnce(&[TensorView\u003c'_>]) -> MlResult\u003cR>,\n    ) -> MlResult\u003cR> {\n        // Owned input snapshots avoid holding provider borrows across arbitrary\n        // user callbacks. Metadata/view APIs themselves remain copy-free.\n        let values = inputs\n            .iter()\n            .map(|x| x.snapshot())\n            .collect::\u003cMlResult\u003cVec\u003c_>>>()?;\n        let views = values.iter().map(TensorBuffer::view).collect::\u003cVec\u003c_>>();\n        compute(&views)\n    }\n    pub fn execute(&self, operation: &Operation, inputs: &[&Tensor]) -> MlResult\u003cVec\u003cTensor>> {\n        #[cfg(feature=\"debugging\")]\n        let _trace=tracing::debug_span!(\"tensor_operation\",context=?self.id(),operation=operation.name(),inputs=inputs.len()).entered();\n        if inputs.is_empty() || operation.input_count().is_some_and(|n| n != inputs.len()) {\n            return Err(TensorError::InvalidOperation {\n                op: operation.name(),\n                reason: \"invalid input count\".into(),\n            }\n            .into());\n        }\n        // Loss targets are constants even when their handles happen to be tracked.\n        for input in inputs {\n            self.validate(input)?;\n        }\n        let tracked = self.tracking(if matches!(operation, Operation::Loss { .. }) {\n            &inputs[..1]\n        } else {\n            inputs\n        })?;\n        #[cfg(feature = \"legacyBenchmark\")]\n        if self.route() == ExecutionRoute::Legacy {\n            return self.execute_legacy(operation, inputs, tracked);\n        }\n        let provider = self\n            .inner\n            .operations\n            .as_deref()\n            .ok_or_else(|| missing(\"operations\", \"forward\", operation.name()))?;\n        let result = self.with_inputs(inputs, |views| provider.execute(operation, views))?;\n        let expected = if matches!(operation, Operation::TopK { .. } | Operation::Matmax { .. }) {\n            2\n        } else {\n            1"},{"path":"src/runtime/legacy_session.rs","bytes":43485,"preview":"//! Native forwarding boundary for the opt-in Legacy route.\n//! The owning thread must not call the raw legacy API during a session.\n#![allow(dead_code)]\nuse crate::legacy as old;\nuse crate::{ContextError, MlError, MlResult, TensorBuffer, TensorError};\nuse old::nn::{Parameter, Variable};\nuse old::tensor::{\n    AutogradFunction, TensorBase,\n    operators::{\n        Abs, Add, ApproxCos, ApproxSin, AvgPool2d, Concat, Conv2dOp, Cos, Div, Exp, Function,\n        GroupNormOp, Log, Matmul, MaxPool2d, Mul, NearestUpsample2d, Neg, Pow, ReshapeOp, Sin,\n        Sqrt, Square, Sub, Sum, Transpose,\n    },\n};\nuse std::{cell::Cell, collections::HashMap, marker::PhantomData, rc::Rc};\n\nthread_local! { static ACTIVE: Cell\u003cbool> = const { Cell::new(false) }; }\n\nuse crate::contracts::{AutogradEngine, GradientRecord, TensorId, TensorStore, TensorView};\nuse std::{cell::RefCell, collections::HashSet};\n\n#[derive(Debug)]\npub(super) struct Bridge {\n    session: NativeSession,\n    handles: HashMap\u003cTensorId, NativeHandle>,\n    graph: HashSet\u003cTensorId>,\n}\nimpl Bridge {\n    pub(super) fn clear_graph(&mut self) {\n        self.session.clear_graph();\n        self.graph.clear();\n    }\n    fn handle(&self, id: TensorId) -> MlResult\u003cNativeHandle> {\n        self.handles\n            .get(&id)\n            .copied()\n            .ok_or_else(|| ContextError::UnknownTensor(id).into())\n    }\n}\n#[derive(Debug)]\nstruct Store(Rc\u003cRefCell\u003cBridge>>);\nimpl TensorStore for Store {\n    fn insert(&mut self, id: TensorId, buffer: TensorBuffer) -> MlResult\u003c()> {\n        let mut bridge = self\n            .0\n            .try_borrow_mut()\n            .map_err(|_| ContextError::BorrowConflict)?;\n        let handle = bridge.session.tensor(buffer)?;\n        bridge.handles.insert(id, handle);\n        Ok(())\n    }\n    fn alias(&mut self, _id: TensorId, _source: TensorId) -> MlResult\u003c()> {\n        Err(unsupported(\"detach alias\"))\n    }\n    fn with_view(\n        &self,\n        id: TensorId,\n        visitor: &mut dyn FnMut(TensorView\u003c'_>) -> MlResult\u003c()>,\n    ) -> MlResult\u003c()> {\n        let bridge = self\n            .0\n            .try_borrow()\n            .map_err(|_| ContextError::BorrowConflict)?;\n        let value = bridge.session.get(bridge.handle(id)?)?.tensor();\n        visitor(TensorView::new(value.data(), value.shape())?)"},{"path":"src/runtime/mod.rs","bytes":26494,"preview":"//! Explicit runtime facade. Provider implementations are replaceable per context.\nuse crate::contracts::*;\nuse crate::{AutogradError, ContextError, MlError, MlResult, TensorError};\nuse std::{\n    cell::{Cell, RefCell},\n    collections::{HashMap, HashSet},\n    rc::{Rc, Weak},\n    sync::atomic::{AtomicU64, Ordering},\n};\n\n#[cfg(feature = \"builtinKernels\")]\nuse crate::backend::CpuBackend;\n#[cfg(feature = \"builtinStorage\")]\nuse crate::backend::CpuTensorStore;\n#[cfg(feature = \"enableBackward\")]\nmod builtin_autograd;\n#[cfg(feature = \"enableBackward\")]\npub use builtin_autograd::ReverseMode;\n\nstatic CONTEXT_IDS: AtomicU64 = AtomicU64::new(1);\nstatic PARAMETER_IDS: AtomicU64 = AtomicU64::new(1);\n\npub mod prepared;\n\npub(crate) fn missing(\n    module: &'static str,\n    capability: &'static str,\n    operation: &'static str,\n) -> MlError {\n    MlError::DependencyUnavailable {\n        module,\n        capability,\n        operation,\n    }\n}\n\n#[derive(Debug, Default)]\npub struct ExecutionContextBuilder {\n    providers_configured: bool,\n    initialization_seed: Option\u003cu64>,\n    model_seed: Option\u003cu64>,\n    storage: Option\u003cBox\u003cdyn TensorStore>>,\n    autograd: Option\u003cBox\u003cdyn AutogradEngine>>,\n    operations: Option\u003cBox\u003cdyn OperationProvider>>,\n}\n/// Selects an execution implementation, independently of graph execution mode.\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)]\npub enum ExecutionRoute {\n    #[default]\n    P1,\n    Legacy,\n}\n\n/// Fallible route selection preserves the existing infallible default builder.\n#[derive(Debug)]\npub struct RoutedContextBuilder {\n    builder: ExecutionContextBuilder,\n    route: ExecutionRoute,\n}\nimpl RoutedContextBuilder {\n    pub fn build(self) -> MlResult\u003cExecutionContext> {\n        match self.route {\n            ExecutionRoute::P1 => Ok(self.builder.build()),\n            ExecutionRoute::Legacy => {\n                #[cfg(not(feature = \"legacyBenchmark\"))]"},{"path":"src/runtime/prepared/executor.rs","bytes":3498,"preview":"use super::{invalid, plan::*};\nuse crate::{ExecutionContext, MlResult, Parameter, Tensor};\n\nimpl PreparedPlan {\n    /// Bind current values and execute allocating replay. The callback may run\n    /// backward and update an optimizer. Scope cleanup also runs after errors.\n    /// Returned tensor handles own independent results across later executions.\n    pub fn with_run\u003cT>(\n        &self,\n        ctx: &ExecutionContext,\n        feeds: &[&Tensor],\n        parameters: &[&Parameter],\n        callback: impl FnOnce(&[Tensor]) -> MlResult\u003cT>,\n    ) -> MlResult\u003cT> {\n        // Validate the complete binding before writing any tensor or graph state.\n        if ctx.id() != self.context {\n            return Err(crate::ContextError::Mismatch.into());\n        }\n        if feeds.len() != self.program.feeds.len() || parameters.len() != self.parameter_ids.len() {\n            return Err(invalid(\"binding count mismatch\"));\n        }\n        for (feed, slot) in feeds.iter().zip(&self.program.feeds) {\n            ctx.validate(feed)?;\n            let spec = &self.program.slots[slot.0];\n            if feed.shape()? != spec.shape {\n                return Err(invalid(\"feed shape mismatch; prepare a separate plan\"));\n            }\n            if let Source::Feed { requires_grad } = spec.source {\n                if feed.as_variable()?.requires_grad()? != requires_grad {\n                    return Err(invalid(\"feed gradient signature mismatch\"));\n                }\n            }\n        }\n        for ((parameter, id), slot) in parameters\n            .iter()\n            .zip(&self.parameter_ids)\n            .zip(&self.program.parameters)\n        {\n            ctx.validate(parameter.tensor())?;\n            if parameter.id() != *id\n                || parameter.tensor().shape()? != self.program.slots[slot.0].shape\n            {\n                return Err(invalid(\n                    \"parameter binding or shape changed; prepare a separate plan\",\n                ));\n            }\n        }\n        let execute = || {\n            let mut slots: Vec\u003cOption\u003cTensor>> = vec![None; self.program.slots.len()];\n            for (feed, slot) in feeds.iter().zip(&self.program.feeds) {\n                slots[slot.0] = Some((*feed).clone());\n            }\n            for (parameter, slot) in parameters.iter().zip(&self.program.parameters) {\n                slots[slot.0] = Some(parameter.tensor().clone());\n            }\n            for node in &self.program.instructions {\n                let inputs = node\n                    .inputs\n                    .iter()\n                    .map(|id| slots[id.0].as_ref().expect(\"validated DAG\"))\n                    .collect::\u003cVec\u003c_>>();\n                let mut outputs = ctx.execute(&node.operation, &inputs)?;\n                if outputs.len() != 1\n                    || outputs[0].shape()? != self.program.slots[node.output.0].shape\n                {"},{"path":"src/runtime/prepared/mod.rs","bytes":713,"preview":"//! Explicit P1 preparation, initially an **allocating replay** implementation.\n//!\n//! Preparation validates immutable descriptions without executing tensors or RNG.\n//! Each run still uses eager kernels and records its own backward graph. There is\n//! no buffer arena, cached backward order, implicit cache, or default-mode change.\n//! S2/S3/S4 will replace these costs; callers must not treat S0 as static memory.\nmod executor;\nmod plan;\nmod prepare;\npub use plan::{PreparedMode, PreparedPlan, PreparedProgram, TensorSlotId};\n\nfn invalid(reason: impl Into\u003cString>) -> crate::MlError {\n    crate::TensorError::InvalidOperation {\n        op: \"prepared replay\",\n        reason: reason.into(),\n    }\n    .into()\n}\n"},{"path":"src/runtime/prepared/plan.rs","bytes":4151,"preview":"use super::{invalid, prepare::infer};\nuse crate::{ContextId, MlResult, ParameterId, contracts::Operation};\nuse std::sync::atomic::{AtomicU64, Ordering};\nstatic PROGRAM_IDS: AtomicU64 = AtomicU64::new(1);\n\n/// A symbolic slot, unrelated to a runtime TensorId or allocation address.\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub struct TensorSlotId(pub(super) usize, pub(super) u64);\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub enum PreparedMode {\n    Training,\n    Inference,\n}\n#[derive(Debug, Clone)]\npub(super) enum Source {\n    Feed { requires_grad: bool },\n    Parameter,\n    Node,\n}\n#[derive(Debug, Clone)]\npub(super) struct Slot {\n    pub shape: Vec\u003cusize>,\n    pub source: Source,\n}\n#[derive(Debug, Clone)]\npub(super) struct Instruction {\n    pub operation: Operation,\n    pub inputs: Vec\u003cTensorSlotId>,\n    pub output: TensorSlotId,\n}\n\n/// Shape-only operation description. All slots are contiguous f32 tensors.\n/// Runtime values (including target/noise/timestep) must be feeds, never captured.\n#[derive(Debug, Clone)]\npub struct PreparedProgram {\n    pub(super) id: u64,\n    pub(super) slots: Vec\u003cSlot>,\n    pub(super) feeds: Vec\u003cTensorSlotId>,\n    pub(super) parameters: Vec\u003cTensorSlotId>,\n    pub(super) instructions: Vec\u003cInstruction>,\n}\nimpl Default for PreparedProgram {\n    fn default() -> Self {\n        Self {\n            id: PROGRAM_IDS.fetch_add(1, Ordering::Relaxed),\n            slots: Vec::new(),\n            feeds: Vec::new(),\n            parameters: Vec::new(),\n            instructions: Vec::new(),\n        }\n    }\n}\nimpl PreparedProgram {\n    pub fn new() -> Self {\n        Self::default()\n    }\n    fn slot(&mut self, shape: &[usize], source: Source) -> MlResult\u003cTensorSlotId> {\n        super::prepare::numel(shape)?;\n        let id = TensorSlotId(self.slots.len(), self.id);\n        self.slots.push(Slot {\n            shape: shape.to_vec(),\n            source,\n        });\n        Ok(id)\n    }"},{"path":"src/runtime/prepared/prepare.rs","bytes":5616,"preview":"use super::{invalid, plan::*};\nuse crate::{\n    ExecutionContext, ExecutionRoute, MlResult, Parameter,\n    contracts::{LossKind, Operation, Reduction},\n};\n\npub(super) fn numel(shape: &[usize]) -> MlResult\u003cusize> {\n    if shape.contains(&0) {\n        return Err(invalid(\"empty axes are not supported by prepared replay\"));\n    }\n    shape\n        .iter()\n        .try_fold(1usize, |size, &dim| size.checked_mul(dim))\n        .filter(|size| *size \u003c= isize::MAX as usize / std::mem::size_of::\u003cf32>())\n        .ok_or_else(|| invalid(\"shape byte size overflow\"))\n}\npub(super) fn infer(operation: &Operation, shapes: &[&[usize]]) -> MlResult\u003cVec\u003cusize>> {\n    if operation.input_count() != Some(shapes.len()) || shapes.is_empty() {\n        return Err(invalid(\"unsupported operation or input arity\"));\n    }\n    let x = shapes[0];\n    let output = match operation {\n        Operation::Add | Operation::Sub | Operation::Mul | Operation::Div => {\n            let y = shapes[1];\n            let rank = x.len().max(y.len());\n            let mut result = vec![1; rank];\n            for offset in 0..rank {\n                let a = x.len().checked_sub(offset + 1).map(|i| x[i]).unwrap_or(1);\n                let b = y.len().checked_sub(offset + 1).map(|i| y[i]).unwrap_or(1);\n                if a != b && a != 1 && b != 1 {\n                    return Err(invalid(\"incompatible broadcast shapes\"));\n                }\n                result[rank - 1 - offset] = a.max(b);\n            }\n            result\n        }\n        Operation::Matmul => {\n            let y = shapes[1];\n            if x.len() != 2 || y.len() != 2 || x[1] != y[0] {\n                return Err(invalid(\n                    \"initial prepared Matmul requires compatible rank-2 matrices\",\n                ));\n            }\n            vec![x[0], y[1]]\n        }\n        Operation::Neg\n        | Operation::Square\n        | Operation::Exp\n        | Operation::Log\n        | Operation::Sqrt\n        | Operation::Relu\n        | Operation::Tanh\n        | Operation::Sigmoid\n        | Operation::Silu\n        | Operation::Sin\n        | Operation::Cos\n        | Operation::Abs => x.to_vec(),\n        Operation::Reshape(shape) => {\n            if numel(shape)? != numel(x)? {\n                return Err(invalid(\"reshape element count mismatch\"));\n            }\n            shape.clone()\n        }\n        Operation::Transpose(axes) => {\n            let mut sorted = axes.clone();"},{"path":"src/runtime/receiver.rs","bytes":28204,"preview":"use super::*;\nimpl ExecutionContext {\n    pub fn add(&self, input: &Tensor, rhs: &Tensor) -> MlResult\u003cTensor> {\n        self.single(Operation::Add, &[input, rhs])\n    }\n}\nimpl Tensor {\n    pub fn add(&self, rhs: &Tensor) -> MlResult\u003cSelf> {\n        self.execution_context()?.add(self, rhs)\n    }\n}\nimpl Variable {\n    pub fn add(&self, rhs: &Tensor) -> MlResult\u003cSelf> {\n        self.tensor.add(rhs)?.as_variable()\n    }\n}\nimpl ExecutionContext {\n    pub fn sub(&self, input: &Tensor, rhs: &Tensor) -> MlResult\u003cTensor> {\n        self.single(Operation::Sub, &[input, rhs])\n    }\n}\nimpl Tensor {\n    pub fn sub(&self, rhs: &Tensor) -> MlResult\u003cSelf> {\n        self.execution_context()?.sub(self, rhs)\n    }\n}\nimpl Variable {\n    pub fn sub(&self, rhs: &Tensor) -> MlResult\u003cSelf> {\n        self.tensor.sub(rhs)?.as_variable()\n    }\n}\nimpl ExecutionContext {\n    pub fn mul(&self, input: &Tensor, rhs: &Tensor) -> MlResult\u003cTensor> {\n        self.single(Operation::Mul, &[input, rhs])\n    }\n}\nimpl Tensor {\n    pub fn mul(&self, rhs: &Tensor) -> MlResult\u003cSelf> {\n        self.execution_context()?.mul(self, rhs)\n    }\n}\nimpl Variable {\n    pub fn mul(&self, rhs: &Tensor) -> MlResult\u003cSelf> {\n        self.tensor.mul(rhs)?.as_variable()\n    }\n}\nimpl ExecutionContext {\n    pub fn div(&self, input: &Tensor, rhs: &Tensor) -> MlResult\u003cTensor> {\n        self.single(Operation::Div, &[input, rhs])\n    }\n}\nimpl Tensor {\n    pub fn div(&self, rhs: &Tensor) -> MlResult\u003cSelf> {\n        self.execution_context()?.div(self, rhs)\n    }\n}\nimpl Variable {\n    pub fn div(&self, rhs: &Tensor) -> MlResult\u003cSelf> {\n        self.tensor.div(rhs)?.as_variable()\n    }\n}\nimpl ExecutionContext {\n    pub fn matmul(&self, input: &Tensor, rhs: &Tensor) -> MlResult\u003cTensor> {\n        self.single(Operation::Matmul, &[input, rhs])\n    }"},{"path":"src/runtime/tests.rs","bytes":37345,"preview":"use super::*;\nuse crate::backend::{\n    approx_cos_value, approx_sin_value, conv2d_forward_data, group_norm_forward_data,\n    max_pool2d_forward_data, nearest_upsample2d_forward_data,\n};\n\n#[test]\nfn contexts_are_isolated_and_mismatch_is_rejected() -> MlResult\u003c()> {\n    let a = ExecutionContext::new();\n    let b = ExecutionContext::new();\n    let x = a.tensor(vec![1.0], &[1])?;\n    let y = b.tensor(vec![2.0], &[1])?;\n    assert!(matches!(\n        a.add(&x, &y),\n        Err(crate::MlError::ContextError(ContextError::Mismatch))\n    ));\n    Ok(())\n}\n\n#[test]\nfn dropped_context_is_reported() -> MlResult\u003c()> {\n    let tensor = {\n        let ctx = ExecutionContext::new();\n        ctx.tensor(vec![1.0], &[1])?\n    };\n    assert!(matches!(\n        tensor.to_vec(),\n        Err(crate::MlError::ContextError(ContextError::Dropped))\n    ));\n    Ok(())\n}\n\n#[test]\nfn no_grad_depth_is_restored_on_error() -> MlResult\u003c()> {\n    let ctx = ExecutionContext::new();\n    let _: Result\u003c(), _> = ctx.no_grad(|| Err(crate::MlError::StringError(\"stop\".into())));\n    assert_eq!(ctx.graph_stats()?.no_grad_depth, 0);\n    Ok(())\n}\n\n#[test]\nfn backward_accumulates_fan_in_and_consumes_graph() -> MlResult\u003c()> {\n    let ctx = ExecutionContext::new();\n    let x = ctx.parameter(vec![3.0], &[])?;\n    let xx = ctx.mul_variable(&x, &x)?;\n    let y = ctx.add_variable(&xx, &x)?;\n    y.backward()?;\n    assert_eq!(x.grad()?.expect(\"leaf gradient\").data, vec![7.0]);\n    assert!(matches!(\n        y.backward(),\n        Err(crate::MlError::AutogradError(\n            AutogradError::GraphAlreadyFreed(_)\n        ))\n    ));\n    Ok(())\n}\n\n#[test]\nfn vector_output_requires_explicit_cotangent() -> MlResult\u003c()> {\n    let ctx = ExecutionContext::new();\n    let x = ctx.parameter(vec![2.0, 3.0], &[2])?;\n    let y = ctx.mul_variable(&x, &x)?;\n    assert!(matches!(\n        y.backward(),\n        Err(crate::MlError::AutogradError("},{"path":"src/runtime/training.rs","bytes":2358,"preview":"use super::*;\npub(crate) struct TrainingScope {\n    context: ExecutionContext,\n    finished: bool,\n}\nimpl ExecutionContext {\n    /// Execute one training unit with exclusive graph ownership.\n    ///\n    /// Rejects nested training and pre-existing graphs before invoking `operation`.\n    /// Graphs and gradients are cleaned on success and on returned errors. If both\n    /// the operation and cleanup fail, both causes are returned in `CleanupError`.\n    /// Parameter updates are not rolled back. Panic unwinding performs best-effort\n    /// cleanup through the private scope guard.\n    pub fn with_training_scope\u003cT>(&self, operation: impl FnOnce() -> MlResult\u003cT>) -> MlResult\u003cT> {\n        let scope = self.begin_training_scope()?;\n        scope.finish(operation())\n    }\n\n    pub(crate) fn begin_training_scope(&self) -> MlResult\u003cTrainingScope> {\n        self.collect()?;\n        let mut state = self\n            .inner\n            .state\n            .try_borrow_mut()\n            .map_err(|_| ContextError::BorrowConflict)?;\n        if self.inner.training_active.get() || !state.engine()?.nodes().is_empty() {\n            return Err(ContextError::ActiveGraphConflict.into());\n        }\n        state.gradients.clear();\n        self.inner.training_active.set(true);\n        Ok(TrainingScope {\n            context: self.clone(),\n            finished: false,\n        })\n    }\n}\nimpl TrainingScope {\n    fn cleanup(&mut self) -> MlResult\u003c()> {\n        let mut state = self\n            .context\n            .inner\n            .state\n            .try_borrow_mut()\n            .map_err(|_| ContextError::BorrowConflict)?;\n        state.clear_graph()?;\n        state.gradients.clear();\n        state.collect()?;\n        self.context.inner.training_active.set(false);\n        self.finished = true;\n        Ok(())\n    }\n    pub(crate) fn finish\u003cT>(mut self, result: MlResult\u003cT>) -> MlResult\u003cT> {\n        match (result, self.cleanup()) {\n            (Ok(v), Ok(())) => Ok(v),\n            (Err(e), Ok(())) | (Ok(_), Err(e)) => Err(e),\n            (Err(primary), Err(cleanup)) => Err(MlError::CleanupError {\n                primary: Box::new(primary),\n                cleanup: Box::new(cleanup),\n            }),\n        }\n    }\n}\nimpl Drop for TrainingScope {\n    fn drop(&mut self) {\n        if !self.finished {"},{"path":"src/runtime/visualization_disabled.rs","bytes":775,"preview":"use super::*;\nuse crate::visualization::{CaptureContext, CaptureProfile, GraphSnapshot};\n\nimpl ExecutionContext {\n    pub fn graph_snapshot(\n        &self,\n        _profile: CaptureProfile,\n        _context: CaptureContext,\n    ) -> MlResult\u003cGraphSnapshot> {\n        Err(missing(\n            \"visualization\",\n            \"graph capture (enableVisualization)\",\n            \"graph_snapshot\",\n        ))\n    }\n    pub fn backward_snapshot(\n        &self,\n        _output: &Variable,\n        _options: BackwardOptions\u003c'_>,\n        _profile: CaptureProfile,\n        _context: CaptureContext,\n    ) -> MlResult\u003cGraphSnapshot> {\n        Err(missing(\n            \"visualization\",\n            \"graph capture (enableVisualization)\",\n            \"backward_snapshot\",\n        ))\n    }\n}\n"},{"path":"src/runtime/visualization.rs","bytes":4428,"preview":"use super::*;\nuse crate::visualization::statistics::tensor_statistics;\nuse crate::visualization::*;\nuse std::collections::BTreeMap;\n\nimpl State {\n    fn graph_snapshot(\n        &self,\n        profile: CaptureProfile,\n        context: CaptureContext,\n    ) -> MlResult\u003cGraphSnapshot> {\n        let engine = self.engine()?;\n        let mut ids = self.tracked.clone();\n        let mut operations = HashMap::new();\n        let mut edges = Vec::new();\n        for id in engine.nodes() {\n            let record = engine.get(id).ok_or(AutogradError::NodeNotFound(id))?;\n            ids.insert(id);\n            operations.insert(id, record.backward.name().to_owned());\n            for input in record.inputs {\n                ids.insert(input);\n                edges.push(GraphEdgeSnapshot {\n                    from: input.0,\n                    to: id.0,\n                    kind: GraphEdgeKind::Data,\n                });\n            }\n        }\n        let mut nodes = Vec::new();\n        for id in ids {\n            let value = self.snapshot(id)?;\n            let leaf = self.leaves.contains(&id);\n            nodes.push(GraphNodeSnapshot {\n                id: id.0,\n                label: format!(\"tensor_{}\", id.0),\n                role: if self.parameters.contains(&id) {\n                    NodeRole::Weight\n                } else if !operations.contains_key(&id) {\n                    NodeRole::Input\n                } else {\n                    NodeRole::Variable\n                },\n                operation: operations.remove(&id),\n                shape: value.shape().to_vec(),\n                dtype: \"f32\",\n                elements: value.data().len(),\n                estimated_bytes: value.data().len() * size_of::\u003cf32>(),\n                is_parameter: self.parameters.contains(&id),\n                is_leaf: leaf,\n                requires_grad: self.tracked.contains(&id),\n                retain_grad: self.retained.contains(&id),\n                value_stats: if profile == CaptureProfile::Analysis {\n                    tensor_statistics(value.data())\n                } else {\n                    None\n                },\n                gradient_stats: if profile == CaptureProfile::Analysis {\n                    self.gradients\n                        .get(&id)\n                        .and_then(|g| tensor_statistics(g.data()))\n                } else {\n                    None\n                },\n                attributes: BTreeMap::new(),\n            });"},{"path":"src/tensor/broadcast.rs","bytes":8123,"preview":"//! NumPy 스타일 브로드캐스트 유틸리티.\n//!\n//! 모든 공개 함수는 **flat offset** 기반으로 동작합니다.\n//! 텐서 데이터는 row-major (C-order) 로 저장된다고 가정합니다.\n\nuse crate::legacy::{MlError, MlResult, TensorError};\n\n/// 두 shape 를 NumPy 규약으로 브로드캐스트한 결과 shape 를 반환합니다.\n///\n/// 규약:\n/// 1. 더 짧은 shape 의 앞쪽에 1을 채워 rank 를 맞춘다.\n/// 2. 각 축에서 `a == b || a == 1 || b == 1` 이어야 호환.\n/// 3. 출력 축 크기는 `max(a, b)`.\n///\n/// 호환 불가능한 shape 쌍은 `TensorError::InvalidShape` 로 반환.\npub fn broadcast_shape(a: &[usize], b: &[usize]) -> MlResult\u003cVec\u003cusize>> {\n    let rank = a.len().max(b.len());\n    let mut out = Vec::with_capacity(rank);\n    for i in 0..rank {\n        let ai = if i \u003c rank - a.len() { 1 } else { a[i - (rank - a.len())] };\n        let bi = if i \u003c rank - b.len() { 1 } else { b[i - (rank - b.len())] };\n        if ai == bi || ai == 1 || bi == 1 {\n            out.push(ai.max(bi));\n        } else {\n            return Err(MlError::TensorError(TensorError::InvalidShape {\n                expected: a.to_vec(),\n                got: b.to_vec(),\n            }));\n        }\n    }\n    Ok(out)\n}\n\n/// 출력 좌표 (row-major flat index) 에 대응하는 a / b 의 flat offset 을 계산합니다.\n///\n/// 결과 벡터 길이는 `out_shape.iter().product()`.\n/// 각 원소는 `(a_offset, b_offset)`.\npub fn broadcast_offsets(\n    a_shape: &[usize],\n    b_shape: &[usize],\n    out_shape: &[usize],\n) -> Vec\u003c(usize, usize)> {\n    let rank = out_shape.len();\n    let total: usize = out_shape.iter().product();\n\n    // leading 1 으로 패딩한 shape\n    let pad = |s: &[usize]| -> Vec\u003cusize> {\n        let mut v = vec![1usize; rank - s.len()];\n        v.extend_from_slice(s);\n        v\n    };\n    let a_pad = pad(a_shape);\n    let b_pad = pad(b_shape);\n\n    // stride (row-major) — broadcast 축(크기 1)은 stride 0 으로.\n    let compute_stride = |shape_padded: &[usize], orig: &[usize]| -> Vec\u003cusize> {\n        let mut stride = vec![0usize; rank];\n        if !orig.is_empty() {\n            let mut s = 1usize;\n            for i in (0..rank).rev() {\n                if shape_padded[i] == 1 {\n                    stride[i] = 0; // broadcast 축\n                } else {\n                    stride[i] = s;\n                    s *= shape_padded[i];"},{"path":"src/tensor/creation.rs","bytes":9832,"preview":"use super::*;\n\nimpl TensorBase for GlobalTensor\u003cf32> {\n    fn new(data: Vec\u003cVec\u003cf32>>) -> Self {\n        let shape = vec![data.len(), data[0].len()];\n        let data: Vec\u003cf32> = data.into_iter().flatten().collect();\n        GlobalTensor { data, shape, dirty: false }\n    }\n\n    fn from_vec(data: Vec\u003cf32>, shape: &[usize]) -> MlResult\u003cSelf> {\n        let expected_len: usize = shape.iter().product();\n        if data.len() != expected_len {\n            return Err(MlError::TensorError(TensorError::InvalidDataLength {\n                expected: expected_len,\n                got: data.len(),\n            }));\n        }\n\n        Ok(GlobalTensor { data, shape: shape.to_vec(), dirty: false })\n    }\n\n    fn as_ptr(&self) -> *const GlobalTensor\u003cf32> {\n        self\n    }\n\n    fn shape(&self) -> &[usize] {\n        &self.shape\n    }\n\n    fn data(&self) -> &[f32] {\n        &self.data\n    }\n\n    fn get(&self, indices: &[usize]) -> Option\u003c&f32> {\n        self.data().get(self.index(indices)?)\n    }\n\n    fn index(&self, indices: &[usize]) -> Option\u003cusize> {\n        if indices.len() != self.shape().len() {\n            return None;\n        }\n        let mut idx = 0;\n        let shape = self.shape();\n        for (i, &ind) in indices.iter().enumerate() {\n            if ind >= shape[i] {\n                return None;\n            }\n            idx = idx * shape[i] + ind;\n        }\n        Some(idx)\n    }\n\n    /// Verifies if two tensors can perform element-wise operations\n    ///\n    /// # Arguments\n    /// * `other` - The tensor to compare shapes with\n    ///\n    /// # Returns\n    /// * `Ok(())` if the shapes match\n    /// * `Err(MlError::TensorError)` if shapes don't match\n    fn chk_shape(&self, other: &dyn TensorBase) -> MlResult\u003c()> {\n        if self.shape() != other.shape() {\n            return Err(MlError::TensorError(TensorError::InvalidShape {\n                expected: self.shape().to_vec(),\n                got: other.shape().to_vec(),"},{"path":"src/tensor/display.rs","bytes":1468,"preview":"use super::*;\n\n#[cfg(feature = \"enableBackward\")]\nimpl Debug for ComputationGraph {\n    fn fmt(&self, f: &mut Formatter\u003c'_>) -> Result {\n        let mut ds = f.debug_struct(\"ComputationGraph\");\n        ds\n            .field(\"nodes\", &self.nodes)\n            .field(\"node_map\", &self.node_map)\n            .field(\"adjacency_list\", &self.adjacency_list)\n            .field(\"topo_order\", &self.topo_order)\n            .field(\"is_sorted\", &self.is_sorted)\n            .finish()\n    }\n}\n\n#[cfg(feature = \"enableBackward\")]\nimpl Debug for ComputationNode {\n    fn fmt(&self, f: &mut Formatter\u003c'_>) -> Result {\n        let mut ds = f.debug_struct(\"ComputationNode\");\n        ds\n            .field(\"id\", &self.id)\n            .field(\"tensor\", &self.tensor)\n            .field(\"grad\", &self.grad)\n            .field(\"requires_grad\", &self.requires_grad)\n            .field(\"function\", &self.function.as_ref().unwrap())\n            .field(\"inputs\", &self.inputs)\n            .field(\"is_leaf\", &self.is_leaf)\n            .finish()\n    }\n}\n\nimpl Debug for Tensor {\n    fn fmt(&self, f: &mut Formatter\u003c'_>) -> Result {\n        write!(\n            f, \"data: {:?}, shape: {:?}\",\n            self.data(), self.shape()\n        )\n    }\n}\n\nimpl Debug for &dyn TensorBase {\n    fn fmt(&self, f: &mut Formatter\u003c'_>) -> Result {\n        write!(\n            f, \"data: {:?}, shape: {:?}\",\n            self.data(), self.shape()\n        )\n    }\n}"},{"path":"src/tensor/graph.rs","bytes":18366,"preview":"use super::*;\n\n#[cfg(feature = \"enableBackward\")]\nimpl Variable {\n    pub fn tpye_name(&self) -> String {\n        std::any::type_name::\u003cSelf>()\n            .split(\"::\")\n            .last()\n            .unwrap_or(\"Unknown\")\n            .replace(\"\u003cf32>\", \"\")\n    }\n\n    pub fn with_grad_fn(&self, operator_name: &str, inputs: &[&Variable]) {\n        COMPUTATION_GRAPH.with(|graph| {\n            let mut graph = graph.lock().unwrap();\n\n            let input_ids: Vec\u003cNodeId> = inputs\n                .iter()\n                .map(|&input_var| {\n                    let input_id = input_var.node_id();\n                    if !graph.node_map.contains_key(&input_id) {\n                        graph.add_input(input_var);\n                    }\n                    input_id\n                })\n                .collect();\n            graph.add_operation(self, operator_name, input_ids);\n        })\n    }\n}\n\n#[cfg(feature = \"enableBackward\")]\nimpl ComputationGraph {\n    /// Visits the graph without cloning tensors or exposing mutable graph storage.\n    pub(crate) fn visit_nodes(&self, mut visitor: impl FnMut(ComputationNodeView\u003c'_>)) {\n        for node in &self.nodes {\n            visitor(ComputationNodeView {\n                id: node.id,\n                tensor: &node.tensor,\n                grad: &node.grad,\n                requires_grad: node.requires_grad,\n                operation: node.function.as_deref(),\n                inputs: &node.inputs,\n                is_leaf: node.is_leaf,\n            });\n        }\n    }\n\n    pub fn new() -> Self {\n        Self {\n            nodes: Vec::new(),\n            node_map: HashMap::new(),\n            adjacency_list: Vec::new(),\n            reverse_adjacency: Vec::new(),\n            topo_order: Vec::new(),\n            is_sorted: false,\n        }\n    }\n\n    pub(crate) fn add_input(&mut self, variable: &Variable) -> NodeId {\n        let node_id = variable.node_id();\n        let node_idx = self.nodes.len();\n\n        #[cfg(feature = \"debugging\")]\n        tracing::debug!("},{"path":"src/tensor/mod.rs","bytes":24872,"preview":"use std::{\n    cell::RefCell,\n    collections::HashMap,\n    fmt::{Debug, Formatter, Result},\n    sync::{Arc, atomic::Ordering},\n};\n\nuse crate::legacy::backend::{Backend, CpuBackend, Device};\n\n#[path = \"broadcast.rs\"]\npub mod broadcast;\n#[path = \"creation.rs\"]\npub mod creation;\n#[path = \"display.rs\"]\npub mod display;\n#[path = \"graph.rs\"]\npub mod graph;\n#[path = \"operators/mod.rs\"]\npub mod operators;\n\n/// 명시적으로 라벨이 부여된 텐서인지 판별합니다.\n/// 기본 생성 레이블(`\"tensor_*\"`, `\"tensor_ref_*\"`)은 false를 반환합니다.\n/// `enableVisualization`이 활성화된 경우에만 label 필드가 존재하므로 함께 게이팅합니다.\n#[cfg(all(feature = \"debugging\", feature = \"enableVisualization\"))]\n#[inline(always)]\nfn tensor_is_labeled(label: &str) -> bool {\n    !label.starts_with(\"tensor_\")\n}\n\nuse crate::legacy::nn::{Parameter, Variable};\nuse crate::legacy::{MlError, MlResult, TensorError, register_operator, tensor::operators::Function};\n\n#[macro_export]\nmacro_rules! tensor_ops {\n    ($tensor:expr, Pow, $exponent:expr) => {{\n        let op = crate::legacy::tensor::operators::Pow::new().unwrap();\n        let power_t = Tensor::scalar($exponent);\n        op.forward(&[&$tensor, &power_t]).unwrap().remove(0)\n    }};\n\n    ($tensor:expr, $op:ident, $second_tensor:expr) => {\n        $op::new()\n            .unwrap()\n            .forward(&[&$tensor, &$second_tensor])\n            .unwrap()\n            .remove(0)\n    };\n\n    ($tensor:expr, $op:ident) => {\n        $op::new().unwrap().forward(&[&$tensor]).unwrap().remove(0)\n    };\n\n    ($tensor:expr, Topk, $k:expr, $sorted:expr) => {{\n        let op = crate::legacy::tensor::operators::Topk::new().unwrap();\n        let k_t = Tensor::scalar($k as f32);\n        let sorted_t = Tensor::scalar(if $sorted { 1.0 } else { 0.0 });\n        let mut result = op.forward(&[&$tensor, &k_t, &sorted_t]).unwrap();\n        (result.remove(0), result.remove(0))\n    }};\n\n    ($tensor:expr, Matmax, $dim:expr, $keepdim:expr) => {{\n        let op = crate::legacy::tensor::operators::Matmax::new().unwrap();\n        let dim_val: Option\u003ci32> = $dim;\n        let dim_t = match dim_val {\n            Some(d) => Tensor::scalar(d as f32),"},{"path":"src/tensor/operators/add.rs","bytes":13061,"preview":"use super::*;\nuse crate::legacy::nn::Variable;\nuse crate::legacy::tensor::AutogradFunction;\nuse crate::legacy::tensor::broadcast::{broadcast_offsets, broadcast_shape, reduce_to_shape};\n\nimpl Function for Add {\n    fn new() -> MlResult\u003cGlobalFunction> {\n        register_operator!(Add)\n    }\n    \n    /// Adds two tensors element-wise\n    ///\n    /// # Arguments\n    /// * `other` - The tensor to add to the current tensor\n    ///\n    /// # Returns\n    /// A new tensor with the result of the element-wise addition\n    fn forward(&self, targets: &[&dyn TensorBase]) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        let first_target = targets[0];\n        let second_target = targets[1];\n        let first_shape = first_target.shape();\n        let second_shape = second_target.shape();\n\n        #[cfg(feature = \"debugging\")]\n        tracing::debug!(\n            \"[Add::forward] {} + {}\",\n            crate::legacy::tensor::operators::debug::summary(\"lhs\", first_target),\n            crate::legacy::tensor::operators::debug::summary(\"rhs\", second_target)\n        );\n\n        let result = if first_shape.len() == 2 && second_shape.len() == 1 && first_shape[1] == second_shape[0] {\n            // Special case for matrix + vector broadcasting (hot path)\n            #[cfg(feature = \"debugging\")]\n            tracing::debug!(\"[Add::forward] broadcast path: [{},{}] + [{}]\", first_shape[0], first_shape[1], second_shape[0]);\n\n            let (batch_size, features) = (first_shape[0], first_shape[1]);\n            let mut data = vec![0.0; first_target.data().len()];\n\n            for i in 0..batch_size {\n                for j in 0..features {\n                    data[i * features + j] = first_target.data()[i * features + j] + second_target.data()[j];\n                }\n            }\n\n            Ok(vec![GlobalTensor::from_vec(data, first_shape)?])\n        } else if first_shape == second_shape {\n            Ok(vec![GlobalTensor::from_vec(\n                self.backend().add(first_target.data(), second_target.data()),\n                first_target.shape(),\n            )?])\n        } else {\n            // 범용 브로드캐스트 경로\n            let out_shape = broadcast_shape(first_shape, second_shape)?;\n            let a = first_target.data();\n            let b = second_target.data();\n            let offsets = broadcast_offsets(first_shape, second_shape, &out_shape);\n            let data: Vec\u003cf32> = offsets.iter().map(|&(ao, bo)| a[ao] + b[bo]).collect();\n            Ok(vec![GlobalTensor::from_vec(data, &out_shape)?])\n        };\n\n        #[cfg(feature = \"debugging\")]\n        if let Ok(ref r) = result {\n            tracing::debug!(\"[Add::forward] → {}\", crate::legacy::tensor::operators::debug::summary_raw(\"out\", &r[0].data, &r[0].shape));\n        }\n"},{"path":"src/tensor/operators/concat.rs","bytes":6479,"preview":"use super::*;\n\nimpl Function for Concat {\n    fn new() -> MlResult\u003cGlobalFunction> {\n        register_operator!(Concat)\n    }\n\n    /// 텐서들을 지정한 축(axis)을 따라 이어붙입니다.\n    ///\n    /// # Arguments\n    /// * `targets` - `[tensor1, tensor2, ..., tensorN, axis_scalar]`\n    ///   마지막 원소는 이어붙일 축을 나타내는 스칼라 텐서입니다.\n    ///\n    /// # Returns\n    /// 모든 입력 텐서를 `axis` 방향으로 이어붙인 텐서 하나를 담은 Vec.\n    fn forward(&self, targets: &[&dyn TensorBase]) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        if targets.len() \u003c 2 {\n            return Err(MlError::StringError(\n                \"Concat: 최소 1개의 텐서와 axis 스칼라가 필요합니다.\".into(),\n            ));\n        }\n\n        let axis = targets[targets.len() - 1].data()[0] as usize;\n        let tensors = &targets[..targets.len() - 1];\n\n        let ref_shape = tensors[0].shape();\n        let ndim = ref_shape.len();\n\n        if axis >= ndim {\n            return Err(MlError::StringError(format!(\n                \"Concat: axis {} 는 {}차원 텐서 범위를 벗어났습니다.\",\n                axis, ndim\n            )));\n        }\n\n        for t in tensors.iter().skip(1) {\n            let s = t.shape();\n            if s.len() != ndim {\n                return Err(MlError::StringError(\n                    \"Concat: 모든 텐서의 차원 수가 동일해야 합니다.\".into(),\n                ));\n            }\n            for (i, (&a, &b)) in ref_shape.iter().zip(s.iter()).enumerate() {\n                if i != axis && a != b {\n                    return Err(MlError::StringError(format!(\n                        \"Concat: dim {} 에서 shape 불일치 ({} vs {})\",\n                        i, a, b\n                    )));\n                }\n            }\n        }\n\n        // 출력 shape 계산\n        let mut out_shape = ref_shape.to_vec();\n        out_shape[axis] = tensors.iter().map(|t| t.shape()[axis]).sum();\n\n        let total_size: usize = out_shape.iter().product();\n        let mut out_data = vec![0.0f32; total_size];\n\n        let outer_size: usize = out_shape[..axis].iter().product();\n        let inner_size: usize = out_shape[axis + 1..].iter().product();\n\n        let mut axis_offset = 0usize;\n        for t in tensors.iter() {\n            let t_axis_dim = t.shape()[axis];"},{"path":"src/tensor/operators/conv2d.rs","bytes":20842,"preview":"use super::*;\n\n// ─── im2col / col2im ────────────────────────────────────────────────────────\n\n/// 입력 텐서를 im2col 형태로 변환합니다.\n///\n/// 입력 shape: `[N, C_in, H, W]`\n/// 출력 shape: `[N, C_in*kH*kW, H_out*W_out]` (행 우선 평탄화)\n///\n/// 반환: (col 데이터, N, H_out, W_out, col_rows=C_in*kH*kW)\nfn im2col(\n    input: &[f32],\n    in_shape: &[usize],\n    kernel: (usize, usize),\n    stride: (usize, usize),\n    padding: (usize, usize),\n) -> (Vec\u003cf32>, usize, usize, usize, usize) {\n    let (n, c_in, h, w) = (in_shape[0], in_shape[1], in_shape[2], in_shape[3]);\n    let (kh, kw) = kernel;\n    let (sh, sw) = stride;\n    let (ph, pw) = padding;\n    let h_out = (h + 2 * ph - kh) / sh + 1;\n    let w_out = (w + 2 * pw - kw) / sw + 1;\n    let col_rows = c_in * kh * kw;\n    let col_cols = h_out * w_out;\n\n    let mut col = vec![0.0f32; n * col_rows * col_cols];\n\n    for ni in 0..n {\n        for ci in 0..c_in {\n            for ki in 0..kh {\n                for kj in 0..kw {\n                    let row = ci * kh * kw + ki * kw + kj;\n                    for hi in 0..h_out {\n                        for wi in 0..w_out {\n                            let h_in_pad = hi * sh + ki;\n                            let w_in_pad = wi * sw + kj;\n                            let col_idx = ni * col_rows * col_cols\n                                + row * col_cols\n                                + hi * w_out\n                                + wi;\n                            if h_in_pad \u003c ph\n                                || w_in_pad \u003c pw\n                                || h_in_pad >= h + ph\n                                || w_in_pad >= w + pw\n                            {\n                                col[col_idx] = 0.0;\n                            } else {\n                                let src_h = h_in_pad - ph;\n                                let src_w = w_in_pad - pw;\n                                let in_idx = ni * c_in * h * w\n                                    + ci * h * w\n                                    + src_h * w\n                                    + src_w;\n                                col[col_idx] = input[in_idx];\n                            }\n                        }\n                    }\n                }\n            }\n        }\n    }\n    (col, n, h_out, w_out, col_rows)\n}\n"},{"path":"src/tensor/operators/debug.rs","bytes":2503,"preview":"use crate::legacy::tensor::TensorBase;\n\n/// shape + 앞 최대 4개 값 + NaN/Inf 여부를 포함한 요약 문자열 반환\npub fn summary(label: &str, t: &dyn TensorBase) -> String {\n    let data = t.data();\n    let preview: Vec\u003cf32> = data.iter().take(4).copied().collect();\n    let has_nan = data.iter().any(|x| x.is_nan());\n    let has_inf = data.iter().any(|x| x.is_infinite());\n    format!(\n        \"[{}] shape={:?} preview={:?}{}{}\",\n        label,\n        t.shape(),\n        preview,\n        if has_nan { \" ⚠NaN\" } else { \"\" },\n        if has_inf { \" ⚠Inf\" } else { \"\" },\n    )\n}\n\n/// shape + 앞 최대 4개 값 + NaN/Inf 여부 (GlobalTensor 전용 - data/shape 직접 접근)\npub fn summary_raw(label: &str, data: &[f32], shape: &[usize]) -> String {\n    let preview: Vec\u003cf32> = data.iter().take(4).copied().collect();\n    let has_nan = data.iter().any(|x| x.is_nan());\n    let has_inf = data.iter().any(|x| x.is_infinite());\n    format!(\n        \"[{}] shape={:?} preview={:?}{}{}\",\n        label,\n        shape,\n        preview,\n        if has_nan { \" ⚠NaN\" } else { \"\" },\n        if has_inf { \" ⚠Inf\" } else { \"\" },\n    )\n}\n\n/// min / max / mean / L2 norm 통계를 trace 레벨로 출력\npub fn stats(label: &str, t: &dyn TensorBase) {\n    let data = t.data();\n    if data.is_empty() {\n        tracing::trace!(\"[{}] (empty)\", label);\n        return;\n    }\n    let min  = data.iter().cloned().fold(f32::INFINITY, f32::min);\n    let max  = data.iter().cloned().fold(f32::NEG_INFINITY, f32::max);\n    let mean = data.iter().sum::\u003cf32>() / data.len() as f32;\n    let norm = data.iter().map(|x| x * x).sum::\u003cf32>().sqrt();\n    tracing::trace!(\n        \"[{}] shape={:?} min={:.4} max={:.4} mean={:.4} norm={:.4}\",\n        label, t.shape(), min, max, mean, norm\n    );\n}\n\n/// min / max / mean / L2 norm 통계 (GlobalTensor 전용 - data/shape 직접 접근)\npub fn stats_raw(label: &str, data: &[f32], shape: &[usize]) {\n    if data.is_empty() {\n        tracing::trace!(\"[{}] (empty)\", label);\n        return;\n    }\n    let min  = data.iter().cloned().fold(f32::INFINITY, f32::min);\n    let max  = data.iter().cloned().fold(f32::NEG_INFINITY, f32::max);\n    let mean = data.iter().sum::\u003cf32>() / data.len() as f32;\n    let norm = data.iter().map(|x| x * x).sum::\u003cf32>().sqrt();\n    tracing::trace!(\n        \"[{}] shape={:?} min={:.4} max={:.4} mean={:.4} norm={:.4}\",\n        label, shape, min, max, mean, norm\n    );\n}"},{"path":"src/tensor/operators/div.rs","bytes":6015,"preview":"use super::*;\n\nimpl Function for Div {\n    fn new() -> MlResult\u003cGlobalFunction> {\n        register_operator!(Div)\n    }\n    \n    /// Divides two tensors element-wise\n    ///\n    /// # Arguments\n    /// * `other` - The tensor to divide the current tensor by\n    ///\n    /// # Returns\n    /// A new tensor with the result of the element-wise division\n    fn forward(&self, targets: &[&dyn TensorBase]) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        #[cfg(feature = \"debugging\")]\n        tracing::debug!(\n            \"[Div::forward] {} / {}\",\n            crate::legacy::tensor::operators::debug::summary(\"lhs\", targets[0]),\n            crate::legacy::tensor::operators::debug::summary(\"rhs\", targets[1])\n        );\n\n        match targets[0].chk_shape(targets[1]) {\n            Err(e) => Err(e),\n            _ => Ok(vec![GlobalTensor::from_vec(self.backend().div(targets[0].data(), targets[1].data()), targets[0].shape())?])\n        }\n    }\n\n    fn assign_forward(&self, targets: &[&dyn TensorBase], node_id: NodeId) -> MlResult\u003cVec\u003cTensor>> {\n        match targets[0].chk_shape(targets[1]) {\n            Err(e) => Err(e),\n            _ => Ok(vec![Tensor::with_id(self.backend().div(targets[0].data(), targets[1].data()), targets[0].shape(), node_id)?])\n        }\n    }\n    \n    #[cfg(all(feature = \"enableBackward\"))]\n    fn backward(&self, targets: &[&dyn TensorBase], grad: &dyn TensorBase) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        #[cfg(feature = \"debugging\")]\n        tracing::debug!(\"[Div::backward] {}\", crate::legacy::tensor::operators::debug::summary(\"grad\", grad));\n\n        let x1 = targets[1];\n        let dlhs = self.forward(&[grad, x1])?.remove(0);                                  // grad / x2\n        // Backward runs with the operator registry borrowed. Use backend kernels\n        // directly rather than overloaded operators that re-enter that registry.\n        let negative_lhs = targets[0].data().iter().map(|&x| -x).collect::\u003cVec\u003c_>>();\n        let denominator = self.backend().multiply(x1.data(), x1.data());\n        let quotient = self.backend().div(&negative_lhs, &denominator);\n        let drhs = GlobalTensor::from_vec(\n            self.backend().multiply(grad.data(), &quotient),\n            x1.shape(),\n        )?;\n\n        #[cfg(feature = \"debugging\")]\n        {\n            crate::legacy::tensor::operators::debug::stats_raw(\"  └─ dlhs\", &dlhs.data, &dlhs.shape);\n            crate::legacy::tensor::operators::debug::stats_raw(\"  └─ drhs\", &drhs.data, &drhs.shape);\n        }\n\n        Ok(vec![dlhs, drhs])\n    }\n\n    fn backend(&self) -> &Arc\u003cdyn Backend> { &self.backend }\n\n    fn node_id(&self) -> &NodeId { &self.node_id }\n}"},{"path":"src/tensor/operators/group_norm.rs","bytes":17072,"preview":"use super::*;\n\n// ─── GroupNorm Function ──────────────────────────────────────────────────────\n//\n// forward 입력 순서:\n//   [0] X:          [N, C, H, W]\n//   [1] γ (gamma):  [C]\n//   [2] β (beta):   [C]\n//   [3] num_groups: scalar\n//   [4] eps:        scalar\n//\n// forward 반환 순서:\n//   [0] Y:     [N, C, H, W]  ← 최종 출력\n//   [1] x_hat: [N, C, H, W]  ← backward 재사용\n//   [2] mean:  [N*G]          ← backward 재사용 (flatten)\n//   [3] var:   [N*G]          ← backward 재사용 (flatten)\n//\n// backward targets 순서 (Layer가 조립):\n//   [0] X, [1] γ, [2] β, [3] num_groups, [4] eps,\n//   [5] x_hat, [6] mean, [7] var\n//\n// backward 반환:\n//   [dX, dγ, dβ, 0, 0, 0, 0, 0]\n\nimpl Function for GroupNormOp {\n    fn new() -> MlResult\u003cGlobalFunction> {\n        register_operator!(GroupNormOp)\n    }\n\n    fn forward(&self, targets: &[&dyn TensorBase]) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        if targets.len() \u003c 5 {\n            return Err(MlError::StringError(\n                \"GroupNorm::forward: [X, gamma, beta, num_groups, eps] 필요\".into(),\n            ));\n        }\n        let x     = targets[0];\n        let gamma = targets[1];\n        let beta  = targets[2];\n        let g     = targets[3].data()[0] as usize;\n        let eps   = targets[4].data()[0];\n\n        let shape = x.shape();\n        if shape.len() != 4 {\n            return Err(MlError::StringError(format!(\n                \"GroupNorm: 입력은 4D [N, C, H, W] 이어야 합니다. 현재: {:?}\",\n                shape\n            )));\n        }\n        let (n, c, h, w) = (shape[0], shape[1], shape[2], shape[3]);\n        if c % g != 0 {\n            return Err(MlError::StringError(format!(\n                \"GroupNorm: C({}) % num_groups({}) != 0\",\n                c, g\n            )));\n        }\n        let cg = c / g;        // 그룹당 채널 수\n        let m  = cg * h * w;   // 그룹당 원소 수\n\n        let x_data     = x.data();\n        let gamma_data = gamma.data();\n        let beta_data  = beta.data();\n\n        let mut y_data    = vec![0.0f32; n * c * h * w];\n        let mut xhat_data = vec![0.0f32; n * c * h * w];\n        let mut mean_data = vec![0.0f32; n * g];"},{"path":"src/tensor/operators/matmax.rs","bytes":6928,"preview":"use super::*;\n\n\nimpl Function for Matmax {\n    fn new() -> MlResult\u003cGlobalFunction> {\n        OPERATOR_STORAGE.with(|ops| {\n            let my = \"Matmax\";\n            let mut ops = ops.borrow_mut();\n            match ops.contains_key(my) {\n                true => Ok(GlobalFunction::new(String::from(my), *ops.get(my).unwrap().node_id())),\n                false => {\n                    ops.insert(\n                        String::from(my),\n                        Box::new(Matmax { backend: Arc::new(CpuBackend::new()?), node_id: NODE_ID_GEN.next() })\n                    );\n                    Ok(GlobalFunction::new(String::from(my), *ops.get(my).unwrap().node_id()))\n                }\n            }\n        })\n    }\n    \n    /// Returns the maximum value of all elements in the input tensor.\n    /// If dim is specified, returns the maximum values along the given dimension.\n    ///\n    /// # Arguments\n    /// * `targets[0]` - Input tensor\n    /// * `targets[1]` - dim (Scalar Tensor). If NaN, treat as None (global max).\n    /// * `targets[2]` - keepdim (Scalar Tensor). > 0.5 is true.\n    ///\n    /// # Returns\n    /// If dim is None, returns a tensor with a single element containing the maximum value.\n    /// If dim is specified, returns a tuple of two tensors (values, indices) containing the\n    /// maximum values and their indices along the specified dimension.\n    fn forward(&self, targets: &[&dyn TensorBase]) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        let target_0 = targets[0];\n        let target_0_shape = target_0.shape();\n        let target_0_data = target_0.data();\n        \n        // Parse dim\n        let dim_val = if targets.len() > 1 {\n            targets[1].data()[0]\n        } else {\n            f32::NAN\n        };\n        \n        let dim_opt = if dim_val.is_nan() {\n            None\n        } else {\n            Some(dim_val as i32)\n        };\n\n        // Parse keepdim\n        let keepdim = if targets.len() > 2 {\n            targets[2].data()[0] > 0.5\n        } else {\n            false\n        };\n\n        let buffer = match dim_opt {\n            None => {\n                // Find global maximum\n                let max_val = target_0_data.iter().fold(f32::NEG_INFINITY, |a, &b| a.max(b));\n                vec![GlobalTensor::from_vec(vec![max_val], &vec![1])?, GlobalTensor::zeros(target_0_shape)]\n            }\n            Some(d) => {"},{"path":"src/tensor/operators/matmul.rs","bytes":20068,"preview":"use super::*;\n\n\nimpl Function for Matmul {\n    fn new() -> MlResult\u003cGlobalFunction> {\n        register_operator!(Matmul)\n    }\n    /// Performs matrix multiplication on two tensors\n    ///\n    /// # Arguments\n    /// * `other` - The tensor to multiply the current tensor by\n    ///\n    /// # Returns\n    /// A new tensor with the result of the matrix multiplication\n    // Handle new_empty tensors\n    fn forward(&self, targets: &[&dyn TensorBase]) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        if targets[0].data().is_empty() || targets[1].data().is_empty() {\n            return Err(MlError::TensorError(TensorError::EmptyTensor));\n        }\n        let target_0 = targets[0];\n        let target_0_shape = target_0.shape();\n        let target_0_data = target_0.data();\n        let target_1 = targets[1];\n        let target_1_shape = target_1.shape();\n        let target_1_data = target_1.data();\n\n        let a = target_0_shape.len();\n        let b = target_1_shape.len();\n\n        #[cfg(feature = \"debugging\")]\n        tracing::debug!(\n            \"[Matmul::forward] {} @ {}  (case {}D×{}D)\",\n            crate::legacy::tensor::operators::debug::summary(\"lhs\", target_0),\n            crate::legacy::tensor::operators::debug::summary(\"rhs\", target_1),\n            a, b\n        );\n\n        let buffer = match (a, b) {\n            // Case 1: 1D * 1D (dot product)\n            (1, 1) => {\n                match target_0.chk_shape(target_1) {\n                    Err(e) => return Err(e),\n                    _ => GlobalTensor::from_vec(vec![target_0_data.iter().zip(target_1_data.iter()).map(|(&a, &b)| a * b).sum::\u003cf32>()], &[])?\n                }\n            }\n\n            // Case 2: 2D * 1D or 1D * 2D\n            (2, 1) => {\n                if target_0_shape[1] != target_1_shape[0] {\n                    return Err(MlError::TensorError(\n                        TensorError::MatrixMultiplicationError {\n                            left_shape: target_0_shape.to_vec(),\n                            right_shape: target_1_shape.to_vec(),\n                        },\n                    ));\n                }\n                let m = target_0_shape[0];\n                let k = target_0_shape[1];\n                let mut data = vec![0.0; m];\n\n                for i in 0..m {\n                    let mut sum = 0.0;\n                    for j in 0..k {\n                        sum += target_0_data[i * k + j] * target_1_data[j];\n                    }"},{"path":"src/tensor/operators/mod.rs","bytes":19790,"preview":"use super::*;\n\n/// 연산자의 `impl Function` 보일러플레이트를 자동 생성합니다.\n/// `new()`, `backend()`, `node_id()`는 자동 생성되며, 개발자는 `forward`와 `backward`만 작성하면 됩니다.\n///\n/// # 사용법\n/// ```ignore\n/// // forward + backward\n/// impl_function!(Exp,\n///     forward(self, targets) {\n///         Ok(vec![GlobalTensor::from_vec(self.backend().exp(targets[0].data()), targets[0].shape())?])\n///     },\n///     backward(self, targets, grad) {\n///         let g: Vec\u003cf32> = grad.data().iter().zip(targets[0].data().iter())\n///             .map(|(g, t)| t.exp() * g).collect();\n///         Ok(vec![GlobalTensor::from_vec(g, targets[0].shape())?])\n///     }\n/// );\n///\n/// // forward only (backward 미구현)\n/// impl_function!(Abs,\n///     forward(self, targets) {\n///         Ok(vec![GlobalTensor::from_vec(targets[0].data().iter().map(|&x| x.abs()).collect(), targets[0].shape())?])\n///     }\n/// );\n/// ```\nmacro_rules! impl_function {\n    // forward + backward\n    ($name:ident,\n     forward($self_f:ident, $tgt_f:ident) $fwd:block,\n     backward($self_b:ident, $tgt_b:ident, $grad:ident) $bwd:block\n    ) => {\n        impl Function for $name {\n            fn new() -> MlResult\u003cGlobalFunction> { register_operator!($name) }\n            fn forward(&$self_f, $tgt_f: &[&dyn TensorBase]) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> $fwd\n            #[cfg(all(feature = \"enableBackward\"))]\n            fn backward(&$self_b, $tgt_b: &[&dyn TensorBase], $grad: &dyn TensorBase) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> $bwd\n            fn backend(&self) -> &Arc\u003cdyn Backend> { &self.backend }\n            fn node_id(&self) -> &NodeId { &self.node_id }\n        }\n    };\n\n    // forward only\n    ($name:ident,\n     forward($self_f:ident, $tgt_f:ident) $fwd:block\n    ) => {\n        impl Function for $name {\n            fn new() -> MlResult\u003cGlobalFunction> { register_operator!($name) }\n            fn forward(&$self_f, $tgt_f: &[&dyn TensorBase]) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> $fwd\n            fn backend(&self) -> &Arc\u003cdyn Backend> { &self.backend }\n            fn node_id(&self) -> &NodeId { &self.node_id }\n        }\n    };\n}\n\n#[cfg(feature = \"debugging\")]\n#[path = \"debug.rs\"]\npub(crate) mod debug;\n\n#[path = \"add.rs\"]\npub mod add;\n#[path = \"sub.rs\"]\npub mod sub;\n#[path = \"mul.rs\"]\npub mod mul;"},{"path":"src/tensor/operators/mul.rs","bytes":10994,"preview":"use super::*;\nuse crate::legacy::nn::Variable;\nuse crate::legacy::tensor::AutogradFunction;\nuse crate::legacy::tensor::broadcast::{broadcast_offsets, broadcast_shape, reduce_to_shape};\n\nimpl Function for Mul {\n    fn new() -> MlResult\u003cGlobalFunction> {\n        register_operator!(Mul)\n    }\n    \n    /// Multiplies two tensors element-wise\n    ///\n    /// # Arguments\n    /// * `other` - The tensor to multiply the current tensor by\n    ///\n    /// # Returns\n    /// A new tensor with the result of the element-wise multiplication\n    fn forward(&self, targets: &[&dyn TensorBase]) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        let shape1 = targets[0].shape();\n        let shape2 = targets[1].shape();\n\n        #[cfg(feature = \"debugging\")]\n        tracing::debug!(\n            \"[Mul::forward] {} ⊙ {}\",\n            crate::legacy::tensor::operators::debug::summary(\"lhs\", targets[0]),\n            crate::legacy::tensor::operators::debug::summary(\"rhs\", targets[1])\n        );\n\n        // [1,1] 텐서인 경우에만 브로드캐스팅 (hot path)\n        if shape2 == &[1, 1] {\n            let scalar_value = targets[1].data()[0];\n            let result = targets[0].data()\n                .iter()\n                .map(|&x| x * scalar_value)\n                .collect::\u003cVec\u003cf32>>();\n            return Ok(vec![GlobalTensor::from_vec(result, shape1)?]);\n        }\n\n        if shape1 == shape2 {\n            return Ok(vec![GlobalTensor::from_vec(\n                self.backend().multiply(targets[0].data(), targets[1].data()),\n                shape1,\n            )?]);\n        }\n\n        // 범용 브로드캐스트 경로\n        let out_shape = broadcast_shape(shape1, shape2)?;\n        let a = targets[0].data();\n        let b = targets[1].data();\n        let offsets = broadcast_offsets(shape1, shape2, &out_shape);\n        let data: Vec\u003cf32> = offsets.iter().map(|&(ao, bo)| a[ao] * b[bo]).collect();\n        Ok(vec![GlobalTensor::from_vec(data, &out_shape)?])\n    }\n\n    fn assign_forward(&self, targets: &[&dyn TensorBase], node_id: NodeId) -> MlResult\u003cVec\u003cTensor>> {\n        let shape1 = targets[0].shape();\n        let shape2 = targets[1].shape();\n\n        if shape2 == &[1, 1] {\n            let scalar_value = targets[1].data()[0];\n            let result = targets[0].data()\n                .iter()\n                .map(|&x| x * scalar_value)\n                .collect::\u003cVec\u003cf32>>();\n            return Ok(vec![Tensor::with_id(result, shape1, node_id)?]);"},{"path":"src/tensor/operators/neg.rs","bytes":1277,"preview":"use super::*;\nuse crate::legacy::nn::Variable;\nuse crate::legacy::tensor::AutogradFunction;\n\nimpl_function!(Neg,\n    forward(self, targets) {\n        Ok(vec![GlobalTensor::from_vec(targets[0].data().iter().map(|&x| -x).collect(), targets[0].shape())?])\n    },\n    backward(self, _targets, grad) {\n        Ok(vec![GlobalTensor::from_vec(grad.data().iter().map(|&x| -x).collect(), grad.shape())?])\n    }\n);\n\nimpl std::ops::Neg for Tensor {\n    type Output = Tensor;\n\n    fn neg(self) -> Self::Output {\n        OPERATOR_STORAGE.with(|ops| ops.borrow().get(\"Neg\").unwrap().forward(&[&self]).unwrap().remove(0))\n            .to_id().unwrap()\n    }\n}\n\nimpl std::ops::Neg for &dyn TensorBase {\n    type Output = GlobalTensor\u003cf32>;\n\n    fn neg(self) -> Self::Output {\n        OPERATOR_STORAGE.with(|ops| ops.borrow().get(\"Neg\").unwrap().forward(&[self]).unwrap().remove(0))\n    }\n}\n\n// Variable operator overloading (graph-tracked)\nimpl std::ops::Neg for &Variable {\n    type Output = Variable;\n    fn neg(self) -> Variable {\n        Neg::new().unwrap().apply(&[self]).unwrap()\n    }\n}\n\nimpl std::ops::Neg for Variable {\n    type Output = Variable;\n    fn neg(self) -> Variable {\n        Neg::new().unwrap().apply(&[&self]).unwrap()\n    }\n}\n"},{"path":"src/tensor/operators/pool2d.rs","bytes":14703,"preview":"use super::*;\n\n// ─── MaxPool2d ───────────────────────────────────────────────────────────────\n\n/// MaxPool2d operator\n///\n/// `forward` 입력 순서:\n///   `[input, kH, kW, stride_h, stride_w]`\n///\n/// 반환: `[Y, mask]`\n///   - Y:    `[N, C, H_out, W_out]`\n///   - mask: `[N, C, H_out, W_out]` — 최댓값 위치의 flattened input index (f32 캐스팅)\n///\n/// `backward` 입력 순서 (forward 결과 포함):\n///   `[input, kH, kW, stride_h, stride_w, mask]`  ← targets에 mask 추가\n///\n/// 반환: `[dX, 0, 0, 0, 0, 0]`\nimpl Function for MaxPool2d {\n    fn new() -> MlResult\u003cGlobalFunction> {\n        register_operator!(MaxPool2d)\n    }\n\n    fn forward(&self, targets: &[&dyn TensorBase]) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        if targets.len() \u003c 5 {\n            return Err(MlError::StringError(\n                \"MaxPool2d::forward: [input, kH, kW, stride_h, stride_w] 필요\".into(),\n            ));\n        }\n        let input    = targets[0];\n        let kh       = targets[1].data()[0] as usize;\n        let kw       = targets[2].data()[0] as usize;\n        let stride_h = targets[3].data()[0] as usize;\n        let stride_w = targets[4].data()[0] as usize;\n\n        let in_shape = input.shape();\n        if in_shape.len() != 4 {\n            return Err(MlError::StringError(\n                format!(\"MaxPool2d: 입력은 4D [N, C, H, W] 이어야 합니다. 현재: {:?}\", in_shape)\n            ));\n        }\n        let (n, c, h, w) = (in_shape[0], in_shape[1], in_shape[2], in_shape[3]);\n        let h_out = (h - kh) / stride_h + 1;\n        let w_out = (w - kw) / stride_w + 1;\n\n        let in_data = input.data();\n        let out_size = n * c * h_out * w_out;\n        let mut y_data    = vec![f32::NEG_INFINITY; out_size];\n        let mut mask_data = vec![0.0f32; out_size];\n\n        for ni in 0..n {\n            for ci in 0..c {\n                for hi in 0..h_out {\n                    for wi in 0..w_out {\n                        let out_idx = ni * c * h_out * w_out\n                            + ci * h_out * w_out\n                            + hi * w_out\n                            + wi;\n                        let mut max_val = f32::NEG_INFINITY;\n                        let mut max_src_idx = 0usize;\n                        for khi in 0..kh {\n                            for kwi in 0..kw {\n                                let src_h = hi * stride_h + khi;\n                                let src_w = wi * stride_w + kwi;\n                                let src_idx = ni * c * h * w\n                                    + ci * h * w"},{"path":"src/tensor/operators/reshape.rs","bytes":1526,"preview":"use super::*;\n\nimpl_function!(ReshapeOp,\n    forward(self, targets) {\n        let target = targets[0];\n        let target_shape = target.shape();\n        let target_size: usize = target_shape.iter().product();\n        let new_shape = targets[1].shape();\n        let new_size: usize = new_shape.iter().product();\n\n        if target_size != new_size {\n            return Err(MlError::TensorError(TensorError::InvalidShape {\n                expected: new_shape.to_vec(),\n                got: target_shape.to_vec(),\n            }));\n        }\n\n        #[cfg(feature = \"debugging\")]\n        tracing::debug!(\"[Reshape::forward] {:?} → {:?}\", target_shape, new_shape);\n\n        Ok(vec![GlobalTensor::from_vec(target.data().to_vec(), new_shape)?])\n    },\n    backward(self, targets, grad) {\n        let target = targets[0];\n        let target_shape = target.shape();\n        let target_size: usize = target_shape.iter().product();\n        let new_shape = targets[1].shape();\n        let new_size: usize = new_shape.iter().product();\n\n        if target_size != new_size {\n            return Err(MlError::TensorError(TensorError::InvalidShape {\n                expected: new_shape.to_vec(),\n                got: target_shape.to_vec(),\n            }));\n        }\n\n        #[cfg(feature = \"debugging\")]\n        tracing::debug!(\"[Reshape::backward] grad {:?} → restore {:?}\", grad.shape(), target_shape);\n\n        Ok(vec![GlobalTensor::from_vec(grad.data().to_vec(), target_shape)?])\n    }\n);\n"},{"path":"src/tensor/operators/sub.rs","bytes":7724,"preview":"use super::*;\nuse crate::legacy::nn::Variable;\nuse crate::legacy::tensor::AutogradFunction;\nuse crate::legacy::tensor::broadcast::{broadcast_shape, broadcast_offsets, reduce_to_shape};\n\nimpl Function for Sub {\n    fn new() -> MlResult\u003cGlobalFunction> {\n        register_operator!(Sub)\n    }\n    /// Subtracts two tensors element-wise\n    ///\n    /// # Arguments\n    /// * `other` - The tensor to subtract from_vec the current tensor\n    ///\n    /// # Returns\n    /// A new tensor with the result of the element-wise subtraction\n    fn forward(&self, targets: &[&dyn TensorBase]) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        #[cfg(feature = \"debugging\")]\n        tracing::debug!(\n            \"[Sub::forward] {} - {}\",\n            crate::legacy::tensor::operators::debug::summary(\"lhs\", targets[0]),\n            crate::legacy::tensor::operators::debug::summary(\"rhs\", targets[1])\n        );\n\n        // Shared broadcast_shape currently treats max(0, 1) as 1. Avoid\n        // indexing empty inputs until that shared contract is corrected.\n        if targets[0].shape() != targets[1].shape() && targets.iter().any(|t| t.data().is_empty()) {\n            return Err(MlError::TensorError(TensorError::InvalidOperation {\n                op: \"sub\", reason: \"broadcast subtraction requires nonempty inputs\".into(),\n            }));\n        }\n\n        if targets[0].shape().len() == 2 && targets[1].shape().len() == 1 && targets[0].shape()[1] == targets[1].shape()[0] {\n            let (batch_size, features) = (targets[0].shape()[0], targets[0].shape()[1]);\n            let mut data = vec![0.0; targets[0].data().len()];\n\n            for i in 0..batch_size {\n                for j in 0..features {\n                    data[i * features + j] = targets[0].data()[i * features + j] - targets[1].data()[j];\n                }\n            }\n            return Ok(vec![GlobalTensor::from_vec(data, &targets[0].shape())?])\n        }\n\n        if targets[0].shape() == targets[1].shape() {\n            return Ok(vec![GlobalTensor::from_vec(self.backend().sub(targets[0].data(), targets[1].data()), targets[0].shape())?]);\n        }\n        let shape = broadcast_shape(targets[0].shape(), targets[1].shape())?;\n        let offsets = broadcast_offsets(targets[0].shape(), targets[1].shape(), &shape);\n        let data = offsets.iter().map(|&(a, b)| targets[0].data()[a] - targets[1].data()[b]).collect();\n        Ok(vec![GlobalTensor::from_vec(data, &shape)?])\n    }\n\n    fn assign_forward(&self, targets: &[&dyn TensorBase], node_id: NodeId) -> MlResult\u003cVec\u003cTensor>> {\n        let result = self.forward(targets)?.remove(0);\n        Ok(vec![Tensor::with_id(result.data().to_vec(), result.shape(), node_id)?])\n    }\n\n    #[cfg(all(feature = \"enableBackward\"))]\n    fn backward(&self, targets: &[&dyn TensorBase], grad: &dyn TensorBase) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        #[cfg(feature = \"debugging\")]\n        tracing::debug!(\"[Sub::backward] {}\", crate::legacy::tensor::operators::debug::summary(\"grad\", grad));\n\n        let expected = broadcast_shape(targets[0].shape(), targets[1].shape())?;\n        if grad.shape() != expected {"},{"path":"src/tensor/operators/sum.rs","bytes":1452,"preview":"use super::*;\n\nimpl_function!(Sum,\n    forward(self, inputs) {\n        if inputs.len() != 1 {\n            return Err(MlError::StringError(format!(\n                \"Sum operation expects 1 input tensor, but got {}\",\n                inputs.len()\n            )));\n        }\n        let target = inputs[0];\n        let total_sum: f32 = target.data().iter().sum();\n\n        #[cfg(feature = \"debugging\")]\n        tracing::debug!(\n            \"[Sum::forward] {} → scalar={:.6}\",\n            crate::legacy::tensor::operators::debug::summary(\"in\", target),\n            total_sum\n        );\n\n        Ok(vec![GlobalTensor::from_vec(vec![total_sum], &[1,1])?])\n    },\n    backward(self, targets, grad) {\n        if targets.is_empty() {\n            return Err(MlError::TensorError(TensorError::EmptyTensor));\n        }\n\n        #[cfg(feature = \"debugging\")]\n        tracing::debug!(\n            \"[Sum::backward] {} → broadcast×{}\",\n            crate::legacy::tensor::operators::debug::summary(\"grad\", grad),\n            targets.len()\n        );\n\n        if targets.len() != 1 || grad.data().len() != 1 {\n            return Err(MlError::StringError(\n                \"Sum backward expects one input and a scalar upstream gradient\".into()\n            ));\n        }\n        Ok(vec![GlobalTensor::from_vec(\n            vec![grad.data()[0]; targets[0].data().len()],\n            targets[0].shape(),\n        )?])\n    }\n);\n"},{"path":"src/tensor/operators/topk.rs","bytes":6489,"preview":"use super::*;\n\nimpl Function for Topk {\n    fn new() -> MlResult\u003cGlobalFunction> {\n        OPERATOR_STORAGE.with(|ops| {\n            let my = \"Topk\";\n            let mut ops = ops.borrow_mut();\n            match ops.contains_key(my) {\n                true => Ok(GlobalFunction::new(String::from(my), *ops.get(my).unwrap().node_id())),\n                false => {\n                    ops.insert(\n                        String::from(my),\n                        Box::new(Topk { backend: Arc::new(CpuBackend::new()?), node_id: NODE_ID_GEN.next() })\n                    );\n                    Ok(GlobalFunction::new(String::from(my), *ops.get(my).unwrap().node_id()))\n                }\n            }\n        })\n    }\n    /// Returns the k largest elements of the tensor along the last dimension.\n    ///\n    /// # Arguments\n    /// * `targets[0]` - Input tensor\n    /// * `targets[1]` - k (Scalar Tensor)\n    /// * `targets[2]` - sorted (Scalar Tensor, > 0.5 is true) (Optional, default true)\n    ///\n    /// # Returns\n    /// A tuple of two tensors (values, indices) containing the top k values and their indices\n    fn forward(&self, targets: &[&dyn TensorBase]) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        let input = targets[0];\n        let k_val = if targets.len() > 1 {\n            targets[1].data()[0] as usize\n        } else {\n             return Err(MlError::TensorError(TensorError::InvalidOperation {\n                op: \"topk\",\n                reason: \"k must be provided\".to_string(),\n            }));\n        };\n        \n        let sorted = if targets.len() > 2 {\n            targets[2].data()[0] > 0.5\n        } else {\n            true\n        };\n\n        if k_val == 0 {\n            return Err(MlError::TensorError(TensorError::InvalidOperation {\n                op: \"topk\",\n                reason: \"k must be greater than 0\".to_string(),\n            }));\n        }\n\n        let last_dim = input.shape().len() - 1;\n        let last_dim_size = input.shape()[last_dim];\n\n        if k_val > last_dim_size {\n            return Err(MlError::TensorError(TensorError::InvalidOperation {\n                op: \"topk\",\n                reason: format!(\n                    \"k ({}) cannot be larger than last dimension size ({})\",\n                    k_val, last_dim_size\n                ),\n            }));\n        }\n"},{"path":"src/tensor/operators/transpose.rs","bytes":5613,"preview":"use super::*;\n\nimpl_function!(Transpose,\n    forward(self, targets) {\n        let input = targets[0];\n        let rank = input.shape().len();\n        if rank \u003c 2 {\n            return Err(MlError::TensorError(TensorError::InvalidOperation {\n                op: \"transpose\",\n                reason: \"Tensor must have at least 2 dimensions\".to_string(),\n            }));\n        }\n\n        let d0_val = if targets.len() > 1 {\n            targets[1].data()[0]\n        } else {\n             return Err(MlError::TensorError(TensorError::InvalidOperation {\n                op: \"transpose\",\n                reason: \"d0 must be provided\".to_string(),\n            }));\n        };\n\n        let d1_val = if targets.len() > 2 {\n            targets[2].data()[0]\n        } else {\n             return Err(MlError::TensorError(TensorError::InvalidOperation {\n                op: \"transpose\",\n                reason: \"d1 must be provided\".to_string(),\n            }));\n        };\n\n        let d0 = if d0_val \u003c 0.0 { (rank as i32 + d0_val as i32) as usize } else { d0_val as usize };\n        let d1 = if d1_val \u003c 0.0 { (rank as i32 + d1_val as i32) as usize } else { d1_val as usize };\n\n        if d0 >= rank || d1 >= rank {\n            return Err(MlError::TensorError(TensorError::InvalidAxis {\n                axis: d0.max(d1),\n                shape: input.shape().to_vec(),\n            }));\n        }\n\n        let mut new_shape = input.shape().to_vec();\n        new_shape.swap(d0, d1);\n\n        let mut strides = vec![1usize; rank];\n        for i in (0..rank - 1).rev() {\n            strides[i] = strides[i + 1] * input.shape()[i + 1];\n        }\n\n        let mut result = vec![0.0; input.data().len()];\n        let mut coords = vec![0usize; rank];\n\n        for i in 0..input.data().len() {\n            let mut idx = i;\n            for j in 0..rank {\n                coords[j] = idx / strides[j];\n                idx %= strides[j];\n            }\n\n            coords.swap(d0, d1);\n\n            let mut target_idx = 0;\n            let mut stride = 1;\n            for j in (0..rank).rev() {\n                target_idx += coords[j] * stride;"},{"path":"src/tensor/operators/trigonometric.rs","bytes":6905,"preview":"use super::*;\n\nimpl_function!(Sin,\n    forward(self, targets) {\n        Ok(vec![GlobalTensor::from_vec(targets[0].data().iter().map(|x| x.sin()).collect(), targets[0].shape())?])\n    },\n    backward(self, targets, grad) {\n        let gradient = grad.data().iter()\n            .zip(targets[0].data().iter())\n            .map(|(grad_data, target)| target.cos() * grad_data)\n            .collect();\n\n        Ok(vec![GlobalTensor::from_vec(gradient, targets[0].shape())?])\n    }\n);\n\nimpl_function!(Cos,\n    forward(self, targets) {\n        Ok(vec![GlobalTensor::from_vec(targets[0].data().iter().map(|x| x.cos()).collect(), targets[0].shape())?])\n    },\n    backward(self, targets, grad) {\n        let gradient = grad.data().iter()\n            .zip(targets[0].data().iter())\n            .map(|(grad_data, target)| -target.sin() * grad_data)\n            .collect();\n\n        Ok(vec![GlobalTensor::from_vec(gradient, targets[0].shape())?])\n    }\n);\n\n/// 표준 sin/cos forward에 디버깅 로그 추가\n#[cfg(feature = \"debugging\")]\nfn log_trig_forward(name: &str, input: &dyn crate::legacy::tensor::TensorBase, output: &[f32], shape: &[usize]) {\n    tracing::debug!(\n        \"[{}::forward] {} → {}\",\n        name,\n        crate::legacy::tensor::operators::debug::summary(\"in\", input),\n        crate::legacy::tensor::operators::debug::summary_raw(\"out\", output, shape)\n    );\n}\n\nimpl Function for ApproxSin {\n    fn new() -> MlResult\u003cGlobalFunction> {\n        OPERATOR_STORAGE.with(|ops| {\n            let my = \"ApproxSin\";\n            let mut ops = ops.borrow_mut();\n            match ops.contains_key(my) {\n                true => Ok(GlobalFunction::new(String::from(my), *ops.get(my).unwrap().node_id())),\n                false => {\n                    ops.insert(\n                        String::from(my),\n                        Box::new(ApproxSin { backend: Arc::new(CpuBackend::new()?), node_id: NODE_ID_GEN.next(), threshold: 0.0001 })\n                    );\n                    Ok(GlobalFunction::new(String::from(my), *ops.get(my).unwrap().node_id()))\n                }\n            }\n        })\n    }\n\n    fn forward(&self, targets: &[&dyn TensorBase]) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        let x = targets[0];\n        let x_data = x.data();\n        let mut result = x_data.to_vec();\n\n        let mut term_sign = -1.0;"},{"path":"src/tensor/operators/unary.rs","bytes":3865,"preview":"use super::*;\n\nimpl_function!(Abs,\n    forward(self, targets) {\n        Ok(vec![GlobalTensor::from_vec(targets[0].data().iter().map(|&x| x.abs()).collect(), targets[0].shape())?])\n    },\n    backward(self, targets, grad) {\n        let gradient = targets[0].data().iter().zip(grad.data().iter())\n            .map(|(&x, &g)| if x > 0.0 { g } else if x \u003c 0.0 { -g } else { 0.0 })\n            .collect();\n        Ok(vec![GlobalTensor::from_vec(gradient, targets[0].shape())?])\n    }\n);\n\nimpl_function!(Exp,\n    forward(self, targets) {\n        #[cfg(feature = \"debugging\")]\n        tracing::debug!(\n            \"[Exp::forward] {}\",\n            crate::legacy::tensor::operators::debug::summary(\"in\", targets[0])\n        );\n\n        Ok(vec![GlobalTensor::from_vec(self.backend().exp(targets[0].data()), targets[0].shape())?])\n    },\n    backward(self, targets, grad) {\n        let gradient: Vec\u003cf32> = grad.data().iter()\n            .zip(targets[0].data().iter())\n            .map(|(grad_data, target_data)| target_data.exp() * grad_data)\n            .collect();\n\n        #[cfg(feature = \"debugging\")]\n        crate::legacy::tensor::operators::debug::stats_raw(\"  └─ dExp\", &gradient, targets[0].shape());\n\n        Ok(vec![GlobalTensor::from_vec(gradient, targets[0].shape())?])\n    }\n);\n\nimpl_function!(Log,\n    forward(self, targets) {\n        Ok(vec![GlobalTensor::from_vec(targets[0].data().iter().map(|&x| x.ln()).collect(), targets[0].shape())?])\n    },\n    backward(self, targets, grad) {\n        let gradient = targets[0].data().iter().zip(grad.data().iter())\n            .map(|(&x, &g)| g / x).collect();\n        Ok(vec![GlobalTensor::from_vec(gradient, targets[0].shape())?])\n    }\n);\n\nimpl_function!(Pow,\n    forward(self, targets) {\n        let power = if targets.len() > 1 {\n            targets[1].data()[0]\n        } else {\n             return Err(MlError::TensorError(TensorError::InvalidOperation {\n                op: \"pow\",\n                reason: \"exponent must be provided\".to_string(),\n            }));\n        };\n        Ok(vec![GlobalTensor::from_vec(self.backend().pow(targets[0].data(), power), targets[0].shape())?])\n    },\n    backward(self, targets, grad) {\n        let power = targets[1].data()[0];\n        let target = targets[0];\n        let forwarded = GlobalTensor::from_vec(self.backend().pow(target.data(), power - 1.0), target.shape())?;\n"},{"path":"src/tensor/operators/upsample.rs","bytes":8381,"preview":"use super::*;\n\n// ─── NearestUpsample2d ──────────────────────────────────────────────────────\n\n/// Nearest-neighbor 2D upsampling operator.\n///\n/// `forward` 입력 순서:\n///   `[input, scale_h, scale_w]`\n///   - input:   `[N, C, H, W]`\n///   - scale_h: 스칼라 `[1, 1]` — 높이 확대 배율 (정수)\n///   - scale_w: 스칼라 `[1, 1]` — 너비 확대 배율 (정수)\n///\n/// 반환: `[Y]`  shape `[N, C, H*scale_h, W*scale_w]`\n///\n/// 각 입력 픽셀을 scale_h × scale_w 블록으로 복제합니다.\n///\n/// `backward` 반환: `[dX, 0, 0]`\n///   - dX의 각 원소 = 대응하는 scale_h × scale_w 블록의 upstream gradient 합\nimpl Function for NearestUpsample2d {\n    fn new() -> MlResult\u003cGlobalFunction> {\n        register_operator!(NearestUpsample2d)\n    }\n\n    fn forward(&self, targets: &[&dyn TensorBase]) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        if targets.len() \u003c 3 {\n            return Err(MlError::StringError(\n                \"NearestUpsample2d::forward: [input, scale_h, scale_w] 필요\".into(),\n            ));\n        }\n        let input   = targets[0];\n        let scale_h = targets[1].data()[0] as usize;\n        let scale_w = targets[2].data()[0] as usize;\n\n        let in_shape = input.shape();\n        if in_shape.len() != 4 {\n            return Err(MlError::StringError(format!(\n                \"NearestUpsample2d: 입력은 4D [N, C, H, W] 이어야 합니다. 현재: {:?}\", in_shape\n            )));\n        }\n        let (n, c, h, w) = (in_shape[0], in_shape[1], in_shape[2], in_shape[3]);\n        let h_out = h * scale_h;\n        let w_out = w * scale_w;\n\n        let in_data = input.data();\n        let mut y_data = vec![0.0f32; n * c * h_out * w_out];\n\n        for ni in 0..n {\n            for ci in 0..c {\n                for hi in 0..h {\n                    for wi in 0..w {\n                        let val = in_data[ni * c * h * w + ci * h * w + hi * w + wi];\n                        // scale_h × scale_w 블록에 복제\n                        for sh in 0..scale_h {\n                            for sw in 0..scale_w {\n                                let oh = hi * scale_h + sh;\n                                let ow = wi * scale_w + sw;\n                                y_data[ni * c * h_out * w_out + ci * h_out * w_out + oh * w_out + ow] = val;\n                            }\n                        }\n                    }\n                }\n            }\n        }\n\n        let out_shape = vec![n, c, h_out, w_out];"},{"path":"src/tests/checkpoint_test.rs","bytes":7097,"preview":"use super::*;\n\n// ── 헬퍼 ────────────────────────────────────────────────────────────────────\n\nfn build_net() -> MlResult\u003cSequential> {\n    let mut net = Sequential::new(\"test_net\");\n    net.push(Box::new(Linear::new(4, 8, \"fc1\")?));\n    net.push(Box::new(Linear::new(8, 2, \"fc2\")?));\n    Ok(net)\n}\n\nfn weights_of(net: &Sequential) -> Vec\u003cVec\u003cf32>> {\n    net.params().iter().map(|p| p.tensor().data().to_vec()).collect()\n}\n\nfn zero_all_weights(net: &mut Sequential) {\n    for p in net.params() {\n        let shape = p.tensor().shape().to_vec();\n        let zeros = crate::legacy::tensor::GlobalTensor::zeros(&shape);\n        p.tensor().replace(zeros);\n    }\n}\n\n// ── JSON 저장/로드 테스트 ─────────────────────────────────────────────────────\n\n#[test]\nfn test_sequential_save_load_json() -> MlResult\u003c()> {\n    let tmp = \"target/test_checkpoints/sequential_test.json\";\n\n    let net = build_net()?;\n    let original_weights = weights_of(&net);\n\n    // 저장\n    net.save(tmp)?;\n    assert!(std::path::Path::new(tmp).exists(), \"JSON 파일이 생성되어야 함\");\n\n    // 로드: 가중치를 0으로 초기화한 뒤 복원\n    let mut net2 = build_net()?;\n    zero_all_weights(&mut net2);\n    net2.load(tmp)?;\n\n    let loaded_weights = weights_of(&net2);\n    assert_eq!(original_weights.len(), loaded_weights.len(), \"파라미터 개수가 일치해야 함\");\n    for (orig, loaded) in original_weights.iter().zip(loaded_weights.iter()) {\n        assert_eq!(orig, loaded, \"가중치 값이 일치해야 함\");\n    }\n\n    std::fs::remove_file(tmp).ok();\n    Ok(())\n}\n\n// ── binary (.tdw) 저장/로드 테스트 ──────────────────────────────────────────\n\n#[test]\nfn test_sequential_save_load_binary() -> MlResult\u003c()> {\n    let tmp = \"target/test_checkpoints/sequential_test.tdw\";\n\n    let net = build_net()?;\n    let original_weights = weights_of(&net);\n\n    net.save(tmp)?;\n    assert!(std::path::Path::new(tmp).exists(), \".tdw 파일이 생성되어야 함\");\n\n    let mut net2 = build_net()?;\n    zero_all_weights(&mut net2);"},{"path":"src/tests/common/data/mnist.rs","bytes":3678,"preview":"use super::*;\n\n#[derive(Debug)]\npub struct MnistConfig {\n    pub n_train: u32,\n    pub n_val: u32,\n    pub n_features: usize,\n    pub n_classes: usize,\n    pub n_hidden_1: usize,\n    pub n_hidden_2: usize,\n    pub learning_rate: f32,\n    pub epochs: usize,\n    pub tolerance: f32,\n    pub required_accuracy: f32,\n    pub model_save_path: String,\n    pub visualization_path: String,\n}\n\nimpl Default for MnistConfig {\n    fn default() -> Self {\n        MnistConfig {\n            n_train: 1000,\n            n_val: 1000,\n            n_features: 784,\n            n_classes: 10,\n            n_hidden_1: 128,\n            n_hidden_2: 30,\n            learning_rate: 0.02,\n            epochs: 10,\n            tolerance: 1e-5,\n            required_accuracy: 80.0,\n            model_save_path: \"model_parameters.json\".to_string(),\n            visualization_path: \"graph/test_model.svg\".to_string(),\n        }\n    }\n}\n\npub struct MnistDataset {\n    pub x_train: Vec\u003cVariable>,\n    pub t_train: Vec\u003cVariable>,\n    pub x_test: Vec\u003cVariable>,\n    pub t_test: Vec\u003cVariable>,\n}\n\nimpl MnistDataset {\n    pub fn new() -> Self {\n        Self {\n            x_train: Vec::new(),\n            t_train: Vec::new(),\n            x_test: Vec::new(),\n            t_test: Vec::new(),\n        }\n    }\n\n    pub fn x_train(&self) -> Vec\u003c&Variable> {\n        self.x_train.iter().collect()\n    }\n\n    pub fn t_train(&self) -> Vec\u003c&Variable> {\n        self.t_train.iter().collect()\n    }\n\n    pub fn x_test(&self) -> Vec\u003c&Variable> {\n        self.x_test.iter().collect()\n    }"},{"path":"src/tests/common/data/mod.rs","bytes":290,"preview":"use super::*; // info, MlResult (from common/mod.rs)\n\n// data 하위 모듈에서 사용\nuse ::mnist::MnistBuilder;\nuse crate::legacy::{\n    nn::{Parameter, Variable},\n    tensor::{Tensor, TensorBase},\n    var_input,\n    var_with_label,\n};\n\n#[path = \"mnist.rs\"]\npub mod mnist;\n"},{"path":"src/tests/common/logging.rs","bytes":1950,"preview":"use super::*;\nuse std::sync::OnceLock;\n\nstatic LOGGING_GUARD: OnceLock\u003ctracing_appender::non_blocking::WorkerGuard> = OnceLock::new();\n\n/// 테스트 프로세스의 전역 tracing subscriber를 한 번만 초기화한다.\n///\n/// `WorkerGuard`는 non-blocking writer보다 오래 살아야 하므로 정적 저장소에\n/// 보관한다. 테스트별 지역 변수로 반환하면 최초 테스트 종료 후 writer가\n/// 닫히고, 이후 테스트의 로그가 유실될 수 있다.\npub fn setup_logging() -> &'static tracing_appender::non_blocking::WorkerGuard {\n    LOGGING_GUARD.get_or_init(|| {\n        let file_appender = tracing_appender::rolling::hourly(\"logs\", \"test_run.log\");\n        let (non_blocking_appender, guard) = tracing_appender::non_blocking(file_appender);\n        // 애플리케이션 실행용 RUST_LOG가 테스트 로그를 우연히 억제하지 않도록\n        // 테스트 전용 환경 변수를 사용한다. 예: TEST_LOG=trace cargo test ...\n        let filter = EnvFilter::try_from_env(\"TEST_LOG\").unwrap_or_else(|_| {\n            if cfg!(feature = \"debugging\") {\n                EnvFilter::new(\"debug\")\n            } else {\n                EnvFilter::new(\"debug\")\n            }\n        });\n        let time_format = format_description!(\"[year]-[month]-[day]T[hour]:[minute]:[second]\");\n        let file_layer = fmt::layer()\n            .with_writer(non_blocking_appender)\n            .with_timer(fmt::time::UtcTime::new(time_format))\n            .with_ansi(false);\n        let stdout_layer = fmt::layer()\n            .with_writer(std::io::stdout)\n            .with_timer(fmt::time::UtcTime::new(time_format));\n\n        tracing_subscriber::registry()\n            .with(filter)\n            .with(file_layer)\n            .with(stdout_layer)\n            .try_init()\n            .expect(\"test logging subscriber must be initialized exactly once\");\n\n        guard\n    })\n}\n"},{"path":"src/tests/common/mod.rs","bytes":608,"preview":"// ── 하위 모듈 공통 import ────────────────────────────────────────────────────\n// logging.rs 에서 사용\nuse time::macros::format_description;\nuse tracing_subscriber::{\n    EnvFilter,\n    fmt,\n    layer::SubscriberExt,\n    util::SubscriberInitExt,\n};\n\n// 다수 하위 모듈에서 사용\nuse tracing::info;\nuse crate::legacy::MlResult;\n\n#[path = \"logging.rs\"]\npub(crate) mod logging;\n#[path = \"data/mod.rs\"]\npub(crate) mod data;\n#[path = \"model/mod.rs\"]\npub mod model;\n"},{"path":"src/tests/common/model/mod.rs","bytes":104,"preview":"//! Compatibility exports; implementations belong to native nn.\npub use crate::legacy::nn::models::*;\n"},{"path":"src/tests/mnist_test.rs","bytes":5052,"preview":"use super::*;\n\nuse crate::legacy::trainer::{ClassificationAccuracy, Trainer, SupervisedDataset, EpochSchedule};\n\n/// 테스트용 간이 평가기: argmax 정확도를 누적 계산해 백분율 반환.\n#[cfg(feature = \"enableBackward\")]\nfn evaluate_accuracy\u003cM: Model>(model: &mut M, x: &[&Variable], t: &[&Variable]) -> MlResult\u003cf32> {\n    let mut acc = ClassificationAccuracy::new();\n    for (xi, ti) in x.iter().zip(t.iter()) {\n        let y = model.predict(xi.tensor())?;\n        acc.update(&y, ti.tensor());\n    }\n    Ok(acc.compute())\n}\n\n#[test]\n#[ignore]\n#[cfg(all(feature = \"enableBackward\"))]\nfn mlp_mnist_classification_integration_test() -> MlResult\u003c()> {\n    let _guard = setup_logging();\n    let config = MnistConfig::default();\n    info!(\"=== Starting MLP MNIST Classification Test ===\");\n    info!(\"{:?}\", config);\n\n    let dataset = MnistDataset::load_and_prepare_data(config.n_train, config.n_val, config.n_features, config.n_classes)?;\n    let mut mlp = MLP::build_model(config.n_features, config.n_hidden_2, config.n_classes)?;\n    let mut opt = SGD::new(config.learning_rate);\n    for p in crate::legacy::trainer::TrainableModel::params(&mlp) {\n        opt.register(p);\n    }\n\n    let x_train = dataset.x_train();\n    let t_train = dataset.t_train();\n    let trainer = Trainer::default();\n    #[cfg(feature = \"enableVisualization\")]\n    let trainer = {\n        let capture_dir = std::env::temp_dir().join(\"trench-deep-mnist-mlp-captures\");\n        let writer = crate::legacy::visualization::FileSnapshotWriter::builder(capture_dir)\n            .render_svg(true)\n            .build()?;\n        trainer.with_observer(Box::new(\n        crate::legacy::trainer::GraphVisualizationObserver::builder()\n            .writer(Box::new(writer))\n            .build()?\n        ))\n    };\n    trainer.supervised().fit(&mut mlp, &mut opt,\n        SupervisedDataset::new(&x_train, &t_train)?,\n        EpochSchedule::new(config.epochs)?.with_tolerance(config.tolerance))?;\n\n    let accuracy = evaluate_accuracy(&mut mlp, &dataset.x_train(), &dataset.t_train())?;\n\n    if accuracy > config.required_accuracy {\n        info!(\"Target accuracy achieved! ({:.2}% > {:.2}%)\", accuracy, config.required_accuracy);\n        crate::legacy::trainer::CheckpointableModel::save_checkpoint(&mlp, std::path::Path::new(&config.model_save_path))?;\n    } else {\n        warn!(\"Target accuracy NOT met. (Actual: {:.2}%, Required: {:.2}%).\", accuracy, config.required_accuracy);\n    }\n\n    info!(\"=== MLP MNIST Test Finished ===\");\n    assert!(accuracy > config.required_accuracy, \"Model did not reach the required accuracy threshold.\");\n\n    Ok(())\n}\n"},{"path":"src/tests/mod.rs","bytes":641,"preview":"#[path = \"common/mod.rs\"]\npub mod common;\n#[path = \"repro.rs\"]\nmod repro;\n\n#[cfg(test)]\n#[path = \"mnist_test.rs\"]\nmod mnist_test;\n\n#[cfg(test)]\n#[path = \"checkpoint_test.rs\"]\nmod checkpoint_test;\n\nuse crate::legacy::{\n    MlResult,\n    nn::{\n        GroupNorm,\n        Layer,\n        Linear,\n        Parameter,\n        Sequential,\n        Model,\n        Variable,\n    },\n    tensor::TensorBase,\n    optimizer::{Optimizer, SGD},\n    tests::common::{\n        data::mnist::{MnistConfig, MnistDataset},\n        logging::setup_logging,\n        model::{MLP, SoftmaxRegression},\n    }\n};\nuse tracing::{info, warn};\n"},{"path":"src/tests/nn/layers_tests.rs","bytes":5735,"preview":"use super::*;\n\n#[test]\n#[cfg(all(feature = \"enableBackward\", feature = \"builtinKernels\", feature = \"builtinStorage\"))]\nfn context_linear_tracks_parameters_and_predicts_without_a_graph() -> MlResult\u003c()> {\n    let context = ExecutionContext::new();\n    let layer = Linear::new(&context, 2, 3, \"linear\")?;\n    let input = context.input(vec![1.0, -2.0, 0.5, 3.0], &[2, 2])?;\n    let output = layer.apply(&input)?;\n    assert_eq!(output.tensor().shape()?, vec![2, 3]);\n    context.sum_variable(&output)?.backward()?;\n    assert!(layer.weight().grad()?.is_some());\n    assert!(layer.bias().grad()?.is_some());\n    context.clear_graph()?;\n    let prediction = layer.predict(input.tensor())?;\n    assert_eq!(prediction.shape()?, vec![2, 3]);\n    assert_eq!(context.graph_stats()?.graph_nodes, 0);\n    Ok(())\n}\n\n#[test]\n#[cfg(all(feature = \"builtinKernels\", feature = \"builtinStorage\"))]\nfn context_linear_rejects_foreign_inputs() -> MlResult\u003c()> {\n    let context = ExecutionContext::new();\n    let foreign = ExecutionContext::new();\n    let layer = Linear::new(&context, 2, 1, \"linear\")?;\n    let input = foreign.input(vec![1.0, 2.0], &[1, 2])?;\n    assert!(matches!(\n        layer.apply(&input),\n        Err(crate::MlError::ContextError(crate::ContextError::Mismatch))\n    ));\n    Ok(())\n}\n\n#[test]\n#[cfg(all(feature = \"enableBackward\"))]\nfn parameter_updates_are_fallible_shared_and_graph_free() -> MlResult\u003c()> {\n    let context = ExecutionContext::new();\n    let layer = Linear::new(&context, 2, 1, \"linear\")?;\n    let detached = layer.weight().variable().detach()?;\n    let before = layer.weight().tensor().to_vec()?;\n    context.sub_assign(\n        layer.weight().variable(),\n        &TensorBuffer::from_vec(vec![0.25, -0.5], &[2, 1])?,\n    )?;\n    let after = layer.weight().tensor().to_vec()?;\n    assert_eq!(after, vec![before[0] - 0.25, before[1] + 0.5]);\n    assert_eq!(detached.tensor().to_vec()?, after);\n    assert_eq!(context.graph_stats()?.graph_nodes, 0);\n    assert!(\n        context\n            .add_assign(\n                layer.weight().variable(),\n                &TensorBuffer::from_vec(vec![1.0], &[1])?,\n            )\n            .is_err()\n    );\n    Ok(())\n}\n\n#[test]\n#[cfg(all(feature = \"enableBackward\"))]\nfn convolution_group_norm_and_pooling_form_a_context_graph() -> MlResult\u003c()> {\n    let context = ExecutionContext::new();\n    let convolution = Conv2D::new(&context, 1, 2, (3, 3), (1, 1), (1, 1), \"conv\")?;"},{"path":"src/tests/nn/native_models/autoregressive/mod_tests.rs","bytes":7649,"preview":"use super::*;\nuse crate::legacy::{\n    optimizer::{Adam, Optimizer},\n    trainer::{AutoregressiveModel, TrainableModel, Trainer},\n};\n\n/// 한 토큰 one-hot 을 한 줄로 만든다 (`[V]` 슬라이스를 누적).\nfn one_hot(token: usize, vocab: usize) -> Vec\u003cf32> {\n    let mut row = vec![0.0; vocab];\n    row[token] = 1.0;\n    row\n}\n\n/// 길이 `L+1` 의 토큰 시퀀스를 `[L+1, V]` one-hot 변수로 패킹한다.\nfn pack_sequence(tokens: &[usize], vocab: usize) -> MlResult\u003cVariable> {\n    let mut data = Vec::with_capacity(tokens.len() * vocab);\n    for &t in tokens {\n        data.extend_from_slice(&one_hot(t, vocab));\n    }\n    Ok(Variable::new(Tensor::from_vec(\n        data,\n        &[tokens.len(), vocab],\n    )?))\n}\n\n/// 작은 합성 코퍼스에서 Bigram LM 이 발산 없이 학습되는지 확인한다.\n/// 파일럿 목적은 AR 트레이너의 **제어 흐름 검증** 이지, 성능 달성이 아니다.\n#[test]\nfn bigram_lm_pilot_runs() -> MlResult\u003c()> {\n    let vocab = 4;\n    let mut model = BigramLM::new(vocab)?;\n    let mut opt = Adam::new(1e-1, 0.9, 0.999, 1e-8);\n    for p in model.params() {\n        opt.register(p);\n    }\n\n    // 코퍼스: 반복되는 bigram 패턴 (0→1→2→3→0 …)\n    let sequences = [\n        vec![0, 1, 2, 3, 0, 1, 2],\n        vec![1, 2, 3, 0, 1, 2, 3],\n        vec![2, 3, 0, 1, 2, 3, 0],\n        vec![3, 0, 1, 2, 3, 0, 1],\n    ];\n    let seq_vars: Vec\u003cVariable> = sequences\n        .iter()\n        .map(|s| pack_sequence(s, vocab))\n        .collect::\u003cMlResult\u003cVec\u003c_>>>()?;\n    let dataset = crate::legacy::trainer::DatasetBuilder::from_source(\n        crate::legacy::trainer::MemorySource::new(seq_vars),\n    )\n    .map(|sequence: Variable| {\n        Ok(crate::legacy::trainer::AutoregressiveSample::new(\n            sequence.tensor().clone(),\n        ))\n    })\n    .build()?;\n    let mut loader = crate::legacy::trainer::DataLoader::builder(dataset)\n        .collator(|samples: &[&crate::legacy::trainer::AutoregressiveSample]| {\n            if samples.len() != 1 {\n                return Err(crate::legacy::MlError::StringError(\n                    \"single-sequence collator expects one sample\".into(),\n                ));\n            }\n            Ok(crate::legacy::trainer::AutoregressiveBatch {\n                sequences: Variable::new(samples[0].sequence.clone()),"},{"path":"src/tests/nn/native_models/diffusion/embedding_tests.rs","bytes":1398,"preview":"use super::*;\nuse crate::legacy::tests::common::logging::setup_logging;\n\n/// TimeEmbeddingMLP 의 shape 변환 검증.\n///\n/// [N, 1] → [N, t_emb_dim]\n#[test]\nfn time_embedding_mlp_shape() -> MlResult\u003c()> {\n    setup_logging();\n    let dim = 8;\n    let t_emb_dim = dim * 4; // 32\n    let mlp = TimeEmbeddingMLP::new(dim, t_emb_dim)?;\n\n    // 배치 크기 3, 타임스텝 스칼라 [3, 1]\n    let t = Tensor::from_vec(vec![0.1, 0.5, 0.9], &[3, 1])?;\n    let out = mlp.predict(&t)?;\n\n    assert_eq!(out.shape(), &[3, t_emb_dim]);\n    // 모든 값이 유한해야 함\n    for &v in out.data() {\n        assert!(v.is_finite(), \"TimeEmbeddingMLP output contains non-finite: {}\", v);\n    }\n    Ok(())\n}\n\n/// t_emb_dim 접근자 테스트.\n#[test]\nfn time_embedding_mlp_dim_accessor() -> MlResult\u003c()> {\n    setup_logging();\n    let mlp = TimeEmbeddingMLP::new(16, 64)?;\n    assert_eq!(mlp.t_emb_dim(), 64);\n    Ok(())\n}\n\n/// 학습 경로 (Variable) shape 검증.\n#[cfg(feature = \"enableBackward\")]\n#[test]\nfn time_embedding_mlp_apply_shape() -> MlResult\u003c()> {\n    setup_logging();\n    let dim = 8;\n    let t_emb_dim = 32;\n    let mut mlp = TimeEmbeddingMLP::new(dim, t_emb_dim)?;\n\n    let t = Variable::new(Tensor::from_vec(vec![0.0, 0.5], &[2, 1])?);\n    let out = mlp.apply(&t)?;\n\n    assert_eq!(out.tensor().shape(), &[2, t_emb_dim]);\n    Ok(())\n}\n"},{"path":"src/tests/nn/native_models/diffusion/encoder_tests.rs","bytes":857,"preview":"use super::*;\n\n#[test]\nfn sinusoidal_pe_predict_shape() -> MlResult\u003c()> {\n    let mut pe = SinusoidalPE::new(4, \"pe\")?;\n    let t = Tensor::from_vec(vec![0.0, 1.0], &[2, 1])?;\n    let out = pe.predict(&t)?;\n    assert_eq!(out.shape(), &[2, 4]);\n    // 앞 half = sin(args), 뒤 half = cos(args).  t=0 → sin=0, cos=1.\n    assert!((out.data()[0] - 0.0).abs() \u003c 1e-6);\n    assert!((out.data()[1] - 0.0).abs() \u003c 1e-6);\n    assert!((out.data()[2] - 1.0).abs() \u003c 1e-6);\n    assert!((out.data()[3] - 1.0).abs() \u003c 1e-6);\n    Ok(())\n}\n\n#[cfg(feature = \"enableBackward\")]\n#[test]\nfn sinusoidal_pe_apply_shape() -> MlResult\u003c()> {\n    let mut pe = SinusoidalPE::new(8, \"pe\")?;\n    let t = Variable::new(Tensor::from_vec(vec![0.0, 1.0, 2.0], &[3, 1])?);\n    let out = pe.apply(&t)?;\n    assert_eq!(out.tensor().shape(), &[3, 8]);\n    Ok(())\n}\n"},{"path":"src/tests/nn/native_models/diffusion/mod_tests.rs","bytes":19474,"preview":"use super::*;\nuse crate::legacy::tensor::operators::Function;\nuse crate::legacy::tests::common::logging::setup_logging;\n\n/// Diffusion 모델 생성 + loss 계산 end-to-end 테스트.\n#[test]\nfn diffusion_compute_loss() -> MlResult\u003c()> {\n    setup_logging();\n    // 작은 모델: 8×8 grayscale, dim=8, 2 stages\n    let model = Diffusion::new(\n        1, 8,               // 1채널, 8×8\n        8,                  // dim\n        &[1, 2],           // dim_mults\n        4,                  // resnet_groups\n        &[false, true],    // attn at last stage\n        10,                 // timesteps (작게)\n        1e-4, 0.02,        // beta schedule\n    )?;\n\n    // 랜덤 \"이미지\" 배치\n    let x_0 = Tensor::from_vec(vec![0.5; 2 * 1 * 8 * 8], &[2, 1, 8, 8])?;\n    let loss = model.compute_loss(&x_0)?;\n\n    // loss 는 유한한 양수여야 함\n    assert!(loss.is_finite(), \"loss = {} (must be finite)\", loss);\n    assert!(loss >= 0.0, \"loss = {} (must be >= 0)\", loss);\n    info!(\"Diffusion compute_loss: {:.6}\", loss);\n    Ok(())\n}\n\n/// Diffusion 샘플링 shape 테스트.\n#[test]\nfn diffusion_sample_shape() -> MlResult\u003c()> {\n    setup_logging();\n    let model = Diffusion::new(\n        1, 8,\n        8, &[1, 2],\n        4, &[false, true],\n        5,           // 아주 적은 timestep (테스트 속도)\n        1e-4, 0.02,\n    )?;\n\n    let samples = model.sample(2)?;\n    assert_eq!(samples.shape(), &[2, 1, 8, 8]);\n    // 값이 유한해야 함 (NaN/Inf 없음)\n    for &v in samples.data() {\n        assert!(v.is_finite(), \"sample contains non-finite value: {}\", v);\n    }\n    Ok(())\n}\n\n/// Scheduler 와 Unet 이 조합되어 동작하는지 확인.\n#[test]\nfn diffusion_forward_reverse_roundtrip() -> MlResult\u003c()> {\n    setup_logging();\n    let model = Diffusion::new(\n        1, 8,\n        8, &[1, 2],\n        4, &[false, false],\n        100,\n        1e-4, 0.02,\n    )?;\n\n    // 원본 이미지\n    let x_0 = Tensor::from_vec(vec![1.0; 1 * 1 * 8 * 8], &[1, 1, 8, 8])?;"},{"path":"src/tests/nn/native_models/diffusion/scheduler_tests.rs","bytes":3503,"preview":"use super::*;\n\n#[test]\nfn linear_schedule_values() {\n    let s = DDPMScheduler::linear_schedule(1000, 1e-4, 0.02);\n    assert_eq!(s.betas.len(), 1000);\n    // β_0 ≈ 1e-4, β_999 ≈ 0.02\n    assert!((s.betas[0] - 1e-4).abs() \u003c 1e-6);\n    assert!((s.betas[999] - 0.02).abs() \u003c 1e-4);\n    // ᾱ 는 단조감소\n    for i in 1..1000 {\n        assert!(s.alpha_bars[i] \u003c s.alpha_bars[i - 1],\n            \"alpha_bars must decrease: t={} ({}) >= t={} ({})\",\n            i, s.alpha_bars[i], i - 1, s.alpha_bars[i - 1]);\n    }\n    // ᾱ_0 ≈ 1, ᾱ_999 ≈ 0\n    assert!(s.alpha_bars[0] > 0.99);\n    assert!(s.alpha_bars[999] \u003c 0.1);\n}\n\n#[test]\nfn cosine_schedule_monotonic() {\n    let s = DDPMScheduler::cosine_schedule(1000, 0.008);\n    assert_eq!(s.betas.len(), 1000);\n    for i in 1..1000 {\n        assert!(s.alpha_bars[i] \u003c= s.alpha_bars[i - 1] + 1e-6,\n            \"cosine alpha_bars must decrease: t={}\", i);\n    }\n}\n\n#[test]\nfn q_sample_shape_preserved() -> MlResult\u003c()> {\n    let s = DDPMScheduler::linear_schedule(100, 1e-4, 0.02);\n    let x_0 = Tensor::from_vec(vec![1.0; 2 * 3 * 4 * 4], &[2, 3, 4, 4])?;\n    let noise = Tensor::randn(&[2, 3, 4, 4]);\n    let x_t = s.q_sample(&x_0, 50, &noise)?;\n    assert_eq!(x_t.shape(), &[2, 3, 4, 4]);\n    Ok(())\n}\n\n#[test]\nfn q_sample_t0_close_to_original() -> MlResult\u003c()> {\n    let s = DDPMScheduler::linear_schedule(1000, 1e-4, 0.02);\n    let x_0 = Tensor::from_vec(vec![1.0; 16], &[1, 1, 4, 4])?;\n    let noise = Tensor::from_vec(vec![0.5; 16], &[1, 1, 4, 4])?;\n\n    // t=0: √ᾱ_0 ≈ 1, √(1-ᾱ_0) ≈ 0 → x_t ≈ x_0\n    let x_t = s.q_sample(&x_0, 0, &noise)?;\n    for (&xt, &x0) in x_t.data().iter().zip(x_0.data().iter()) {\n        assert!((xt - x0).abs() \u003c 0.05, \"t=0: x_t should be close to x_0\");\n    }\n    Ok(())\n}\n\n#[test]\nfn q_sample_large_t_dominated_by_noise() -> MlResult\u003c()> {\n    let s = DDPMScheduler::linear_schedule(1000, 1e-4, 0.02);\n    let x_0 = Tensor::from_vec(vec![10.0; 16], &[1, 1, 4, 4])?;\n    let noise = Tensor::from_vec(vec![0.0; 16], &[1, 1, 4, 4])?;\n\n    // t=999: ᾱ_999 ≈ 0 → x_t ≈ noise (≈0)\n    let x_t = s.q_sample(&x_0, 999, &noise)?;\n    let mean: f32 = x_t.data().iter().sum::\u003cf32>() / x_t.data().len() as f32;\n    assert!(mean.abs() \u003c 2.0,\n        \"t=999: signal should be nearly gone, mean={}\", mean);"},{"path":"src/tests/nn/native_models/diffusion/unet_tests.rs","bytes":9941,"preview":"use super::*;\nuse crate::legacy::nn::activation::SoftmaxOp;\nuse crate::legacy::tensor::operators::Function;\n\n// ── SelfAttentionBlock 테스트 ────────────────────────────────────────────\n\n#[test]\nfn self_attention_predict_shape() -> MlResult\u003c()> {\n    let block = SelfAttentionBlock::new(16, 8, \"attn\")?;\n    let n = 2;\n    let data: Vec\u003cf32> = (0..(n * 16 * 4 * 4)).map(|i| i as f32 * 0.01).collect();\n    let x = Tensor::from_vec(data, &[n, 16, 4, 4])?;\n    let out = block.predict(&x)?;\n    assert_eq!(out.shape(), &[n, 16, 4, 4]);\n    Ok(())\n}\n\n#[test]\nfn self_attention_new_rejects_invalid_groups() {\n    assert!(SelfAttentionBlock::new(16, 5, \"attn\").is_err());\n}\n\n#[cfg(feature = \"enableBackward\")]\n#[test]\nfn self_attention_apply_shape() -> MlResult\u003c()> {\n    let mut block = SelfAttentionBlock::new(8, 4, \"attn\")?;\n    let data: Vec\u003cf32> = (0..(1 * 8 * 2 * 2)).map(|i| i as f32 * 0.01).collect();\n    let x = Variable::new(Tensor::from_vec(data, &[1, 8, 2, 2])?);\n    let out = block.apply(&x)?;\n    assert_eq!(out.tensor().shape(), &[1, 8, 2, 2]);\n    Ok(())\n}\n\n// ── Axis-aware Softmax 테스트 ────────────────────────────────────────────\n\n#[test]\nfn softmax_axis_2d_row_sum() -> MlResult\u003c()> {\n    let input = GlobalTensor::from_vec(vec![1.0, 2.0, 3.0, 1.0, 1.0, 1.0], &[2, 3])?;\n    let axis = GlobalTensor::from_vec(vec![1.0], &[1, 1])?;\n    let out = SoftmaxOp::new()?.forward(&[&input, &axis])?.remove(0);\n\n    assert_eq!(out.shape(), &[2, 3]);\n    let row0_sum: f32 = out.data()[0..3].iter().sum();\n    assert!((row0_sum - 1.0).abs() \u003c 1e-6, \"row0 sum = {}\", row0_sum);\n    let row1_sum: f32 = out.data()[3..6].iter().sum();\n    assert!((row1_sum - 1.0).abs() \u003c 1e-6, \"row1 sum = {}\", row1_sum);\n    for &v in &out.data()[3..6] {\n        assert!((v - 1.0 / 3.0).abs() \u003c 1e-6);\n    }\n    Ok(())\n}\n\n#[test]\nfn softmax_axis_3d_last() -> MlResult\u003c()> {\n    let n = 2 * 3 * 4;\n    let data: Vec\u003cf32> = (0..n).map(|i| i as f32 * 0.1).collect();\n    let input = GlobalTensor::from_vec(data, &[2, 3, 4])?;\n    let axis = GlobalTensor::from_vec(vec![-1.0], &[1, 1])?;\n    let out = SoftmaxOp::new()?.forward(&[&input, &axis])?.remove(0);\n\n    assert_eq!(out.shape(), &[2, 3, 4]);\n    for batch in 0..2 {\n        for row in 0..3 {\n            let start = batch * 12 + row * 4;\n            let row_sum: f32 = out.data()[start..start + 4].iter().sum();"},{"path":"src/tests/nn/native_models/linear/linear_data_loader_tests.rs","bytes":1410,"preview":"use super::*;\nuse crate::legacy::{\n    optimizer::{Optimizer, SGD},\n    trainer::{\n        DataLoader, DatasetBuilder, EpochSchedule, MemorySource, SupervisedSample,\n        SupervisedStackCollator, Trainer,\n    },\n};\n\n#[test]\nfn supervised_trainer_accepts_built_loader() -> MlResult\u003c()> {\n    let dataset = DatasetBuilder::from_source(MemorySource::new(vec![\n        ([0.0_f32, 0.0], [0.0_f32]),\n        ([1.0, 0.0], [1.0]),\n        ([0.0, 1.0], [1.0]),\n        ([1.0, 1.0], [2.0]),\n    ]))\n    .map(|(input, target): ([f32; 2], [f32; 1])| {\n        Ok(SupervisedSample::new(\n            Tensor::from_vec(input.to_vec(), &[2])?,\n            Tensor::from_vec(target.to_vec(), &[1])?,\n        ))\n    })\n    .build()?;\n    let mut loader = DataLoader::builder(dataset)\n        .collator(SupervisedStackCollator::new())\n        .batch_size(2)\n        .shuffle(false)\n        .build()?;\n\n    let mut model = LinearRegression::build_model(2, 1)?;\n    let mut optimizer = SGD::new(0.01);\n    for parameter in crate::legacy::trainer::TrainableModel::params(&model) {\n        optimizer.register(parameter);\n    }\n\n    let result = Trainer::silent().supervised().fit(\n        &mut model,\n        &mut optimizer,\n        &mut loader,\n        EpochSchedule::new(2)?,\n    )?;\n    assert_eq!(result.units_completed, 2);\n    assert!(result.final_loss.is_finite());\n    Ok(())\n}\n"},{"path":"src/tests/nn/native_models/reinforcement/mod_tests.rs","bytes":2481,"preview":"use super::*;\nuse crate::legacy::{\n    optimizer::{Adam, Optimizer},\n    trainer::{RLModel, Trainer, TrainableModel},\n};\n\n/// REINFORCE 가 발산 없이 돌고, 결과적으로 최적 행동(1)의 로짓이 더 커지는지 검증.\n/// 파일럿 목적은 트레이너 인터페이스 검증이므로 엄격한 통계 테스트는 하지 않는다.\n#[test]\nfn reinforce_bandit_pilot_runs() -> MlResult\u003c()> {\n    let mut policy = LinearPolicy::new(1, 2)?;\n    let mut env    = TwoArmedBandit::default();\n    let mut opt    = Adam::new(1e-1, 0.9, 0.999, 1e-8);\n    for p in policy.params() {\n        opt.register(p);\n    }\n\n    let trainer = Trainer::silent().reinforcement()\n        .with_gamma(1.0)        // 1-step bandit, 할인 불필요\n        .with_baseline(true);\n\n    let result = trainer.fit(&mut policy, &mut env, &mut opt,\n        crate::legacy::trainer::EpisodeSchedule::new(100, 1)?)?;\n\n    assert_eq!(result.units_completed, 100);\n    assert!(\n        result.final_loss.is_finite(),\n        \"최종 손실이 유한해야 함: got {}\", result.final_loss\n    );\n\n    // 학습 후 정책이 최적 행동(1) 쪽으로 치우쳐야 함.\n    let obs = Tensor::from_vec(vec![1.0], &[1, 1])?;\n    let logits = policy.predict_policy_raw(&obs)?;\n    let data   = logits.data.as_slice();\n    assert_eq!(data.len(), 2);\n    assert!(\n        data[1] > data[0],\n        \"학습 후 action 1 의 로짓이 더 커야 함: logits = {:?}\", data\n    );\n\n    Ok(())\n}\n\n/// `use_baseline=false` 일 때도 학습이 발산 없이 동작하는지 확인.\n#[test]\nfn reinforce_bandit_no_baseline() -> MlResult\u003c()> {\n    let mut policy = LinearPolicy::new(1, 2)?;\n    let mut env    = TwoArmedBandit::default();\n    let mut opt    = Adam::new(5e-2, 0.9, 0.999, 1e-8);\n    for p in policy.params() {\n        opt.register(p);\n    }\n\n    let trainer = Trainer::silent().reinforcement()\n        .with_gamma(1.0)\n        .with_baseline(false);\n\n    let result = trainer.fit(&mut policy, &mut env, &mut opt,\n        crate::legacy::trainer::EpisodeSchedule::new(10, 1)?)?;\n\n    assert_eq!(result.units_completed, 10);\n    assert!(result.final_loss.is_finite());\n    Ok(())\n}\n"},{"path":"src/tests/nn/native_models/semi_supervised/mod_tests.rs","bytes":4569,"preview":"use super::*;\nuse crate::legacy::{\n    optimizer::{Adam, Optimizer},\n    trainer::{ConsistencyRamp, SemiSupervisedModel, Trainer, TrainableModel},\n};\n\n/// labeled 4 개 + unlabeled 8 개로 Pi-model 학습이 **발산 없이** 동작하는지 확인.\n/// 파일럿의 목적은 트레이너 인터페이스 검증이지 정확도 달성이 아니다.\n#[test]\nfn pi_model_pilot_runs() -> MlResult\u003c()> {\n    // ── 모델 + 옵티마이저 ────────────────────────────────────────────\n    let mut model = PiToyClassifier::new(2, 2, 0.1)?;\n    let mut opt   = Adam::new(1e-2, 0.9, 0.999, 1e-8);\n    for p in model.params() {\n        opt.register(p);\n    }\n\n    // ── 데이터 (2D 이진 분류: 사분면 기반) ──────────────────────────\n    // class 0 : (+1, +1) 근처\n    // class 1 : (-1, -1) 근처\n    let labeled = vec![\n        ([ 1.0,  1.0], [1.0, 0.0]),\n        ([ 0.9,  1.1], [1.0, 0.0]),\n        ([-1.0, -1.0], [0.0, 1.0]),\n        ([-1.1, -0.9], [0.0, 1.0]),\n    ];\n    // unlabeled: 두 군집 주변에 흩뿌린 8 개 점\n    let unlabeled = vec![\n        [ 1.2,  0.8], [ 0.7,  1.3], [ 1.1,  1.0], [ 0.8,  0.9],\n        [-1.2, -0.8], [-0.7, -1.3], [-1.1, -1.0], [-0.8, -0.9],\n    ];\n    let labeled_dataset = crate::legacy::trainer::DatasetBuilder::from_source(\n        crate::legacy::trainer::MemorySource::new(labeled),\n    )\n    .map(|(input, target): ([f32; 2], [f32; 2])| Ok(crate::legacy::trainer::SupervisedSample::new(\n        Tensor::from_vec(input.to_vec(), &[2])?,\n        Tensor::from_vec(target.to_vec(), &[2])?,\n    )))\n    .build()?;\n    let unlabeled_dataset = crate::legacy::trainer::DatasetBuilder::from_source(\n        crate::legacy::trainer::MemorySource::new(unlabeled),\n    )\n    .map(|input: [f32; 2]| Ok(crate::legacy::trainer::UnsupervisedSample::new(\n        Tensor::from_vec(input.to_vec(), &[2])?,\n    )))\n    .build()?;\n    let mut loader = crate::legacy::trainer::SemiSupervisedDataLoader::builder(\n        labeled_dataset,\n        unlabeled_dataset,\n    )\n    .labeled_collator(crate::legacy::trainer::SupervisedStackCollator::new())\n    .unlabeled_collator(crate::legacy::trainer::UnsupervisedStackCollator::new())\n    .labeled_batch_size(2)\n    .unlabeled_batch_size(4)\n    .build()?;\n\n    // ── 트레이너: silent + 짧은 램프 ────────────────────────────────\n    let trainer = Trainer::silent().semi_supervised()\n        .with_ramp(ConsistencyRamp::Sigmoid { max_weight: 1.0, ramp_epochs: 5 });\n\n    let result = trainer.fit(&mut model, &mut opt,\n        &mut loader,\n        crate::legacy::trainer::EpochSchedule::new(8)?.with_tolerance(1e-10))?;\n\n    // ── 검증 ────────────────────────────────────────────────────────"},{"path":"src/tests/README.md","bytes":845},{"path":"src/tests/repro.rs","bytes":2850,"preview":"#[cfg(test)]\nmod reproduction_test {\n    use crate::legacy::{\n        MlResult,\n        nn::Parameter,\n        tensor::{ComputationGraph, Tensor, TENSOR_STORAGE, TensorBase},\n        var_input\n    };\n\n    #[test]\n    #[cfg(all(feature = \"enableBackward\"))]\n    fn test_graph_reset_drops_variable_data() -> MlResult\u003c()> {\n        let _guard = crate::legacy::tests::common::logging::setup_logging();\n\n        // 1. Create a variable\n        let data = vec![1.0, 2.0, 3.0, 4.0];\n        let shape = vec![2, 2];\n        let tensor = Tensor::from_vec(data.clone(), &shape)?;\n        let id = tensor.id();\n        let var = var_input!(tensor);\n\n        println!(\"Variable created. ID: {:?}\", id);\n\n        // Verify data exists\n        let exists = TENSOR_STORAGE.with(|s| s.borrow().contains_key(&id));\n        assert!(exists, \"Data should exist initially\");\n\n        // 2. Add to graph (simulate what happens in training)\n        // We need to simulate with_grad_fn or manual addition\n        var.with_grad_fn(\"test_op\", &[]);\n\n        println!(\"Added to graph.\");\n\n        // 3. Reset graph\n        ComputationGraph::reset_graph();\n        println!(\"Graph reset.\");\n\n        // 4. Check if data still exists\n        let exists_after = TENSOR_STORAGE.with(|s| s.borrow().contains_key(&id));\n\n        if !exists_after {\n            println!(\"CRITICAL: Data released after reset_graph!\");\n        } else {\n            println!(\"Data still exists.\");\n        }\n\n        assert!(exists_after, \"Data should persist because 'var' holds a reference\");\n\n        Ok(())\n    }\n\n    #[test]\n    \n    fn test_tensor_with_id_destruction() -> MlResult\u003c()> {\n        let _guard = crate::legacy::tests::common::logging::setup_logging();\n\n        // 1. Create a tensor\n        let data = vec![1.0, 2.0];\n        let shape = vec![1, 2];\n        let tensor = Tensor::from_vec(data.clone(), &shape)?;\n        let id = tensor.id();\n\n        println!(\"Tensor created. ID: {:?}\", id);\n\n        // 2. Create a temporary tensor with SAME ID using Tensor::with_id"},{"path":"src/tests/trainer/autoregressive_tests.rs","bytes":1904,"preview":"use super::*;\n\n#[test]\nfn default_preset_auto_attaches_perplexity_hook() {\n    let trainer = AutoregressiveTrainer::default();\n    assert_eq!(\n        trainer.core.hook_count(),\n        1,\n        \"default() 는 Perplexity 훅을 자동 장착해야 함\"\n    );\n}\n\n#[test]\nfn verbose_preset_auto_attaches_perplexity_hook() {\n    let trainer = AutoregressiveTrainer::verbose();\n    assert_eq!(\n        trainer.core.hook_count(),\n        1,\n        \"verbose() 는 Perplexity 훅을 자동 장착해야 함\"\n    );\n}\n\n#[test]\nfn minimal_preset_has_no_perplexity_hook() {\n    let trainer = AutoregressiveTrainer::minimal();\n    assert_eq!(\n        trainer.core.hook_count(),\n        0,\n        \"minimal() 은 핵심 메트릭만 표시하므로 PPL 훅이 없어야 함\"\n    );\n}\n\n#[test]\nfn silent_preset_has_no_auto_hooks() {\n    let trainer = AutoregressiveTrainer::silent();\n    assert_eq!(\n        trainer.core.hook_count(),\n        0,\n        \"silent() 은 epoch_log_interval=MAX → 훅 없음 (최대 성능 모드)\"\n    );\n}\n\n#[test]\nfn from_config_raw_path_has_no_auto_hooks() {\n    // epoch_log_interval 이 유효해도 from_config 경로는 자동 장착 안 함.\n    let cfg = LogConfig {\n        batch_log_interval: 1,\n        batch_summary_interval: 100,\n        epoch_log_interval: 1,\n        nan_check_interval: 1,\n        metrics: Metrics::default(),\n        show_progress: false,\n        checkpoint_dir: None,\n        seed: 0,\n    };\n    let trainer = AutoregressiveTrainer::from_config(cfg);\n    assert_eq!(trainer.core.hook_count(), 0);\n}\n\n#[test]\nfn with_perplexity_shortcut_adds_single_hook_from_silent() {\n    let trainer = AutoregressiveTrainer::silent().with_perplexity();\n    assert_eq!(\n        trainer.core.hook_count(),\n        1,"},{"path":"src/tests/trainer/core/convergence_tests.rs","bytes":1067,"preview":"use super::*;\n\n#[test]\nfn from_tolerance_maps_zero_and_negative_to_off() {\n    assert_eq!(Convergence::from_tolerance(0.0), Convergence::Off);\n    assert_eq!(Convergence::from_tolerance(-1e-6), Convergence::Off);\n    assert_eq!(Convergence::from_tolerance(f32::NAN), Convergence::Off);\n}\n\n#[test]\nfn from_tolerance_maps_positive_to_absdelta() {\n    assert_eq!(\n        Convergence::from_tolerance(1e-6),\n        Convergence::AbsDelta(1e-6)\n    );\n}\n\n#[test]\nfn tolerance_roundtrip() {\n    assert_eq!(Convergence::Off.tolerance(), 0.0);\n    assert_eq!(Convergence::AbsDelta(3.25e-4).tolerance(), 3.25e-4);\n}\n\n#[test]\nfn should_stop_first_epoch_never_converges() {\n    let c = Convergence::AbsDelta(1.0);\n    assert!(!c.should_stop(f32::INFINITY, 0.5));\n}\n\n#[test]\nfn should_stop_converges_when_delta_small() {\n    let c = Convergence::AbsDelta(1e-3);\n    assert!(c.should_stop(0.50000, 0.50005));\n    assert!(!c.should_stop(0.50, 0.60));\n}\n\n#[test]\nfn off_never_stops() {\n    assert!(!Convergence::Off.should_stop(0.5, 0.5));\n}\n"},{"path":"src/tests/trainer/core/epoch_loop_tests.rs","bytes":6981,"preview":"use super::*;\n\nstruct UnknownLengthLoader { next: usize }\nimpl BatchLoader for UnknownLengthLoader {\n    type Batch = usize;\n    fn begin_epoch(&mut self, _epoch: usize, _runtime: &TrainingRuntime) -> MlResult\u003c()> { self.next = 0; Ok(()) }\n    fn next_batch(&mut self) -> MlResult\u003cOption\u003cSelf::Batch>> {\n        if self.next == 3 { return Ok(None); }\n        let value = self.next;\n        self.next += 1;\n        Ok(Some(value))\n    }\n    fn batch_count(&self) -> Option\u003cusize> { None }\n}\n\nstruct TestStep;\nimpl EpochStep for TestStep {\n    type Batch = usize;\n    fn forward_backward(&mut self, batch: usize, _value: usize, _cfg: &LogConfig) -> MlResult\u003cStepOutput> {\n        Ok(StepOutput {\n            loss: batch as f32 + 1.0,\n            loss_weight: 1,\n            observations: BatchObservations::default(),\n            diagnostics: StepDiagnostics {\n                has_nan: false, fw_dur: None, bw_dur: None, grad_norm: None,\n                update_ratio: None, extra_msg: Vec::new(),\n            },\n        })\n    }\n    fn optimizer_step(&mut self) -> MlResult\u003c()> { Ok(()) }\n    fn current_lr(&self) -> f32 { 0.0 }\n}\n\n#[test]\nfn unknown_batch_count_uses_processed_count_without_fake_percentage() -> MlResult\u003c()> {\n    let config = LogConfig {\n        batch_log_interval: 1,\n        batch_summary_interval: 2,\n        epoch_log_interval: 1,\n        nan_check_interval: usize::MAX,\n        metrics: Metrics::none(),\n        show_progress: false,\n        checkpoint_dir: None,\n        seed: 0,\n    };\n    let core = TrainerCore::new(config);\n    let mut loader = UnknownLengthLoader { next: 0 };\n    let mut step = TestStep;\n    let outcome = core.run_epoch(\n        &mut step, &mut loader, \"test\", 1, 1, 0, 1, &EpochProgress::new(1, false), None,\n    )?;\n    assert_eq!(outcome.processed_batches, 3);\n    assert!(outcome.batch_summaries.iter().any(|line| line.contains(\"Batch 2\")));\n    assert!(outcome.batch_summaries.iter().all(|line| !line.contains(\"Batch  %\")));\n    Ok(())\n}\n\n#[cfg(feature = \"enableVisualization\")]\n#[test]\nfn trainer_captures_only_the_requested_batch() -> MlResult\u003c()> {\n    use crate::legacy::{\n        nn::{Parameter, Variable},\n        tensor::{AutogradFunction, Tensor, TensorBase, operators::{Add, Function}},\n        visualization::{CaptureProfile, GraphSnapshot},\n    };"},{"path":"src/tests/trainer/core/legacy_metrics_tests.rs","bytes":2964,"preview":"use super::*;\nuse crate::legacy::tensor::Tensor;\n\n#[test]\nfn classification_accuracy_hook_matches_direct() {\n    // 3 개 배치. 정답 예측 2 개, 오답 예측 1 개 → 66.67%.\n    let preds  = [\n        Tensor::from_vec(vec![0.1, 0.9, 0.0], &[1, 3]).unwrap(),  // 1\n        Tensor::from_vec(vec![0.7, 0.2, 0.1], &[1, 3]).unwrap(),  // 0\n        Tensor::from_vec(vec![0.2, 0.3, 0.5], &[1, 3]).unwrap(),  // 2\n    ];\n    let targets = [\n        Tensor::from_vec(vec![0.0, 1.0, 0.0], &[1, 3]).unwrap(),  // 1 ✓\n        Tensor::from_vec(vec![1.0, 0.0, 0.0], &[1, 3]).unwrap(),  // 0 ✓\n        Tensor::from_vec(vec![0.0, 1.0, 0.0], &[1, 3]).unwrap(),  // 1 ✗\n    ];\n\n    // 직접 경로\n    let mut direct = ClassificationAccuracy::new();\n    for (p, t) in preds.iter().zip(targets.iter()) {\n        direct.update(p, t);\n    }\n\n    // 훅 경로\n    let mut hook = ClassificationAccuracy::new();\n    for (i, (p, t)) in preds.iter().zip(targets.iter()).enumerate() {\n        let ctx = BatchContext {\n            batch_idx: i,\n            pred:      Some(p as &dyn TensorBase),\n            target:    Some(t as &dyn TensorBase),\n            loss:      0.0,\n            n_tokens:  None,\n            lambda:    None,\n            lr:        1e-3,\n        };\n        \u003cClassificationAccuracy as MetricHook>::update(&mut hook, &ctx);\n    }\n\n    assert!((direct.compute() - MetricHook::compute(&hook)).abs() \u003c 1e-6);\n    assert!((MetricHook::compute(&hook) - 200.0 / 3.0).abs() \u003c 1e-4);\n}\n\n#[test]\nfn perplexity_hook_weighted_by_tokens() {\n    // 두 배치: loss 1.0 @ 10 tokens, loss 2.0 @ 20 tokens\n    // mean_nll = (1*10 + 2*20) / 30 = 50/30\n    let mut hook = Perplexity::new();\n    for (loss, n) in [(1.0_f32, 10usize), (2.0_f32, 20usize)] {\n        let ctx = BatchContext {\n            batch_idx: 0,\n            pred:      None,\n            target:    None,\n            loss,\n            n_tokens:  Some(n),\n            lambda:    None,\n            lr:        1e-3,\n        };\n        \u003cPerplexity as MetricHook>::update(&mut hook, &ctx);\n    }\n    let expected = (50.0_f32 / 30.0).exp();\n    let actual   = MetricHook::compute(&hook);\n    assert!((actual - expected).abs() \u003c 1e-4, \"expected {}, got {}\", expected, actual);\n}\n\n#[test]"},{"path":"src/tests/trainer/core/legacy_observer_tests.rs","bytes":3604,"preview":"use super::*;\nuse crate::legacy::visualization::{\n    CaptureContext, GRAPH_SNAPSHOT_SCHEMA_VERSION, VisualizationError, WriteReport,\n};\nuse std::{\n    collections::BTreeMap,\n    sync::{Arc, Mutex},\n};\n\nstruct NullWriter;\n\nimpl SnapshotWriter for NullWriter {\n    fn write(\n        &mut self,\n        _snapshot: &GraphSnapshot,\n        _stem: &str,\n    ) -> Result\u003cWriteReport, VisualizationError> {\n        Ok(WriteReport::default())\n    }\n}\n\nfn builder() -> GraphVisualizationObserverBuilder {\n    GraphVisualizationObserver::builder().writer(Box::new(NullWriter))\n}\n\nfn context(epoch: usize, batch: usize) -> BatchStartContext {\n    BatchStartContext {\n        paradigm: \"test\",\n        epoch,\n        batch,\n        total_epochs: 20,\n        total_batches: Some(100),\n        episode: None,\n    }\n}\n\n#[test]\nfn selectors_are_deduplicated_and_default_to_first_batch() {\n    let default = builder().build().unwrap();\n    assert_eq!(default.requested.len(), 1);\n    assert!(default.requested.contains(&CaptureSelector::FirstBatch));\n\n    let selected = builder()\n        .selectors([\n            CaptureSelector::EpochBatch { epoch: 2, batch: 3 },\n            CaptureSelector::EpochBatch { epoch: 2, batch: 3 },\n        ])\n        .build()\n        .unwrap();\n    assert_eq!(selected.requested.len(), 1);\n    assert!(selected.matching_selector(&context(2, 3)).is_some());\n    assert!(selected.matching_selector(&context(1, 1)).is_none());\n}\n\n#[test]\nfn zero_based_coordinates_are_rejected() {\n    assert!(\n        builder()\n            .selectors([CaptureSelector::EpochBatch { epoch: 0, batch: 1 }])\n            .build()\n            .is_err()\n    );\n}\n\n#[test]"},{"path":"src/tests/trainer/core/observer_tests.rs","bytes":3596,"preview":"use super::*;\nuse crate::visualization::{\n    CaptureContext, GRAPH_SNAPSHOT_SCHEMA_VERSION, VisualizationError, WriteReport,\n};\nuse std::{\n    collections::BTreeMap,\n    sync::{Arc, Mutex},\n};\n\nstruct NullWriter;\n\nimpl SnapshotWriter for NullWriter {\n    fn write(\n        &mut self,\n        _snapshot: &GraphSnapshot,\n        _stem: &str,\n    ) -> Result\u003cWriteReport, VisualizationError> {\n        Ok(WriteReport::default())\n    }\n}\n\nfn builder() -> GraphVisualizationObserverBuilder {\n    GraphVisualizationObserver::builder().writer(Box::new(NullWriter))\n}\n\nfn context(epoch: usize, batch: usize) -> BatchStartContext {\n    BatchStartContext {\n        paradigm: \"test\",\n        epoch,\n        batch,\n        total_epochs: 20,\n        total_batches: Some(100),\n        episode: None,\n    }\n}\n\n#[test]\nfn selectors_are_deduplicated_and_default_to_first_batch() {\n    let default = builder().build().unwrap();\n    assert_eq!(default.requested.len(), 1);\n    assert!(default.requested.contains(&CaptureSelector::FirstBatch));\n\n    let selected = builder()\n        .selectors([\n            CaptureSelector::EpochBatch { epoch: 2, batch: 3 },\n            CaptureSelector::EpochBatch { epoch: 2, batch: 3 },\n        ])\n        .build()\n        .unwrap();\n    assert_eq!(selected.requested.len(), 1);\n    assert!(selected.matching_selector(&context(2, 3)).is_some());\n    assert!(selected.matching_selector(&context(1, 1)).is_none());\n}\n\n#[test]\nfn zero_based_coordinates_are_rejected() {\n    assert!(\n        builder()\n            .selectors([CaptureSelector::EpochBatch { epoch: 0, batch: 1 }])\n            .build()\n            .is_err()\n    );\n}\n\n#[test]"},{"path":"src/tests/trainer/data/legacy_mod_tests.rs","bytes":7727,"preview":"use std::{fs, path::PathBuf};\n\nuse super::*;\nuse crate::legacy::{\n    MlResult,\n    nn::Parameter,\n    tensor::{Tensor, TensorBase},\n    trainer::TrainingRuntime,\n};\n\nfn tensor(value: f32, shape: &[usize]) -> Tensor {\n    Tensor::from_vec(vec![value; shape.iter().product()], shape).unwrap()\n}\n\n#[test]\nfn memory_dataset_and_loader_stack_samples() -> MlResult\u003c()> {\n    let dataset = DatasetBuilder::from_source(MemorySource::new(vec![1.0, 2.0, 3.0]))\n        .map(|value| {\n            Ok(SupervisedSample::new(\n                tensor(value, &[2]),\n                tensor(value, &[1]),\n            ))\n        })\n        .build()?;\n    let mut loader = DataLoader::builder(dataset)\n        .collator(SupervisedStackCollator::new())\n        .batch_size(2)\n        .shuffle(false)\n        .build()?;\n    loader.begin_epoch(0, &TrainingRuntime::new(0))?;\n    let first = loader.next_batch()?.unwrap();\n    let second = loader.next_batch()?.unwrap();\n    assert_eq!(first.inputs.tensor().shape(), &[2, 2]);\n    assert_eq!(first.targets.tensor().shape(), &[2, 1]);\n    assert_eq!(second.inputs.tensor().shape(), &[1, 2]);\n    assert!(loader.next_batch()?.is_none());\n    Ok(())\n}\n\n#[test]\nfn drop_last_and_invalid_batch_configuration_are_validated() -> MlResult\u003c()> {\n    let make_dataset = || {\n        InMemoryDataset::new(vec![\n            UnsupervisedSample::new(tensor(1.0, &[1])),\n            UnsupervisedSample::new(tensor(2.0, &[1])),\n            UnsupervisedSample::new(tensor(3.0, &[1])),\n        ])\n        .unwrap()\n    };\n    let loader = DataLoader::builder(make_dataset())\n        .collator(UnsupervisedStackCollator::new())\n        .batch_size(2)\n        .drop_last(true)\n        .build()?;\n    assert_eq!(loader.batch_count(), Some(1));\n    assert!(\n        DataLoader::builder(make_dataset())\n            .collator(UnsupervisedStackCollator::new())\n            .batch_size(0)\n            .build()\n            .is_err()\n    );\n    assert!(\n        DataLoader::builder(make_dataset())\n            .collator(UnsupervisedStackCollator::new())"},{"path":"src/tests/trainer/legacy_api_tests.rs","bytes":1705,"preview":"use super::*;\nuse crate::legacy::{nn::Variable, tensor::{Tensor, TensorBase}};\n#[test] fn schedules_reject_zero() {\n    assert!(EpochSchedule::new(0).is_err());\n    assert!(EpisodeSchedule::new(1, 0).is_err());\n}\n\n#[test] fn datasets_validate_contracts() {\n    let x = Variable::new(Tensor::from_vec(vec![1.0], &[1, 1]).unwrap());\n    let t = Variable::new(Tensor::from_vec(vec![0.0], &[1, 1]).unwrap());\n    let xs = [&x]; let ts = [&t];\n    assert!(SupervisedDataset::new(&xs, &ts).is_ok());\n    assert!(SupervisedDataset::new(&[], &[]).is_err());\n    assert!(SupervisedDataset::new(&xs, &[]).is_err());\n    assert!(UnsupervisedDataset::new(&[]).is_err());\n    assert!(AutoregressiveDataset::new(&[]).is_err());\n    assert!(SemiSupervisedDataset::new(&xs, &ts, &[]).is_err());\n}\n\n#[test] fn identical_seed_produces_identical_stream() {\n    let a = super::super::TrainingRuntime::new(7);\n    let b = super::super::TrainingRuntime::new(7);\n    let mut left = vec![1, 2, 3, 4, 5];\n    let mut right = left.clone();\n    a.shuffle(&mut left); b.shuffle(&mut right);\n    assert_eq!(left, right);\n    assert_eq!(a.random_f32(), b.random_f32());\n}\n\n#[test] fn facade_selectors_are_statically_typed() {\n    let _: super::super::SupervisedTrainer = super::super::Trainer::silent().supervised();\n    let _: super::super::UnsupervisedTrainer = super::super::Trainer::silent().unsupervised();\n    let _: super::super::SemiSupervisedTrainer = super::super::Trainer::silent().semi_supervised();\n    let _: super::super::AutoregressiveTrainer = super::super::Trainer::silent().autoregressive();\n    let _: super::super::RLTrainer = super::super::Trainer::silent().reinforcement();\n}\n"},{"path":"src/tests/trainer/legacy_checkpoint_tests.rs","bytes":3820,"preview":"use super::*;\n\nfn tmp_path(name: &str) -> String {\n    let dir = std::env::temp_dir();\n    let unique = format!(\"{:?}\", std::time::SystemTime::now())\n        .chars().filter(|c| c.is_alphanumeric()).collect::\u003cString>();\n    format!(\"{}/trench_deep_ckpt_{}_{}.json\", dir.display(), unique, name)\n}\n\nfn sample(paradigm: Option\u003cParadigmTag>) -> TrainingCheckpoint {\n    TrainingCheckpoint {\n        schema_version: CHECKPOINT_SCHEMA_VERSION,\n        epochs_done: 3,\n        total_epochs: 10,\n        last_loss: 0.1234,\n        tolerance: 1e-6,\n        optimizer_lr: 1e-3,\n        model_path: \"ignore\".into(),\n        timestamp: \"t0\".into(),\n        paradigm,\n        rng_seed: 42,\n        optimizer_snapshot: None,\n    }\n}\n\n#[test]\nfn roundtrip_preserves_all_fields() {\n    let path = tmp_path(\"rt\");\n    let ckpt = sample(Some(ParadigmTag::Supervised));\n    ckpt.save(&path).expect(\"save\");\n    let loaded = TrainingCheckpoint::load(&path).expect(\"load\");\n    let _ = std::fs::remove_file(&path);\n\n    assert_eq!(loaded.epochs_done,  ckpt.epochs_done);\n    assert_eq!(loaded.total_epochs, ckpt.total_epochs);\n    assert!((loaded.last_loss - ckpt.last_loss).abs() \u003c 1e-6);\n    assert_eq!(loaded.paradigm,     Some(ParadigmTag::Supervised));\n    assert_eq!(loaded.rng_seed,     42);\n}\n\n#[test]\nfn verify_paradigm_catches_mismatch() {\n    let ckpt = sample(Some(ParadigmTag::Supervised));\n    assert!(ckpt.verify_paradigm(ParadigmTag::Supervised).is_ok());\n    let err = ckpt.verify_paradigm(ParadigmTag::Autoregressive)\n        .expect_err(\"should refuse paradigm mismatch\");\n    let msg = format!(\"{:?}\", err);\n    assert!(msg.contains(\"supervised\") && msg.contains(\"autoregressive\"),\n            \"에러 메시지에 두 패러다임 이름이 포함되어야 함: {}\", msg);\n}\n\n#[test]\nfn verify_paradigm_allows_legacy_untagged() {\n    // Phase 3 이전 체크포인트는 paradigm=None 으로 로드됨 → 통과해야 함.\n    let ckpt = sample(None);\n    assert!(ckpt.verify_paradigm(ParadigmTag::Supervised).is_ok());\n    assert!(ckpt.verify_paradigm(ParadigmTag::Reinforcement).is_ok());\n}\n\n#[test]\nfn legacy_checkpoint_loads_without_paradigm_field() {\n    // 구버전 포맷: paradigm / rng_seed 필드가 JSON 에 아예 없어야 로드 가능.\n    let path = tmp_path(\"legacy\");\n    let legacy_json = r#\"{\n        \"epochs_done\": 5,"},{"path":"src/tests/trainer/legacy_mod_preset_tests.rs","bytes":1636,"preview":"use super::*;\n\n#[test]\nfn logging_presets_keep_metric_layers_separate() {\n    let silent = Trainer::silent().core;\n    assert!(!silent.config().show_progress);\n    assert_eq!(silent.config().batch_log_interval, usize::MAX);\n    assert_eq!(silent.config().epoch_log_interval, usize::MAX);\n    assert!(!silent.config().metrics.paradigm);\n\n    let minimal = Trainer::minimal().core;\n    assert!(minimal.config().show_progress);\n    assert_eq!(minimal.config().batch_log_interval, 1);\n    assert_eq!(minimal.config().batch_summary_interval, usize::MAX);\n    assert_eq!(minimal.config().epoch_log_interval, 10);\n    assert!(!minimal.config().metrics.paradigm);\n    assert!(!minimal.config().metrics.grad_norm);\n    assert!(!minimal.config().metrics.update_ratio);\n    assert!(!minimal.config().metrics.fw_bw_timing);\n\n    let default = Trainer::default().core;\n    assert!(default.config().metrics.paradigm);\n    assert!(!default.config().metrics.grad_norm);\n    assert!(!default.config().metrics.update_ratio);\n    assert!(!default.config().metrics.fw_bw_timing);\n    assert_eq!(default.config().batch_summary_interval, usize::MAX);\n    assert_eq!(default.config().epoch_log_interval, 10);\n\n    let verbose = Trainer::verbose().core;\n    assert!(verbose.config().metrics.paradigm);\n    assert!(verbose.config().metrics.grad_norm);\n    assert!(verbose.config().metrics.update_ratio);\n    assert!(verbose.config().metrics.accuracy);\n    assert!(verbose.config().metrics.fw_bw_timing);\n    assert_eq!(verbose.config().batch_summary_interval, 100);\n    assert_eq!(verbose.config().epoch_log_interval, 1);\n}\n"},{"path":"src/tests/trainer/legacy_reinforcement_tests.rs","bytes":847,"preview":"use super::*;\n\n#[test]\nfn sample_categorical_respects_distribution() {\n    // logits = [-10, 10] → 거의 확실하게 action=1\n    let mut count_one = 0;\n    for i in 0..200 {\n        if sample_categorical(&[-10.0, 10.0], (i as f32 + 0.5) / 200.0) == 1 {\n            count_one += 1;\n        }\n    }\n    assert!(count_one > 190, \"action 1 이 거의 항상 선택되어야 함: {}/200\", count_one);\n}\n\n#[test]\nfn sample_categorical_handles_edge_cases() {\n    // 동일 로짓 → 어떤 값도 반환 가능하지만 panic 하면 안 됨\n    for i in 0..50 {\n        let a = sample_categorical(&[0.0, 0.0, 0.0], i as f32 / 50.0);\n        assert!(a \u003c 3);\n    }\n    // 단일 원소\n    assert_eq!(sample_categorical(&[5.0], 0.5), 0);\n    // 비정상 로짓\n    assert!(sample_categorical(&[f32::NAN, 1.0], 0.5) \u003c 2);\n}\n"},{"path":"src/tests/trainer/semi_supervised_tests.rs","bytes":1024,"preview":"use super::*;\n\n#[test]\nfn consistency_ramp_constant() {\n    let r = ConsistencyRamp::Constant(0.5);\n    assert_eq!(r.value(0), 0.5);\n    assert_eq!(r.value(100), 0.5);\n}\n\n#[test]\nfn consistency_ramp_sigmoid_monotonic() {\n    let r = ConsistencyRamp::Sigmoid {\n        max_weight: 1.0,\n        ramp_epochs: 30,\n    };\n    let v0 = r.value(0);\n    let v15 = r.value(15);\n    let v30 = r.value(30);\n    let v40 = r.value(40);\n    assert!(v0 \u003c v15, \"0 \u003c 15 실패: {} \u003c {}\", v0, v15);\n    assert!(v15 \u003c v30, \"15 \u003c 30 실패: {} \u003c {}\", v15, v30);\n    assert!(\n        (v30 - 1.0).abs() \u003c 1e-5,\n        \"램프 끝에서 max_weight 수렴 실패: {}\",\n        v30\n    );\n    assert!(\n        (v40 - 1.0).abs() \u003c 1e-5,\n        \"램프 초과 영역에서 max_weight 유지 실패: {}\",\n        v40\n    );\n}\n\n#[test]\nfn consistency_ramp_sigmoid_zero_length() {\n    let r = ConsistencyRamp::Sigmoid {\n        max_weight: 0.7,\n        ramp_epochs: 0,\n    };\n    assert_eq!(r.value(0), 0.7);\n}\n"},{"path":"src/tests/trainer/supervised_tests.rs","bytes":2468,"preview":"use super::*;\n\n#[test]\nfn default_preset_auto_attaches_accuracy_hook() {\n    let trainer = SupervisedTrainer::default();\n    assert_eq!(\n        trainer.core.hook_count(),\n        1,\n        \"default() 는 ClassificationAccuracy 훅을 자동 장착해야 함\"\n    );\n}\n\n#[test]\nfn verbose_preset_auto_attaches_accuracy_hook() {\n    let trainer = SupervisedTrainer::verbose();\n    assert_eq!(\n        trainer.core.hook_count(),\n        1,\n        \"verbose() 는 ClassificationAccuracy 훅을 자동 장착해야 함\"\n    );\n}\n\n#[test]\nfn minimal_preset_has_no_paradigm_hook() {\n    let trainer = SupervisedTrainer::minimal();\n    assert_eq!(\n        trainer.core.hook_count(),\n        0,\n        \"minimal() 은 핵심 메트릭만 표시하므로 accuracy 훅이 없어야 함\"\n    );\n}\n\n#[test]\nfn silent_preset_has_no_auto_hooks() {\n    let trainer = SupervisedTrainer::silent();\n    assert_eq!(\n        trainer.core.hook_count(),\n        0,\n        \"silent() 은 metrics.accuracy=false → 훅 없음\"\n    );\n}\n\n#[test]\nfn builder_without_accuracy_metric_skips_auto_hook() {\n    let trainer: SupervisedTrainer = Trainer::builder()\n        .metrics(Metrics::none().grad_norm())\n        .build()\n        .into();\n    assert_eq!(\n        trainer.core.hook_count(),\n        0,\n        \"metrics.accuracy=false 빌더 경로는 자동 훅이 없어야 함\"\n    );\n}\n\n#[test]\nfn builder_with_accuracy_metric_auto_attaches_hook() {\n    let trainer: SupervisedTrainer = Trainer::builder()\n        .metrics(Metrics::none().accuracy())\n        .build()\n        .into();\n    assert_eq!(\n        trainer.core.hook_count(),\n        1,\n        \"metrics.accuracy=true 빌더 경로는 자동 훅이 장착되어야 함\""},{"path":"src/trainer/api.rs","bytes":4260,"preview":"//! Shared public API types for all training paradigms.\n\nuse crate::{MlError, MlResult};\nuse std::{collections::BTreeMap, path::Path, time::Duration};\n\npub trait CheckpointableModel {\n    fn save_checkpoint(&self, _path: &Path) -> MlResult\u003c()> {\n        Err(MlError::UnsupportedCapability {\n            module: \"model\",\n            capability: \"checkpoint save\",\n            operation: \"save_checkpoint\",\n        })\n    }\n    fn load_checkpoint(&mut self, _path: &Path) -> MlResult\u003c()> {\n        Err(MlError::UnsupportedCapability {\n            module: \"model\",\n            capability: \"checkpoint restore\",\n            operation: \"load_checkpoint\",\n        })\n    }\n}\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub enum StopReason {\n    Completed,\n    Converged,\n    Interrupted,\n}\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub enum StepUnit {\n    Epoch,\n    Episode,\n    Token,\n}\n\npub type MetricValues = BTreeMap\u003cString, f32>;\n\n#[derive(Debug, Clone, PartialEq, Eq)]\npub struct CheckpointPaths {\n    pub model: std::path::PathBuf,\n    pub metadata: std::path::PathBuf,\n}\n\n#[derive(Debug)]\npub struct TrainResult {\n    pub stop_reason: StopReason,\n    pub units_completed: usize,\n    pub unit: StepUnit,\n    pub final_loss: f32,\n    pub metrics: MetricValues,\n    pub checkpoint: Option\u003cCheckpointPaths>,\n    pub total_duration: Duration,\n}\n\nimpl TrainResult {\n    pub(crate) fn epochs(\n        reason: StopReason,\n        completed: usize,\n        loss: f32,\n        duration: Duration,\n    ) -> Self {\n        Self {\n            stop_reason: reason,\n            units_completed: completed,"},{"path":"src/trainer/autoregressive.rs","bytes":21730,"preview":"//! 자기회귀(Autoregressive) 아키텍처용 트레이너 네임스페이스.\n//!\n//! 언어모델(LM), 음성 합성, 코드 생성 등 **이전 토큰들로부터 다음 토큰을 예측**\n//! 하는 모델을 학습한다. 학습 시에는 teacher forcing 으로 전체 시퀀스를 한 번에\n//! 처리하며, 손실은 **토큰 단위 음 로그우도(NLL)** 의 평균이다.\n//!\n//! ## 지도/비지도 트레이너와의 차이\n//!\n//! | 항목              | Supervised            | Unsupervised     | **Autoregressive**        |\n//! |------------------|-----------------------|------------------|---------------------------|\n//! | `forward_loss`   | `(x, t)`              | `(x)`            | `(x)` — t 는 shift-by-one |\n//! | 대표 메트릭      | Classification Acc.   | (없음)           | **Perplexity = exp(NLL)** |\n//! | 타깃 생성        | 사용자가 직접 제공    | 모델 내부 생성   | 입력을 shift 해서 생성    |\n//!\n//! AR 트레이너는 입력 인자 측면에서 `UnsupervisedTrainer` 와 동일하지만,\n//! **퍼플렉서티 추적** 과 **장기적으로는 토큰-레벨 메트릭 훅** 을 전용으로 장착한다.\n//! 현재(Phase 1) 단계에서는 공통 학습 루프를 `UnsupervisedTrainer` 와 나란히 두어\n//! 의도적 중복을 유지하고, Phase 2 에서 `TrainerCore::run_epoch` 로 통합한다.\n\nuse super::*;\n\n// ────────────────────────────────────────────────────────────────────────────\n// AutoregressiveModel — AR 학습 대상 모델의 인터페이스\n// ────────────────────────────────────────────────────────────────────────────\n\n/// 자기회귀 트레이너가 학습할 수 있는 모델이 구현해야 하는 인터페이스.\n///\n/// `forward_loss` 는 시퀀스 입력 `x` **하나만** 받는다. 모델 내부에서\n/// `x[:, :-1]` 을 입력으로, `x[:, 1:]` 을 타깃으로 사용해 teacher forcing\n/// 손실을 계산한다(구체적인 shift 규약은 모델의 자유).\n///\n/// # 구현 예시 (토이 Bigram LM)\n/// ```ignore\n/// impl AutoregressiveModel for BigramLM {\n///     fn forward_loss(&mut self, x: &Variable) -> MlResult\u003c(Variable, Variable, usize)> {\n///         // 내부에서 shift + SoftmaxCrossEntropy 적용\n///         let (logits, loss, n_tokens) = self.teacher_forced_loss(x)?;\n///         Ok((logits, loss, n_tokens))\n///     }\n///     fn params(&self) -> Vec\u003c&dyn Parameter> { vec![&self.embed] }\n///     fn predict_raw(&mut self, x: &dyn TensorBase) -> MlResult\u003cGlobalTensor\u003cf32>> {\n///         self.forward_logits_no_grad(x)\n///     }\n/// }\n/// ```\n#[cfg(feature = \"enableBackward\")]\npub trait AutoregressiveModel: TrainableModel {\n    /// 순전파와 손실 계산을 수행한다.\n    ///\n    /// 반환값 `(logits, loss, n_tokens)`:\n    /// - `logits`    : 순방향 예측 텐서. 훅이 관찰용으로 받아가지만 루프 자체는 쓰지 않는다.\n    /// - `loss`      : 스칼라 평균 NLL Variable (backward 가능).\n    /// - `n_tokens`  : 이번 배치에서 **실제로 손실에 기여한 타깃 토큰 수** (padding 제외).\n    ///                 퍼플렉서티를 정확히 계산하기 위해 필요.\n    fn forward_loss(\n        &mut self,\n        x: &crate::legacy::nn::Variable,\n    ) -> MlResult\u003c(crate::legacy::nn::Variable, crate::legacy::nn::Variable, usize)>;\n\n    /// No-grad 순전파. 초기 손실 표시 및 평가에 사용한다.\n    fn predict_raw(&mut self, x: &dyn TensorBase) -> MlResult\u003ccrate::legacy::tensor::GlobalTensor\u003cf32>>;\n}\n\n// ────────────────────────────────────────────────────────────────────────────\n// AutoregressiveTrainer"},{"path":"src/trainer/checkpoint.rs","bytes":7414,"preview":"use super::*;\nuse serde::{Deserialize, Serialize};\nuse std::{\n    path::Path,\n    sync::{\n        OnceLock,\n        atomic::{AtomicBool, Ordering},\n    },\n};\npub const CHECKPOINT_SCHEMA_VERSION: u32 = 2;\nfn default_schema_version() -> u32 {\n    CHECKPOINT_SCHEMA_VERSION\n}\nstatic INTERRUPTED: AtomicBool = AtomicBool::new(false);\nstatic HANDLER: OnceLock\u003cResult\u003c(), String>> = OnceLock::new();\npub fn request_interrupt() {\n    INTERRUPTED.store(true, Ordering::SeqCst);\n}\npub fn clear_interrupt() {\n    INTERRUPTED.store(false, Ordering::SeqCst);\n}\npub fn interrupted() -> bool {\n    INTERRUPTED.load(Ordering::SeqCst)\n}\npub fn install_interrupt_handler() -> MlResult\u003c()> {\n    HANDLER\n        .get_or_init(|| ctrlc::set_handler(request_interrupt).map_err(|e| e.to_string()))\n        .clone()\n        .map_err(MlError::StringError)\n}\n#[derive(Serialize, Deserialize, Debug, Clone, Copy, PartialEq, Eq)]\npub enum ParadigmTag {\n    Supervised,\n    Unsupervised,\n    SemiSupervised,\n    Autoregressive,\n    Reinforcement,\n}\n\nimpl ParadigmTag {\n    pub fn as_str(self) -> &'static str {\n        match self {\n            ParadigmTag::Supervised => \"supervised\",\n            ParadigmTag::Unsupervised => \"unsupervised\",\n            ParadigmTag::SemiSupervised => \"semi-supervised\",\n            ParadigmTag::Autoregressive => \"autoregressive\",\n            ParadigmTag::Reinforcement => \"reinforcement\",\n        }\n    }\n}\n\n// ────────────────────────────────────────────────────────────────────────────\n// TrainingCheckpoint — 학습 상태 직렬화\n// ────────────────────────────────────────────────────────────────────────────\n\n/// 학습 중단 시 저장되는 체크포인트.\n///\n/// 에폭 단위로 저장되며, 재개 시 다음 에폭부터 학습을 이어간다.\n///\n/// ## 하위 호환성\n///\n/// Phase 4 에서 `paradigm` / `rng_seed` 필드가 추가되었다. 이전 버전에서\n/// 저장된 JSON 은 두 필드가 누락되어 있으므로 `#[serde(default)]` 로 처리해\n/// 무태그 체크포인트는 `None` / `0` 으로 로드된다. 로더가 패러다임 체크를\n/// 강제하지 않는 한 구버전 체크포인트도 그대로 읽힌다."},{"path":"src/trainer/core/config.rs","bytes":9210,"preview":"use super::*;\n\n/// 학습 루프에서 활성화할 메트릭 집합.\n///\n/// 모든 메서드가 `const fn`이므로 `const` 변수로 컴파일 타임에 확정 가능.\n///\n/// # 예시\n/// ```no_run\n/// use trench_deep::trainer::Metrics;\n///\n/// let m = Metrics::none().grad_norm().accuracy();\n/// ```\n#[derive(Clone, Copy)]\npub struct Metrics {\n    /// 현재 패러다임의 대표 메트릭(accuracy, perplexity, return, lambda 등)\n    pub paradigm: bool,\n    /// 전체 파라미터의 그래디언트 L2 노름\n    pub grad_norm: bool,\n    /// Update Ratio = ||lr·grad|| / ||W||  (학습률 스케일 진단용)\n    pub update_ratio: bool,\n    /// argmax 기반 분류 정확도\n    pub accuracy: bool,\n    /// Forward / Backward 패스 소요 시간\n    pub fw_bw_timing: bool,\n}\n\nimpl Metrics {\n    /// 모든 메트릭 비활성.\n    pub const fn none() -> Self {\n        Self {\n            paradigm: false,\n            grad_norm: false,\n            update_ratio: false,\n            accuracy: false,\n            fw_bw_timing: false,\n        }\n    }\n\n    /// 모든 메트릭 활성.\n    pub const fn all() -> Self {\n        Self {\n            paradigm: true,\n            grad_norm: true,\n            update_ratio: true,\n            accuracy: true,\n            fw_bw_timing: true,\n        }\n    }\n\n    pub const fn paradigm(mut self) -> Self {\n        self.paradigm = true;\n        self\n    }\n    pub const fn grad_norm(mut self) -> Self {\n        self.grad_norm = true;\n        self\n    }\n    pub const fn update_ratio(mut self) -> Self {\n        self.update_ratio = true;\n        self\n    }\n    pub const fn accuracy(mut self) -> Self {\n        self.accuracy = true;\n        self\n    }"},{"path":"src/trainer/core/convergence.rs","bytes":2884,"preview":"// 학습 루프의 조기 종료(수렴) 판정 추상화.\n//\n// 기존 `fit`/`resume` API는 `tolerance: f32` 하나로 수렴을 표현했다.\n// 앞으로 아키텍처별 트레이너(비지도·반지도·강화학습)에서\n// 서로 다른 수렴 기준이 필요해지므로, 판정 규칙을 enum 으로 캡슐화함.\n//\n// P0 단계에서는 기존 동작을 보존하기 위해 두 가지 변종만 제공함.\n//\n// ```text\n// Off                  — 조기 종료하지 않음 (tolerance == 0 또는 음수)\n// AbsDelta(tol)        — |last_loss - avg_loss| \u003c tol 이면 종료\n// ```\n//\n// 향후 `RelDelta`, `Patience`, `ValLoss` 등을 확장 지점으로 추가할 수 있다.\n// 에폭 경계에서 학습을 조기 종료할지 판정하는 규칙.\n//\n// `Trainer::fit` 은 외부에서 `tolerance: f32` 를 받아\n// `Convergence::from_tolerance(tolerance)` 로 변환한 뒤 내부 루프에 전달함.\n// 체크포인트 직렬화는 여전히 `tolerance: f32` 로 왕복하므로\n// `tolerance()` 메서드로 원래 값을 복원할 수 있다.\n#[derive(Debug, Clone, Copy, PartialEq)]\npub enum Convergence {\n    /// 조기 종료를 수행하지 않음.\n    Off,\n    /// 연속 두 에폭 평균 손실의 절대차가 `tol` 미만이면 종료.\n    AbsDelta(f32),\n}\n\nimpl Convergence {\n    // 기존 `tolerance: f32` 인자와의 호환 변환.\n    //\n    // - `tolerance \u003c= 0.0` 또는 NaN 은 `Off` 로 해석함.\n    // - 그 외에는 `AbsDelta(tolerance)`.\n    pub fn from_tolerance(tolerance: f32) -> Self {\n        if tolerance.is_nan() || tolerance \u003c= 0.0 {\n            Convergence::Off\n        } else {\n            Convergence::AbsDelta(tolerance)\n        }\n    }\n\n    /// 체크포인트 직렬화용 원값(tolerance) 복원.\n    ///\n    /// `Off` 는 `0.0` 으로 반환함 (기존 스키마와의 왕복 호환).\n    pub fn tolerance(&self) -> f32 {\n        match self {\n            Convergence::Off => 0.0,\n            Convergence::AbsDelta(tol) => *tol,\n        }\n    }\n\n    /// 조기 종료 판정.\n    ///\n    /// # 파라미터\n    /// - `last_loss`: 직전 에폭의 평균 손실. 첫 에폭에서는 `f32::INFINITY` 를 전달할 수 있다.\n    /// - `current_loss`: 방금 끝난 에폭의 평균 손실.\n    pub fn should_stop(&self, last_loss: f32, current_loss: f32) -> bool {\n        match self {\n            Convergence::Off => false,\n            Convergence::AbsDelta(tol) => {\n                // last_loss 가 INFINITY 면 첫 에폭이므로 종료하지 않음.\n                if !last_loss.is_finite() {\n                    false\n                } else {\n                    (last_loss - current_loss).abs() \u003c *tol"},{"path":"src/trainer/core/debug_trace.rs","bytes":2072,"preview":"use super::*;\nuse crate::legacy::trainer::TrainableModel;\n\nimpl TrainerCore {\n    /// `debugging` feature 전용 모델 구조 요약.\n    ///\n    /// 일반 빌드에서는 빈 함수로 컴파일되어 파라미터 순회 비용이 없다.\n    #[cfg(feature = \"debugging\")]\n    pub(crate) fn trace_model\u003cM: TrainableModel + ?Sized>(\n        &self,\n        paradigm: &str,\n        model: &M,\n        work_units: usize,\n        batches_per_unit: Option\u003cusize>,\n    ) {\n        let params = model.params();\n        let total_elements: usize = params.iter().map(|p| p.tensor().data().len()).sum();\n\n        tracing::debug!(\n            target: \"trench_deep::trainer::debug\",\n            paradigm,\n            model = std::any::type_name::\u003cM>(),\n            parameters = params.len(),\n            total_elements,\n            work_units,\n            batches_per_unit = ?batches_per_unit,\n            \"training model structure\"\n        );\n\n        for (index, param) in params.iter().enumerate() {\n            #[cfg(feature = \"enableVisualization\")]\n            let label = param.label();\n            #[cfg(not(feature = \"enableVisualization\"))]\n            let label = \"unlabeled\";\n\n            tracing::debug!(\n                target: \"trench_deep::trainer::debug\",\n                index,\n                label,\n                node_id = ?param.node_id(),\n                shape = ?param.tensor().shape(),\n                elements = param.tensor().data().len(),\n                retain_grad = param.is_retain_grad(),\n                \"model parameter\"\n            );\n            crate::legacy::tensor::operators::debug::stats(\n                &format!(\"model.param[{index}]/{label}\"),\n                param.tensor(),\n            );\n        }\n    }\n\n    #[cfg(not(feature = \"debugging\"))]\n    #[inline(always)]\n    pub(crate) fn trace_model\u003cM: TrainableModel + ?Sized>(\n        &self,\n        _paradigm: &str,\n        _model: &M,\n        _work_units: usize,\n        _batches_per_unit: Option\u003cusize>,\n    ) {\n    }\n}\n"},{"path":"src/trainer/core/epoch_loop.rs","bytes":22867,"preview":"//! 에폭 공용 루프 (`TrainerCore::run_epoch`).\n//!\n//! Phase 2 에서 4 개 트레이너(Supervised / Unsupervised / SemiSupervised /\n//! Autoregressive) 의 `fit_inner` 에 거의 동일하게 반복되던 배치 루프 —\n//! progress bar 생성, NaN 검사, 배치 로그 포매팅, optimizer.step, 인터럽트\n//! 감지 — 을 하나로 끌어올린다. RL 은 에피소드 시맨틱이라 범위 외.\n//!\n//! 패러다임별 차이점은 [`EpochStep`] 트레잇 구현체에 캡슐화된다.\n//! 호출부의 `fit_inner` 는 `EpochStep` 구현을 임시로 조립해 `run_epoch` 에\n//! 넘기기만 하면 된다.\n\nuse std::sync::atomic::AtomicBool;\nuse std::time::{Duration, Instant};\n\nuse super::metric_hook::BatchContext;\nuse super::*;\nuse crate::legacy::trainer::checkpoint::{clear_interrupt, confirm_interrupt, is_interrupted};\nuse crate::legacy::trainer::data::BatchLoader;\nuse crate::legacy::trainer::progress::{BatchProgress, EpochProgress};\n\n// ────────────────────────────────────────────────────────────────────────────\n// StepOutput — 한 배치의 forward/backward 결과 요약\n// ────────────────────────────────────────────────────────────────────────────\n\n/// 배치 한 번의 실행 결과. `EpochStep::forward_backward` 반환값.\n///\n/// `optimizer.step()` 호출 **전** 의 관측치만 담는다. optimizer step 이후\n/// 갱신된 param/grad 는 다음 배치의 `StepOutput` 에 반영된다.\n#[derive(Default)]\npub struct BatchObservations {\n    pub pred: Option\u003ccrate::legacy::nn::Variable>,\n    pub target: Option\u003ccrate::legacy::nn::Variable>,\n    pub n_tokens: Option\u003cusize>,\n    pub lambda: Option\u003cf32>,\n}\n\npub struct StepDiagnostics {\n    /// 이번 배치의 스칼라 손실값 (fan-in 된 평균, 모델 정의에 따름).\n    /// 그래디언트에 NaN/Inf 가 포함되어 있는지. `true` 이면 run_epoch 가\n    /// 즉시 에폭을 중단하고 에러를 반환한다.\n    pub has_nan: bool,\n    /// forward 소요 시간. `cfg.metrics.fw_bw_timing = false` 이면 `None`.\n    pub fw_dur: Option\u003cDuration>,\n    /// backward 소요 시간. 동일.\n    pub bw_dur: Option\u003cDuration>,\n    /// 전체 파라미터의 grad L2 노름. `cfg.metrics.grad_norm = false` 이면 `None`.\n    pub grad_norm: Option\u003cf32>,\n    /// Update ratio = ||lr·g|| / ||W||. `cfg.metrics.update_ratio = false` 이면 `None`.\n    pub update_ratio: Option\u003cf32>,\n    /// 패러다임별 추가 배치 로그 조각. 예: SemiSup 의 `\"λ: 0.321\"`, AR 의 `\"PPL: 12.34\"`.\n    pub extra_msg: Vec\u003cString>,\n}\n\nimpl StepDiagnostics {\n    /// 배치 로그 메시지를 조립한다. FW/BW 타이밍 → GN → UR → paradigm extras 순.\n    pub fn build_batch_msg(&self) -> String {\n        let mut parts: Vec\u003cString> = Vec::with_capacity(4 + self.extra_msg.len());\n        if let (Some(fw), Some(bw)) = (self.fw_dur, self.bw_dur) {\n            parts.push(format!(\"FW: {:>7.2?} | BW: {:>7.2?}\", fw, bw));\n        }\n        if let Some(gn) = self.grad_norm {\n            parts.push(format!(\"GN: {:.2e}\", gn));\n        }\n        if let Some(ur) = self.update_ratio {\n            parts.push(format!(\"UR: {:.2e}\", ur));"},{"path":"src/trainer/core/graph_capture.rs","bytes":2403,"preview":"//! Shared graph-capture lifecycle for every training paradigm.\n\nuse super::{BatchStartContext, MlResult, TrainerCore};\nuse crate::legacy::visualization::{CaptureContext, GraphSnapshot, VisualizationCapture};\n\npub(crate) struct TrainingGraphCapture {\n    context: BatchStartContext,\n    capture: Option\u003cVisualizationCapture>,\n}\n\npub(crate) struct PendingGraphSnapshot {\n    context: BatchStartContext,\n    snapshot: Option\u003cGraphSnapshot>,\n}\n\nimpl TrainerCore {\n    pub(crate) fn begin_graph_capture(\n        &self,\n        context: &BatchStartContext,\n    ) -> MlResult\u003cTrainingGraphCapture> {\n        let capture = self\n            .requested_capture_profile(context)\n            .map(|profile| {\n                let capture_context = if let Some(episode) = context.episode {\n                    CaptureContext {\n                        paradigm: Some(context.paradigm.to_owned()),\n                        epoch: None,\n                        batch: None,\n                        episode: Some(episode),\n                    }\n                } else {\n                    CaptureContext {\n                        paradigm: Some(context.paradigm.to_owned()),\n                        epoch: Some(context.epoch),\n                        batch: Some(context.batch),\n                        episode: None,\n                    }\n                };\n                VisualizationCapture::builder(profile)\n                    .context(capture_context)\n                    .begin()\n            })\n            .transpose()?;\n\n        Ok(TrainingGraphCapture {\n            context: context.clone(),\n            capture,\n        })\n    }\n}\n\nimpl TrainingGraphCapture {\n    /// Finalizes after backward/validation, while gradients are still present.\n    pub(crate) fn finish(mut self) -> MlResult\u003cPendingGraphSnapshot> {\n        let snapshot = self\n            .capture\n            .take()\n            .map(VisualizationCapture::finish)\n            .transpose()?;\n        Ok(PendingGraphSnapshot {\n            context: self.context,\n            snapshot,\n        })\n    }\n}"},{"path":"src/trainer/core/legacy_config.rs","bytes":8928,"preview":"use super::*;\n\n/// 학습 루프에서 활성화할 메트릭 집합.\n///\n/// 모든 메서드가 `const fn`이므로 `const` 변수로 컴파일 타임에 확정 가능.\n///\n/// # 예시\n/// ```no_run\n/// use trench_deep::trainer::Metrics;\n///\n/// let m = Metrics::none().grad_norm().accuracy();\n/// ```\n#[derive(Clone, Copy)]\npub struct Metrics {\n    /// 현재 패러다임의 대표 메트릭(accuracy, perplexity, return, lambda 등)\n    pub paradigm:     bool,\n    /// 전체 파라미터의 그래디언트 L2 노름\n    pub grad_norm:    bool,\n    /// Update Ratio = ||lr·grad|| / ||W||  (학습률 스케일 진단용)\n    pub update_ratio: bool,\n    /// argmax 기반 분류 정확도\n    pub accuracy:     bool,\n    /// Forward / Backward 패스 소요 시간\n    pub fw_bw_timing: bool,\n}\n\nimpl Metrics {\n    /// 모든 메트릭 비활성.\n    pub const fn none() -> Self {\n        Self { paradigm: false, grad_norm: false, update_ratio: false, accuracy: false, fw_bw_timing: false }\n    }\n\n    /// 모든 메트릭 활성.\n    pub const fn all() -> Self {\n        Self { paradigm: true, grad_norm: true, update_ratio: true, accuracy: true, fw_bw_timing: true }\n    }\n\n    pub const fn paradigm(mut self)    -> Self { self.paradigm     = true; self }\n    pub const fn grad_norm(mut self)    -> Self { self.grad_norm    = true; self }\n    pub const fn update_ratio(mut self) -> Self { self.update_ratio = true; self }\n    pub const fn accuracy(mut self)     -> Self { self.accuracy     = true; self }\n    pub const fn fw_bw_timing(mut self) -> Self { self.fw_bw_timing = true; self }\n\n    pub const fn without_paradigm(mut self)     -> Self { self.paradigm     = false; self }\n    pub const fn without_grad_norm(mut self)    -> Self { self.grad_norm    = false; self }\n    pub const fn without_update_ratio(mut self) -> Self { self.update_ratio = false; self }\n    pub const fn without_accuracy(mut self)     -> Self { self.accuracy     = false; self }\n    pub const fn without_fw_bw_timing(mut self) -> Self { self.fw_bw_timing = false; self }\n}\n\nimpl Default for Metrics {\n    fn default() -> Self {\n        Self::none().paradigm()\n    }\n}\n\n// ────────────────────────────────────────────────────────────────────────────\n// 내부 최종 설정 구조체 (TrainerBuilder → Trainer 변환 결과)\n// ────────────────────────────────────────────────────────────────────────────\n\n/// Trainer 내부에서 사용하는 확정된 로그·메트릭 설정.\n/// `TrainerBuilder::build()`가 생성.\npub struct LogConfig {\n    /// 몇 배치마다 메트릭 계산 및 배치 progress bar 를 갱신할지 여부.\n    /// `usize::MAX` = 배치 레벨 로그 완전 비활성."},{"path":"src/trainer/core/legacy_convergence.rs","bytes":101,"preview":"//! Shared convergence policy for both trainer APIs.\npub use crate::trainer::core::convergence::*;\n"},{"path":"src/trainer/core/legacy_metric_hook.rs","bytes":4313,"preview":"//! 기존 `LogConfig.metrics` 비트플래그는 내장 지표(grad_norm, accuracy 등)만 다룰 수 있었다.\n//! 아키텍처별 트레이너(비지도·반지도·강화학습)가 추가되면서 각기 다른 지표\n//! (diffusion 의 SNR, RL 의 평균 보상, 반지도의 consistency loss 등)를 필요로 하므로,\n//! 플러그인 방식으로 확장 가능한 훅 프로토콜을 제공한다.\n//!\n//! ## 동작 경로 (Phase 3 이후)\n//!\n//! 1. 사용자 또는 트레이너 프리셋이 `TrainerCore::add_hook(Box::new(MyHook::new()))` 로 장착.\n//! 2. 매 배치마다 `TrainerCore::run_epoch` 내부에서 [`BatchContext`] 를 조립해\n//!    `hook.update(&ctx)` 호출. NaN 감지로 조기 종료 시에는 호출하지 않는다.\n//! 3. 에폭 경계에서 `hook.format()` 결과가 에폭 요약 로그 뒷부분에 합류.\n//! 4. `hook.reset()` 은 다음 에폭 시작 시 자동 호출된다.\n//!\n//! ## 컨텍스트 노출 규약\n//!\n//! 패러다임별 [`crate::legacy::trainer::EpochStep`] 구현이 `last_pred / last_target /\n//! last_n_tokens / last_lambda / current_lr` 메서드를 통해 노출하는 값만\n//! `BatchContext` 에 채워진다. 패러다임에 의미 없는 필드는 `None` 이다.\n\nuse super::*;\n\n/// 배치 한 번의 순방향·역방향이 끝난 시점에 훅으로 전달되는 문맥.\n///\n/// 모든 필드는 **패러다임이 노출한 경우에만** 값이 들어온다. 훅은 필요한\n/// 필드만 꺼내 쓰고 나머지는 무시한다.\n///\n/// 수명 파라미터 `'a` 는 `EpochStep` 내부에서 stash 된 참조(예: 마지막\n/// 배치의 `pred` Variable) 의 수명과 연동된다. 따라서 `BatchContext` 는\n/// `update` 호출 범위를 넘어 저장해서는 안 된다.\npub struct BatchContext\u003c'a> {\n    /// 현재 에폭 내의 0-indexed 배치 번호.\n    pub batch_idx: usize,\n    /// 모델 순방향 출력 텐서. 스텝이 노출하지 않으면 `None`.\n    pub pred:      Option\u003c&'a dyn TensorBase>,\n    /// 정답 텐서. 비지도/자기회귀처럼 명시적 타깃이 없으면 `None`.\n    pub target:    Option\u003c&'a dyn TensorBase>,\n    /// 이번 배치의 스칼라 손실값 (`StepOutput::loss`).\n    pub loss:      f32,\n    /// 이 배치에서 유효한 타깃 토큰 수. 자기회귀(AR) 외에는 일반적으로 `None`.\n    pub n_tokens:  Option\u003cusize>,\n    /// 반지도 학습의 현재 일관성 가중치. 해당 없으면 `None`.\n    pub lambda:    Option\u003cf32>,\n    /// 옵티마이저의 현재 학습률.\n    pub lr:        f32,\n}\n\n/// 학습 루프의 매 배치마다 호출되는 플러그인 가능한 메트릭.\n///\n/// # 설계 원칙\n/// - **Dyn-compatible**: 제네릭 메서드·연관 타입 없이 `Box\u003cdyn MetricHook>` 으로 보관 가능해야 한다.\n/// - **Side-effect free between batches**: `update` 는 내부 상태만 누적하고, 외부 I/O 를 수행하지 않는다.\n/// - **Cheap `compute`**: 에폭 요약 시 한 번 호출되므로 복잡한 연산을 피한다.\n///\n/// # Send 경계 미부여 사유\n/// 현재 학습 루프는 단일 스레드이며, 훅이 `Variable` 이나 `GlobalTensor` 같은\n/// 비-Send 타입을 내부에 붙잡을 자유를 유지하려고 의도적으로 `Send` 를 요구하지 않는다.\n/// 병렬 학습을 도입할 때 별도의 `MetricHookSend` 를 만들거나 bound 를 추가한다.\npub trait MetricHook {\n    /// 배치 한 번의 결과를 누적한다.\n    fn update(&mut self, ctx: &BatchContext\u003c'_>) -> MlResult\u003c()>;\n\n    /// 현재까지 누적된 값을 단일 f32 로 환산해 반환한다.\n    ///\n    /// 일반적으로 에폭 종료 시 한 번 호출되며, 로그 포맷터가 이 값을\n    /// `format()` 과 함께 사용해 표시 문자열을 만든다."},{"path":"src/trainer/core/legacy_metrics.rs","bytes":9833,"preview":"use super::*;\nuse super::metric_hook::{MetricHook, BatchContext};\n\n// ────────────────────────────────────────────────────────────────────────────\n// 수치 유틸 함수 — 학습 루프 안팎에서 독립적으로 사용 가능\n// ────────────────────────────────────────────────────────────────────────────\n\n/// 등록된 파라미터 전체의 그래디언트 L2 노름.\n///\n/// `params`가 비어 있거나 grad가 아직 계산되지 않았으면 `0.0`을 반환.\npub fn grad_norm(params: &[&dyn Parameter]) -> f32 {\n    params.iter()\n        .flat_map(|p| p.grad().data().iter().map(|&g| g * g))\n        .sum::\u003cf32>()\n        .sqrt()\n}\n\n/// 등록된 파라미터 전체의 가중치 L2 노름.\npub fn weight_norm(params: &[&dyn Parameter]) -> f32 {\n    params.iter()\n        .flat_map(|p| p.tensor().data().iter().map(|&w| w * w))\n        .sum::\u003cf32>()\n        .sqrt()\n}\n\n/// Update Ratio = ||lr · grad|| / ||W||.\n///\n/// 현재 학습률에서 한 스텝의 업데이트 크기가 가중치 크기 대비 얼마나 되는지를 나타냄.\n/// 일반적으로 `1e-3` 근처가 건강한 범위.\n/// `||W|| ≤ 1e-12`이면 `0.0`을 반환 (제로 나눗셈 방지).\npub fn update_ratio(params: &[&dyn Parameter], lr: f32) -> f32 {\n    let update_sq: f32 = params.iter()\n        .flat_map(|p| p.grad().data().iter().map(|&g| { let u = lr * g; u * u }))\n        .sum();\n    let weight_sq: f32 = params.iter()\n        .flat_map(|p| p.tensor().data().iter().map(|&w| w * w))\n        .sum();\n    if weight_sq > 1e-12 { update_sq.sqrt() / weight_sq.sqrt() } else { 0.0 }\n}\n\n/// 모든 파라미터의 그래디언트에 NaN 또는 Inf가 있는지 검사.\n///\n/// 조기 종료(short-circuit)로 구현되어 첫 비정상값 발견 시 즉시 반환.\n/// Gradient explosion 시 NaN보다 Inf가 먼저 발생하는 경우가 많으므로\n/// 두 조건을 모두 검사.\npub fn has_invalid_grad(params: &[&dyn Parameter]) -> bool {\n    params.iter().any(|p|\n        p.grad().data().iter().any(|x| x.is_nan() || x.is_infinite())\n    )\n}\n\n// ────────────────────────────────────────────────────────────────────────────\n// ClassificationAccuracy — argmax 기반 분류 정확도 누적기\n// ────────────────────────────────────────────────────────────────────────────\n\n/// argmax 비교로 배치 단위 분류 정확도를 누적한다.\n///\n/// # 주의\n/// argmax가 유효하지 않은 샘플(all-zero 등)은 분모에서 제외.\n/// 이는 손실의 분모(`total_loss_count`)와 다를 수 있으므로 두 값을 혼용하지 않도록 함.\n#[derive(Default)]\npub struct ClassificationAccuracy {\n    correct: usize,\n    total:   usize,\n}"},{"path":"src/trainer/core/legacy_mod.rs","bytes":6256,"preview":"//! 학습 루프 공통 인프라.\n//!\n//! 모든 패러다임별 트레이너(`SupervisedTrainer`, `UnsupervisedTrainer`,\n//! `SemiSupervisedTrainer`, `RLTrainer`) 가 공유하는 타입·함수를 모아둔다.\n//!\n//! - [`TrainerCore`] : 로그 설정 + 메트릭 훅 보관 (아키텍처-불문 공용 상태).\n//! - [`LogConfig`], [`Metrics`], [`TrainerBuilder`] — 학습 루프 구성.\n//! - [`MetricHook`], [`BatchContext`] — 플러그인 메트릭 프로토콜.\n//! - [`Convergence`] — 조기 종료 판정.\n//! - `grad_norm`, `weight_norm`, `update_ratio`, `ClassificationAccuracy` 등 내장 메트릭.\n\n#[path = \"legacy_config.rs\"]\npub mod config;\n#[path = \"legacy_metric_hook.rs\"]\npub mod metric_hook;\n#[path = \"legacy_metrics.rs\"]\npub mod metrics;\n#[path = \"legacy_convergence.rs\"]\npub mod convergence;\n#[path = \"epoch_loop.rs\"]\npub mod epoch_loop;\n#[path = \"legacy_runtime.rs\"]\npub mod runtime;\n#[path = \"legacy_observer.rs\"]\npub mod observer;\n#[path = \"debug_trace.rs\"]\nmod debug_trace;\n#[cfg(feature = \"enableVisualization\")]\n#[path = \"graph_capture.rs\"]\nmod graph_capture;\n\nuse std::cell::RefCell;\n\n// 부모(`trainer`) 로부터 상속받는 공통 심볼.\n// 하위 모듈이 `use super::*;` 로 가져다 쓴다.\npub(crate) use super::{MlError, MlResult, Parameter, TensorBase};\n\n// 공용 API 재수출.\npub use config::{LogConfig, TrainerConfig, Metrics, TrainerBuilder};\npub use metric_hook::{MetricHook, BatchContext};\npub use metrics::{\n    grad_norm, weight_norm, update_ratio, has_invalid_grad,\n    argmax, ClassificationAccuracy, Perplexity,\n};\npub use convergence::Convergence;\npub use epoch_loop::{EpochStep, StepOutput, StepDiagnostics, BatchObservations, EpochOutcome};\npub use runtime::TrainingRuntime;\npub use observer::{\n    BatchEndContext, BatchStartContext, EpochContext, TrainEndContext, TrainStartContext,\n    TrainingObserver,\n};\n#[cfg(feature = \"enableVisualization\")]\npub use observer::{CaptureSelector, GraphVisualizationObserver, GraphVisualizationObserverBuilder};\n\n/// 아키텍처-불문 공용 상태.\n///\n/// - `config` : 기존 `LogConfig` 그대로. 내장 메트릭 플래그와 로그 주기를 담는다.\n/// - `hooks`  : `MetricHook` 의 동적 디스패치 목록. `RefCell` 로 감싸두어\n///              `&TrainerCore` 에서도 훅을 업데이트할 수 있게 한다\n///              (Phase 3 에서 `run_epoch(&self, ...)` 가 훅을 돌리기 위함).\npub struct TrainerCore {\n    pub(crate) config: LogConfig,\n    pub(crate) hooks:  RefCell\u003cVec\u003cBox\u003cdyn MetricHook>>>,\n    pub(crate) runtime: TrainingRuntime,\n    pub(crate) observers: RefCell\u003cVec\u003cBox\u003cdyn TrainingObserver>>>,"},{"path":"src/trainer/core/legacy_observer.rs","bytes":9059,"preview":"//! Training lifecycle observers and optional graph-capture integration.\n\n#[derive(Debug, Clone)]\npub struct TrainStartContext {\n    pub paradigm: &'static str,\n    pub total_units: usize,\n}\n\n#[derive(Debug, Clone)]\npub struct EpochContext {\n    pub paradigm: &'static str,\n    pub epoch: usize,\n    pub total_epochs: usize,\n    pub total_batches: Option\u003cusize>,\n}\n\n#[derive(Debug, Clone)]\npub struct BatchStartContext {\n    pub paradigm: &'static str,\n    pub epoch: usize,\n    pub batch: usize,\n    pub total_epochs: usize,\n    pub total_batches: Option\u003cusize>,\n    pub episode: Option\u003cusize>,\n}\n\n#[derive(Debug, Clone)]\npub struct BatchEndContext {\n    pub batch: BatchStartContext,\n    pub loss: f32,\n}\n\n#[derive(Debug, Clone)]\npub struct TrainEndContext {\n    pub paradigm: &'static str,\n    pub units_completed: usize,\n    pub interrupted: bool,\n}\n\npub trait TrainingObserver {\n    fn on_train_start(&mut self, _context: &TrainStartContext) {}\n    fn on_epoch_start(&mut self, _context: &EpochContext) {}\n    fn on_batch_end(&mut self, _context: &BatchEndContext) {}\n    fn on_epoch_end(&mut self, _context: &EpochContext) {}\n    fn on_train_end(&mut self, _context: &TrainEndContext) {}\n    fn on_train_error(&mut self, _message: &str) {}\n\n    #[cfg(feature = \"enableVisualization\")]\n    fn capture_profile(\n        &self,\n        _context: &BatchStartContext,\n    ) -> Option\u003ccrate::legacy::visualization::CaptureProfile> {\n        None\n    }\n\n    #[cfg(feature = \"enableVisualization\")]\n    fn on_graph_snapshot(&mut self, _snapshot: crate::legacy::visualization::GraphSnapshot) {}\n}\n\n#[cfg(feature = \"enableVisualization\")]\nmod graph_observer {\n    use super::*;\n    use crate::legacy::visualization::{CaptureProfile, GraphSnapshot, SnapshotWriter, VisualizationError};\n    use std::collections::HashSet;\n"},{"path":"src/trainer/core/legacy_runtime.rs","bytes":700,"preview":"use std::cell::RefCell;\nuse rand::{Rng, SeedableRng, rngs::StdRng, seq::SliceRandom};\n\n/// Deterministic single-threaded runtime shared by every trainer paradigm.\n/// TODO(ParallelRuntime): add a Send + Sync implementation with parallel training.\npub struct TrainingRuntime { rng: RefCell\u003cStdRng> }\n\nimpl TrainingRuntime {\n    pub fn new(seed: u64) -> Self { Self { rng: RefCell::new(StdRng::seed_from_u64(seed)) } }\n    pub fn reseed(&self, seed: u64) { *self.rng.borrow_mut() = StdRng::seed_from_u64(seed); }\n    pub fn shuffle\u003cT>(&self, values: &mut [T]) { values.shuffle(&mut *self.rng.borrow_mut()); }\n    pub fn random_f32(&self) -> f32 { self.rng.borrow_mut().random::\u003cf32>() }\n}\n"},{"path":"src/trainer/core/metric_hook.rs","bytes":4277,"preview":"//! 기존 `LogConfig.metrics` 비트플래그는 내장 지표(grad_norm, accuracy 등)만 다룰 수 있었다.\n//! 아키텍처별 트레이너(비지도·반지도·강화학습)가 추가되면서 각기 다른 지표\n//! (diffusion 의 SNR, RL 의 평균 보상, 반지도의 consistency loss 등)를 필요로 하므로,\n//! 플러그인 방식으로 확장 가능한 훅 프로토콜을 제공한다.\n//!\n//! ## 동작 경로 (Phase 3 이후)\n//!\n//! 1. 사용자 또는 트레이너 프리셋이 `TrainerCore::add_hook(Box::new(MyHook::new()))` 로 장착.\n//! 2. 매 배치마다 `TrainerCore::run_epoch` 내부에서 [`BatchContext`] 를 조립해\n//!    `hook.update(&ctx)` 호출. NaN 감지로 조기 종료 시에는 호출하지 않는다.\n//! 3. 에폭 경계에서 `hook.format()` 결과가 에폭 요약 로그 뒷부분에 합류.\n//! 4. `hook.reset()` 은 다음 에폭 시작 시 자동 호출된다.\n//!\n//! ## 컨텍스트 노출 규약\n//!\n//! 패러다임별 [`crate::trainer::EpochStep`] 구현이 `last_pred / last_target /\n//! last_n_tokens / last_lambda / current_lr` 메서드를 통해 노출하는 값만\n//! `BatchContext` 에 채워진다. 패러다임에 의미 없는 필드는 `None` 이다.\n\nuse super::*;\n\n/// 배치 한 번의 순방향·역방향이 끝난 시점에 훅으로 전달되는 문맥.\n///\n/// 모든 필드는 **패러다임이 노출한 경우에만** 값이 들어온다. 훅은 필요한\n/// 필드만 꺼내 쓰고 나머지는 무시한다.\n///\n/// 수명 파라미터 `'a` 는 `EpochStep` 내부에서 stash 된 참조(예: 마지막\n/// 배치의 `pred` Variable) 의 수명과 연동된다. 따라서 `BatchContext` 는\n/// `update` 호출 범위를 넘어 저장해서는 안 된다.\npub struct BatchContext\u003c'a> {\n    /// 현재 에폭 내의 0-indexed 배치 번호.\n    pub batch_idx: usize,\n    /// 모델 순방향 출력 텐서. 스텝이 노출하지 않으면 `None`.\n    pub pred: Option\u003c&'a TensorBuffer>,\n    /// 정답 텐서. 비지도/자기회귀처럼 명시적 타깃이 없으면 `None`.\n    pub target: Option\u003c&'a TensorBuffer>,\n    /// 이번 배치의 스칼라 손실값 (`StepOutput::loss`).\n    pub loss: f32,\n    /// 이 배치에서 유효한 타깃 토큰 수. 자기회귀(AR) 외에는 일반적으로 `None`.\n    pub n_tokens: Option\u003cusize>,\n    /// 반지도 학습의 현재 일관성 가중치. 해당 없으면 `None`.\n    pub lambda: Option\u003cf32>,\n    /// 옵티마이저의 현재 학습률.\n    pub lr: f32,\n}\n\n/// 학습 루프의 매 배치마다 호출되는 플러그인 가능한 메트릭.\n///\n/// # 설계 원칙\n/// - **Dyn-compatible**: 제네릭 메서드·연관 타입 없이 `Box\u003cdyn MetricHook>` 으로 보관 가능해야 한다.\n/// - **Side-effect free between batches**: `update` 는 내부 상태만 누적하고, 외부 I/O 를 수행하지 않는다.\n/// - **Cheap `compute`**: 에폭 요약 시 한 번 호출되므로 복잡한 연산을 피한다.\n///\n/// # Send 경계 미부여 사유\n/// 현재 학습 루프는 단일 스레드이며, 훅이 `Variable` 이나 `GlobalTensor` 같은\n/// 비-Send 타입을 내부에 붙잡을 자유를 유지하려고 의도적으로 `Send` 를 요구하지 않는다.\n/// 병렬 학습을 도입할 때 별도의 `MetricHookSend` 를 만들거나 bound 를 추가한다.\npub trait MetricHook {\n    /// 배치 한 번의 결과를 누적한다.\n    fn update(&mut self, ctx: &BatchContext\u003c'_>) -> MlResult\u003c()>;\n\n    /// 현재까지 누적된 값을 단일 f32 로 환산해 반환한다.\n    ///\n    /// 일반적으로 에폭 종료 시 한 번 호출되며, 로그 포맷터가 이 값을\n    /// `format()` 과 함께 사용해 표시 문자열을 만든다."},{"path":"src/trainer/core/metrics.rs","bytes":8270,"preview":"use super::*;\n#[derive(Default)]\npub struct ClassificationAccuracy {\n    correct: usize,\n    total: usize,\n}\n\nimpl ClassificationAccuracy {\n    pub fn new() -> Self {\n        Self::default()\n    }\n\n    /// 예측 텐서와 정답 텐서를 받아 내부 상태를 갱신.\n    pub fn update(&mut self, pred: &TensorBuffer, target: &TensorBuffer) {\n        if pred.shape() != target.shape() {\n            return;\n        }\n        let Some(&classes) = pred.shape().last() else {\n            return;\n        };\n        if classes == 0 {\n            return;\n        }\n        for (prediction, target) in pred\n            .data()\n            .chunks_exact(classes)\n            .zip(target.data().chunks_exact(classes))\n        {\n            if let (Some(p), Some(t)) = (argmax(prediction), argmax(target)) {\n                if p == t {\n                    self.correct += 1;\n                }\n                self.total += 1;\n            }\n        }\n    }\n\n    /// 현재까지 누적된 정확도를 백분율(0.0 ~ 100.0)로 반환.\n    pub fn compute(&self) -> f32 {\n        if self.total > 0 {\n            (self.correct as f32 / self.total as f32) * 100.0\n        } else {\n            0.0\n        }\n    }\n\n    /// 에폭 시작 시 상태를 초기화.\n    pub fn reset(&mut self) {\n        self.correct = 0;\n        self.total = 0;\n    }\n}\n\n// ────────────────────────────────────────────────────────────────────────────\n// MetricHook 어댑터\n//\n// `ClassificationAccuracy` 는 고유 `update/compute/reset` 메서드를 그대로 두고\n// 훅 트레잇 경로는 경로 수식(Self::update)으로 위임한다. 이렇게 해야 기존\n// 호출자(`accuracy.update(y.tensor(), t.tensor())`)가 그대로 동작하면서도\n// `Box\u003cdyn MetricHook>` 컨테이너에도 넣을 수 있다.\n// ────────────────────────────────────────────────────────────────────────────\n\nimpl MetricHook for ClassificationAccuracy {\n    fn update(&mut self, ctx: &BatchContext\u003c'_>) -> MlResult\u003c()> {\n        // pred/target 가 모두 노출된 경우에만 누적."},{"path":"src/trainer/core/mod.rs","bytes":5989,"preview":"//! 학습 루프 공통 인프라.\n//!\n//! 모든 패러다임별 트레이너(`SupervisedTrainer`, `UnsupervisedTrainer`,\n//! `SemiSupervisedTrainer`, `RLTrainer`) 가 공유하는 타입·함수를 모아둔다.\n//!\n//! - [`TrainerCore`] : 로그 설정 + 메트릭 훅 보관 (아키텍처-불문 공용 상태).\n//! - [`LogConfig`], [`Metrics`], [`TrainerBuilder`] — 학습 루프 구성.\n//! - [`MetricHook`], [`BatchContext`] — 플러그인 메트릭 프로토콜.\n//! - [`Convergence`] — 조기 종료 판정.\n//! - `grad_norm`, `weight_norm`, `update_ratio`, `ClassificationAccuracy` 등 내장 메트릭.\n\npub mod config;\npub mod convergence;\npub mod metric_hook;\npub mod metrics;\n\npub mod observer;\npub mod runtime;\n\nuse std::cell::RefCell;\n\n// 부모(`trainer`) 로부터 상속받는 공통 심볼.\n// 하위 모듈이 `use super::*;` 로 가져다 쓴다.\npub(crate) use crate::{MlError, MlResult, Parameter, TensorBuffer};\n\n// 공용 API 재수출.\npub use config::{LogConfig, Metrics, TrainerBuilder, TrainerConfig};\npub use convergence::Convergence;\npub use metric_hook::{BatchContext, MetricHook};\npub use metrics::{\n    ClassificationAccuracy, Perplexity, argmax, grad_norm, has_invalid_grad, update_ratio,\n    weight_norm,\n};\n\npub use observer::{\n    BatchEndContext, BatchStartContext, EpochContext, TrainEndContext, TrainStartContext,\n    TrainingObserver,\n};\npub use runtime::TrainingRuntime;\n\npub use observer::{\n    CaptureSelector, GraphVisualizationObserver, GraphVisualizationObserverBuilder,\n};\n\n/// 아키텍처-불문 공용 상태.\n///\n/// - `config` : 기존 `LogConfig` 그대로. 내장 메트릭 플래그와 로그 주기를 담는다.\n/// - `hooks`  : `MetricHook` 의 동적 디스패치 목록. `RefCell` 로 감싸두어\n///              `&TrainerCore` 에서도 훅을 업데이트할 수 있게 한다\n///              (Phase 3 에서 `run_epoch(&self, ...)` 가 훅을 돌리기 위함).\npub struct TrainerCore {\n    pub(crate) config: LogConfig,\n    pub(crate) hooks: RefCell\u003cVec\u003cBox\u003cdyn MetricHook>>>,\n    pub(crate) runtime: TrainingRuntime,\n    pub(crate) observers: RefCell\u003cVec\u003cBox\u003cdyn TrainingObserver>>>,\n}\n\nimpl TrainerCore {\n    /// 빈 훅 목록과 함께 `LogConfig` 로부터 새 `TrainerCore` 를 생성함.\n    pub fn new(config: LogConfig) -> Self {\n        let runtime = TrainingRuntime::new(config.seed);\n        Self {\n            config,\n            hooks: RefCell::new(Vec::new()),\n            runtime,"},{"path":"src/trainer/core/observer.rs","bytes":8856,"preview":"//! Training lifecycle observers and optional graph-capture integration.\n\n#[derive(Debug, Clone)]\npub struct TrainStartContext {\n    pub paradigm: &'static str,\n    pub total_units: usize,\n}\n\n#[derive(Debug, Clone)]\npub struct EpochContext {\n    pub paradigm: &'static str,\n    pub epoch: usize,\n    pub total_epochs: usize,\n    pub total_batches: Option\u003cusize>,\n}\n\n#[derive(Debug, Clone)]\npub struct BatchStartContext {\n    pub paradigm: &'static str,\n    pub epoch: usize,\n    pub batch: usize,\n    pub total_epochs: usize,\n    pub total_batches: Option\u003cusize>,\n    pub episode: Option\u003cusize>,\n}\n\n#[derive(Debug, Clone)]\npub struct BatchEndContext {\n    pub batch: BatchStartContext,\n    pub loss: f32,\n}\n\n#[derive(Debug, Clone)]\npub struct TrainEndContext {\n    pub paradigm: &'static str,\n    pub units_completed: usize,\n    pub interrupted: bool,\n}\n\npub trait TrainingObserver {\n    fn on_train_start(&mut self, _context: &TrainStartContext) {}\n    fn on_epoch_start(&mut self, _context: &EpochContext) {}\n    fn on_batch_end(&mut self, _context: &BatchEndContext) {}\n    fn on_epoch_end(&mut self, _context: &EpochContext) {}\n    fn on_train_end(&mut self, _context: &TrainEndContext) {}\n    fn on_train_error(&mut self, _message: &str) {}\n\n    fn capture_profile(\n        &self,\n        _context: &BatchStartContext,\n    ) -> Option\u003ccrate::visualization::CaptureProfile> {\n        None\n    }\n\n    fn on_graph_snapshot(&mut self, _snapshot: crate::visualization::GraphSnapshot) {}\n}\n\nmod graph_observer {\n    use super::*;\n    use crate::visualization::{CaptureProfile, GraphSnapshot, SnapshotWriter, VisualizationError};\n    use std::collections::HashSet;\n\n    #[non_exhaustive]\n    #[derive(Debug, Clone, PartialEq, Eq, Hash)]\n    pub enum CaptureSelector {"},{"path":"src/trainer/core/runtime.rs","bytes":1097,"preview":"use rand::{Rng, SeedableRng, rngs::StdRng, seq::SliceRandom};\nuse std::cell::RefCell;\n\n/// Deterministic single-threaded runtime shared by every trainer paradigm.\n/// TODO(ParallelRuntime): add a Send + Sync implementation with parallel training.\npub struct TrainingRuntime {\n    data_rng: RefCell\u003cStdRng>,\n    action_rng: RefCell\u003cStdRng>,\n}\n\nimpl TrainingRuntime {\n    pub fn new(seed: u64) -> Self {\n        Self {\n            data_rng: RefCell::new(StdRng::seed_from_u64(seed)),\n            action_rng: RefCell::new(StdRng::seed_from_u64(seed ^ 0xA076_1D64_78BD_642F)),\n        }\n    }\n    pub fn reseed(&self, seed: u64) {\n        *self.data_rng.borrow_mut() = StdRng::seed_from_u64(seed);\n        *self.action_rng.borrow_mut() = StdRng::seed_from_u64(seed ^ 0xA076_1D64_78BD_642F);\n    }\n    pub fn shuffle\u003cT>(&self, values: &mut [T]) {\n        values.shuffle(&mut *self.data_rng.borrow_mut());\n    }\n    /// RL action randomness is independent of data shuffling.\n    pub fn random_f32(&self) -> f32 {\n        self.action_rng.borrow_mut().random::\u003cf32>()\n    }\n}\n"},{"path":"src/trainer/data/legacy_loader.rs","bytes":18041,"preview":"use crate::legacy::trainer::TrainingRuntime;\nuse crate::legacy::{\n    MlError, MlResult,\n    nn::{Parameter, Variable},\n    tensor::{Tensor, TensorBase},\n};\n\nuse super::DataError;\n\nfn classify_collate_error(error: MlError) -> MlError {\n    match error {\n        shape @ MlError::DataError(DataError::ShapeMismatch { .. }) => shape,\n        empty @ MlError::DataError(DataError::EmptyBatch) => empty,\n        other => DataError::Collate {\n            message: other.to_string(),\n        }\n        .into(),\n    }\n}\n\npub trait Dataset {\n    type Sample;\n    fn len(&self) -> usize;\n    fn get(&self, index: usize) -> Option\u003c&Self::Sample>;\n    fn is_empty(&self) -> bool {\n        self.len() == 0\n    }\n}\n\n#[derive(Debug)]\npub struct InMemoryDataset\u003cS> {\n    samples: Vec\u003cS>,\n}\n\nimpl\u003cS> InMemoryDataset\u003cS> {\n    pub fn new(samples: Vec\u003cS>) -> Result\u003cSelf, DataError> {\n        if samples.is_empty() {\n            return Err(DataError::EmptyDataset);\n        }\n        Ok(Self { samples })\n    }\n    pub fn samples(&self) -> &[S] {\n        &self.samples\n    }\n    pub fn into_samples(self) -> Vec\u003cS> {\n        self.samples\n    }\n}\n\nimpl\u003cS> Dataset for InMemoryDataset\u003cS> {\n    type Sample = S;\n    fn len(&self) -> usize {\n        self.samples.len()\n    }\n    fn get(&self, index: usize) -> Option\u003c&S> {\n        self.samples.get(index)\n    }\n}\n\npub trait Collator\u003cS> {\n    type Batch;\n    fn collate(&mut self, samples: &[&S]) -> MlResult\u003cSelf::Batch>;\n}\n\nimpl\u003cS, B, F> Collator\u003cS> for F"},{"path":"src/trainer/data/legacy_mod.rs","bytes":2270,"preview":"//! Eager dataset construction and batching infrastructure.\n//!\n//! `DatasetBuilder` owns decoding and record-to-sample transforms. `DataLoader`\n//! owns batching and epoch ordering. The small borrowed dataset wrappers in\n//! [`crate::legacy::trainer::api`] remain the pre-batched convenience path.\n\n#[path = \"legacy.rs\"]\nmod legacy;\n#[path = \"legacy_loader.rs\"]\nmod loader;\n#[path = \"legacy_source.rs\"]\nmod source;\n\npub use loader::{\n    AutoregressiveBatch, AutoregressiveSample, AutoregressiveStackCollator, BatchLoader, Collator,\n    DataLoader, DataLoaderBuilder, Dataset, InMemoryDataset, IntoBatchLoader, MissingCollator,\n    SemiSupervisedBatch, SemiSupervisedDataLoader, SemiSupervisedDataLoaderBuilder,\n    SupervisedBatch, SupervisedSample, SupervisedStackCollator, UnsupervisedBatch,\n    UnsupervisedSample, UnsupervisedStackCollator,\n};\npub use source::{\n    CsvRecord, CsvSource, DatasetBuilder, JsonLinesSource, JsonRecord, LocatedRecord, MemorySource,\n    RecordSource, Transform,\n};\n\nuse std::path::PathBuf;\n\n/// Errors raised while decoding, transforming, and batching training data.\n#[derive(Debug, thiserror::Error)]\npub enum DataError {\n    #[error(\"data I/O failed for {path}: {message}\")]\n    Io { path: PathBuf, message: String },\n    #[error(\"data decode failed for {path} at line/row {line}: {message}\")]\n    Decode {\n        path: PathBuf,\n        line: usize,\n        message: String,\n    },\n    #[error(\"data transform failed{location}: {message}\")]\n    Transform { location: String, message: String },\n    #[error(\"dataset must not be empty\")]\n    EmptyDataset,\n    #[error(\"batch_size must be greater than zero\")]\n    InvalidBatchSize,\n    #[error(\"data loader would produce zero batches; disable drop_last or reduce batch_size\")]\n    NoBatches,\n    #[error(\"cannot collate an empty batch\")]\n    EmptyBatch,\n    #[error(\"batch collation failed: {message}\")]\n    Collate { message: String },\n    #[error(\"shape mismatch at sample {sample_index}: expected {expected:?}, got {got:?}\")]\n    ShapeMismatch {\n        sample_index: usize,\n        expected: Vec\u003cusize>,\n        got: Vec\u003cusize>,\n    },\n}\n\n#[cfg(test)]\n#[path = \"../../tests/trainer/data/legacy_mod_tests.rs\"]\nmod tests;\n"},{"path":"src/trainer/data/legacy_source.rs","bytes":8478,"preview":"use std::{\n    collections::BTreeMap,\n    fs::File,\n    io::{BufRead, BufReader},\n    path::{Path, PathBuf},\n};\n\nuse crate::legacy::{MlError, MlResult};\n\nuse super::{DataError, InMemoryDataset};\n\n/// A decoded record together with its source location.\npub struct LocatedRecord\u003cR> {\n    pub record: R,\n    pub path: Option\u003cPathBuf>,\n    pub line: usize,\n}\n\n/// Eager source of raw records.\npub trait RecordSource {\n    type Record;\n\n    fn read(self) -> MlResult\u003cVec\u003cSelf::Record>>;\n\n    fn read_located(self) -> MlResult\u003cVec\u003cLocatedRecord\u003cSelf::Record>>>\n    where\n        Self: Sized,\n    {\n        Ok(self\n            .read()?\n            .into_iter()\n            .enumerate()\n            .map(|(index, record)| LocatedRecord {\n                record,\n                path: None,\n                line: index + 1,\n            })\n            .collect())\n    }\n}\n\npub struct MemorySource\u003cR> {\n    records: Vec\u003cR>,\n}\n\nimpl\u003cR> MemorySource\u003cR> {\n    pub fn new(records: Vec\u003cR>) -> Self {\n        Self { records }\n    }\n}\n\nimpl\u003cR> From\u003cVec\u003cR>> for MemorySource\u003cR> {\n    fn from(records: Vec\u003cR>) -> Self {\n        Self::new(records)\n    }\n}\n\nimpl\u003cR> RecordSource for MemorySource\u003cR> {\n    type Record = R;\n\n    fn read(self) -> MlResult\u003cVec\u003cR>> {\n        Ok(self.records)\n    }\n}\n"},{"path":"src/trainer/data/legacy.rs","bytes":6096,"preview":"use crate::legacy::{\n    MlResult,\n    nn::Variable,\n    trainer::{\n        AutoregressiveDataset, SemiSupervisedDataset, SupervisedDataset, TrainingRuntime,\n        UnsupervisedDataset,\n    },\n};\n\nuse super::{\n    AutoregressiveBatch, BatchLoader, IntoBatchLoader, SemiSupervisedBatch, SupervisedBatch,\n    UnsupervisedBatch,\n};\n\npub struct PreBatchedSupervised\u003c'a> {\n    dataset: SupervisedDataset\u003c'a>,\n    order: Vec\u003cusize>,\n    cursor: usize,\n}\npub struct PreBatchedUnsupervised\u003c'a> {\n    dataset: UnsupervisedDataset\u003c'a>,\n    order: Vec\u003cusize>,\n    cursor: usize,\n}\npub struct PreBatchedAutoregressive\u003c'a> {\n    dataset: AutoregressiveDataset\u003c'a>,\n    order: Vec\u003cusize>,\n    cursor: usize,\n}\npub struct PreBatchedSemiSupervised\u003c'a> {\n    dataset: SemiSupervisedDataset\u003c'a>,\n    labeled: Vec\u003cusize>,\n    unlabeled: Vec\u003cusize>,\n    cursor: usize,\n}\n\nimpl\u003c'a> IntoBatchLoader for SupervisedDataset\u003c'a> {\n    type Batch = SupervisedBatch;\n    type Loader = PreBatchedSupervised\u003c'a>;\n    fn into_batch_loader(self) -> Self::Loader {\n        PreBatchedSupervised {\n            order: (0..self.inputs.len()).collect(),\n            dataset: self,\n            cursor: 0,\n        }\n    }\n}\nimpl\u003c'a> IntoBatchLoader for UnsupervisedDataset\u003c'a> {\n    type Batch = UnsupervisedBatch;\n    type Loader = PreBatchedUnsupervised\u003c'a>;\n    fn into_batch_loader(self) -> Self::Loader {\n        PreBatchedUnsupervised {\n            order: (0..self.samples.len()).collect(),\n            dataset: self,\n            cursor: 0,\n        }\n    }\n}\nimpl\u003c'a> IntoBatchLoader for AutoregressiveDataset\u003c'a> {\n    type Batch = AutoregressiveBatch;\n    type Loader = PreBatchedAutoregressive\u003c'a>;\n    fn into_batch_loader(self) -> Self::Loader {\n        PreBatchedAutoregressive {\n            order: (0..self.sequences.len()).collect(),\n            dataset: self,"},{"path":"src/trainer/data/loader.rs","bytes":18118,"preview":"use crate::trainer::TrainingRuntime;\nuse crate::{MlError, MlResult, nn::Variable, tensor::Tensor};\n\nuse super::DataError;\n\nfn classify_collate_error(error: MlError) -> MlError {\n    error\n}\n\npub trait Dataset {\n    type Sample;\n    fn len(&self) -> usize;\n    fn get(&self, index: usize) -> Option\u003c&Self::Sample>;\n    fn is_empty(&self) -> bool {\n        self.len() == 0\n    }\n}\n\n#[derive(Debug)]\npub struct InMemoryDataset\u003cS> {\n    samples: Vec\u003cS>,\n}\n\nimpl\u003cS> InMemoryDataset\u003cS> {\n    pub fn new(samples: Vec\u003cS>) -> Result\u003cSelf, DataError> {\n        if samples.is_empty() {\n            return Err(DataError::EmptyDataset);\n        }\n        Ok(Self { samples })\n    }\n    pub fn samples(&self) -> &[S] {\n        &self.samples\n    }\n    pub fn into_samples(self) -> Vec\u003cS> {\n        self.samples\n    }\n}\n\nimpl\u003cS> Dataset for InMemoryDataset\u003cS> {\n    type Sample = S;\n    fn len(&self) -> usize {\n        self.samples.len()\n    }\n    fn get(&self, index: usize) -> Option\u003c&S> {\n        self.samples.get(index)\n    }\n}\n\npub trait Collator\u003cS> {\n    type Batch;\n    fn collate(&mut self, samples: &[&S]) -> MlResult\u003cSelf::Batch>;\n}\n\nimpl\u003cS, B, F> Collator\u003cS> for F\nwhere\n    F: FnMut(&[&S]) -> MlResult\u003cB>,\n{\n    type Batch = B;\n    fn collate(&mut self, samples: &[&S]) -> MlResult\u003cB> {\n        self(samples)\n    }\n}\n\npub trait BatchLoader {\n    type Batch;"},{"path":"src/trainer/data/mod.rs","bytes":990,"preview":"//! Eager dataset construction and batching infrastructure.\n//!\n//! `DatasetBuilder` owns decoding and record-to-sample transforms. `DataLoader`\n//! owns batching and epoch ordering. The small borrowed dataset wrappers in\n//! [`crate::trainer::api`] remain the pre-batched convenience path.\n\nmod loader;\nmod source;\n\npub use loader::{\n    AutoregressiveBatch, AutoregressiveSample, AutoregressiveStackCollator, BatchLoader, Collator,\n    DataLoader, DataLoaderBuilder, Dataset, InMemoryDataset, IntoBatchLoader, MissingCollator,\n    SemiSupervisedBatch, SemiSupervisedDataLoader, SemiSupervisedDataLoaderBuilder,\n    SupervisedBatch, SupervisedSample, SupervisedStackCollator, UnsupervisedBatch,\n    UnsupervisedSample, UnsupervisedStackCollator,\n};\npub use source::{\n    CsvRecord, CsvSource, DatasetBuilder, JsonLinesSource, JsonRecord, LocatedRecord, MemorySource,\n    RecordSource, Transform,\n};\n\npub use crate::DataError;\nmod prebatched;\npub use prebatched::*;\n"},{"path":"src/trainer/data/prebatched.rs","bytes":8405,"preview":"use super::*;\nuse crate::trainer::TrainingRuntime;\nuse crate::{MlError, MlResult, Tensor, Variable};\n\npub struct Prebatched\u003cB> {\n    invalid: Option\u003c&'static str>,\n    batches: Vec\u003cB>,\n    order: Vec\u003cusize>,\n    cursor: usize,\n}\nimpl\u003cB: Clone> BatchLoader for Prebatched\u003cB> {\n    type Batch = B;\n    fn begin_epoch(&mut self, _epoch: usize, runtime: &TrainingRuntime) -> MlResult\u003c()> {\n        if let Some(message) = self.invalid {\n            return Err(DataError::Collate {\n                message: message.into(),\n            }\n            .into());\n        }\n        if self.batches.is_empty() {\n            return Err(DataError::EmptyDataset.into());\n        }\n        self.order = (0..self.batches.len()).collect();\n        runtime.shuffle(&mut self.order);\n        self.cursor = 0;\n        Ok(())\n    }\n    fn next_batch(&mut self) -> MlResult\u003cOption\u003cB>> {\n        let Some(&index) = self.order.get(self.cursor) else {\n            return Ok(None);\n        };\n        self.cursor += 1;\n        Ok(Some(self.batches[index].clone()))\n    }\n    fn batch_count(&self) -> Option\u003cusize> {\n        Some(self.batches.len())\n    }\n}\nfn prebatched\u003cB>(batches: Vec\u003cB>) -> Prebatched\u003cB> {\n    Prebatched {\n        invalid: None,\n        batches,\n        order: Vec::new(),\n        cursor: 0,\n    }\n}\nfn invalid_prebatched\u003cB>(message: &'static str) -> Prebatched\u003cB> {\n    let mut loader = prebatched(Vec::new());\n    loader.invalid = Some(message);\n    loader\n}\nimpl IntoBatchLoader for &SupervisedDataset\u003c'_> {\n    type Batch = SupervisedBatch;\n    type Loader = Prebatched\u003cSelf::Batch>;\n    fn into_batch_loader(self) -> Self::Loader {\n        if self.inputs.len() != self.targets.len() {\n            return invalid_prebatched(\"input/target length mismatch\");\n        }\n        prebatched(\n            self.inputs\n                .iter()\n                .zip(self.targets)\n                .map(|(x, t)| SupervisedBatch {\n                    inputs: (*x).clone(),\n                    targets: (*t).clone(),"},{"path":"src/trainer/data/source.rs","bytes":8489,"preview":"use std::{\n    collections::BTreeMap,\n    fs::File,\n    io::{BufRead, BufReader},\n    path::{Path, PathBuf},\n};\n\nuse crate::{MlError, MlResult};\n\nuse super::{DataError, InMemoryDataset};\n\n/// A decoded record together with its source location.\npub struct LocatedRecord\u003cR> {\n    pub record: R,\n    pub path: Option\u003cPathBuf>,\n    pub line: usize,\n}\n\n/// Eager source of raw records.\npub trait RecordSource {\n    type Record;\n\n    fn read(self) -> MlResult\u003cVec\u003cSelf::Record>>;\n\n    fn read_located(self) -> MlResult\u003cVec\u003cLocatedRecord\u003cSelf::Record>>>\n    where\n        Self: Sized,\n    {\n        Ok(self\n            .read()?\n            .into_iter()\n            .enumerate()\n            .map(|(index, record)| LocatedRecord {\n                record,\n                path: None,\n                line: index + 1,\n            })\n            .collect())\n    }\n}\n\npub struct MemorySource\u003cR> {\n    records: Vec\u003cR>,\n}\n\nimpl\u003cR> MemorySource\u003cR> {\n    pub fn new(records: Vec\u003cR>) -> Self {\n        Self { records }\n    }\n}\n\nimpl\u003cR> From\u003cVec\u003cR>> for MemorySource\u003cR> {\n    fn from(records: Vec\u003cR>) -> Self {\n        Self::new(records)\n    }\n}\n\nimpl\u003cR> RecordSource for MemorySource\u003cR> {\n    type Record = R;\n\n    fn read(self) -> MlResult\u003cVec\u003cR>> {\n        Ok(self.records)\n    }\n}\n"},{"path":"src/trainer/legacy_api.rs","bytes":5957,"preview":"//! Shared public API types for all training paradigms.\n\nuse std::{collections::BTreeMap, path::Path, time::Duration};\nuse crate::legacy::{MlError, MlResult, nn::{Parameter, Variable}};\n\npub trait TrainableModel { fn params(&self) -> Vec\u003c&dyn Parameter>; }\n\npub trait CheckpointableModel {\n    fn save_checkpoint(&self, path: &Path) -> MlResult\u003c()> {\n        Err(MlError::StringError(format!(\"checkpoint save is not implemented: {}\", path.display())))\n    }\n    fn load_checkpoint(&mut self, path: &Path) -> MlResult\u003c()> {\n        Err(MlError::StringError(format!(\"checkpoint load is not implemented: {}\", path.display())))\n    }\n}\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub enum StopReason { Completed, Converged, Interrupted }\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub enum StepUnit { Epoch, Episode, Token }\n\npub type MetricValues = BTreeMap\u003cString, f32>;\n\n#[derive(Debug, Clone, PartialEq, Eq)]\npub struct CheckpointPaths {\n    pub model: std::path::PathBuf,\n    pub metadata: std::path::PathBuf,\n}\n\n#[derive(Debug)]\npub struct TrainResult {\n    pub stop_reason: StopReason,\n    pub units_completed: usize,\n    pub unit: StepUnit,\n    pub final_loss: f32,\n    pub metrics: MetricValues,\n    pub checkpoint: Option\u003cCheckpointPaths>,\n    pub total_duration: Duration,\n}\n\nimpl TrainResult {\n    pub(crate) fn epochs(reason: StopReason, completed: usize, loss: f32, duration: Duration) -> Self {\n        Self { stop_reason: reason, units_completed: completed, unit: StepUnit::Epoch,\n            final_loss: loss, metrics: MetricValues::new(), checkpoint: None, total_duration: duration }\n    }\n    pub(crate) fn episodes(completed: usize, loss: f32, duration: Duration) -> Self {\n        Self { stop_reason: StopReason::Completed, units_completed: completed, unit: StepUnit::Episode,\n            final_loss: loss, metrics: MetricValues::new(), checkpoint: None, total_duration: duration }\n    }\n\n    pub(crate) fn with_checkpoint(mut self, checkpoint: Option\u003cCheckpointPaths>) -> Self {\n        self.checkpoint = checkpoint;\n        self\n    }\n\n    pub(crate) fn with_metrics(mut self, metrics: MetricValues) -> Self {\n        self.metrics = metrics;\n        self\n    }\n}\n\n#[derive(Debug, Clone, Copy)]\npub struct EpochSchedule { pub epochs: usize, pub convergence: super::Convergence }\nimpl EpochSchedule {"},{"path":"src/trainer/legacy_checkpoint.rs","bytes":13603,"preview":"use super::*;\n\n// checkpoint 전용 import\nuse serde::{Deserialize, Serialize};\nuse std::sync::atomic::{AtomicBool, Ordering};\nuse std::sync::{Arc, OnceLock};\nuse std::path::Path;\n\npub const CHECKPOINT_SCHEMA_VERSION: u32 = 2;\nfn default_schema_version() -> u32 { 1 }\n\n// ────────────────────────────────────────────────────────────────────────────\n// 시그널 핸들러 — 프로세스당 한 번만 등록\n// ────────────────────────────────────────────────────────────────────────────\n\nstatic INTERRUPT_FLAG: OnceLock\u003cArc\u003cAtomicBool>> = OnceLock::new();\n\n/// Ctrl+C 인터럽트 플래그를 반환한다.\n/// 최초 호출 시 시그널 핸들러를 등록하며, 이후 호출에서는 같은 플래그를 공유한다.\npub(crate) fn interrupt_flag() -> Arc\u003cAtomicBool> {\n    INTERRUPT_FLAG.get_or_init(|| {\n        let flag = Arc::new(AtomicBool::new(false));\n        let flag_clone = flag.clone();\n        ctrlc::set_handler(move || {\n            flag_clone.store(true, Ordering::SeqCst);\n        }).expect(\"Ctrl+C 핸들러 등록 실패\");\n        flag\n    }).clone()\n}\n\n/// 인터럽트 플래그가 설정되어 있는지 확인한다.\npub(crate) fn is_interrupted(flag: &AtomicBool) -> bool {\n    flag.load(Ordering::SeqCst)\n}\n\n/// 인터럽트 플래그를 초기화한다.\npub(crate) fn clear_interrupt(flag: &AtomicBool) {\n    flag.store(false, Ordering::SeqCst);\n}\n\n// ────────────────────────────────────────────────────────────────────────────\n// 사용자 확인 프롬프트\n// ────────────────────────────────────────────────────────────────────────────\n\n/// 사용자에게 학습 중단 여부를 확인한다.\n///\n/// `true`를 반환하면 체크포인트를 저장하고 종료,\n/// `false`를 반환하면 학습을 계속한다.\npub(crate) fn confirm_interrupt() -> bool {\n    use std::io::{self, BufRead, Write};\n\n    eprint!(\"\\n⚠ 학습 중단 요청이 감지되었습니다.\\n\");\n    eprint!(\"체크포인트를 저장하고 종료하시겠습니까? (y/n): \");\n    io::stderr().flush().ok();\n\n    let mut input = String::new();\n    match io::stdin().lock().read_line(&mut input) {\n        Ok(_) => input.trim().eq_ignore_ascii_case(\"y\"),\n        Err(_) => {\n            // stdin 읽기 실패 시 안전하게 저장 후 종료\n            eprintln!(\"입력을 읽을 수 없습니다. 안전하게 체크포인트를 저장합니다.\");\n            true\n        }\n    }\n}"},{"path":"src/trainer/legacy_mod.rs","bytes":10232,"preview":"//! 학습 루프 인프라. 패러다임별로 모델 트레잇과 트레이너가 분리되어 있다.\n//!\n//! | 패러다임   | 모델 트레잇              | 트레이너                  | 손실 시그니처            |\n//! |-----------|-------------------------|--------------------------|-------------------------|\n//! | 지도학습   | [`SupervisedModel`]     | [`SupervisedTrainer`]    | `forward_loss(x, t)`    |\n//! | 비지도학습 | [`UnsupervisedModel`]   | [`UnsupervisedTrainer`]  | `forward_loss(x)`       |\n//! | 반지도학습 | [`SemiSupervisedModel`] | [`SemiSupervisedTrainer`]| `forward_loss(x_l, t_l, x_u, λ)` |\n//! | 강화학습   | [`RLModel`] + [`Environment`] | [`RLTrainer`]      | REINFORCE 내부 구성     |\n//! | 자기회귀   | [`AutoregressiveModel`] | [`AutoregressiveTrainer`]| `forward_loss(x) → (y, loss, n_tokens)` |\n//!\n//! 공통 인프라:\n//! - [`TrainerCore`] — 로그 설정, 메트릭 훅, 체크포인트 인터럽트.\n//! - [`TrainResult`] — 모든 트레이너의 공통 반환 타입.\n//! - [`Convergence`] — 연속 두 에폭의 손실 변화 기준 조기 종료.\n//! - [`MetricHook`]  — 배치마다 커스텀 메트릭을 주입하는 훅.\n//!\n//! `Trainer` 는 공통 설정을 소유하는 단일 facade다. `supervised()` 등으로\n//! 정적 타입이 보존된 패러다임별 runner를 선택한다.\n//!\n//! `debugging` feature를 활성화하면 모델 파라미터 구조와 에폭/배치 실행\n//! 컨텍스트가 기존 연산별 forward/backward trace에 자동으로 추가된다.\n//! 동적 progress bar는 상세 trace와 터미널 행이 충돌하지 않도록 비활성화된다.\n\n#[path = \"core/legacy_mod.rs\"]\npub mod core;\n#[path = \"legacy_api.rs\"]\npub mod api;\n#[path = \"legacy_checkpoint.rs\"]\npub mod checkpoint;\n#[path = \"supervised.rs\"]\npub mod supervised;\n#[path = \"unsupervised.rs\"]\npub mod unsupervised;\n#[path = \"semi_supervised.rs\"]\npub mod semi_supervised;\n#[path = \"legacy_reinforcement.rs\"]\npub mod reinforcement;\n#[path = \"autoregressive.rs\"]\npub mod autoregressive;\n#[path = \"data/legacy_mod.rs\"]\npub mod data;\n#[path = \"legacy_progress.rs\"]\npub(crate) mod progress;\n\npub use core::{\n    TrainerCore, LogConfig, TrainerConfig, TrainingRuntime, Metrics, TrainerBuilder,\n    MetricHook, BatchContext, Convergence,\n    grad_norm, weight_norm, update_ratio, has_invalid_grad,\n    argmax, ClassificationAccuracy, Perplexity,\n    EpochStep, StepOutput, StepDiagnostics, BatchObservations, EpochOutcome,\n    TrainingObserver, BatchStartContext, BatchEndContext, EpochContext, TrainStartContext,\n    TrainEndContext,\n};\n#[cfg(feature = \"enableVisualization\")]\npub use core::{CaptureSelector, GraphVisualizationObserver, GraphVisualizationObserverBuilder};\npub use api::{TrainableModel, CheckpointableModel, StopReason, StepUnit, MetricValues,\n    CheckpointPaths, TrainResult, EpochSchedule, EpisodeSchedule, SupervisedDataset,\n    UnsupervisedDataset, SemiSupervisedDataset, AutoregressiveDataset};\npub use api::{SupervisedOptions, SemiSupervisedOptions, AutoregressiveOptions, ReinforcementOptions};\npub use checkpoint::{TrainingCheckpoint, ParadigmTag, CheckpointManager, CHECKPOINT_SCHEMA_VERSION};\npub use data::{\n    AutoregressiveBatch, AutoregressiveSample, AutoregressiveStackCollator, BatchLoader,\n    Collator, CsvRecord, CsvSource, DataError, DataLoader, DataLoaderBuilder, Dataset,\n    DatasetBuilder, InMemoryDataset, IntoBatchLoader, JsonLinesSource, JsonRecord, MemorySource,\n    RecordSource, SemiSupervisedBatch, SemiSupervisedDataLoader, Transform,"},{"path":"src/trainer/legacy_progress.rs","bytes":6315,"preview":"#[allow(unused_imports)]\nuse super::*;\n\n// progress 전용 import\nuse std::time::Duration;\n\nuse indicatif::{MultiProgress, ProgressBar, ProgressDrawTarget, ProgressStyle};\n\n// ────────────────────────────────────────────────────────────────────────────\n// EpochProgress — 에폭 레벨 progress bar 래퍼\n// ────────────────────────────────────────────────────────────────────────────\n\n/// 에폭 레벨 progress bar 를 관리.\n///\n/// `show = false`일 때 hidden ProgressBar를 반환하므로 호출부에 분기 없음.\npub(crate) struct EpochProgress {\n    multi:     MultiProgress,\n    epoch_bar: ProgressBar,\n    show:      bool,\n}\n\nimpl EpochProgress {\n    pub fn new(epochs: usize, show: bool) -> Self {\n        // 연산 trace와 동적 progress 출력은 동일 터미널 행을 공유할 수 없다.\n        // debugging 빌드에서는 구조화 로그를 우선하고 progress만 숨긴다.\n        let show = show && !cfg!(feature = \"debugging\");\n        // 짧은 배치도 에폭 바와 동시에 보이도록 기본 15Hz보다 자주 그린다.\n        let multi = MultiProgress::with_draw_target(ProgressDrawTarget::stderr_with_hz(60));\n        let epoch_bar = if show {\n            let pb = multi.add(ProgressBar::new(epochs as u64));\n            pb.set_style(\n                ProgressStyle::default_bar()\n                    .template(\n                        \"{spinner:.green} [{elapsed_precise}] \\\n                         [ {wide_bar:.cyan/blue} ] {percent:>3}% Epochs ({eta}) | {msg}\"\n                    )\n                    .unwrap()\n                    .progress_chars(\"▉ \"),\n            );\n            pb\n        } else {\n            ProgressBar::hidden()\n        };\n        Self { multi, epoch_bar, show }\n    }\n\n    /// 배치 레벨 progress bar 를 에폭 바 아래에 추가하여 반환.\n    pub fn start_batch_bar(\n        &self,\n        epoch:     usize,\n        epochs:    usize,\n        n_batches: Option\u003cusize>,\n    ) -> BatchProgress {\n        if !self.show {\n            return BatchProgress { bar: ProgressBar::hidden(), active: false };\n        }\n        // MultiProgress는 각 bar가 한 번 draw되어야 해당 행을 합성한다.\n        // 첫 에폭부터 에폭/배치 두 행이 함께 보이도록 에폭 상태를 먼저 등록한다.\n        self.epoch_bar.tick();\n        let epoch_percent = (epoch + 1) * 100 / epochs.max(1);\n        let template = format!(\n            \"  > Epoch {:>3}% \\\n             [ {{wide_bar:.green/blue}} ] {{percent:>3}}% Batches ({{eta}}) | {{msg}}\",\n            epoch_percent,\n        );"},{"path":"src/trainer/legacy_reinforcement.rs","bytes":22670,"preview":"//! 강화학습 아키텍처용 트레이너 네임스페이스.\n//!\n//! 지도·비지도·반지도 트레이너가 **고정 데이터셋** 위에서 학습하는 반면,\n//! RL 트레이너는 **환경과의 상호작용을 통해 데이터(trajectory) 를 스스로 생성** 한다.\n//!\n//! ## 구성 요소\n//!\n//! - [`Environment`] : RL 환경 추상화 (reset / step).\n//! - [`RLModel`]     : 정책 네트워크가 구현하는 인터페이스.\n//! - [`RLTrainer`]   : REINFORCE (policy gradient) 학습 루프.\n//!\n//! ## 채택 알고리즘: REINFORCE (Monte-Carlo Policy Gradient)\n//!\n//! ```text\n//! 1. π_θ 로 한 에피소드 rollout → (s₀,a₀,r₀), ..., (s_T,a_T,r_T)\n//! 2. return G_t = r_t + γ·r_{t+1} + γ²·r_{t+2} + ...\n//! 3. advantage A_t = G_t - b(s)   (baseline = mean return)\n//! 4. loss = -Σ_t A_t · log π_θ(a_t|s_t)\n//! 5. ∇θ ← backward, optimizer.step\n//! ```\n//!\n//! `-log π(a|s) = SoftmaxCrossEntropy(logits, one_hot(a))` 항등식을 이용해\n//! 기존 `SoftmaxCrossEntropyLoss` 로 policy gradient 를 재활용한다.\n\nuse super::*;\n\n// ────────────────────────────────────────────────────────────────────────────\n// Environment — RL 환경 추상화\n// ────────────────────────────────────────────────────────────────────────────\n\n/// 환경 스텝의 결과.\npub struct StepResult {\n    /// 다음 관측치.\n    pub next_observation: crate::legacy::tensor::Tensor,\n    /// 이 스텝에서 받은 보상.\n    pub reward: f32,\n    /// 에피소드 종료 여부.\n    pub done: bool,\n}\n\n/// RL 환경이 구현해야 하는 인터페이스.\n///\n/// Gym 스타일의 최소 API 를 따른다. Observation 은 `Tensor` 로 고정하여\n/// dyn-compatible 을 유지한다.\n///\n/// # 구현 예시 (multi-armed bandit)\n/// ```ignore\n/// impl Environment for TwoArmedBandit {\n///     fn reset(&mut self) -> MlResult\u003cTensor> {\n///         Ok(Tensor::from_vec(vec![1.0], &[1, 1])?)\n///     }\n///     fn step(&mut self, action: usize) -> MlResult\u003cStepResult> {\n///         let reward = if action == 0 { 0.2 } else { 0.8 };\n///         Ok(StepResult {\n///             next_observation: Tensor::from_vec(vec![1.0], &[1, 1])?,\n///             reward,\n///             done: true,\n///         })\n///     }\n///     fn num_actions(&self) -> usize { 2 }\n///     fn observation_shape(&self) -> Vec\u003cusize> { vec![1, 1] }\n/// }\n/// ```\npub trait Environment {\n    /// 환경을 초기 상태로 리셋하고 초기 관측치를 반환."},{"path":"src/trainer/mod.rs","bytes":5673,"preview":"//! One training service shared by every paradigm.\nuse crate::{\n    ContextError, ContextId, ExecutionContext, MlError, MlResult, Parameter, Tensor, TensorBuffer,\n    Variable,\n};\npub mod api;\npub mod checkpoint;\npub mod core;\npub mod data;\npub(crate) mod progress;\nmod reinforcement;\nmod runners;\nmod service;\npub use api::*;\npub use core::*;\npub use data::*;\npub use reinforcement::*;\npub use runners::*;\npub trait TrainableModel {\n    fn context_id(&self) -> ContextId;\n    fn parameters(&self) -> Vec\u003c&Parameter>;\n}\npub trait SupervisedModel: TrainableModel {\n    fn forward_loss(&mut self, input: &Variable, target: &Tensor)\n    -> MlResult\u003c(Variable, Variable)>;\n}\npub trait UnsupervisedModel: TrainableModel {\n    fn forward_loss(&mut self, input: &Variable) -> MlResult\u003c(Variable, Variable)>;\n}\npub trait SemiSupervisedModel: TrainableModel {\n    fn forward_loss(\n        &mut self,\n        labeled_input: &Variable,\n        labeled_target: &Tensor,\n        unlabeled_input: &Variable,\n        lambda: f32,\n    ) -> MlResult\u003c(Variable, Variable)>;\n}\npub trait AutoregressiveModel: TrainableModel {\n    fn forward_loss(&mut self, input: &Variable) -> MlResult\u003c(Variable, Variable, usize)>;\n}\npub struct Trainer {\n    pub(crate) core: TrainerCore,\n}\n\nimpl Trainer {\n    /// 커스텀 빌더를 반환.\n    pub fn builder() -> TrainerBuilder {\n        TrainerBuilder::new()\n    }\n\n    pub fn with_observer(self, observer: Box\u003cdyn TrainingObserver>) -> Self {\n        self.core.add_observer(observer);\n        self\n    }\n\n    // ── 프리셋 ────────────────────────────────────────────────────────────\n\n    /// 최대 성능 모드. 모든 로그·NaN 검사가 비활성화.\n    ///\n    /// # 주의\n    /// NaN 검사가 꺼져 있으므로 발산이 발생해도 감지되지 않음.\n    /// 완전히 검증된 모델과 학습률 조합에서만 사용.\n    pub fn silent() -> Self {\n        Self::builder()"},{"path":"src/trainer/progress.rs","bytes":6704,"preview":"#[allow(unused_imports)]\nuse super::*;\n\n// progress 전용 import\nuse std::time::Duration;\n\nuse indicatif::{MultiProgress, ProgressBar, ProgressDrawTarget, ProgressStyle};\n\n// ────────────────────────────────────────────────────────────────────────────\n// EpochProgress — 에폭 레벨 progress bar 래퍼\n// ────────────────────────────────────────────────────────────────────────────\n\n/// 에폭 레벨 progress bar 를 관리.\n///\n/// `show = false`일 때 hidden ProgressBar를 반환하므로 호출부에 분기 없음.\npub(crate) struct EpochProgress {\n    multi: MultiProgress,\n    epoch_bar: ProgressBar,\n    show: bool,\n}\n\nimpl EpochProgress {\n    pub fn new(epochs: usize, show: bool) -> Self {\n        // 연산 trace와 동적 progress 출력은 동일 터미널 행을 공유할 수 없다.\n        // debugging 빌드에서는 구조화 로그를 우선하고 progress만 숨긴다.\n        let show = show && !cfg!(feature = \"debugging\");\n        // 짧은 배치도 에폭 바와 동시에 보이도록 기본 15Hz보다 자주 그린다.\n        let multi = MultiProgress::with_draw_target(ProgressDrawTarget::stderr_with_hz(60));\n        let epoch_bar = if show {\n            let pb = multi.add(ProgressBar::new(epochs as u64));\n            pb.set_style(\n                ProgressStyle::default_bar()\n                    .template(\n                        \"{spinner:.green} [{elapsed_precise}] \\\n                         [ {wide_bar:.cyan/blue} ] {percent:>3}% Epochs ({eta}) | {msg}\",\n                    )\n                    .unwrap_or_else(|_| ProgressStyle::default_bar())\n                    .progress_chars(\"▉ \"),\n            );\n            pb\n        } else {\n            ProgressBar::hidden()\n        };\n        Self {\n            multi,\n            epoch_bar,\n            show,\n        }\n    }\n\n    /// 배치 레벨 progress bar 를 에폭 바 아래에 추가하여 반환.\n    pub fn start_batch_bar(\n        &self,\n        epoch: usize,\n        epochs: usize,\n        n_batches: Option\u003cusize>,\n    ) -> BatchProgress {\n        if !self.show {\n            return BatchProgress {\n                bar: ProgressBar::hidden(),\n                active: false,\n            };\n        }\n        // MultiProgress는 각 bar가 한 번 draw되어야 해당 행을 합성한다.\n        // 첫 에폭부터 에폭/배치 두 행이 함께 보이도록 에폭 상태를 먼저 등록한다."},{"path":"src/trainer/reinforcement.rs","bytes":14989,"preview":"use super::service::{StepData, TrainingService, validate_parameters};\nuse super::*;\nuse crate::{RequiresGrad, optimizer::Optimizer};\nuse std::time::Instant;\npub struct StepResult {\n    pub next_observation: TensorBuffer,\n    pub reward: f32,\n    pub done: bool,\n}\n\npub trait Environment {\n    fn reset(&mut self) -> MlResult\u003cTensorBuffer>;\n    fn step(&mut self, action: usize) -> MlResult\u003cStepResult>;\n    fn num_actions(&self) -> usize;\n    fn observation_shape(&self) -> Vec\u003cusize>;\n}\n\npub trait RLModel: TrainableModel {\n    fn policy_logits(&mut self, observation: &Variable) -> MlResult\u003cVariable>;\n    fn predict_policy_raw(&mut self, observation: &Tensor) -> MlResult\u003cTensorBuffer> {\n        let context = observation.execution_context()?;\n        if self.context_id() != context.id() {\n            return Err(ContextError::Mismatch.into());\n        }\n        context.no_grad(|| {\n            let output = self.policy_logits(&observation.as_variable()?)?;\n            context.validate(output.tensor())?;\n            output.tensor().snapshot()\n        })\n    }\n}\n\npub struct RLTrainer {\n    service: TrainingService,\n    gamma: f32,\n    use_baseline: bool,\n}\nimpl RLTrainer {\n    pub fn new(ctx: &ExecutionContext) -> Self {\n        Self::silent(ctx)\n    }\n    pub fn from_trainer(ctx: &ExecutionContext, trainer: Trainer) -> Self {\n        Self {\n            service: TrainingService::new(ctx, trainer),\n            gamma: 0.99,\n            use_baseline: true,\n        }\n    }\n    pub fn silent(ctx: &ExecutionContext) -> Self {\n        Self::from_trainer(ctx, Trainer::silent())\n    }\n    pub fn minimal(ctx: &ExecutionContext) -> Self {\n        Self::from_trainer(ctx, Trainer::minimal())\n    }\n    pub fn default(ctx: &ExecutionContext) -> Self {\n        Self::from_trainer(ctx, Trainer::default())\n    }\n    pub fn verbose(ctx: &ExecutionContext) -> Self {\n        Self::from_trainer(ctx, Trainer::verbose())\n    }\n    pub fn with_gamma(mut self, gamma: f32) -> MlResult\u003cSelf> {\n        if !gamma.is_finite() || !(0.0..=1.0).contains(&gamma) {\n            return Err(MlError::StringError(\"invalid gamma\".into()));\n        }\n        self.gamma = gamma;"},{"path":"src/trainer/runners.rs","bytes":19027,"preview":"use super::service::{StepData, TrainingService, sample_count};\nuse super::*;\nuse crate::optimizer::Optimizer;\npub struct SupervisedTrainer {\n    service: TrainingService,\n}\nimpl SupervisedTrainer {\n    pub fn new(context: &ExecutionContext) -> Self {\n        Self::silent(context)\n    }\n    pub fn from_trainer(context: &ExecutionContext, trainer: Trainer) -> Self {\n        Self {\n            service: TrainingService::new(context, trainer),\n        }\n    }\n    pub fn silent(context: &ExecutionContext) -> Self {\n        Self::from_trainer(context, Trainer::silent())\n    }\n    pub fn minimal(context: &ExecutionContext) -> Self {\n        Self::from_trainer(context, Trainer::minimal())\n    }\n    pub fn default(context: &ExecutionContext) -> Self {\n        Self::from_trainer(context, Trainer::default())\n    }\n    pub fn verbose(context: &ExecutionContext) -> Self {\n        Self::from_trainer(context, Trainer::verbose())\n    }\n    pub fn with_seed(mut self, seed: u64) -> Self {\n        self.service.core.config.seed = seed;\n        self.service.core.runtime.reseed(seed);\n        self\n    }\n    pub fn with_hook(self, hook: Box\u003cdyn MetricHook>) -> Self {\n        self.service.core.add_hook(hook);\n        self\n    }\n    pub fn with_observer(self, observer: Box\u003cdyn TrainingObserver>) -> Self {\n        self.service.core.add_observer(observer);\n        self\n    }\n    pub fn check_finite_gradients(mut self, enabled: bool) -> Self {\n        self.service.core.config.nan_check_interval = if enabled { 1 } else { usize::MAX };\n        self\n    }\n    pub fn with_max_grad_norm(mut self, max: f32) -> MlResult\u003cSelf> {\n        if !max.is_finite() || max \u003c= 0.0 {\n            return Err(MlError::StringError(\n                \"max_grad_norm must be finite and positive\".into(),\n            ));\n        }\n        self.service.max_grad_norm = Some(max);\n        Ok(self)\n    }\n    pub fn fit\u003cM: SupervisedModel, I: IntoBatchLoader\u003cBatch = SupervisedBatch>>(\n        &self,\n        model: &mut M,\n        optimizer: &mut dyn Optimizer,\n        input: I,\n        schedule: EpochSchedule,\n    ) -> MlResult\u003cTrainResult> {\n        self.service.fit(\n            model,\n            optimizer,\n            input,\n            schedule,"},{"path":"src/trainer/semi_supervised.rs","bytes":23956,"preview":"//! 반지도학습 아키텍처용 트레이너 네임스페이스.\n//!\n//! 반지도 학습은 **소량의 labeled 데이터** 와 **대량의 unlabeled 데이터** 를\n//! 함께 활용한다. 대표적인 전략:\n//!\n//! | 방법              | 아이디어                                                       |\n//! |-------------------|---------------------------------------------------------------|\n//! | Pi-model          | 동일 입력의 두 stochastic forward pass 간 출력이 일치하도록 제약 |\n//! | Mean Teacher      | student 모델이 EMA teacher 의 출력에 수렴하도록 일관성 손실      |\n//! | FixMatch          | 약증강 pseudo-label 로 강증강 출력에 지도                       |\n//! | VAT               | 입력에 adversarial perturbation 을 준 출력과의 일관성            |\n//! | Self-training     | confidence 가 높은 unlabeled 예측을 pseudo-label 로 재귀 학습    |\n//!\n//! P4 에서는 가장 기본적인 패턴만 추상화하고, 모델이 손실 결합 방식을 직접\n//! 결정한다. 이렇게 하면 위 변종들을 모두 동일 트레이너로 구동할 수 있다.\n\nuse super::*;\n\n// ────────────────────────────────────────────────────────────────────────────\n// SemiSupervisedModel — 반지도 학습 대상 모델의 인터페이스\n// ────────────────────────────────────────────────────────────────────────────\n\n/// 반지도학습 트레이너가 학습할 수 있는 모델이 구현해야 하는 인터페이스.\n///\n/// 트레이너는 매 배치마다 labeled 쌍 `(x_l, t_l)` 과 unlabeled 입력 `x_u`,\n/// 그리고 현재 에폭에서 결정된 **일관성 가중치 `lambda`** 를 전달한다.\n/// 모델은 이를 사용해 자신만의 손실 결합 전략을 구현한다:\n///\n/// ```text\n/// loss_total = loss_supervised(x_l, t_l) + lambda · loss_consistency(x_u)\n/// ```\n///\n/// 이 분담 방식은 Pi-model, Mean Teacher, VAT, FixMatch 등 대부분의\n/// consistency-based 반지도 기법을 단일 트레이너 인터페이스로 포용한다.\n///\n/// # 구현 예시 (Pi-model 스타일)\n/// ```ignore\n/// impl SemiSupervisedModel for ToyClassifier {\n///     fn forward_loss(\n///         &mut self,\n///         x_l: &Variable, t_l: &Variable,\n///         x_u: &Variable, lambda: f32,\n///     ) -> MlResult\u003c(Variable, Variable)> {\n///         let y_l   = self.apply(x_l)?;\n///         let l_sup = self.ce.apply_with_label(&[&y_l, t_l], \"sup\")?;\n///\n///         // Pi-model: 동일 입력에 서로 다른 stochastic noise 를 주어 두 번 순전파\n///         let x_u1  = add_noise(x_u, 0.1)?;\n///         let x_u2  = add_noise(x_u, 0.1)?;\n///         let y_u1  = self.apply(&x_u1)?;\n///         let y_u2  = self.apply(&x_u2)?;\n///         let l_con = self.mse.apply_with_label(&[&y_u1, &y_u2], \"con\")?;\n///\n///         // total = sup + lambda · consistency\n///         let total = combine(&l_sup, &l_con, lambda)?;\n///         Ok((y_l, total))\n///     }\n///     // ...\n/// }\n/// ```\n#[cfg(feature = \"enableBackward\")]\npub trait SemiSupervisedModel: TrainableModel {\n    /// 지도 + 비지도(일관성) 손실을 합쳐 `(예측값, 총 손실)` 을 반환.\n    ///\n    /// - `x_labeled`, `t_labeled` : labeled 배치와 그 타깃."},{"path":"src/trainer/service.rs","bytes":17361,"preview":"use super::*;\nuse crate::optimizer::{Optimizer, clip_context_grad_norm};\nuse std::{\n    collections::HashSet,\n    path::Path,\n    time::{Duration, Instant},\n};\n\npub(super) struct StepData {\n    pub loss: Variable,\n    pub prediction: Option\u003cVariable>,\n    pub target: Option\u003cTensor>,\n    pub weight: usize,\n    pub tokens: Option\u003cusize>,\n    pub lambda: Option\u003cf32>,\n}\npub(super) struct StepOutcome {\n    pub loss: f32,\n    pub weight: usize,\n    pub metrics: MetricValues,\n\n    pub snapshot: Option\u003ccrate::visualization::GraphSnapshot>,\n}\npub(super) trait BatchInputs {\n    fn tensors(&self) -> Vec\u003c&Tensor>;\n}\nimpl BatchInputs for SupervisedBatch {\n    fn tensors(&self) -> Vec\u003c&Tensor> {\n        vec![self.inputs.tensor(), &self.targets]\n    }\n}\nimpl BatchInputs for UnsupervisedBatch {\n    fn tensors(&self) -> Vec\u003c&Tensor> {\n        vec![self.samples.tensor()]\n    }\n}\nimpl BatchInputs for AutoregressiveBatch {\n    fn tensors(&self) -> Vec\u003c&Tensor> {\n        vec![self.sequences.tensor()]\n    }\n}\nimpl BatchInputs for SemiSupervisedBatch {\n    fn tensors(&self) -> Vec\u003c&Tensor> {\n        vec![\n            self.labeled_inputs.tensor(),\n            &self.labeled_targets,\n            self.unlabeled_inputs.tensor(),\n        ]\n    }\n}\n\npub(super) fn validate_parameters\u003cM: TrainableModel + ?Sized>(\n    ctx: &ExecutionContext,\n    model: &M,\n    optimizer: &dyn Optimizer,\n) -> MlResult\u003c()> {\n    if model.context_id() != ctx.id() || optimizer.context_id() != ctx.id() {\n        return Err(ContextError::Mismatch.into());\n    }\n    let expected = model.parameters();\n    let actual = optimizer.registered_parameters();\n    for p in expected.iter().chain(&actual) {\n        ctx.validate(p.tensor())?;\n    }\n    let expected = expected.iter().map(|p| p.id()).collect::\u003cHashSet\u003c_>>();"},{"path":"src/trainer/supervised.rs","bytes":21505,"preview":"//! 지도학습 아키텍처용 트레이너 네임스페이스.\n//!\n//! 이 모듈은 4-분법(지도·비지도·반지도·강화학습) 중 **지도학습** 을 담당한다.\n//! `SupervisedTrainer` 는 자체 `fit_inner` 루프를 가지며 `(x, t)` 쌍과\n//! 정확도 메트릭을 처리한다. `TrainerCore` 인프라(로그·훅·진행률)는\n//! 다른 패러다임과 공유한다.\n//!\n//! # 이름\n//!\n//! - `SupervisedModel`   — 지도학습 가능한 모델이 구현하는 트레잇.\n//! - `SupervisedTrainer` — `(x, t)` 지도학습 루프를 실행하는 트레이너.\n\nuse super::*;\n\n// ────────────────────────────────────────────────────────────────────────────\n// SupervisedModel — 지도학습 대상 모델의 인터페이스\n// ────────────────────────────────────────────────────────────────────────────\n\n/// 지도학습 트레이너가 학습할 수 있는 모델이 구현해야 하는 인터페이스.\n///\n/// `(x, t)` 쌍을 입력으로 받아 손실을 계산하는 **지도학습** 루프 전용이다.\n/// 비지도·반지도·강화학습 모델은 각 아키텍처별 모듈의 대응 트레잇을 사용한다.\n///\n/// # 구현 예시\n/// ```ignore\n/// impl SupervisedModel for SoftmaxRegression {\n///     fn forward_loss(&mut self, x: &Variable, t: &Variable) -> MlResult\u003c(Variable, Variable)> {\n///         let y = self.apply(x)?;\n///         let loss = self.loss_fn.apply_with_label(&[&y, t], \"loss\")?;\n///         Ok((y, loss))\n///     }\n///     fn params(&self) -> Vec\u003c&dyn Parameter> { vec![&self.w1, &self.b1] }\n///     fn predict_raw(&mut self, x: &dyn TensorBase) -> MlResult\u003cGlobalTensor\u003cf32>> {\n///         self.predict(x)\n///     }\n/// }\n/// ```\n#[cfg(feature = \"enableBackward\")]\npub trait SupervisedModel: TrainableModel {\n    /// 순전파와 손실 계산을 수행하여 `(예측값, 손실값)` 을 반환한다.\n    ///\n    /// - `x`: 입력 배치\n    /// - `t`: 정답(타깃) 배치\n    fn forward_loss(\n        &mut self,\n        x: &crate::legacy::nn::Variable,\n        t: &crate::legacy::nn::Variable,\n    ) -> MlResult\u003c(crate::legacy::nn::Variable, crate::legacy::nn::Variable)>;\n\n    /// No-grad 순전파. 초기 손실 표시 및 평가에 사용한다.\n    fn predict_raw(&mut self, x: &dyn TensorBase) -> MlResult\u003ccrate::legacy::tensor::GlobalTensor\u003cf32>>;\n}\n\n// ────────────────────────────────────────────────────────────────────────────\n// SupervisedTrainer\n// ────────────────────────────────────────────────────────────────────────────\n\n/// 지도학습 전용 트레이너.\n///\n/// `(x, t)` 쌍을 받아 `forward_loss(x, t)` → backprop → optimizer.step 루프를\n/// 실행하며, `ClassificationAccuracy` 를 기본 에폭 요약에 포함한다.\n/// `TrainerCore` 를 래핑하므로 로그·훅·체크포인트 인프라는 다른 패러다임과 공유한다.\n///\n/// # 생성\n/// ```ignore"},{"path":"src/trainer/unsupervised.rs","bytes":20036,"preview":"//! 비지도학습 아키텍처용 트레이너 네임스페이스.\n//!\n//! 지도학습 트레이너가 `(x, t)` 쌍을 가정하는 반면, 비지도 트레이너는\n//! 입력 `x` 만 받고 모델 내부에서 손실을 자기생성(self-supervised)한다.\n//!\n//! ## 대상 모델\n//!\n//! | 패러다임       | forward_loss 입력 | 손실 자기생성 방식               |\n//! |----------------|-------------------|----------------------------------|\n//! | Autoencoder    | x                 | 재구성 오차 `‖x − x̂‖²`           |\n//! | VAE            | x                 | ELBO = recon + KL                |\n//! | DDPM/Diffusion | x₀                | `‖ε − ε_θ(x_t, t)‖²` (내부 노이즈) |\n//! | Contrastive    | x                 | 증강쌍 간 InfoNCE                |\n//!\n//! ## 재사용 전략\n//!\n//! `Trainer` 의 학습 루프와 대부분이 동일하므로 `TrainerCore` 인프라를 공유한다.\n//! `UnsupervisedTrainer` 는 자체 `fit_inner` 를 가지며 정확도 메트릭과\n//! 타깃 관련 처리를 제거한다. P3 단계에서는 의도적으로 중복을 허용하고,\n//! 이후 공통 루프를 `TrainerCore::run_epoch` 로 끌어올릴 여지를 남겨둔다.\n\nuse super::*;\n\n// ────────────────────────────────────────────────────────────────────────────\n// UnsupervisedModel — 비지도학습 대상 모델의 인터페이스\n// ────────────────────────────────────────────────────────────────────────────\n\n/// 비지도학습 트레이너가 학습할 수 있는 모델이 구현해야 하는 인터페이스.\n///\n/// `forward_loss` 는 입력 `x` **하나만** 받으며, 손실에 필요한 타깃은\n/// 모델 내부에서 생성한다 (예: DDPM 은 랜덤 노이즈, AE 는 입력 자체).\n///\n/// # 구현 예시 (DDPM)\n/// ```ignore\n/// impl UnsupervisedModel for Diffusion {\n///     fn forward_loss(&mut self, x: &Variable) -> MlResult\u003c(Variable, Variable)> {\n///         // 내부에서 랜덤 노이즈/타임스텝을 샘플링하고 MSE loss 를 계산\n///         self.forward_loss_diffusion(x)\n///     }\n///     fn params(&self) -> Vec\u003c&dyn Parameter> { self.unet.params() }\n///     fn predict_raw(&mut self, x: &dyn TensorBase) -> MlResult\u003cGlobalTensor\u003cf32>> {\n///         self.unet.predict(x)\n///     }\n/// }\n/// ```\n#[cfg(feature = \"enableBackward\")]\npub trait UnsupervisedModel: TrainableModel {\n    /// 순전파와 손실 계산을 수행하여 `(예측값, 손실값)` 을 반환한다.\n    ///\n    /// 지도학습과 달리 타깃 `t` 인자는 없다. 필요한 타깃은 모델 내부에서\n    /// 스토캐스틱하게 생성되거나 (`Diffusion::noise`) 입력 자체로부터 유도된다\n    /// (`Autoencoder::reconstruct`).\n    ///\n    /// - `x`     : 학습 입력 배치\n    /// - 반환 `y`: 모델 출력. 훅이 관찰할 수 있도록 노출하지만, 루프 자체는 사용하지 않음.\n    /// - 반환 `loss`: 스칼라 손실 Variable.\n    fn forward_loss(\n        &mut self,\n        x: &crate::legacy::nn::Variable,\n    ) -> MlResult\u003c(crate::legacy::nn::Variable, crate::legacy::nn::Variable)>;\n\n    /// No-grad 순전파. 초기 손실 표시 및 평가에 사용한다.\n    fn predict_raw(&mut self, x: &dyn TensorBase) -> MlResult\u003ccrate::legacy::tensor::GlobalTensor\u003cf32>>;\n}\n"},{"path":"src/visualization/capture.rs","bytes":2995,"preview":"use super::{CaptureContext, CaptureProfile, GraphSnapshot, VisualizationError, recording};\nuse std::cell::RefCell;\n\n#[derive(Debug)]\nenum CaptureState {\n    Disabled,\n    Capturing(recording::SessionState),\n}\n\nthread_local! {\n    static STATE: RefCell\u003cCaptureState> = const { RefCell::new(CaptureState::Disabled) };\n}\n\npub struct VisualizationCaptureBuilder {\n    profile: CaptureProfile,\n    context: CaptureContext,\n}\n\nimpl VisualizationCaptureBuilder {\n    pub fn context(mut self, context: CaptureContext) -> Self {\n        self.context = context;\n        self\n    }\n\n    pub fn begin(self) -> Result\u003cVisualizationCapture, VisualizationError> {\n        begin_session(self.profile, self.context)?;\n        let capture = VisualizationCapture { finished: false };\n        crate::legacy::tensor::ComputationGraph::reset_graph();\n        Ok(capture)\n    }\n}\n\npub struct VisualizationCapture {\n    finished: bool,\n}\n\nimpl VisualizationCapture {\n    pub fn builder(profile: CaptureProfile) -> VisualizationCaptureBuilder {\n        VisualizationCaptureBuilder {\n            profile,\n            context: CaptureContext::default(),\n        }\n    }\n\n    pub fn finish(mut self) -> Result\u003cGraphSnapshot, VisualizationError> {\n        let session = session()?;\n        let snapshot = super::snapshot::build_snapshot(&session);\n        crate::legacy::tensor::ComputationGraph::reset_graph();\n        disable();\n        self.finished = true;\n        snapshot\n    }\n}\n\nimpl Drop for VisualizationCapture {\n    fn drop(&mut self) {\n        if !self.finished {\n            crate::legacy::tensor::ComputationGraph::reset_graph();\n            disable();\n        }\n    }\n}\n\nfn begin_session(\n    profile: CaptureProfile,"},{"path":"src/visualization/dot.rs","bytes":7415,"preview":"use super::{GraphEdgeKind, GraphSnapshot, NodeRole};\nuse std::collections::HashSet;\n\nconst LARGE_GRAPH_THRESHOLD: usize = 180;\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub enum DotProfile {\n    Auto,\n    Overview,\n    Detailed,\n}\n\npub struct DotEncoder;\n\nimpl DotEncoder {\n    pub fn encode(snapshot: &GraphSnapshot) -> String {\n        Self::encode_with_profile(snapshot, DotProfile::Auto)\n    }\n\n    pub fn encode_with_profile(snapshot: &GraphSnapshot, profile: DotProfile) -> String {\n        let overview = match profile {\n            DotProfile::Auto => snapshot.nodes.len() >= LARGE_GRAPH_THRESHOLD,\n            DotProfile::Overview => true,\n            DotProfile::Detailed => false,\n        };\n        let hidden: HashSet\u003cu64> = if overview {\n            snapshot\n                .nodes\n                .iter()\n                .filter(|node| {\n                    matches!(\n                        node.role,\n                        NodeRole::Saved | NodeRole::Weight | NodeRole::Bias\n                    )\n                })\n                .map(|node| node.id)\n                .collect()\n        } else {\n            HashSet::new()\n        };\n        let mut dot = String::from(\"digraph ComputationGraph {\\n\");\n        if overview {\n            dot.push_str(\"  graph [bgcolor=\\\"#FAFBFC\\\", rankdir=TB, splines=polyline, concentrate=true, newrank=true, nodesep=0.12, ranksep=0.35, pad=0.2, ratio=compress, size=\\\"16,100\\\"];\\n\");\n            dot.push_str(\"  node [fontname=\\\"Segoe UI\\\", fontsize=9, style=\\\"rounded,filled\\\", fontcolor=white, margin=\\\"0.06,0.04\\\", height=0.25];\\n\");\n            dot.push_str(\"  edge [color=\\\"#94A3B8\\\", penwidth=1.0, arrowsize=0.55];\\n\");\n        } else {\n            dot.push_str(\"  graph [bgcolor=\\\"#FAFBFC\\\", rankdir=LR, splines=curved, nodesep=0.25, ranksep=0.5, pad=0.2];\\n\");\n            dot.push_str(\n                \"  node [fontname=\\\"Segoe UI\\\", fontsize=10, style=filled, fontcolor=white];\\n\",\n            );\n            dot.push_str(\"  edge [color=\\\"#6B7280\\\", penwidth=1.5, arrowsize=0.7];\\n\");\n        }\n\n        for node in snapshot\n            .nodes\n            .iter()\n            .filter(|node| !hidden.contains(&node.id))\n        {\n            let (shape, color) = node_style(&node.role, overview);\n            let label = node_label(node, overview);\n            dot.push_str(&format!(\n                \"  \\\"node-{}\\\" [id=\\\"node-{}\\\", label=\\\"{}\\\", shape={}, fillcolor=\\\"{}\\\"];\\n\",\n                node.id,\n                node.id,\n                escape(&label),"},{"path":"src/visualization/error.rs","bytes":1097,"preview":"use std::path::PathBuf;\n\n#[derive(Debug, thiserror::Error)]\npub enum VisualizationError {\n    #[error(\"a visualization capture is already active on this thread\")]\n    CaptureAlreadyActive,\n    #[error(\"visualization capture is not active\")]\n    CaptureNotActive,\n    #[error(\"GraphVisualizationObserver requires a SnapshotWriter\")]\n    MissingWriter,\n    #[error(\"visualization capture coordinates are 1-based\")]\n    InvalidCaptureCoordinate,\n    #[error(\"invalid visualization artifact stem: {0}\")]\n    InvalidArtifactStem(String),\n    #[error(\"visualization I/O failed for {path}: {source}\")]\n    Io {\n        path: PathBuf,\n        #[source]\n        source: std::io::Error,\n    },\n    #[error(\"visualization JSON serialization failed: {0}\")]\n    Serialization(#[from] serde_json::Error),\n    #[error(\"computation graph lock is poisoned\")]\n    GraphLockPoisoned,\n}\n\nimpl VisualizationError {\n    pub(crate) fn io(path: impl Into\u003cPathBuf>, source: std::io::Error) -> Self {\n        Self::Io {\n            path: path.into(),\n            source,\n        }\n    }\n}\n"},{"path":"src/visualization/graphviz.rs","bytes":2790,"preview":"use std::{\n    io::Write,\n    path::Path,\n    process::{Command, Stdio},\n};\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub enum GraphvizFailureKind {\n    NotInstalled,\n    Io,\n    NonZeroExit,\n}\n\n#[derive(Debug)]\npub struct GraphvizFailure {\n    pub kind: GraphvizFailureKind,\n    pub message: String,\n}\n\npub(crate) fn render(program: &Path, dot: &str, output: &Path) -> Result\u003c(), GraphvizFailure> {\n    let mut child = Command::new(program)\n        .arg(\"-Tsvg\")\n        .arg(\"-o\")\n        .arg(output)\n        .stdin(Stdio::piped())\n        .stderr(Stdio::piped())\n        .spawn()\n        .map_err(|error| GraphvizFailure {\n            kind: if error.kind() == std::io::ErrorKind::NotFound {\n                GraphvizFailureKind::NotInstalled\n            } else {\n                GraphvizFailureKind::Io\n            },\n            message: format!(\"failed to start Graphviz '{}': {error}\", program.display()),\n        })?;\n    child\n        .stdin\n        .take()\n        .ok_or_else(|| GraphvizFailure {\n            kind: GraphvizFailureKind::Io,\n            message: \"Graphviz stdin was unavailable\".to_string(),\n        })?\n        .write_all(dot.as_bytes())\n        .map_err(|error| GraphvizFailure {\n            kind: GraphvizFailureKind::Io,\n            message: format!(\"failed to write DOT to Graphviz: {error}\"),\n        })?;\n    let output = child.wait_with_output().map_err(|error| GraphvizFailure {\n        kind: GraphvizFailureKind::Io,\n        message: format!(\"Graphviz wait failed: {error}\"),\n    })?;\n    if output.status.success() {\n        Ok(())\n    } else {\n        Err(GraphvizFailure {\n            kind: GraphvizFailureKind::NonZeroExit,\n            message: format!(\n                \"Graphviz failed: {}\",\n                String::from_utf8_lossy(&output.stderr)\n            ),\n        })\n    }\n}\n\n#[cfg(test)]"},{"path":"src/visualization/legacy_dot.rs","bytes":7950,"preview":"use super::{GraphEdgeKind, GraphSnapshot, NodeRole};\nuse std::collections::HashSet;\n\nconst LARGE_GRAPH_THRESHOLD: usize = 180;\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub enum DotProfile {\n    Auto,\n    Overview,\n    Detailed,\n}\n\npub struct DotEncoder;\n\nimpl DotEncoder {\n    pub fn encode(snapshot: &GraphSnapshot) -> String {\n        Self::encode_with_profile(snapshot, DotProfile::Auto)\n    }\n\n    pub fn encode_with_profile(snapshot: &GraphSnapshot, profile: DotProfile) -> String {\n        let overview = match profile {\n            DotProfile::Auto => snapshot.nodes.len() >= LARGE_GRAPH_THRESHOLD,\n            DotProfile::Overview => true,\n            DotProfile::Detailed => false,\n        };\n        let hidden: HashSet\u003cu64> = if overview {\n            snapshot\n                .nodes\n                .iter()\n                .filter(|node| {\n                    matches!(\n                        node.role,\n                        NodeRole::Saved | NodeRole::Weight | NodeRole::Bias\n                    ) || (node.role == NodeRole::Input && node.elements \u003c= 1)\n                })\n                .map(|node| node.id)\n                .collect()\n        } else {\n            HashSet::new()\n        };\n        let mut dot = String::from(\"digraph ComputationGraph {\\n\");\n        if overview {\n            dot.push_str(\"  graph [bgcolor=\\\"#FAFBFC\\\", rankdir=TB, splines=polyline, concentrate=true, newrank=true, nodesep=0.12, ranksep=0.35, pad=0.2, ratio=compress, size=\\\"16,100\\\"];\\n\");\n            dot.push_str(\"  node [fontname=\\\"Segoe UI\\\", fontsize=9, style=\\\"rounded,filled\\\", fontcolor=white, margin=\\\"0.06,0.04\\\", height=0.25];\\n\");\n            dot.push_str(\"  edge [color=\\\"#94A3B8\\\", penwidth=1.0, arrowsize=0.55];\\n\");\n        } else {\n            dot.push_str(\"  graph [bgcolor=\\\"#FAFBFC\\\", rankdir=LR, splines=curved, nodesep=0.25, ranksep=0.5, pad=0.2];\\n\");\n            dot.push_str(\n                \"  node [fontname=\\\"Segoe UI\\\", fontsize=10, style=filled, fontcolor=white];\\n\",\n            );\n            dot.push_str(\"  edge [color=\\\"#6B7280\\\", penwidth=1.5, arrowsize=0.7];\\n\");\n        }\n\n        for node in snapshot\n            .nodes\n            .iter()\n            .filter(|node| !hidden.contains(&node.id))\n        {\n            let (shape, color) = node_style(&node.role, overview);\n            let label = node_label(node, overview);\n            dot.push_str(&format!(\n                \"  \\\"node-{}\\\" [id=\\\"node-{}\\\", label=\\\"{}\\\", shape={}, fillcolor=\\\"{}\\\"];\\n\",\n                node.id,\n                node.id,\n                escape(&label),"},{"path":"src/visualization/legacy_mod.rs","bytes":7269,"preview":"//! Explicit, low-overhead computation graph capture and rendering.\n\n#[path = \"capture.rs\"]\nmod capture;\n#[path = \"legacy_dot.rs\"]\nmod dot;\n#[path = \"error.rs\"]\nmod error;\n#[path = \"graphviz.rs\"]\nmod graphviz;\n#[path = \"recording.rs\"]\npub(crate) mod recording;\n#[path = \"legacy_snapshot.rs\"]\nmod snapshot;\n#[path = \"statistics.rs\"]\nmod statistics;\n#[path = \"writer.rs\"]\nmod writer;\n\npub use capture::{VisualizationCapture, VisualizationCaptureBuilder};\npub use dot::{DotEncoder, DotProfile};\npub use error::VisualizationError;\npub use graphviz::{GraphvizFailure, GraphvizFailureKind};\npub use snapshot::{\n    CaptureContext, CaptureProfile, GRAPH_SNAPSHOT_SCHEMA_VERSION, GraphAttributeValue,\n    GraphEdgeKind, GraphEdgeSnapshot, GraphNodeSnapshot, GraphSnapshot, NodeRole, TensorStatistics,\n};\npub use writer::{\n    FileSnapshotWriter, FileSnapshotWriterBuilder, SnapshotWriter, WriteReport, WriteWarning,\n};\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    use crate::legacy::{\n        nn::{Parameter, Variable},\n        tensor::{\n            AutogradFunction, ComputationGraph, Tensor, TensorBase,\n            operators::{Add, Function},\n        },\n    };\n\n    fn captured_add(profile: CaptureProfile, backward: bool) -> crate::legacy::MlResult\u003cGraphSnapshot> {\n        let capture = VisualizationCapture::builder(profile)\n            .context(CaptureContext {\n                paradigm: Some(\"test\".into()),\n                epoch: Some(1),\n                batch: Some(1),\n                episode: None,\n            })\n            .begin()?;\n        let x = Variable::new(Tensor::from_vec(vec![1.0, 2.0], &[2])?);\n        x.retain_grad();\n        let y = Variable::new(Tensor::from_vec(vec![3.0, 4.0], &[2])?);\n        let output = Add::new()?.apply(&[&x, &y])?;\n        if backward {\n            output.backward()?;\n        }\n        Ok(capture.finish()?)\n    }\n\n    #[test]\n    fn inactive_recording_skips_sidecar_and_statistics() -> crate::legacy::MlResult\u003c()> {\n        ComputationGraph::reset_graph();\n        statistics::reset_collection_count();"},{"path":"src/visualization/legacy_snapshot.rs","bytes":5346,"preview":"use super::{VisualizationError, recording::SessionState, statistics::tensor_statistics};\nuse crate::legacy::tensor::{COMPUTATION_GRAPH, TensorBase};\nuse serde::Serialize;\nuse std::collections::BTreeMap;\n\npub const GRAPH_SNAPSHOT_SCHEMA_VERSION: u32 = 1;\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Serialize)]\n#[serde(rename_all = \"snake_case\")]\npub enum CaptureProfile {\n    Structure,\n    Analysis,\n}\n\n#[derive(Debug, Clone, Default, Serialize)]\npub struct CaptureContext {\n    pub paradigm: Option\u003cString>,\n    pub epoch: Option\u003cusize>,\n    pub batch: Option\u003cusize>,\n    pub episode: Option\u003cusize>,\n}\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize)]\n#[serde(rename_all = \"snake_case\")]\npub enum NodeRole {\n    Variable,\n    Function,\n    Saved,\n    Input,\n    Weight,\n    Bias,\n    Loss,\n    Activation,\n    Output,\n}\n\n#[derive(Debug, Clone, Serialize)]\npub struct TensorStatistics {\n    pub min: Option\u003cf32>,\n    pub max: Option\u003cf32>,\n    pub mean: Option\u003cf32>,\n    pub std_dev: Option\u003cf32>,\n    pub l1_norm: Option\u003cf32>,\n    pub l2_norm: Option\u003cf32>,\n    pub zeros: usize,\n    pub nan: usize,\n    pub positive_infinity: usize,\n    pub negative_infinity: usize,\n}\n\n#[derive(Debug, Clone, Serialize)]\n#[serde(untagged)]\npub enum GraphAttributeValue {\n    Boolean(bool),\n    Integer(i64),\n    Float(f64),\n    String(String),\n    Integers(Vec\u003ci64>),\n    Floats(Vec\u003cf64>),\n    Strings(Vec\u003cString>),\n}\n\n#[derive(Debug, Clone, Serialize)]\npub struct GraphNodeSnapshot {\n    pub id: u64,"},{"path":"src/visualization/mod.rs","bytes":409,"preview":"//! Owned snapshots of an explicit execution context.\nmod dot;\nmod error;\nmod graphviz;\nmod snapshot;\npub(crate) mod statistics;\nmod writer;\npub use dot::{DotEncoder, DotProfile};\npub use error::VisualizationError;\npub use graphviz::{GraphvizFailure, GraphvizFailureKind};\npub use snapshot::*;\npub use writer::{\n    FileSnapshotWriter, FileSnapshotWriterBuilder, SnapshotWriter, WriteReport, WriteWarning,\n};\n"},{"path":"src/visualization/recording.rs","bytes":2552,"preview":"use super::{CaptureContext, CaptureProfile, NodeRole, capture};\nuse crate::legacy::tensor::{NodeId, Tensor, TensorBase};\nuse std::collections::HashMap;\n\n#[derive(Debug, Clone)]\npub(crate) struct RecordedMetadata {\n    pub label: String,\n    pub role: NodeRole,\n}\n\n#[derive(Debug, Clone)]\npub(crate) struct SessionState {\n    pub profile: CaptureProfile,\n    pub context: CaptureContext,\n    pub metadata: HashMap\u003cNodeId, RecordedMetadata>,\n    pub(crate) label_counters: HashMap\u003cString, usize>,\n}\n\nimpl SessionState {\n    pub(crate) fn new(profile: CaptureProfile, context: CaptureContext) -> Self {\n        Self {\n            profile,\n            context,\n            metadata: HashMap::new(),\n            label_counters: HashMap::new(),\n        }\n    }\n\n    pub(crate) fn clear(&mut self) {\n        self.metadata.clear();\n        self.label_counters.clear();\n    }\n}\n\n#[inline(always)]\npub(crate) fn is_active() -> bool {\n    capture::is_active()\n}\n\npub(crate) fn clear_temporary() {\n    capture::clear_temporary();\n}\n\npub(crate) fn record_node(\n    id: NodeId,\n    tensor: &Tensor,\n    explicit_label: Option\u003c&str>,\n    explicit_role: Option\u003c&NodeRole>,\n    fallback_role: NodeRole,\n) {\n    if !is_active() {\n        return;\n    }\n    capture::with_session_mut(|session| {\n        if session.metadata.contains_key(&id) {\n            return;\n        }\n        let role = explicit_role.cloned().unwrap_or(fallback_role);\n        let base = explicit_label\n            .map(str::to_owned)\n            .unwrap_or_else(|| inferred_label(tensor.shape(), &role));\n        let count = session.label_counters.entry(base.clone()).or_insert(0);\n        *count += 1;\n        let label = if *count == 1 {\n            base"},{"path":"src/visualization/snapshot.rs","bytes":2410,"preview":"use serde::Serialize;\nuse std::collections::BTreeMap;\n\npub const GRAPH_SNAPSHOT_SCHEMA_VERSION: u32 = 1;\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Serialize)]\n#[serde(rename_all = \"snake_case\")]\npub enum CaptureProfile {\n    Structure,\n    Analysis,\n}\n\n#[derive(Debug, Clone, Default, Serialize)]\npub struct CaptureContext {\n    pub paradigm: Option\u003cString>,\n    pub epoch: Option\u003cusize>,\n    pub batch: Option\u003cusize>,\n    pub episode: Option\u003cusize>,\n}\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize)]\n#[serde(rename_all = \"snake_case\")]\npub enum NodeRole {\n    Variable,\n    Function,\n    Saved,\n    Input,\n    Weight,\n    Bias,\n    Loss,\n    Activation,\n    Output,\n}\n\n#[derive(Debug, Clone, Serialize)]\npub struct TensorStatistics {\n    pub min: Option\u003cf32>,\n    pub max: Option\u003cf32>,\n    pub mean: Option\u003cf32>,\n    pub std_dev: Option\u003cf32>,\n    pub l1_norm: Option\u003cf32>,\n    pub l2_norm: Option\u003cf32>,\n    pub zeros: usize,\n    pub nan: usize,\n    pub positive_infinity: usize,\n    pub negative_infinity: usize,\n}\n\n#[derive(Debug, Clone, Serialize)]\n#[serde(untagged)]\npub enum GraphAttributeValue {\n    Boolean(bool),\n    Integer(i64),\n    Float(f64),\n    String(String),\n    Integers(Vec\u003ci64>),\n    Floats(Vec\u003cf64>),\n    Strings(Vec\u003cString>),\n}\n\n#[derive(Debug, Clone, Serialize)]\npub struct GraphNodeSnapshot {\n    pub id: u64,\n    pub label: String,\n    pub role: NodeRole,"},{"path":"src/visualization/statistics.rs","bytes":2723,"preview":"use super::TensorStatistics;\n\n#[cfg(test)]\nthread_local! {\n    static COLLECTION_COUNT: std::cell::Cell\u003cusize> = const { std::cell::Cell::new(0) };\n}\n\npub(crate) fn tensor_statistics(data: &[f32]) -> Option\u003cTensorStatistics> {\n    #[cfg(test)]\n    COLLECTION_COUNT.with(|count| count.set(count.get() + 1));\n    if data.is_empty() {\n        return None;\n    }\n\n    let mut min = f32::INFINITY;\n    let mut max = f32::NEG_INFINITY;\n    let mut sum = 0.0f64;\n    let mut squared = 0.0f64;\n    let mut l1 = 0.0f64;\n    let mut finite = 0usize;\n    let mut zeros = 0usize;\n    let mut nan = 0usize;\n    let mut positive_infinity = 0usize;\n    let mut negative_infinity = 0usize;\n\n    for &value in data {\n        if value.is_nan() {\n            nan += 1;\n            continue;\n        }\n        if value == f32::INFINITY {\n            positive_infinity += 1;\n            continue;\n        }\n        if value == f32::NEG_INFINITY {\n            negative_infinity += 1;\n            continue;\n        }\n        min = min.min(value);\n        max = max.max(value);\n        let value = value as f64;\n        sum += value;\n        squared += value * value;\n        l1 += value.abs();\n        finite += 1;\n        if value == 0.0 {\n            zeros += 1;\n        }\n    }\n\n    let mean = (finite > 0).then(|| (sum / finite as f64) as f32);\n    let variance =\n        mean.map(|mean| (squared / finite as f64 - (mean as f64).powi(2)).max(0.0) as f32);\n    let finite_value = |value: f64| value.is_finite().then_some(value as f32);\n    Some(TensorStatistics {\n        min: (finite > 0).then_some(min),\n        max: (finite > 0).then_some(max),\n        mean,\n        std_dev: variance.map(f32::sqrt),\n        l1_norm: finite_value(l1),\n        l2_norm: finite_value(squared.sqrt()),\n        zeros,\n        nan,\n        positive_infinity,\n        negative_infinity,"},{"path":"src/visualization/writer.rs","bytes":4224,"preview":"use super::{DotEncoder, DotProfile, GraphSnapshot, VisualizationError, graphviz};\nuse std::path::{Path, PathBuf};\n\n#[derive(Debug, Clone)]\npub struct WriteWarning {\n    pub artifact: PathBuf,\n    pub kind: graphviz::GraphvizFailureKind,\n    pub message: String,\n}\n\n#[derive(Debug, Clone, Default)]\npub struct WriteReport {\n    pub artifacts: Vec\u003cPathBuf>,\n    pub warnings: Vec\u003cWriteWarning>,\n}\n\npub trait SnapshotWriter: Send {\n    fn write(\n        &mut self,\n        snapshot: &GraphSnapshot,\n        stem: &str,\n    ) -> Result\u003cWriteReport, VisualizationError>;\n}\n\npub struct FileSnapshotWriterBuilder {\n    output_directory: PathBuf,\n    render_svg: bool,\n    graphviz_program: PathBuf,\n    dot_profile: DotProfile,\n}\n\nimpl FileSnapshotWriterBuilder {\n    pub fn render_svg(mut self, render: bool) -> Self {\n        self.render_svg = render;\n        self\n    }\n\n    pub fn graphviz_program\u003cP: AsRef\u003cPath>>(mut self, program: P) -> Self {\n        self.graphviz_program = program.as_ref().to_path_buf();\n        self\n    }\n\n    pub fn dot_profile(mut self, profile: DotProfile) -> Self {\n        self.dot_profile = profile;\n        self\n    }\n\n    pub fn build(self) -> Result\u003cFileSnapshotWriter, VisualizationError> {\n        if self.output_directory.as_os_str().is_empty() {\n            return Err(VisualizationError::InvalidArtifactStem(\n                \"empty output directory\".into(),\n            ));\n        }\n        Ok(FileSnapshotWriter {\n            output_directory: self.output_directory,\n            render_svg: self.render_svg,\n            graphviz_program: self.graphviz_program,\n            dot_profile: self.dot_profile,\n        })\n    }\n}\n\npub struct FileSnapshotWriter {\n    output_directory: PathBuf,\n    render_svg: bool,"},{"path":"tests/cleanup.rs","bytes":7310,"preview":"mod support;\nuse support::{ReferenceOps, SlotStore, Tape};\nuse trench_deep::{\n    contracts::*,\n    optimizer::{Optimizer, SGD},\n    trainer::*,\n    *,\n};\n\nfn repeated_batches_release_tensors(ctx: ExecutionContext) -> MlResult\u003c()> {\n    let parameter = ctx.parameter(vec![2.0], &[])?;\n    let baseline = ctx.graph_stats()?;\n    for batch in 0..64 {\n        let fail = batch % 3 == 1;\n        let result = ctx.with_training_scope(|| {\n            let input = ctx.scalar(0.5)?;\n            // Fan-out/fan-in exercises multiple graph references to one value.\n            let branch = parameter.mul(&input)?;\n            let loss = branch.mul(branch.tensor())?;\n            assert!(ctx.graph_stats()?.tensors > baseline.tensors);\n            if fail {\n                return Err(MlError::UnsupportedCapability {\n                    module: \"batch failure fixture\",\n                    capability: \"forward\",\n                    operation: \"batch\",\n                });\n            }\n            loss.backward()?;\n            let gradient = parameter\n                .grad()?\n                .expect(\"successful backward must produce a gradient\");\n            assert_eq!(gradient.data(), &[1.0]);\n            // A returned tensor must survive scope cleanup until its owner drops it.\n            Ok(loss)\n        });\n        if fail {\n            assert!(matches!(\n                result,\n                Err(MlError::UnsupportedCapability {\n                    module: \"batch failure fixture\",\n                    ..\n                })\n            ));\n        } else {\n            let loss = result?;\n            assert_eq!(loss.tensor().item()?, 1.0);\n            let held = ctx.graph_stats()?;\n            assert_eq!(held.graph_nodes, 0);\n            assert_eq!(held.tensors, baseline.tensors + 1);\n            drop(loss);\n        }\n        assert!(parameter.grad()?.is_none());\n        assert_eq!(\n            ctx.graph_stats()?,\n            baseline,\n            \"batch {batch} leaked runtime state\"\n        );\n    }\n    Ok(())\n}\n\n#[test]\nfn repeated_batches_release_external_provider_tensors() -> MlResult\u003c()> {\n    repeated_batches_release_tensors(\n        ExecutionContextBuilder::empty()"},{"path":"tests/counting_allocator.rs","bytes":1542,"preview":"#![cfg(feature = \"benchmarkAlloc\")]\n#[path = \"../benches/support/counting_allocator.rs\"]\nmod allocation;\n\n#[test]\nfn allocator_tracks_zeroed_resize_and_release() {\n    use std::alloc::{GlobalAlloc, Layout};\n    let allocator = allocation::CountingAllocator;\n    let start = allocation::begin();\n    let small = Layout::from_size_align(16, 8).unwrap();\n    // SAFETY: all pointers are obtained from this allocator, accessed within\n    // their allocated lengths, and released exactly once with matching layout.\n    unsafe {\n        let ptr = allocator.alloc_zeroed(small);\n        assert!(!ptr.is_null());\n        assert!(std::slice::from_raw_parts(ptr, 16).iter().all(|&v| v == 0));\n        ptr.write(42);\n        let grown = allocator.realloc(ptr, small, 64);\n        assert!(!grown.is_null());\n        assert_eq!(grown.read(), 42);\n        let mid = allocation::delta(start);\n        assert_eq!(mid.allocations, 1);\n        assert_eq!(mid.reallocations, 1);\n        assert_eq!(mid.requested_bytes, 80);\n        assert_eq!(mid.live_bytes, start.live_bytes + 64);\n        let shrunk = allocator.realloc(grown, Layout::from_size_align(64, 8).unwrap(), 8);\n        assert!(!shrunk.is_null());\n        assert_eq!(shrunk.read(), 42);\n        allocator.dealloc(shrunk, Layout::from_size_align(8, 8).unwrap());\n    }\n    let end = allocation::delta(start);\n    assert_eq!(end.live_bytes, start.live_bytes);\n    assert_eq!(end.peak_live_bytes, start.live_bytes + 64);\n    assert_eq!(end.requested_bytes, 88);\n    assert_eq!(end.deallocations, 1);\n}\n"},{"path":"tests/diffusion_routes.rs","bytes":9828,"preview":"#![cfg(all(\n    feature = \"legacyBenchmark\",\n    feature = \"builtinStorage\",\n    feature = \"builtinKernels\"\n))]\n//! Direct product-model training and sampling on both execution routes.\nuse std::{cell::RefCell, collections::BTreeMap, rc::Rc};\nuse trench_deep::{\n    nn::{Diffusion, DiffusionScheduler, Unet},\n    optimizer::{Adam, Optimizer},\n    trainer::*,\n    *,\n};\n\n#[derive(Default)]\nstruct Trace {\n    losses: Vec\u003cf32>,\n    weights: Vec\u003cBTreeMap\u003cString, Vec\u003cf32>>>,\n}\nstruct Observer {\n    ctx: ExecutionContext,\n    named: BTreeMap\u003cString, Parameter>,\n    tensors: usize,\n    trace: Rc\u003cRefCell\u003cTrace>>,\n}\nimpl TrainingObserver for Observer {\n    fn on_batch_end(&mut self, event: &BatchEndContext) {\n        self.trace.borrow_mut().losses.push(event.loss);\n    }\n    fn on_epoch_end(&mut self, _: &EpochContext) {\n        assert_eq!(self.ctx.graph_stats().unwrap().graph_nodes, 0);\n        assert_eq!(self.ctx.graph_stats().unwrap().tensors, self.tensors);\n        assert!(self.named.values().all(|p| p.grad().unwrap().is_none()));\n        self.trace.borrow_mut().weights.push(\n            self.named\n                .iter()\n                .map(|(name, p)| (name.clone(), p.tensor().to_vec().unwrap()))\n                .collect(),\n        );\n    }\n}\n\nfn run(route: ExecutionRoute) -> MlResult\u003c(BTreeMap\u003cString, Vec\u003cf32>>, Trace)> {\n    let ctx = ExecutionContext::builder()\n        .initialization_seed(7)\n        .route(route)\n        .build()?;\n    assert_eq!(ctx.route(), route);\n    let mut model = Diffusion::new(\n        &ctx,\n        Unet::new(&ctx, 1, 8, &[1, 2], 4, &[])?,\n        DiffusionScheduler::linear(10, 1e-4, 0.02)?,\n        11,\n    )?;\n    let named: BTreeMap\u003c_, _> = model\n        .unet\n        .named_parameters()\n        .into_iter()\n        .map(|(name, p)| (name, p.clone()))\n        .collect();\n    let initial = named\n        .iter()\n        .map(|(name, p)| Ok((name.clone(), p.tensor().to_vec()?)))\n        .collect::\u003cMlResult\u003cBTreeMap\u003c_, _>>>()?;\n    let mut optimizer = Adam::new(&ctx, 1e-3, 0.9, 0.999, 1e-8)?;"},{"path":"tests/diffusion.rs","bytes":3441,"preview":"#![cfg(all(\n    feature = \"builtinStorage\",\n    feature = \"builtinKernels\",\n    feature = \"enableBackward\"\n))]\nuse trench_deep::nn::{Diffusion, DiffusionScheduler, Unet};\nuse trench_deep::optimizer::{Optimizer, SGD};\nuse trench_deep::trainer::*;\nuse trench_deep::*;\n\n#[test]\nfn full_unet_trains_and_samples_through_public_api() -> MlResult\u003c()> {\n    let ctx = ExecutionContext::new();\n    let unet = Unet::new(&ctx, 1, 2, &[1, 2], 1, &[0, 1])?;\n    let mut model = Diffusion::new(&ctx, unet, DiffusionScheduler::linear(2, 0.001, 0.02)?, 7)?;\n    let image = ctx.input((0..16).map(|i| i as f32 / 16.0).collect(), &[1, 1, 4, 4])?;\n    let noise = ctx.tensor(vec![0.1; 16], &[1, 1, 4, 4])?;\n    let (prediction, loss) = model.forward_loss_with_noise(&image, &noise, 1)?;\n    assert_eq!(prediction.tensor().shape()?, vec![1, 1, 4, 4]);\n    assert!(loss.tensor().item()?.is_finite());\n    loss.backward()?;\n    assert!(\n        model\n            .parameters()\n            .iter()\n            .all(|p| p.grad().is_ok_and(|g| g.is_some()))\n    );\n    drop(prediction);\n    drop(loss);\n    let mut optimizer = SGD::new(&ctx, 0.001)?;\n    optimizer.register_all(&model.parameters())?;\n    let images = [&image];\n    let dataset = UnsupervisedDataset::new(&ctx, &images)?;\n    UnsupervisedTrainer::silent(&ctx).fit(\n        &mut model,\n        &mut optimizer,\n        &dataset,\n        EpochSchedule::new(1)?,\n    )?;\n    assert_eq!(ctx.graph_stats()?.graph_nodes, 0);\n    let sample = model.sample(&[1, 1, 4, 4])?;\n    assert_eq!(sample.shape()?, vec![1, 1, 4, 4]);\n    assert!(sample.to_vec()?.iter().all(|x| x.is_finite()));\n    assert_eq!(ctx.graph_stats()?.graph_nodes, 0);\n    Ok(())\n}\n\n#[test]\nfn scheduler_matches_closed_form_and_t0_ignores_noise() -> MlResult\u003c()> {\n    let ctx = ExecutionContext::new();\n    let scheduler = DiffusionScheduler::from_betas(vec![0.1, 0.2])?;\n    let x = ctx.scalar(2.0)?;\n    let noise = ctx.scalar(0.5)?;\n    assert!(\n        (scheduler.q_sample(&x, &noise, 1)?.item()?\n            - (0.72f32.sqrt() * 2.0 + 0.28f32.sqrt() * 0.5))\n            .abs()\n            \u003c 1e-6\n    );\n    let a = scheduler.reverse_step(&x, &noise, &ctx.scalar(1.0)?, 0)?;\n    let b = scheduler.reverse_step(&x, &noise, &ctx.scalar(999.0)?, 0)?;\n    assert_eq!(a.item()?, b.item()?);\n    Ok(())\n}\n"},{"path":"tests/execution_route.rs","bytes":5121,"preview":"use trench_deep::*;\n\n#[test]\nfn default_and_explicit_p1_preserve_provider_composition() -> MlResult\u003c()> {\n    assert_eq!(ExecutionContext::new().route(), ExecutionRoute::P1);\n    let ctx = ExecutionContextBuilder::empty()\n        .route(ExecutionRoute::P1)\n        .build()?;\n    assert_eq!(ctx.route(), ExecutionRoute::P1);\n    assert!(matches!(\n        ctx.scalar(1.0),\n        Err(MlError::DependencyUnavailable {\n            module: \"storage\",\n            ..\n        })\n    ));\n    Ok(())\n}\n\n#[test]\nfn legacy_selection_never_silently_runs_p1() {\n    let result = ExecutionContext::builder()\n        .route(ExecutionRoute::Legacy)\n        .build();\n    #[cfg(not(feature = \"legacyBenchmark\"))]\n    assert!(matches!(\n        result,\n        Err(MlError::DependencyUnavailable {\n            module: \"legacy execution\",\n            ..\n        })\n    ));\n    #[cfg(feature = \"legacyBenchmark\")]\n    assert_eq!(result.unwrap().route(), ExecutionRoute::Legacy);\n}\n\n#[cfg(feature = \"legacyBenchmark\")]\n#[test]\nfn legacy_public_scope_preserves_owned_outputs_and_rejects_unsupported_requests() -> MlResult\u003c()> {\n    let ctx = ExecutionContext::builder()\n        .route(ExecutionRoute::Legacy)\n        .build()?;\n    assert!(\n        ExecutionContext::builder()\n            .route(ExecutionRoute::Legacy)\n            .build()\n            .is_err()\n    );\n    let p = ctx.parameter(vec![2.0], &[])?;\n    let baseline = ctx.graph_stats()?.tensors;\n    for _ in 0..16 {\n        let output = ctx.with_training_scope(|| {\n            assert!(ctx.with_training_scope(|| Ok(())).is_err());\n            let loss = p.mul(p.tensor())?;\n            loss.backward()?;\n            assert_eq!(p.grad()?.unwrap().data(), &[4.0]);\n            Ok(loss)\n        })?;\n        assert_eq!(output.tensor().item()?, 4.0);\n        assert_eq!(ctx.graph_stats()?.tensors, baseline + 1);\n        assert!(p.grad()?.is_none());\n        drop(output);\n        assert_eq!(ctx.graph_stats()?.tensors, baseline);\n    }\n    let failure: MlResult\u003c()> = ctx.with_training_scope(|| {"},{"path":"tests/legacy_conv_gradient.rs","bytes":2192,"preview":"#![cfg(all(\n    feature = \"legacyBenchmark\",\n    feature = \"builtinStorage\",\n    feature = \"builtinKernels\"\n))]\nuse old::tensor::{\n    TensorBase,\n    operators::{Conv2dOp, Function},\n};\nuse trench_deep::{legacy as old, *};\n\n#[test]\nfn corrected_legacy_conv_input_gradient_matches_context_and_finite_difference()\n-> Result\u003c(), Box\u003cdyn std::error::Error>> {\n    let ctx = ExecutionContext::new();\n    let x = ctx.parameter(vec![0.5, 0.7], &[1, 2, 1, 1])?;\n    let w = ctx.tensor(vec![1.0, 2.0, 3.0, 4.0], &[2, 2, 1, 1])?;\n    let b = ctx.tensor(vec![0.0, 0.0], &[2])?;\n    let y = x.variable().conv2d(&w, &b, (1, 1), (0, 0))?;\n    y.sum()?.backward()?;\n    assert_eq!(x.grad()?.ok_or(\"gradient\")?.data(), &[4.0, 6.0]);\n    let ox = old::tensor::GlobalTensor::from_vec(vec![0.5, 0.7], &[1, 2, 1, 1])?;\n    let ow = old::tensor::GlobalTensor::from_vec(vec![1.0, 2.0, 3.0, 4.0], &[2, 2, 1, 1])?;\n    let ob = old::tensor::GlobalTensor::from_vec(vec![0.0, 0.0], &[2])?;\n    let one = old::tensor::GlobalTensor::from_vec(vec![1.0], &[1, 1])?;\n    let zero = old::tensor::GlobalTensor::from_vec(vec![0.0], &[1, 1])?;\n    let grad = old::tensor::GlobalTensor::from_vec(vec![1.0, 1.0], &[1, 2, 1, 1])?;\n    let op = Conv2dOp::new()?;\n    let inputs: [&dyn TensorBase; 7] = [&ox, &ow, &ob, &one, &one, &zero, &zero];\n    let original = op.forward(&inputs)?;\n    assert_eq!(y.tensor().to_vec()?, original[0].data());\n    let gradients = op.backward(&inputs, &grad)?;\n    assert_eq!(gradients[0].data(), &[4.0, 6.0]);\n    // Independent finite difference of the unchanged baseline forward sum.\n    for index in 0..2 {\n        let evaluate = |delta: f32| -> old::MlResult\u003cf32> {\n            let mut values = vec![0.5, 0.7];\n            values[index] += delta;\n            let input = old::tensor::GlobalTensor::from_vec(values, &[1, 2, 1, 1])?;\n            Ok(\n                op.forward(&[&input, &ow, &ob, &one, &one, &zero, &zero])?[0]\n                    .data()\n                    .iter()\n                    .sum(),\n            )\n        };\n        let derivative = (evaluate(0.001)? - evaluate(-0.001)?) / 0.002;\n        assert!((derivative - [4.0, 6.0][index]).abs() \u003c 0.001);\n    }\n    Ok(())\n}\n"},{"path":"tests/legacy_operations.rs","bytes":34479,"preview":"#![cfg(all(\n    feature = \"legacyBenchmark\",\n    feature = \"builtinStorage\",\n    feature = \"builtinKernels\"\n))]\nuse trench_deep::{contracts::Operation, *};\n\nfn close(actual: &[f32], expected: &[f32]) {\n    assert_eq!(actual.len(), expected.len());\n    for (&a, &b) in actual.iter().zip(expected) {\n        assert!(\n            a.is_finite() && b.is_finite() && (a - b).abs() \u003c= 1e-5 * (1.0 + b.abs()),\n            \"{a} != {b}\"\n        );\n    }\n}\n\n// A weighted dot product exercises every output gradient independently of Sum.\nfn compare_operation(\n    op: Operation,\n    inputs: Vec\u003c(Vec\u003cf32>, Vec\u003cusize>)>,\n    backward: bool,\n) -> MlResult\u003c()> {\n    let run = |route| -> MlResult\u003c_> {\n        let ctx = ExecutionContext::builder().route(route).build()?;\n        let parameters = inputs\n            .iter()\n            .map(|(data, shape)| ctx.parameter(data.clone(), shape))\n            .collect::\u003cMlResult\u003cVec\u003c_>>>()?;\n        let baseline = ctx.graph_stats()?;\n        let result = ctx.with_training_scope(|| {\n            let refs = parameters.iter().map(|p| p.tensor()).collect::\u003cVec\u003c_>>();\n            let compute = || ctx.execute(&op, &refs);\n            let outputs = if backward {\n                compute()?\n            } else {\n                ctx.no_grad(compute)?\n            };\n            let values = outputs\n                .iter()\n                .map(|t| t.snapshot())\n                .collect::\u003cMlResult\u003cVec\u003c_>>>()?;\n            if backward {\n                let n = values[0].data().len();\n                let flat = outputs[0].as_variable()?.reshape(&[n])?;\n                let weights = ctx.tensor((0..n).map(|i| 0.5 + i as f32 * 0.07).collect(), &[n])?;\n                flat.matmul(&weights)?.backward()?;\n            }\n            let gradients = parameters\n                .iter()\n                .map(|p| p.grad())\n                .collect::\u003cMlResult\u003cVec\u003c_>>>()?;\n            Ok((values, gradients))\n        })?;\n        assert_eq!(ctx.graph_stats()?, baseline);\n        Ok(result)\n    };\n    let p1 = run(ExecutionRoute::P1)?;\n    let legacy = run(ExecutionRoute::Legacy)?;\n    assert_eq!(p1.0.len(), legacy.0.len(), \"{op:?}\");\n    for (a, b) in p1.0.iter().zip(&legacy.0) {\n        assert_eq!(a.shape(), b.shape(), \"{op:?}\");\n        close(a.data(), b.data());\n    }\n    for (a, b) in p1.1.iter().zip(&legacy.1) {"},{"path":"tests/legacy_pilots.rs","bytes":9444,"preview":"#![cfg(all(\n    feature = \"legacyBenchmark\",\n    feature = \"builtinStorage\",\n    feature = \"builtinKernels\"\n))]\nuse old::{\n    nn::Parameter as OldParameter,\n    optimizer::Optimizer as OldOptimizer,\n    tensor::{AutogradFunction, TensorBase, operators::Function},\n    trainer::TrainableModel as OldModel,\n};\nuse trench_deep::legacy as old;\nuse trench_deep::{\n    nn::{BigramLm, LinearPolicy, PiClassifier},\n    optimizer::{Optimizer, SGD},\n    trainer::*,\n    *,\n};\ntype Result\u003cT> = std::result::Result\u003cT, Box\u003cdyn std::error::Error>>;\n\n#[test]\nfn pi_model_matches_the_original_nonzero_sampled_augmentations() -> Result\u003c()> {\n    let ctx = ExecutionContext::new();\n    let model = PiClassifier::new(&ctx, 2, 2, 0.2)?;\n    let mut baseline = old::comparison::semi_supervised::PiToyClassifier::new(2, 2, 0.2)?;\n    initialize(&ctx, model.parameters(), baseline.params())?;\n    let (x, ox) = input(&ctx, vec![0.2, 0.8], &[1, 2])?;\n    let (t, ot) = input(&ctx, vec![0.0, 1.0], &[1, 2])?;\n    let (_, ou) = input(&ctx, vec![0.3, 0.7], &[1, 2])?;\n    let (oy, oloss) =\n        old::trainer::SemiSupervisedModel::forward_loss(&mut baseline, &ox, &ot, &ou, 0.4)?;\n    let views = old::comparison::direct_add_outputs(&ou)?;\n    assert_eq!(\n        views.len(),\n        2,\n        \"expected original Pi-model's two noisy Add outputs\"\n    );\n    let first = ctx.input(views[0].data().to_vec(), views[0].shape())?;\n    let second = ctx.input(views[1].data().to_vec(), views[1].shape())?;\n    let (y, loss) = model.forward_loss_with_augmentations(&x, t.tensor(), &first, &second, 0.4)?;\n    close(&y.tensor().to_vec()?, oy.tensor().data());\n    verify(&ctx, &loss, &oloss, model.parameters(), baseline.params())\n}\nfn close(a: &[f32], b: &[f32]) {\n    assert_eq!(a.len(), b.len());\n    for (&a, &b) in a.iter().zip(b) {\n        assert!(\n            a.is_finite()\n                && b.is_finite()\n                && (a - b).abs() \u003c= 1e-3f32.max(1e-3 * a.abs().max(b.abs())),\n            \"{a} != {b}\"\n        );\n    }\n}\nfn initialize(ctx: &ExecutionContext, a: Vec\u003c&Parameter>, b: Vec\u003c&dyn OldParameter>) -> Result\u003c()> {\n    assert_eq!(a.len(), b.len());\n    for (i, (p, q)) in a.iter().zip(b).enumerate() {\n        let shape = p.tensor().shape()?;\n        assert_eq!(shape, q.tensor().shape());\n        let data = (0..p.tensor().numel()?)\n            .map(|j| ((i * 7 + j * 3) % 13) as f32 * 0.02 - 0.1)\n            .collect::\u003cVec\u003c_>>();\n        ctx.replace_parameter(p.variable(), TensorBuffer::from_vec(data.clone(), &shape)?)?;\n        q.tensor()\n            .replace(old::tensor::GlobalTensor::from_vec(data, &shape)?);"},{"path":"tests/legacy_reference_diffusion.rs","bytes":10128,"preview":"#![cfg(all(\n    feature = \"legacyBenchmark\",\n    feature = \"builtinStorage\",\n    feature = \"builtinKernels\"\n))]\nuse old::{\n    nn::{Layer, Parameter as OldParameter},\n    optimizer::Optimizer as OldOptimizer,\n    tensor::TensorBase,\n};\nuse trench_deep::{\n    legacy as old,\n    nn::{Diffusion, DiffusionScheduler, Unet},\n    optimizer::{Adam, Optimizer},\n    trainer::TrainableModel,\n    *,\n};\n#[path = \"../examples/replay_reference_diffusion.rs\"]\nmod replay;\n\nfn close(actual: &[f32], expected: &[f32], label: &str) {\n    assert_eq!(actual.len(), expected.len(), \"{label}\");\n    for (index, (&a, &b)) in actual.iter().zip(expected).enumerate() {\n        assert!(\n            a.is_finite()\n                && b.is_finite()\n                && (a - b).abs() \u003c= 1e-3f32.max(1e-3 * a.abs().max(b.abs())),\n            \"{label}[{index}]: {a} != {b}\"\n        );\n    }\n}\n\n/// Capture native DDPM draws and validate numerical replay; direct Trainer routes are tested separately.\n#[test]\nfn original_reference_ddpm_draws_replay_through_context_and_adam()\n-> Result\u003c(), Box\u003cdyn std::error::Error>> {\n    let ctx = ExecutionContext::new();\n    let model = Diffusion::new(\n        &ctx,\n        Unet::new(&ctx, 1, 8, &[1, 2], 4, &[])?,\n        DiffusionScheduler::linear(10, 1e-4, 0.02)?,\n        0,\n    )?;\n    let mut baseline = old::comparison::ReferenceDiffusion::new(\n        1,\n        8,\n        8,\n        &[1, 2],\n        4,\n        &[false, false],\n        10,\n        1e-4,\n        0.02,\n    )?;\n    let parameters = model.parameters();\n    assert_eq!(parameters.len(), baseline.unet.params().len());\n    let named_entries = model.unet.named_parameters();\n    let old_descriptors = replay::mapping::describe(\n        baseline\n            .unet\n            .comparison_named_parameters()\n            .into_iter()\n            .map(|(name, p)| (name, p.tensor().shape().to_vec(), p.node_id()))\n            .collect(),\n    )?;"},{"path":"tests/metrics.rs","bytes":702,"preview":"use trench_deep::{\n    MlResult, TensorBuffer,\n    trainer::{ClassificationAccuracy, argmax},\n};\n\n#[test]\nfn accuracy_counts_each_sample_in_a_batch() -> MlResult\u003c()> {\n    let prediction = TensorBuffer::from_vec(vec![9.0, 1.0, 2.0, 3.0, 5.0, 1.0], &[3, 2])?;\n    let target = TensorBuffer::from_vec(vec![1.0, 0.0, 1.0, 0.0, 1.0, 0.0], &[3, 2])?;\n    let mut metric = ClassificationAccuracy::new();\n    metric.update(&prediction, &target);\n    assert!((metric.compute() - 200.0 / 3.0).abs() \u003c 1e-5);\n    metric.reset();\n    assert_eq!(metric.compute(), 0.0);\n    Ok(())\n}\n\n#[test]\nfn argmax_preserves_first_tie() {\n    assert_eq!(argmax(&[2.0, 2.0, 1.0]), Some(0));\n    assert_eq!(argmax(&[]), None);\n}\n"},{"path":"tests/models/autoregressive.rs","bytes":3339,"preview":"use trench_deep::{nn::*, trainer::TrainableModel, ExecutionContext, MlResult, Variable};\nuse trench_deep::optimizer::{Adam, Optimizer};\nuse trench_deep::trainer::{\n    AutoregressiveDataset, AutoregressiveSample, AutoregressiveStackCollator,\n    AutoregressiveTrainer, DataLoader, EpochSchedule, InMemoryDataset,\n};\n\nfn sequence(context: &ExecutionContext, tokens: &[usize], vocab: usize) -> MlResult\u003cVariable> {\n    let mut data = vec![0.0; tokens.len() * vocab];\n    for (row, token) in tokens.iter().copied().enumerate() {\n        data[row * vocab + token] = 1.0;\n    }\n    context.input(data, &[tokens.len(), vocab])\n}\n\n#[test]\nfn bigram_pilot_trains_end_to_end() -> MlResult\u003c()> {\n    let context = ExecutionContext::new();\n    let mut model = BigramLm::new(&context, 4)?;\n    let samples = [\n        sequence(&context, &[0, 1, 2, 3, 0], 4)?,\n        sequence(&context, &[1, 2, 3, 0, 1], 4)?,\n    ];\n    let refs = samples.iter().collect::\u003cVec\u003c_>>();\n    let dataset = AutoregressiveDataset::new(&context, &refs)?;\n    let mut optimizer = Adam::new(&context, 0.05, 0.9, 0.999, 1e-8)?;\n    optimizer.register_all(&model.parameters())?;\n    let result = AutoregressiveTrainer::silent(&context).fit(\n        &mut model,\n        &mut optimizer,\n        &dataset,\n        EpochSchedule::new(5)?.with_tolerance(0.0),\n    )?;\n    assert!(result.final_loss.is_finite());\n    assert_eq!(context.graph_stats()?.graph_nodes, 0);\n    Ok(())\n}\n\n#[test]\nfn bigram_pilot_accepts_stacked_loader_batches() -> MlResult\u003c()> {\n    let context = ExecutionContext::new();\n    let mut model = BigramLm::new(&context, 4)?;\n    let samples = [\n        sequence(&context, &[0, 1, 2, 3, 0], 4)?,\n        sequence(&context, &[1, 2, 3, 0, 1], 4)?,\n    ];\n    let refs = samples.iter().collect::\u003cVec\u003c_>>();\n    let dataset = AutoregressiveDataset::new(&context, &refs)?;\n    let mut loader = DataLoader::builder(InMemoryDataset::new(\n        dataset\n            .sequences\n            .iter()\n            .map(|v| AutoregressiveSample::new(v.tensor().clone()))\n            .collect(),\n    )?)\n    .collator(AutoregressiveStackCollator::new())\n    .batch_size(2)\n    .shuffle(false)\n    .build()?;\n    let mut optimizer = Adam::new(&context, 0.02, 0.9, 0.999, 1e-8)?;\n    optimizer.register_all(&model.parameters())?;\n    let result = AutoregressiveTrainer::silent(&context).fit_loader(\n        &mut model,\n        &mut optimizer,\n        &mut loader,"},{"path":"tests/models/reinforcement.rs","bytes":1044,"preview":"use trench_deep::{nn::*, trainer::TrainableModel, ExecutionContext, MlResult, Variable};\nuse trench_deep::optimizer::{Adam, Optimizer};\nuse trench_deep::trainer::{EpisodeSchedule, RLTrainer};\n\n#[test]\nfn bandit_policy_pilot_trains_end_to_end() -> MlResult\u003c()> {\n    let context = ExecutionContext::new();\n    let mut model = LinearPolicy::new(&context, 1, 2)?;\n    let mut environment = TwoArmedBandit::default();\n    environment.mean_rewards = [-1.0, 1.0];\n    environment.noise_scale = 0.0;\n    let mut optimizer = Adam::new(&context, 0.05, 0.9, 0.999, 1e-8)?;\n    optimizer.register_all(&model.parameters())?;\n    let result = RLTrainer::silent(&context)\n        .with_seed(19)\n        .with_baseline(false)\n        .fit(\n            &mut model,\n            &mut environment,\n            &mut optimizer,\n            EpisodeSchedule::new(20, 1)?,\n        )?;\n    assert!(result.final_loss.is_finite());\n    assert_eq!(result.units_completed, 20);\n    assert_eq!(context.graph_stats()?.graph_nodes, 0);\n    Ok(())\n}\n"},{"path":"tests/models/semi_supervised.rs","bytes":2555,"preview":"use trench_deep::{nn::*, trainer::TrainableModel, ExecutionContext, MlResult, Variable};\nuse trench_deep::optimizer::{Adam, Optimizer};\nuse trench_deep::trainer::{\n    ConsistencyRamp, EpochSchedule, SemiSupervisedDataset, SemiSupervisedTrainer,\n};\n\n#[test]\nfn supplied_views_reject_broadcasting_and_empty_batches_before_recording() -> MlResult\u003c()> {\n    let context = ExecutionContext::new();\n    let model = PiClassifier::new(&context, 2, 2, 0.1)?;\n    let input = context.input(vec![1.0, 2.0], &[1, 2])?;\n    let target = context.tensor(vec![1.0, 0.0], &[1, 2])?;\n    let other = context.input(vec![1.0; 4], &[2, 2])?;\n    let empty = context.input(vec![], &[0, 2])?;\n    for (first, second) in [(&input, &other), (&empty, &empty)] {\n        assert!(matches!(\n            model.forward_loss_with_augmentations(&input, &target, first, second, 0.4),\n            Err(trench_deep::MlError::TensorError(\n                trench_deep::TensorError::InvalidOperation { op: \"pi_model\", .. }\n            ))\n        ));\n        assert_eq!(context.graph_stats()?.graph_nodes, 0);\n    }\n    Ok(())\n}\n\n#[test]\nfn pi_model_pilot_trains_end_to_end() -> MlResult\u003c()> {\n    let context = ExecutionContext::new();\n    let mut model = PiClassifier::new(&context, 2, 2, 0.1)?;\n    let labeled = [\n        context.input(vec![1.0, 1.0], &[1, 2])?,\n        context.input(vec![-1.0, -1.0], &[1, 2])?,\n    ];\n    let targets = [\n        context.tensor(vec![1.0, 0.0], &[1, 2])?,\n        context.tensor(vec![0.0, 1.0], &[1, 2])?,\n    ];\n    let unlabeled = [\n        context.input(vec![0.9, 1.1], &[1, 2])?,\n        context.input(vec![-0.9, -1.1], &[1, 2])?,\n    ];\n    let labeled_refs = labeled.iter().collect::\u003cVec\u003c_>>();\n    let target_refs = targets.iter().collect::\u003cVec\u003c_>>();\n    let unlabeled_refs = unlabeled.iter().collect::\u003cVec\u003c_>>();\n    let dataset =\n        SemiSupervisedDataset::new(&context, &labeled_refs, &target_refs, &unlabeled_refs)?;\n    let mut optimizer = Adam::new(&context, 0.02, 0.9, 0.999, 1e-8)?;\n    optimizer.register_all(&model.parameters())?;\n    let result = SemiSupervisedTrainer::silent(&context)\n        .with_ramp(ConsistencyRamp::Sigmoid {\n            max_weight: 1.0,\n            ramp_epochs: 2,\n        })\n        .fit(\n            &mut model,\n            &mut optimizer,\n            &dataset,\n            EpochSchedule::new(3)?.with_tolerance(0.0),\n        )?;\n    assert!(result.final_loss.is_finite());\n    assert_eq!(context.graph_stats()?.graph_nodes, 0);\n    Ok(())\n}\n"},{"path":"tests/models/supervised.rs","bytes":1449,"preview":"use trench_deep::{nn::*, trainer::TrainableModel, ExecutionContext, MlResult, Variable};\nuse trench_deep::optimizer::{Adam, Optimizer};\nuse trench_deep::trainer::{EpochSchedule, SupervisedDataset, SupervisedTrainer};\n\n#[test]\nfn mlp_pilot_trains_end_to_end_and_predicts_probabilities() -> MlResult\u003c()> {\n    let context = ExecutionContext::new();\n    let mut model = Mlp::new(&context, 2, 4, 2)?;\n    let inputs = [\n        context.input(vec![0.0, 0.0], &[1, 2])?,\n        context.input(vec![1.0, 1.0], &[1, 2])?,\n    ];\n    let targets = [\n        context.tensor(vec![1.0, 0.0], &[1, 2])?,\n        context.tensor(vec![0.0, 1.0], &[1, 2])?,\n    ];\n    let input_refs: Vec\u003c_> = inputs.iter().collect();\n    let target_refs: Vec\u003c_> = targets.iter().collect();\n    let dataset = SupervisedDataset::new(&context, &input_refs, &target_refs)?;\n    let mut optimizer = Adam::new(&context, 0.05, 0.9, 0.999, 1e-8)?;\n    optimizer.register_all(&model.parameters())?;\n\n    let result = SupervisedTrainer::new(&context).fit(\n        &mut model,\n        &mut optimizer,\n        &dataset,\n        EpochSchedule::new(20)?,\n    )?;\n    assert!(result.final_loss.is_finite());\n    assert_eq!(result.units_completed, 20);\n    assert_eq!(context.graph_stats()?.graph_nodes, 0);\n\n    let probabilities = model.predict(inputs[0].tensor())?.to_vec()?;\n    assert!((probabilities.iter().sum::\u003cf32>() - 1.0).abs() \u003c 1e-5);\n    Ok(())\n}\n"},{"path":"tests/models.rs","bytes":386,"preview":"#![cfg(all(feature = \"builtinStorage\", feature = \"builtinKernels\", feature = \"enableBackward\"))]\n//! Context models exercised through public training APIs.\n#[path = \"models/autoregressive.rs\"]\nmod autoregressive;\n#[path = \"models/reinforcement.rs\"]\nmod reinforcement;\n#[path = \"models/semi_supervised.rs\"]\nmod semi_supervised;\n#[path = \"models/supervised.rs\"]\nmod supervised;\n"},{"path":"tests/parameter_mapping.rs","bytes":1647,"preview":"#[path = \"../examples/support/reference_parameters.rs\"]\nmod mapping;\nuse mapping::{describe, validate};\n\n#[test]\nfn names_and_sharing_are_independent_of_order_and_local_ids() {\n    let a = describe(vec![\n        (\"b\".into(), vec![2], 1),\n        (\"a\".into(), vec![2], 1),\n        (\"c\".into(), vec![2], 2),\n    ])\n    .unwrap();\n    let b = describe(vec![\n        (\"c\".into(), vec![2], 90),\n        (\"a\".into(), vec![2], 80),\n        (\"b\".into(), vec![2], 80),\n    ])\n    .unwrap();\n    validate(&a, &b).unwrap();\n    let unshared = describe(vec![\n        (\"a\".into(), vec![2], 1),\n        (\"b\".into(), vec![2], 2),\n        (\"c\".into(), vec![2], 3),\n    ])\n    .unwrap();\n    assert!(validate(&a, &unshared).is_err());\n    let mut wrong = b.clone();\n    wrong[2].shape = vec![1, 2];\n    assert!(validate(&a, &wrong).is_err());\n    wrong = b.clone();\n    wrong[2].name = \"d\".into();\n    wrong[2].shared_with = \"d\".into();\n    assert!(validate(&a, &wrong).is_err());\n    wrong = b.clone();\n    wrong.push(b[0].clone());\n    assert!(validate(&a, &wrong).is_err());\n    assert!(describe(vec![(\"a\".into(), vec![2], 1), (\"a\".into(), vec![2], 2)]).is_err());\n}\n\n#[test]\nfn invalid_shared_references_are_rejected() {\n    let valid = describe(vec![(\"a\".into(), vec![2], 1), (\"b\".into(), vec![2], 1)]).unwrap();\n    let mut invalid = valid.clone();\n    invalid[1].shared_with = \"missing\".into();\n    assert!(validate(&valid, &invalid).is_err());\n    invalid = valid.clone();\n    invalid[0].shared_with = \"b\".into();\n    assert!(validate(&valid, &invalid).is_err());\n    assert!(describe(vec![(\"a\".into(), vec![2], 1), (\"b\".into(), vec![3], 1)]).is_err());\n}\n"},{"path":"tests/prepared.rs","bytes":9271,"preview":"#![cfg(all(\n    feature = \"builtinStorage\",\n    feature = \"builtinKernels\",\n    feature = \"enableBackward\"\n))]\nuse trench_deep::{\n    contracts::{LossKind, Operation},\n    optimizer::{Adam, Optimizer},\n    runtime::prepared::{PreparedMode, PreparedProgram},\n    *,\n};\n\nfn program() -> MlResult\u003c(PreparedProgram, Vec\u003cruntime::prepared::TensorSlotId>)> {\n    let mut p = PreparedProgram::new();\n    let x = p.input(&[1, 2], false)?;\n    let target = p.input(&[1, 1], false)?;\n    let weight = p.parameter(&[2, 1])?;\n    let bias = p.parameter(&[1])?;\n    let dot = p.operation(Operation::Matmul, &[x, weight])?;\n    let linear = p.operation(Operation::Add, &[dot, bias])?;\n    let prediction = p.operation(Operation::Relu, &[linear])?;\n    let loss = p.operation(\n        Operation::Loss {\n            kind: LossKind::Mse,\n            reduction: Reduction::Mean,\n        },\n        &[prediction, target],\n    )?;\n    Ok((p, vec![prediction, loss]))\n}\n#[test]\nfn linear_plan_reuses_topology_with_new_feeds_and_adam_values() -> MlResult\u003c()> {\n    let ctx = ExecutionContext::new();\n    let eager = ExecutionContext::new();\n    let w = ctx.parameter(vec![0.4, 0.7], &[2, 1])?;\n    let b = ctx.parameter(vec![0.2], &[1])?;\n    let ew = eager.parameter(vec![0.4, 0.7], &[2, 1])?;\n    let eb = eager.parameter(vec![0.2], &[1])?;\n    let mut adam = Adam::new(&ctx, 0.001, 0.9, 0.999, 1e-8)?;\n    adam.register_all(&[&w, &b])?;\n    let mut other_adam = Adam::new(&eager, 0.001, 0.9, 0.999, 1e-8)?;\n    other_adam.register_all(&[&ew, &eb])?;\n    let (program, outputs) = program()?;\n    let baseline = ctx.graph_stats()?;\n    let plan = ctx.prepare(&program, &[&w, &b], &outputs, PreparedMode::Training)?;\n    assert_eq!(ctx.graph_stats()?, baseline);\n    assert_eq!(plan.node_count(), 4);\n    assert!(!plan.uses_static_buffers());\n    let mut held = None;\n    let mut held_values = Vec::new();\n    for step in 0..10 {\n        let values = vec![0.5 + step as f32 * 0.1, -0.1];\n        let x = ctx.tensor(values.clone(), &[1, 2])?;\n        let target = ctx.tensor(vec![0.3], &[1, 1])?;\n        let ex = eager.tensor(values, &[1, 2])?;\n        let et = eager.tensor(vec![0.3], &[1, 1])?;\n        let actual = plan.with_run(&ctx, &[&x, &target], &[&w, &b], |out| {\n            out[1].as_variable()?.backward()?;\n            assert!(out[1].as_variable()?.backward().is_err());\n            let result = (\n                out[0].to_vec()?,\n                out[1].to_vec()?,\n                w.grad()?.unwrap(),\n                b.grad()?.unwrap(),\n            );"},{"path":"tests/providers.rs","bytes":8741,"preview":"mod support;\nuse support::*;\nuse trench_deep::{\n    contracts::*,\n    nn::LinearRegression,\n    optimizer::{Optimizer, SGD},\n    trainer::*,\n    *,\n};\n\nfn reference() -> ExecutionContext {\n    ExecutionContextBuilder::empty()\n        .storage(SlotStore::default())\n        .autograd(Tape::default())\n        .operations(ReferenceOps)\n        .build()\n}\n\n#[test]\nfn prepared_replay_requires_provider_opt_in() -> MlResult\u003c()> {\n    use trench_deep::runtime::prepared::{PreparedMode,PreparedProgram};\n    let ctx = reference();\n    let mut program = PreparedProgram::new();\n    let x = program.input(&[1],false)?;\n    let y = program.operation(Operation::Square,&[x])?;\n    let baseline = ctx.graph_stats()?;\n    assert!(matches!(ctx.prepare(&program,&[],&[y],PreparedMode::Inference),\n        Err(MlError::UnsupportedCapability { .. })));\n    assert_eq!(ctx.graph_stats()?,baseline);\n    let empty = ExecutionContextBuilder::empty().build();\n    assert!(empty.prepare(&program,&[],&[y],PreparedMode::Inference).is_err());\n    Ok(())\n}\nfn train(ctx: &ExecutionContext) -> MlResult\u003cVec\u003cf32>> {\n    let mut model = LinearRegression::new(ctx, 1, 1)?;\n    ctx.replace_parameter(\n        model.layer().weight().variable(),\n        TensorBuffer::from_vec(vec![0.25], &[1, 1])?,\n    )?;\n    ctx.replace_parameter(\n        model.layer().bias().variable(),\n        TensorBuffer::from_vec(vec![0.0], &[1])?,\n    )?;\n    let input = ctx.input(vec![1.0, 2.0], &[2, 1])?;\n    let target = ctx.tensor(vec![2.0, 4.0], &[2, 1])?;\n    let x = [&input];\n    let y = [&target];\n    let data = SupervisedDataset::new(ctx, &x, &y)?;\n    let mut optimizer = SGD::new(ctx, 0.05)?;\n    optimizer.register_all(&model.parameters())?;\n    SupervisedTrainer::silent(ctx).fit(\n        &mut model,\n        &mut optimizer,\n        &data,\n        EpochSchedule::new(3)?,\n    )?;\n    assert_eq!(ctx.graph_stats()?.graph_nodes, 0);\n    model\n        .parameters()\n        .into_iter()\n        .map(|p| p.tensor().item())\n        .collect()\n}\n#[test]\nfn all_providers_can_be_replaced_without_default_features() -> MlResult\u003c()> {"},{"path":"tests/rl_checkpoint.rs","bytes":2570,"preview":"#![cfg(all(\n    feature = \"builtinStorage\",\n    feature = \"builtinKernels\",\n    feature = \"enableBackward\"\n))]\nuse trench_deep::{\n    nn::{LinearPolicy, TwoArmedBandit},\n    optimizer::{Optimizer, SGD},\n    trainer::*,\n    *,\n};\nstruct StopAfterStep {\n    ctx: ExecutionContext,\n}\nimpl TrainingObserver for StopAfterStep {\n    fn on_batch_end(&mut self, _: &BatchEndContext) {\n        assert_eq!(self.ctx.graph_stats().expect(\"context\").graph_nodes, 0);\n        checkpoint::request_interrupt();\n    }\n}\n#[test]\nfn rl_interrupt_saves_completed_episode_after_scope_cleanup()\n-> Result\u003c(), Box\u003cdyn std::error::Error>> {\n    struct Clear;\n    impl Drop for Clear {\n        fn drop(&mut self) {\n            checkpoint::clear_interrupt();\n        }\n    }\n    let _clear = Clear;\n    checkpoint::clear_interrupt();\n    let directory = std::env::temp_dir().join(format!(\n        \"trench-rl-{}-{}\",\n        std::process::id(),\n        std::time::SystemTime::now()\n            .duration_since(std::time::UNIX_EPOCH)?\n            .as_nanos()\n    ));\n    let ctx = ExecutionContext::new();\n    let mut model = LinearPolicy::new(&ctx, 1, 2)?;\n    let mut environment = TwoArmedBandit::default().with_seed(91);\n    let mut optimizer = SGD::new(&ctx, 0.01)?;\n    optimizer.register_all(&model.parameters())?;\n    let trainer = RLTrainer::from_trainer(\n        &ctx,\n        Trainer::builder()\n            .show_progress(false)\n            .checkpoint_dir(directory.to_str().ok_or(\"path\")?)\n            .build(),\n    )\n    .with_observer(Box::new(StopAfterStep { ctx: ctx.clone() }));\n    let result = trainer.fit_checkpointed(\n        &mut model,\n        &mut environment,\n        &mut optimizer,\n        EpisodeSchedule::new(5, 1)?,\n    )?;\n    assert_eq!(result.stop_reason, StopReason::Interrupted);\n    assert_eq!(result.units_completed, 1);\n    let paths = result.checkpoint.ok_or(\"missing checkpoint\")?;\n    let metadata = checkpoint::TrainingCheckpoint::load(&paths.metadata)?;\n    assert_eq!(metadata.epochs_done, 1);\n    assert_eq!(metadata.total_epochs, 5);\n    assert_eq!(\n        metadata.paradigm,"},{"path":"tests/support/mod.rs","bytes":10394,"preview":"use trench_deep::{AutogradError, ContextError, MlError, MlResult, TensorError, contracts::*};\n\n/// Slot storage deliberately differs from the built-in hash map of Rc buffers.\n#[derive(Debug, Default)]\npub struct SlotStore {\n    names: Vec\u003c(TensorId, usize)>,\n    slots: Vec\u003cOption\u003cTensorBuffer>>,\n}\nimpl SlotStore {\n    fn slot(&self, id: TensorId) -> MlResult\u003cusize> {\n        self.names\n            .iter()\n            .find(|(key, _)| *key == id)\n            .map(|(_, slot)| *slot)\n            .ok_or_else(|| ContextError::UnknownTensor(id).into())\n    }\n}\nimpl TensorStore for SlotStore {\n    fn insert(&mut self, id: TensorId, value: TensorBuffer) -> MlResult\u003c()> {\n        if self.names.iter().any(|(key, _)| *key == id) {\n            return Err(MlError::StringError(\"duplicate ID\".into()));\n        }\n        let slot = self\n            .slots\n            .iter()\n            .position(Option::is_none)\n            .unwrap_or(self.slots.len());\n        if slot == self.slots.len() {\n            self.slots.push(Some(value));\n        } else {\n            self.slots[slot] = Some(value);\n        }\n        self.names.push((id, slot));\n        Ok(())\n    }\n    fn alias(&mut self, id: TensorId, source: TensorId) -> MlResult\u003c()> {\n        let slot = self.slot(source)?;\n        self.names.push((id, slot));\n        Ok(())\n    }\n    fn with_view(\n        &self,\n        id: TensorId,\n        f: &mut dyn FnMut(TensorView\u003c'_>) -> MlResult\u003c()>,\n    ) -> MlResult\u003c()> {\n        let slot = self.slot(id)?;\n        f(self.slots[slot]\n            .as_ref()\n            .ok_or(ContextError::UnknownTensor(id))?\n            .view())\n    }\n    fn replace(&mut self, id: TensorId, value: TensorBuffer) -> MlResult\u003c()> {\n        let slot = self.slot(id)?;\n        let old = self.slots[slot]\n            .as_ref()\n            .ok_or(ContextError::UnknownTensor(id))?;\n        if old.shape() != value.shape() {\n            return Err(TensorError::InvalidShape {\n                expected: old.shape().to_vec(),\n                got: value.shape().to_vec(),\n            }\n            .into());\n        }\n        self.slots[slot] = Some(value);\n        Ok(())"},{"path":"tests/visualization.rs","bytes":4833,"preview":"#![cfg(all(\n    feature = \"enableVisualization\",\n    feature = \"builtinStorage\",\n    feature = \"builtinKernels\"\n))]\nuse std::{cell::RefCell, rc::Rc};\nuse trench_deep::visualization::*;\nuse trench_deep::*;\nuse trench_deep::{\n    nn::LinearRegression,\n    optimizer::{Optimizer, SGD},\n    trainer::*,\n};\n\nstruct CaptureOrder {\n    ctx: ExecutionContext,\n    parameters: Vec\u003cParameter>,\n    events: Rc\u003cRefCell\u003cVec\u003c&'static str>>>,\n}\nimpl CaptureOrder {\n    fn cleaned(&self) {\n        assert_eq!(self.ctx.graph_stats().unwrap().graph_nodes, 0);\n        assert!(self.parameters.iter().all(|p| p.grad().unwrap().is_none()));\n    }\n}\nimpl TrainingObserver for CaptureOrder {\n    fn capture_profile(&self, _: &BatchStartContext) -> Option\u003cCaptureProfile> {\n        Some(CaptureProfile::Analysis)\n    }\n    fn on_graph_snapshot(&mut self, snapshot: GraphSnapshot) {\n        self.cleaned();\n        assert!(snapshot.nodes.iter().any(|n| n.gradient_stats.is_some()));\n        self.events.borrow_mut().push(\"snapshot\");\n    }\n    fn on_batch_end(&mut self, _: &BatchEndContext) {\n        self.cleaned();\n        self.events.borrow_mut().push(\"batch\");\n    }\n}\nstruct FailHook;\nimpl MetricHook for FailHook {\n    fn update(&mut self, _: &BatchContext\u003c'_>) -> MlResult\u003c()> {\n        Err(TensorError::InvalidOperation {\n            op: \"test_hook\",\n            reason: \"injected failure\".into(),\n        }\n        .into())\n    }\n    fn compute(&self) -> f32 {\n        0.0\n    }\n    fn reset(&mut self) -> MlResult\u003c()> {\n        Ok(())\n    }\n    fn name(&self) -> &str {\n        \"failure\"\n    }\n}\n\n#[test]\nfn capture_is_published_after_cleanup_and_suppressed_when_hook_fails() -> MlResult\u003c()> {\n    for fail in [false, true] {\n        let ctx = ExecutionContext::new();\n        let mut model = LinearRegression::new(&ctx, 1, 1)?;\n        let mut optimizer = SGD::new(&ctx, 0.01)?;"}],"sources":{"tests/diffusion.rs#let ctx = ExecutionContext::new()":{"path":"tests/diffusion.rs","line":13,"start":11,"verified":true,"hash":"5c8550144cf9034e2f2048f3db665a1a342e1d38b2fa295d5a6a5e7ef4c0273b","code":"#[test]\nfn full_unet_trains_and_samples_through_public_api() -> MlResult\u003c()> {\n    let ctx = ExecutionContext::new();\n    let unet = Unet::new(&ctx, 1, 2, &[1, 2], 1, &[0, 1])?;\n    let mut model = Diffusion::new(&ctx, unet, DiffusionScheduler::linear(2, 0.001, 0.02)?, 7)?;\n    let image = ctx.input((0..16).map(|i| i as f32 / 16.0).collect(), &[1, 1, 4, 4])?;\n    let noise = ctx.tensor(vec![0.1; 16], &[1, 1, 4, 4])?;\n    let (prediction, loss) = model.forward_loss_with_noise(&image, &noise, 1)?;\n    assert_eq!(prediction.tensor().shape()?, vec![1, 1, 4, 4]);\n    assert!(loss.tensor().item()?.is_finite());\n    loss.backward()?;\n    assert!(\n        model\n            .parameters()\n            .iter()\n            .all(|p| p.grad().is_ok_and(|g| g.is_some()))\n    );\n    drop(prediction);\n    drop(loss);\n    let mut optimizer = SGD::new(&ctx, 0.001)?;\n    optimizer.register_all(&model.parameters())?;\n    let images = [&image];\n    let dataset = UnsupervisedDataset::new(&ctx, &images)?;\n    UnsupervisedTrainer::silent(&ctx).fit("},"tests/diffusion.rs#let image = ctx.input":{"path":"tests/diffusion.rs","line":16,"start":14,"verified":true,"hash":"5c8550144cf9034e2f2048f3db665a1a342e1d38b2fa295d5a6a5e7ef4c0273b","code":"    let unet = Unet::new(&ctx, 1, 2, &[1, 2], 1, &[0, 1])?;\n    let mut model = Diffusion::new(&ctx, unet, DiffusionScheduler::linear(2, 0.001, 0.02)?, 7)?;\n    let image = ctx.input((0..16).map(|i| i as f32 / 16.0).collect(), &[1, 1, 4, 4])?;\n    let noise = ctx.tensor(vec![0.1; 16], &[1, 1, 4, 4])?;\n    let (prediction, loss) = model.forward_loss_with_noise(&image, &noise, 1)?;\n    assert_eq!(prediction.tensor().shape()?, vec![1, 1, 4, 4]);\n    assert!(loss.tensor().item()?.is_finite());\n    loss.backward()?;\n    assert!(\n        model\n            .parameters()\n            .iter()\n            .all(|p| p.grad().is_ok_and(|g| g.is_some()))\n    );\n    drop(prediction);\n    drop(loss);\n    let mut optimizer = SGD::new(&ctx, 0.001)?;\n    optimizer.register_all(&model.parameters())?;\n    let images = [&image];\n    let dataset = UnsupervisedDataset::new(&ctx, &images)?;\n    UnsupervisedTrainer::silent(&ctx).fit(\n        &mut model,\n        &mut optimizer,\n        &dataset,"},"tests/diffusion.rs#let (prediction, loss)":{"path":"tests/diffusion.rs","line":18,"start":16,"verified":true,"hash":"5c8550144cf9034e2f2048f3db665a1a342e1d38b2fa295d5a6a5e7ef4c0273b","code":"    let image = ctx.input((0..16).map(|i| i as f32 / 16.0).collect(), &[1, 1, 4, 4])?;\n    let noise = ctx.tensor(vec![0.1; 16], &[1, 1, 4, 4])?;\n    let (prediction, loss) = model.forward_loss_with_noise(&image, &noise, 1)?;\n    assert_eq!(prediction.tensor().shape()?, vec![1, 1, 4, 4]);\n    assert!(loss.tensor().item()?.is_finite());\n    loss.backward()?;\n    assert!(\n        model\n            .parameters()\n            .iter()\n            .all(|p| p.grad().is_ok_and(|g| g.is_some()))\n    );\n    drop(prediction);\n    drop(loss);\n    let mut optimizer = SGD::new(&ctx, 0.001)?;\n    optimizer.register_all(&model.parameters())?;\n    let images = [&image];\n    let dataset = UnsupervisedDataset::new(&ctx, &images)?;\n    UnsupervisedTrainer::silent(&ctx).fit(\n        &mut model,\n        &mut optimizer,\n        &dataset,\n        EpochSchedule::new(1)?,\n    )?;"},"tests/diffusion.rs#loss.backward()":{"path":"tests/diffusion.rs","line":21,"start":19,"verified":true,"hash":"5c8550144cf9034e2f2048f3db665a1a342e1d38b2fa295d5a6a5e7ef4c0273b","code":"    assert_eq!(prediction.tensor().shape()?, vec![1, 1, 4, 4]);\n    assert!(loss.tensor().item()?.is_finite());\n    loss.backward()?;\n    assert!(\n        model\n            .parameters()\n            .iter()\n            .all(|p| p.grad().is_ok_and(|g| g.is_some()))\n    );\n    drop(prediction);\n    drop(loss);\n    let mut optimizer = SGD::new(&ctx, 0.001)?;\n    optimizer.register_all(&model.parameters())?;\n    let images = [&image];\n    let dataset = UnsupervisedDataset::new(&ctx, &images)?;\n    UnsupervisedTrainer::silent(&ctx).fit(\n        &mut model,\n        &mut optimizer,\n        &dataset,\n        EpochSchedule::new(1)?,\n    )?;\n    assert_eq!(ctx.graph_stats()?.graph_nodes, 0);\n    let sample = model.sample(&[1, 1, 4, 4])?;\n    assert_eq!(sample.shape()?, vec![1, 1, 4, 4]);"},"tests/diffusion.rs#let mut optimizer":{"path":"tests/diffusion.rs","line":30,"start":28,"verified":true,"hash":"5c8550144cf9034e2f2048f3db665a1a342e1d38b2fa295d5a6a5e7ef4c0273b","code":"    drop(prediction);\n    drop(loss);\n    let mut optimizer = SGD::new(&ctx, 0.001)?;\n    optimizer.register_all(&model.parameters())?;\n    let images = [&image];\n    let dataset = UnsupervisedDataset::new(&ctx, &images)?;\n    UnsupervisedTrainer::silent(&ctx).fit(\n        &mut model,\n        &mut optimizer,\n        &dataset,\n        EpochSchedule::new(1)?,\n    )?;\n    assert_eq!(ctx.graph_stats()?.graph_nodes, 0);\n    let sample = model.sample(&[1, 1, 4, 4])?;\n    assert_eq!(sample.shape()?, vec![1, 1, 4, 4]);\n    assert!(sample.to_vec()?.iter().all(|x| x.is_finite()));\n    assert_eq!(ctx.graph_stats()?.graph_nodes, 0);\n    Ok(())\n}\n\n#[test]\nfn scheduler_matches_closed_form_and_t0_ignores_noise() -> MlResult\u003c()> {\n    let ctx = ExecutionContext::new();\n    let scheduler = DiffusionScheduler::from_betas(vec![0.1, 0.2])?;"},"tests/diffusion.rs#let dataset =":{"path":"tests/diffusion.rs","line":33,"start":31,"verified":true,"hash":"5c8550144cf9034e2f2048f3db665a1a342e1d38b2fa295d5a6a5e7ef4c0273b","code":"    optimizer.register_all(&model.parameters())?;\n    let images = [&image];\n    let dataset = UnsupervisedDataset::new(&ctx, &images)?;\n    UnsupervisedTrainer::silent(&ctx).fit(\n        &mut model,\n        &mut optimizer,\n        &dataset,\n        EpochSchedule::new(1)?,\n    )?;\n    assert_eq!(ctx.graph_stats()?.graph_nodes, 0);\n    let sample = model.sample(&[1, 1, 4, 4])?;\n    assert_eq!(sample.shape()?, vec![1, 1, 4, 4]);\n    assert!(sample.to_vec()?.iter().all(|x| x.is_finite()));\n    assert_eq!(ctx.graph_stats()?.graph_nodes, 0);\n    Ok(())\n}\n\n#[test]\nfn scheduler_matches_closed_form_and_t0_ignores_noise() -> MlResult\u003c()> {\n    let ctx = ExecutionContext::new();\n    let scheduler = DiffusionScheduler::from_betas(vec![0.1, 0.2])?;\n    let x = ctx.scalar(2.0)?;\n    let noise = ctx.scalar(0.5)?;\n    assert!("},"tests/diffusion.rs#UnsupervisedTrainer::silent":{"path":"tests/diffusion.rs","line":34,"start":32,"verified":true,"hash":"5c8550144cf9034e2f2048f3db665a1a342e1d38b2fa295d5a6a5e7ef4c0273b","code":"    let images = [&image];\n    let dataset = UnsupervisedDataset::new(&ctx, &images)?;\n    UnsupervisedTrainer::silent(&ctx).fit(\n        &mut model,\n        &mut optimizer,\n        &dataset,\n        EpochSchedule::new(1)?,\n    )?;\n    assert_eq!(ctx.graph_stats()?.graph_nodes, 0);\n    let sample = model.sample(&[1, 1, 4, 4])?;\n    assert_eq!(sample.shape()?, vec![1, 1, 4, 4]);\n    assert!(sample.to_vec()?.iter().all(|x| x.is_finite()));\n    assert_eq!(ctx.graph_stats()?.graph_nodes, 0);\n    Ok(())\n}\n\n#[test]\nfn scheduler_matches_closed_form_and_t0_ignores_noise() -> MlResult\u003c()> {\n    let ctx = ExecutionContext::new();\n    let scheduler = DiffusionScheduler::from_betas(vec![0.1, 0.2])?;\n    let x = ctx.scalar(2.0)?;\n    let noise = ctx.scalar(0.5)?;\n    assert!(\n        (scheduler.q_sample(&x, &noise, 1)?.item()?"},"tests/diffusion.rs#let sample = model.sample":{"path":"tests/diffusion.rs","line":41,"start":39,"verified":true,"hash":"5c8550144cf9034e2f2048f3db665a1a342e1d38b2fa295d5a6a5e7ef4c0273b","code":"    )?;\n    assert_eq!(ctx.graph_stats()?.graph_nodes, 0);\n    let sample = model.sample(&[1, 1, 4, 4])?;\n    assert_eq!(sample.shape()?, vec![1, 1, 4, 4]);\n    assert!(sample.to_vec()?.iter().all(|x| x.is_finite()));\n    assert_eq!(ctx.graph_stats()?.graph_nodes, 0);\n    Ok(())\n}\n\n#[test]\nfn scheduler_matches_closed_form_and_t0_ignores_noise() -> MlResult\u003c()> {\n    let ctx = ExecutionContext::new();\n    let scheduler = DiffusionScheduler::from_betas(vec![0.1, 0.2])?;\n    let x = ctx.scalar(2.0)?;\n    let noise = ctx.scalar(0.5)?;\n    assert!(\n        (scheduler.q_sample(&x, &noise, 1)?.item()?\n            - (0.72f32.sqrt() * 2.0 + 0.28f32.sqrt() * 0.5))\n            .abs()\n            \u003c 1e-6\n    );\n    let a = scheduler.reverse_step(&x, &noise, &ctx.scalar(1.0)?, 0)?;\n    let b = scheduler.reverse_step(&x, &noise, &ctx.scalar(999.0)?, 0)?;\n    assert_eq!(a.item()?, b.item()?);"},"tests/diffusion.rs#assert!(sample.to_vec()":{"path":"tests/diffusion.rs","line":43,"start":41,"verified":true,"hash":"5c8550144cf9034e2f2048f3db665a1a342e1d38b2fa295d5a6a5e7ef4c0273b","code":"    let sample = model.sample(&[1, 1, 4, 4])?;\n    assert_eq!(sample.shape()?, vec![1, 1, 4, 4]);\n    assert!(sample.to_vec()?.iter().all(|x| x.is_finite()));\n    assert_eq!(ctx.graph_stats()?.graph_nodes, 0);\n    Ok(())\n}\n\n#[test]\nfn scheduler_matches_closed_form_and_t0_ignores_noise() -> MlResult\u003c()> {\n    let ctx = ExecutionContext::new();\n    let scheduler = DiffusionScheduler::from_betas(vec![0.1, 0.2])?;\n    let x = ctx.scalar(2.0)?;\n    let noise = ctx.scalar(0.5)?;\n    assert!(\n        (scheduler.q_sample(&x, &noise, 1)?.item()?\n            - (0.72f32.sqrt() * 2.0 + 0.28f32.sqrt() * 0.5))\n            .abs()\n            \u003c 1e-6\n    );\n    let a = scheduler.reverse_step(&x, &noise, &ctx.scalar(1.0)?, 0)?;\n    let b = scheduler.reverse_step(&x, &noise, &ctx.scalar(999.0)?, 0)?;\n    assert_eq!(a.item()?, b.item()?);\n    Ok(())\n}"},"tests/diffusion.rs#fn scheduler_matches":{"path":"tests/diffusion.rs","line":49,"start":47,"verified":true,"hash":"5c8550144cf9034e2f2048f3db665a1a342e1d38b2fa295d5a6a5e7ef4c0273b","code":"\n#[test]\nfn scheduler_matches_closed_form_and_t0_ignores_noise() -> MlResult\u003c()> {\n    let ctx = ExecutionContext::new();\n    let scheduler = DiffusionScheduler::from_betas(vec![0.1, 0.2])?;\n    let x = ctx.scalar(2.0)?;\n    let noise = ctx.scalar(0.5)?;\n    assert!(\n        (scheduler.q_sample(&x, &noise, 1)?.item()?\n            - (0.72f32.sqrt() * 2.0 + 0.28f32.sqrt() * 0.5))\n            .abs()\n            \u003c 1e-6\n    );\n    let a = scheduler.reverse_step(&x, &noise, &ctx.scalar(1.0)?, 0)?;\n    let b = scheduler.reverse_step(&x, &noise, &ctx.scalar(999.0)?, 0)?;\n    assert_eq!(a.item()?, b.item()?);\n    Ok(())\n}\n\n#[test]\nfn diffusion_checkpoint_round_trip_preserves_sampling() -> Result\u003c(), Box\u003cdyn std::error::Error>> {\n    let ctx = ExecutionContext::new();\n    let make = || {\n        Diffusion::new("},"tests/diffusion.rs#fn diffusion_checkpoint":{"path":"tests/diffusion.rs","line":67,"start":65,"verified":true,"hash":"5c8550144cf9034e2f2048f3db665a1a342e1d38b2fa295d5a6a5e7ef4c0273b","code":"\n#[test]\nfn diffusion_checkpoint_round_trip_preserves_sampling() -> Result\u003c(), Box\u003cdyn std::error::Error>> {\n    let ctx = ExecutionContext::new();\n    let make = || {\n        Diffusion::new(\n            &ctx,\n            Unet::new(&ctx, 1, 2, &[1], 1, &[])?,\n            DiffusionScheduler::cosine(2, 0.008)?,\n            7,\n        )\n    };\n    let original = make()?;\n    let mut restored = make()?;\n    let path = std::env::temp_dir().join(format!(\n        \"trench-ddpm-{}-{}.tdw\",\n        std::process::id(),\n        std::time::SystemTime::now()\n            .duration_since(std::time::UNIX_EPOCH)?\n            .as_nanos()\n    ));\n    original.save_checkpoint(&path)?;\n    restored.load_checkpoint(&path)?;\n    std::fs::remove_file(&path)?;"},"tests/diffusion.rs#fn full_unet":{"path":"tests/diffusion.rs","line":12,"start":10,"verified":true,"hash":"5c8550144cf9034e2f2048f3db665a1a342e1d38b2fa295d5a6a5e7ef4c0273b","code":"\n#[test]\nfn full_unet_trains_and_samples_through_public_api() -> MlResult\u003c()> {\n    let ctx = ExecutionContext::new();\n    let unet = Unet::new(&ctx, 1, 2, &[1, 2], 1, &[0, 1])?;\n    let mut model = Diffusion::new(&ctx, unet, DiffusionScheduler::linear(2, 0.001, 0.02)?, 7)?;\n    let image = ctx.input((0..16).map(|i| i as f32 / 16.0).collect(), &[1, 1, 4, 4])?;\n    let noise = ctx.tensor(vec![0.1; 16], &[1, 1, 4, 4])?;\n    let (prediction, loss) = model.forward_loss_with_noise(&image, &noise, 1)?;\n    assert_eq!(prediction.tensor().shape()?, vec![1, 1, 4, 4]);\n    assert!(loss.tensor().item()?.is_finite());\n    loss.backward()?;\n    assert!(\n        model\n            .parameters()\n            .iter()\n            .all(|p| p.grad().is_ok_and(|g| g.is_some()))\n    );\n    drop(prediction);\n    drop(loss);\n    let mut optimizer = SGD::new(&ctx, 0.001)?;\n    optimizer.register_all(&model.parameters())?;\n    let images = [&image];\n    let dataset = UnsupervisedDataset::new(&ctx, &images)?;"},"src/lib.rs#pub mod backend":{"path":"src/lib.rs","line":2,"start":1,"verified":true,"hash":"5b1c50e549c053d4e90fead9192e20873d02fb956cf45e354dc979dc8873c2db","code":"//! Explicit, composable tensor and training runtime.\npub mod backend;\npub mod contracts;\npub mod runtime;\npub use contracts::{\n    AutogradError, BackwardOp, ContextError, ContextId, CustomOp, DataError, LossError, MlError,\n    MlResult, OpOutput, OptimError, ParameterId, Reduction, TensorBuffer, TensorError, TensorId,\n    TensorView,\n};\npub use runtime::{\n    BackwardOptions, ExecutionContext, ExecutionContextBuilder, GraphStats, MaxResult, Parameter,\n    RequiresGrad, Tensor, TopKResult, Variable,\n    ExecutionRoute, RoutedContextBuilder,\n};\npub mod tensor {\n    pub use crate::{Tensor, TensorBuffer, TensorView};\n}\npub mod loss {\n    pub use crate::{LossError, Reduction};\n}\n#[cfg(feature = \"legacyBenchmark\")]\npub mod legacy;\npub mod nn;\npub mod optimizer;"},"src/nn/diffusion.rs#pub struct Unet":{"path":"src/nn/diffusion.rs","line":185,"start":183,"verified":true,"hash":"14ca3d8800d801d0191542c38c2062902a87e7e5a1fb222e1a7936eca6ab2965","code":"\n#[derive(Debug)]\npub struct Unet {\n    config: serde_json::Value,\n    output_channels: usize,\n    context: ExecutionContext,\n    channels: usize,\n    dim: usize,\n    initial: Conv2D,\n    time1: Linear,\n    time2: Linear,\n    down: Vec\u003cStage>,\n    middle1: Residual,\n    middle_attention: Attention,\n    middle2: Residual,\n    up: Vec\u003cStage>,\n    final_residual: Residual,\n    final_conv: Conv2D,\n}\nimpl Unet {\n    pub fn new(\n        ctx: &ExecutionContext,\n        channels: usize,\n        dim: usize,"},"src/nn/layers.rs#pub trait Layer":{"path":"src/nn/layers.rs","line":7,"start":5,"verified":true,"hash":"6a2f4bf85147b2de1c740fb235ab3db186e32b7e7de51d4f9eec7664a1499634","code":"use super::{LayerState, ModelState, ParamState};\n\npub trait Layer: std::fmt::Debug {\n    fn forward(&self, input: &Variable) -> MlResult\u003cVariable>;\n    /// Compatibility entry point during the public API migration.\n    fn apply(&self, input: &Variable) -> MlResult\u003cVariable> {\n        self.validate_input(input.tensor())?;\n        self.forward(input)\n    }\n\n    fn predict(&self, input: &Tensor) -> MlResult\u003cTensor> {\n        self.validate_input(input)?;\n        input.execution_context()?.no_grad(|| {\n            let variable = input.as_variable()?;\n            let output = self.forward(&variable)?;\n            self.validate_input(output.tensor())?;\n            Ok(output.tensor().clone())\n        })\n    }\n\n    fn validate_input(&self, input: &Tensor) -> MlResult\u003c()> {\n        if input.context_id() != self.context_id() {\n            return Err(crate::ContextError::Mismatch.into());\n        }"},"src/trainer/runners.rs#pub struct UnsupervisedTrainer":{"path":"src/trainer/runners.rs","line":138,"start":136,"verified":true,"hash":"49452a00b3d5a21567fee28a3e78c635a4f0c01c698b531c49b00d4312c4fc72","code":"    }\n}\npub struct UnsupervisedTrainer {\n    service: TrainingService,\n}\nimpl UnsupervisedTrainer {\n    pub fn new(context: &ExecutionContext) -> Self {\n        Self::silent(context)\n    }\n    pub fn from_trainer(context: &ExecutionContext, trainer: Trainer) -> Self {\n        Self {\n            service: TrainingService::new(context, trainer),\n        }\n    }\n    pub fn silent(context: &ExecutionContext) -> Self {\n        Self::from_trainer(context, Trainer::silent())\n    }\n    pub fn minimal(context: &ExecutionContext) -> Self {\n        Self::from_trainer(context, Trainer::minimal())\n    }\n    pub fn default(context: &ExecutionContext) -> Self {\n        Self::from_trainer(context, Trainer::default())\n    }\n    pub fn verbose(context: &ExecutionContext) -> Self {"},"src/trainer/service.rs#pub fn finish_step":{"path":"src/trainer/service.rs","line":95,"start":93,"verified":true,"hash":"74c9653cd556a891acb9d789bb40c5ee7d7c9b4e1e5ebd6dba7fc055a20abae8","code":"    }\n    /// The caller owns the scope so loading, rollout and forward failures share cleanup.\n    pub fn finish_step\u003cM: TrainableModel>(\n        &self,\n        model: &M,\n        optimizer: &mut dyn Optimizer,\n        data: StepData,\n        batch: &BatchStartContext,\n        forward: Duration,\n    ) -> MlResult\u003cStepOutcome> {\n        validate_parameters(&self.context, model, optimizer)?;\n        self.context.validate(data.loss.tensor())?;\n        if let Some(p) = &data.prediction {\n            self.context.validate(p.tensor())?;\n        }\n        if let Some(t) = &data.target {\n            self.context.validate(t)?;\n        }\n        let loss = data.loss.tensor().item()?;\n        if !loss.is_finite() {\n            return Err(crate::TensorError::InvalidOperation {\n                op: \"training\",\n                reason: \"non-finite loss\".into(),\n            }"},"src/trainer/data/prebatched.rs#pub struct UnsupervisedDataset":{"path":"src/trainer/data/prebatched.rs","line":180,"start":178,"verified":true,"hash":"e926b998694acbad4f9ac7f826f0305fab02f3c9bcc45873c963bbcd5058ae9a","code":"\n#[derive(Clone, Copy)]\npub struct UnsupervisedDataset\u003c'a> {\n    pub samples: &'a [&'a Variable],\n}\nimpl\u003c'a> UnsupervisedDataset\u003c'a> {\n    pub fn new(context: &crate::ExecutionContext, samples: &'a [&'a Variable]) -> MlResult\u003cSelf> {\n        if samples.is_empty() {\n            return Err(MlError::StringError(\n                \"unsupervised dataset must not be empty\".into(),\n            ));\n        }\n        for sample in samples {\n            context.validate(sample.tensor())?;\n        }\n        Ok(Self { samples })\n    }\n}\n\n#[derive(Clone, Copy)]\npub struct SemiSupervisedDataset\u003c'a> {\n    pub labeled_inputs: &'a [&'a Variable],\n    pub labeled_targets: &'a [&'a Tensor],\n    pub unlabeled_inputs: &'a [&'a Variable],"},"src/runtime/dispatch.rs#pub fn execute":{"path":"src/runtime/dispatch.rs","line":33,"start":31,"verified":true,"hash":"543525a0786c4d8420f632a9174f85e3c6ac5a401e9ca84661257e69596abebd","code":"        compute(&views)\n    }\n    pub fn execute(&self, operation: &Operation, inputs: &[&Tensor]) -> MlResult\u003cVec\u003cTensor>> {\n        #[cfg(feature=\"debugging\")]\n        let _trace=tracing::debug_span!(\"tensor_operation\",context=?self.id(),operation=operation.name(),inputs=inputs.len()).entered();\n        if inputs.is_empty() || operation.input_count().is_some_and(|n| n != inputs.len()) {\n            return Err(TensorError::InvalidOperation {\n                op: operation.name(),\n                reason: \"invalid input count\".into(),\n            }\n            .into());\n        }\n        // Loss targets are constants even when their handles happen to be tracked.\n        for input in inputs {\n            self.validate(input)?;\n        }\n        let tracked = self.tracking(if matches!(operation, Operation::Loss { .. }) {\n            &inputs[..1]\n        } else {\n            inputs\n        })?;\n        #[cfg(feature = \"legacyBenchmark\")]\n        if self.route() == ExecutionRoute::Legacy {\n            return self.execute_legacy(operation, inputs, tracked);"},"src/runtime/backward.rs#pub fn backward":{"path":"src/runtime/backward.rs","line":3,"start":1,"verified":true,"hash":"11ab9676de8c43881655bdc6604402cc5d1f37bf6b8d5e5a9a9196b4beb9ec8d","code":"use super::*;\nimpl ExecutionContext {\n    pub fn backward(&self, output: &Variable, options: BackwardOptions\u003c'_>) -> MlResult\u003c()> {\n        #[cfg(feature = \"debugging\")]\n        let _trace =\n            tracing::debug_span!(\"backward\",context=?self.id(),output=?output.tensor().id())\n                .entered();\n        self.backward_observed(output, options, |_| Ok(()))\n    }\n    pub(super) fn backward_observed(\n        &self,\n        output: &Variable,\n        options: BackwardOptions\u003c'_>,\n        observe: impl FnOnce(&State) -> MlResult\u003c()>,\n    ) -> MlResult\u003c()> {\n        self.validate(output.tensor())?;\n        #[cfg(feature = \"legacyBenchmark\")]\n        if self.route() == ExecutionRoute::Legacy {\n            return self.backward_legacy(output, options, observe);\n        }\n        let mut state = self\n            .inner\n            .state\n            .try_borrow_mut()"},"src/runtime/legacy_session.rs#pub(super) fn execute_legacy":{"path":"src/runtime/legacy_session.rs","line":134,"start":132,"verified":true,"hash":"5f04a758a41da718d05b535e4574d2e81317f8b0553bd223478c4f4c6e862c66","code":"}\nimpl super::ExecutionContext {\n    pub(super) fn execute_legacy(\n        &self,\n        operation: &crate::contracts::Operation,\n        inputs: &[&super::Tensor],\n        tracked: bool,\n    ) -> MlResult\u003cVec\u003csuper::Tensor>> {\n        let bridge = self\n            .inner\n            .state\n            .borrow()\n            .legacy\n            .clone()\n            .ok_or_else(|| unsupported(\"native session\"))?;\n        let native = {\n            let mut bridge = bridge\n                .try_borrow_mut()\n                .map_err(|_| ContextError::BorrowConflict)?;\n            let handles = inputs\n                .iter()\n                .map(|input| bridge.handle(input.id()))\n                .collect::\u003cMlResult\u003cVec\u003c_>>>()?;\n            if tracked {"},"src/backend/cpu/operations/forward.rs#impl OperationProvider":{"path":"src/backend/cpu/operations/forward.rs","line":4,"start":2,"verified":true,"hash":"d5a34e4144e565547f795856efcef6451b96477ce6d8f195634a623982e9a12c","code":"\nuse crate::backend::CpuBackend;\nimpl OperationProvider for CpuBackend {\n    fn supports_prepared_replay(&self) -> bool { true }\n    fn execute(\n        &self,\n        operation: &Operation,\n        inputs: &[TensorView\u003c'_>],\n    ) -> MlResult\u003cOperationOutput> {\n        if inputs.is_empty() || operation.input_count().is_some_and(|n| n != inputs.len()) {\n            return Err(TensorError::InvalidOperation {\n                op: operation.name(),\n                reason: \"invalid input count\".into(),\n            }\n            .into());\n        }\n        let values = inputs\n            .iter()\n            .map(|x| (*x).to_owned())\n            .collect::\u003cMlResult\u003cVec\u003c_>>>()?;\n        let x = &values[0];\n        let mut saved = Vec::new();\n        let (output, backward) = match operation {\n            Operation::Add | Operation::Sub | Operation::Mul | Operation::Div => {"},"src/tensor/operators/conv2d.rs#fn backward":{"path":"src/tensor/operators/conv2d.rs","line":261,"start":259,"verified":true,"hash":"42d2664e659c74d11f2bba608e6101f8184fe9818baa953f95e96b6b964517d9","code":"\n    #[cfg(feature = \"enableBackward\")]\n    fn backward(\n        &self,\n        targets: &[&dyn TensorBase],\n        grad: &dyn TensorBase,\n    ) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        let input  = targets[0];\n        let weight = targets[1];\n        let stride_h = targets[3].data()[0] as usize;\n        let stride_w = targets[4].data()[0] as usize;\n        let pad_h    = targets[5].data()[0] as usize;\n        let pad_w    = targets[6].data()[0] as usize;\n\n        let in_shape = input.shape();\n        let w_shape  = weight.shape();\n        let (n, _c_in, _h, _w) = (in_shape[0], in_shape[1], in_shape[2], in_shape[3]);\n        let (c_out, _c_in2, kh, kw) = (w_shape[0], w_shape[1], w_shape[2], w_shape[3]);\n\n        #[cfg(feature = \"debugging\")]\n        tracing::debug!(\n            \"[Conv2dOp::backward] in={:?} w={:?} {}\",\n            in_shape, w_shape,\n            crate::legacy::tensor::operators::debug::summary(\"grad\", grad)"},"src/optimizer/algorithms.rs#fn step(&mut self)":{"path":"src/optimizer/algorithms.rs","line":18,"start":16,"verified":true,"hash":"7e64b46dd5a47bc799a520eb35a227aa9ef92828ed3799a15343063e8ec78d3d","code":"        Ok(())\n    }\n    fn step(&mut self) -> MlResult\u003c()>;\n    fn zero_grad(&self) -> MlResult\u003c()>;\n    fn lr(&self) -> f32;\n    fn set_lr(&mut self, learning_rate: f32) -> MlResult\u003c()>;\n    fn registered_param_count(&self) -> usize;\n    fn registered_parameters(&self) -> Vec\u003c&Parameter>;\n    fn context_id(&self) -> ContextId;\n}\n\n#[derive(Clone, Copy, Debug)]\nenum Algorithm {\n    Sgd,\n    Momentum {\n        momentum: f32,\n    },\n    AdaGrad {\n        epsilon: f32,\n    },\n    RmsProp {\n        rho: f32,\n        epsilon: f32,\n    },"},"src/runtime/prepared/mod.rs#//! Explicit P1":{"path":"src/runtime/prepared/mod.rs","line":1,"start":1,"verified":true,"hash":"c315eff8bfa03439468d739d9eec4a34c74ec700d562b77528cb58861e8113a9","code":"//! Explicit P1 preparation, initially an **allocating replay** implementation.\n//!\n//! Preparation validates immutable descriptions without executing tensors or RNG.\n//! Each run still uses eager kernels and records its own backward graph. There is\n//! no buffer arena, cached backward order, implicit cache, or default-mode change.\n//! S2/S3/S4 will replace these costs; callers must not treat S0 as static memory.\nmod executor;\nmod plan;\nmod prepare;\npub use plan::{PreparedMode, PreparedPlan, PreparedProgram, TensorSlotId};\n\nfn invalid(reason: impl Into\u003cString>) -> crate::MlError {\n    crate::TensorError::InvalidOperation {\n        op: \"prepared replay\",\n        reason: reason.into(),\n    }\n    .into()\n}\n"},"src/nn/checkpoint.rs#pub struct ModelState":{"path":"src/nn/checkpoint.rs","line":11,"start":9,"verified":true,"hash":"9af297bde94588b373770c6a858697f4ca137ee55e7a76f2cb32a21ed8f35fda","code":"\n#[derive(Serialize, Deserialize, Debug, Clone)]\npub struct ModelState {\n    pub version: String,\n    pub layers: Vec\u003cLayerState>,\n}\n\n#[derive(Serialize, Deserialize, Debug, Clone)]\npub struct LayerState {\n    pub layer_type: String,\n    pub label: String,\n    pub config: serde_json::Value,\n    pub params: Vec\u003cParamState>,\n}\n\n#[derive(Serialize, Deserialize, Debug, Clone)]\npub struct ParamState {\n    pub name: String,\n    pub shape: Vec\u003cusize>,\n    /// JSON 포맷에서 실제 가중치 데이터. binary 포맷에서는 비어있음.\n    #[serde(default)]\n    pub data: Vec\u003cf32>,\n    /// binary(.tdw) 포맷 전용: blob 내 f32 시작 인덱스\n    #[serde(skip_serializing_if = \"Option::is_none\")]"},"src/trainer/legacy_mod.rs##[path = \"unsupervised.rs\"]":{"path":"src/trainer/legacy_mod.rs","line":32,"start":30,"verified":true,"hash":"9e6d425f6942d1c92e56a80db06e181fbd78eb3797d982526b0b3a62bb420008","code":"#[path = \"supervised.rs\"]\npub mod supervised;\n#[path = \"unsupervised.rs\"]\npub mod unsupervised;\n#[path = \"semi_supervised.rs\"]\npub mod semi_supervised;\n#[path = \"legacy_reinforcement.rs\"]\npub mod reinforcement;\n#[path = \"autoregressive.rs\"]\npub mod autoregressive;\n#[path = \"data/legacy_mod.rs\"]\npub mod data;\n#[path = \"legacy_progress.rs\"]\npub(crate) mod progress;\n\npub use core::{\n    TrainerCore, LogConfig, TrainerConfig, TrainingRuntime, Metrics, TrainerBuilder,\n    MetricHook, BatchContext, Convergence,\n    grad_norm, weight_norm, update_ratio, has_invalid_grad,\n    argmax, ClassificationAccuracy, Perplexity,\n    EpochStep, StepOutput, StepDiagnostics, BatchObservations, EpochOutcome,\n    TrainingObserver, BatchStartContext, BatchEndContext, EpochContext, TrainStartContext,\n    TrainEndContext,\n};"},"src/runtime/mod.rs#pub fn new() -> Self":{"path":"src/runtime/mod.rs","line":358,"start":356,"verified":true,"hash":"e74878e25ca08409a65ed2cebb3f7a3abc2d6e6b69fb8140586d784ea8bb5bc2","code":"        builder\n    }\n    pub fn new() -> Self {\n        Self::builder().build()\n    }\n    pub fn id(&self) -> ContextId {\n        self.id\n    }\n    fn collect(&self) -> MlResult\u003c()> {\n        if self.inner.gc_pending.get() {\n            self.inner\n                .state\n                .try_borrow_mut()\n                .map_err(|_| ContextError::BorrowConflict)?\n                .collect()?;\n            self.inner.gc_pending.set(false);\n        }\n        Ok(())\n    }\n    pub fn validate(&self, tensor: &Tensor) -> MlResult\u003c()> {\n        self.collect()?;\n        if tensor.context_id() != self.id {\n            return Err(ContextError::Mismatch.into());\n        }"},"src/nn/diffusion.rs#impl UnsupervisedModel for Diffusion":{"path":"src/nn/diffusion.rs","line":622,"start":620,"verified":true,"hash":"14ca3d8800d801d0191542c38c2062902a87e7e5a1fb222e1a7936eca6ab2965","code":"    }\n}\nimpl UnsupervisedModel for Diffusion {\n    fn forward_loss(&mut self, image: &Variable) -> MlResult\u003c(Variable, Variable)> {\n        let noise = self.noise(&image.tensor().shape()?)?;\n        let t = self.noise.random_range(0..self.scheduler.timesteps());\n        self.forward_loss_with_noise(image, &noise, t)\n    }\n}\n\nimpl crate::trainer::CheckpointableModel for Diffusion {\n    fn save_checkpoint(&self, path: &std::path::Path) -> MlResult\u003c()> {\n        let path = path\n            .to_str()\n            .ok_or_else(|| invalid(\"checkpoint path is not valid UTF-8\"))?;\n        let mut params = Vec::new();\n        for (i, p) in self.parameters().iter().enumerate() {\n            params.push(super::ParamState {\n                name: format!(\"parameter_{i}\"),\n                shape: p.tensor().shape()?,\n                data: p.tensor().to_vec()?,\n                blob_offset: None,\n                blob_length: None,\n            });"},"src/nn/diffusion.rs#fn noise(&mut self":{"path":"src/nn/diffusion.rs","line":546,"start":544,"verified":true,"hash":"14ca3d8800d801d0191542c38c2062902a87e7e5a1fb222e1a7936eca6ab2965","code":"        })\n    }\n    fn noise(&mut self, shape: &[usize]) -> MlResult\u003cTensor> {\n        let count = shape\n            .iter()\n            .try_fold(1usize, |n, d| n.checked_mul(*d))\n            .ok_or_else(|| invalid(\"shape overflow\"))?;\n        let data = (0..count)\n            .map(|_| {\n                (-2.0 * self.noise.random::\u003cf32>().max(f32::MIN_POSITIVE).ln()).sqrt()\n                    * (std::f32::consts::TAU * self.noise.random::\u003cf32>()).cos()\n            })\n            .collect();\n        self.context.tensor(data, shape)\n    }\n    pub fn forward_loss_with_noise(\n        &self,\n        image: &Variable,\n        noise: &Tensor,\n        t: usize,\n    ) -> MlResult\u003c(Variable, Variable)> {\n        let noisy = self\n            .scheduler\n            .q_sample(image.tensor(), noise, t)?"},"src/nn/diffusion.rs#pub fn q_sample":{"path":"src/nn/diffusion.rs","line":475,"start":473,"verified":true,"hash":"14ca3d8800d801d0191542c38c2062902a87e7e5a1fb222e1a7936eca6ab2965","code":"        self.betas.len()\n    }\n    pub fn q_sample(&self, image: &Tensor, noise: &Tensor, t: usize) -> MlResult\u003cTensor> {\n        let &alpha = self\n            .alpha_bars\n            .get(t)\n            .ok_or_else(|| invalid(\"timestep out of range\"))?;\n        if image.shape()? != noise.shape()? {\n            return Err(invalid(\"noise shape differs from image\"));\n        }\n        let ctx = image.execution_context()?;\n        image\n            .mul(&ctx.scalar(alpha.sqrt())?)?\n            .add(&noise.mul(&ctx.scalar((1.0 - alpha).sqrt())?)?)\n    }\n    pub fn reverse_step(\n        &self,\n        image: &Tensor,\n        prediction: &Tensor,\n        noise: &Tensor,\n        t: usize,\n    ) -> MlResult\u003cTensor> {\n        let &beta = self\n            .betas"},"src/nn/diffusion.rs#pub fn forward(&self, input: &Variable":{"path":"src/nn/diffusion.rs","line":316,"start":314,"verified":true,"hash":"14ca3d8800d801d0191542c38c2062902a87e7e5a1fb222e1a7936eca6ab2965","code":"    }\n    /// Timesteps are normalized to [0, 1] with shape [batch, 1].\n    pub fn forward(&self, input: &Variable, timesteps: &Tensor) -> MlResult\u003cVariable> {\n        self.context.validate(input.tensor())?;\n        self.context.validate(timesteps)?;\n        let shape = input.tensor().shape()?;\n        let divisor = 1usize\n            .checked_shl(self.down.len() as u32)\n            .ok_or_else(|| invalid(\"too many stages\"))?;\n        if shape.len() != 4\n            || shape[0] == 0\n            || shape[1] != self.channels\n            || shape[2] == 0\n            || shape[3] == 0\n            || shape[2] % divisor != 0\n            || shape[3] % divisor != 0\n        {\n            return Err(invalid(\n                \"expected NCHW images with spatial dimensions divisible by 2^stages\",\n            ));\n        }\n        if timesteps.shape()? != vec![shape[0], 1] {\n            return Err(invalid(\"timesteps must have shape [batch, 1]\"));\n        }"},"src/nn/diffusion.rs#let loss = prediction.mse_loss":{"path":"src/nn/diffusion.rs","line":578,"start":576,"verified":true,"hash":"14ca3d8800d801d0191542c38c2062902a87e7e5a1fb222e1a7936eca6ab2965","code":"        )?;\n        let prediction = self.unet.forward(&noisy, &times)?;\n        let loss = prediction.mse_loss(noise, crate::Reduction::Mean)?;\n        Ok((prediction, loss))\n    }\n    pub fn sample_with_noise(&self, initial: &Tensor, step_noise: &[Tensor]) -> MlResult\u003cTensor> {\n        self.context.validate(initial)?;\n        if step_noise.len() != self.scheduler.timesteps() {\n            return Err(invalid(\"one noise tensor per timestep is required\"));\n        }\n        self.context.no_grad(|| {\n            let mut image = initial.clone();\n            let shape = image.shape()?;\n            if shape.len() != 4 {\n                return Err(invalid(\"expected NCHW image\"));\n            }\n            for t in (0..self.scheduler.timesteps()).rev() {\n                let times = self.context.tensor(\n                    vec![t as f32 / self.scheduler.timesteps() as f32; shape[0]],\n                    &[shape[0], 1],\n                )?;\n                let prediction = self.unet.predict(&image, &times)?;\n                image = self\n                    .scheduler"},"src/runtime/receiver.rs#pub fn backward":{"path":"src/runtime/receiver.rs","line":1,"start":1,"verified":false,"hash":"a19ca9815b02f16c0660bd3424cdc7e87b4e29c3ecedea59c86df5d2f06d6641","code":"use super::*;\nimpl ExecutionContext {\n    pub fn add(&self, input: &Tensor, rhs: &Tensor) -> MlResult\u003cTensor> {\n        self.single(Operation::Add, &[input, rhs])\n    }\n}\nimpl Tensor {\n    pub fn add(&self, rhs: &Tensor) -> MlResult\u003cSelf> {\n        self.execution_context()?.add(self, rhs)\n    }\n}\nimpl Variable {\n    pub fn add(&self, rhs: &Tensor) -> MlResult\u003cSelf> {\n        self.tensor.add(rhs)?.as_variable()\n    }\n}\nimpl ExecutionContext {\n    pub fn sub(&self, input: &Tensor, rhs: &Tensor) -> MlResult\u003cTensor> {\n        self.single(Operation::Sub, &[input, rhs])\n    }\n}\nimpl Tensor {\n    pub fn sub(&self, rhs: &Tensor) -> MlResult\u003cSelf> {\n        self.execution_context()?.sub(self, rhs)"},"src/trainer/data/prebatched.rs#impl IntoBatchLoader for &UnsupervisedDataset":{"path":"src/trainer/data/prebatched.rs","line":71,"start":69,"verified":true,"hash":"e926b998694acbad4f9ac7f826f0305fab02f3c9bcc45873c963bbcd5058ae9a","code":"    }\n}\nimpl IntoBatchLoader for &UnsupervisedDataset\u003c'_> {\n    type Batch = UnsupervisedBatch;\n    type Loader = Prebatched\u003cSelf::Batch>;\n    fn into_batch_loader(self) -> Self::Loader {\n        prebatched(\n            self.samples\n                .iter()\n                .map(|x| UnsupervisedBatch {\n                    samples: (*x).clone(),\n                })\n                .collect(),\n        )\n    }\n}\nimpl IntoBatchLoader for &AutoregressiveDataset\u003c'_> {\n    type Batch = AutoregressiveBatch;\n    type Loader = AutoregressivePrebatched;\n    fn into_batch_loader(self) -> Self::Loader {\n        AutoregressivePrebatched {\n            inner: prebatched(\n                self.sequences\n                    .iter()"},"src/trainer/runners.rs#impl UnsupervisedTrainer":{"path":"src/trainer/runners.rs","line":141,"start":139,"verified":true,"hash":"49452a00b3d5a21567fee28a3e78c635a4f0c01c698b531c49b00d4312c4fc72","code":"    service: TrainingService,\n}\nimpl UnsupervisedTrainer {\n    pub fn new(context: &ExecutionContext) -> Self {\n        Self::silent(context)\n    }\n    pub fn from_trainer(context: &ExecutionContext, trainer: Trainer) -> Self {\n        Self {\n            service: TrainingService::new(context, trainer),\n        }\n    }\n    pub fn silent(context: &ExecutionContext) -> Self {\n        Self::from_trainer(context, Trainer::silent())\n    }\n    pub fn minimal(context: &ExecutionContext) -> Self {\n        Self::from_trainer(context, Trainer::minimal())\n    }\n    pub fn default(context: &ExecutionContext) -> Self {\n        Self::from_trainer(context, Trainer::default())\n    }\n    pub fn verbose(context: &ExecutionContext) -> Self {\n        Self::from_trainer(context, Trainer::verbose())\n    }\n    pub fn with_seed(mut self, seed: u64) -> Self {"},"src/optimizer/algorithms.rs#pub trait Optimizer":{"path":"src/optimizer/algorithms.rs","line":7,"start":5,"verified":true,"hash":"7e64b46dd5a47bc799a520eb35a227aa9ef92828ed3799a15343063e8ec78d3d","code":"use super::OptimError;\n\npub trait Optimizer {\n    fn register(&mut self, parameter: &Parameter) -> MlResult\u003c()>;\n    fn register_all(&mut self, parameters: &[&Parameter]) -> MlResult\u003c()> {\n        let mut seen = std::collections::HashSet::new();\n        for parameter in parameters {\n            if seen.insert(parameter.id()) {\n                self.register(parameter)?;\n            }\n        }\n        Ok(())\n    }\n    fn step(&mut self) -> MlResult\u003c()>;\n    fn zero_grad(&self) -> MlResult\u003c()>;\n    fn lr(&self) -> f32;\n    fn set_lr(&mut self, learning_rate: f32) -> MlResult\u003c()>;\n    fn registered_param_count(&self) -> usize;\n    fn registered_parameters(&self) -> Vec\u003c&Parameter>;\n    fn context_id(&self) -> ContextId;\n}\n\n#[derive(Clone, Copy, Debug)]\nenum Algorithm {"},"src/nn/diffusion.rs#pub fn sample_with_noise":{"path":"src/nn/diffusion.rs","line":581,"start":579,"verified":true,"hash":"14ca3d8800d801d0191542c38c2062902a87e7e5a1fb222e1a7936eca6ab2965","code":"        Ok((prediction, loss))\n    }\n    pub fn sample_with_noise(&self, initial: &Tensor, step_noise: &[Tensor]) -> MlResult\u003cTensor> {\n        self.context.validate(initial)?;\n        if step_noise.len() != self.scheduler.timesteps() {\n            return Err(invalid(\"one noise tensor per timestep is required\"));\n        }\n        self.context.no_grad(|| {\n            let mut image = initial.clone();\n            let shape = image.shape()?;\n            if shape.len() != 4 {\n                return Err(invalid(\"expected NCHW image\"));\n            }\n            for t in (0..self.scheduler.timesteps()).rev() {\n                let times = self.context.tensor(\n                    vec![t as f32 / self.scheduler.timesteps() as f32; shape[0]],\n                    &[shape[0], 1],\n                )?;\n                let prediction = self.unet.predict(&image, &times)?;\n                image = self\n                    .scheduler\n                    .reverse_step(&image, &prediction, &step_noise[t], t)?;\n            }\n            Ok(image)"},"src/nn/diffusion.rs#pub fn reverse_step":{"path":"src/nn/diffusion.rs","line":488,"start":486,"verified":true,"hash":"14ca3d8800d801d0191542c38c2062902a87e7e5a1fb222e1a7936eca6ab2965","code":"            .add(&noise.mul(&ctx.scalar((1.0 - alpha).sqrt())?)?)\n    }\n    pub fn reverse_step(\n        &self,\n        image: &Tensor,\n        prediction: &Tensor,\n        noise: &Tensor,\n        t: usize,\n    ) -> MlResult\u003cTensor> {\n        let &beta = self\n            .betas\n            .get(t)\n            .ok_or_else(|| invalid(\"timestep out of range\"))?;\n        if image.shape()? != prediction.shape()? || image.shape()? != noise.shape()? {\n            return Err(invalid(\"reverse-step shapes differ\"));\n        }\n        let ctx = image.execution_context()?;\n        ctx.validate(noise)?;\n        let alpha_bar = self.alpha_bars[t];\n        let coefficient = beta / (1.0 - alpha_bar).sqrt().max(1e-8);\n        let mean = image\n            .sub(&prediction.mul(&ctx.scalar(coefficient)?)?)?\n            .mul(&ctx.scalar(1.0 / (1.0 - beta).sqrt())?)?;\n        if t == 0 {"},"src/nn/diffusion.rs#impl crate::trainer::CheckpointableModel":{"path":"src/nn/diffusion.rs","line":630,"start":628,"verified":true,"hash":"14ca3d8800d801d0191542c38c2062902a87e7e5a1fb222e1a7936eca6ab2965","code":"}\n\nimpl crate::trainer::CheckpointableModel for Diffusion {\n    fn save_checkpoint(&self, path: &std::path::Path) -> MlResult\u003c()> {\n        let path = path\n            .to_str()\n            .ok_or_else(|| invalid(\"checkpoint path is not valid UTF-8\"))?;\n        let mut params = Vec::new();\n        for (i, p) in self.parameters().iter().enumerate() {\n            params.push(super::ParamState {\n                name: format!(\"parameter_{i}\"),\n                shape: p.tensor().shape()?,\n                data: p.tensor().to_vec()?,\n                blob_offset: None,\n                blob_length: None,\n            });\n        }\n        super::ModelState::new(vec![super::LayerState {\n            layer_type: \"Diffusion\".into(),\n            label: \"ddpm\".into(),\n            config: serde_json::json!({\"unet\":self.unet.config,\"betas\":self.scheduler.betas}),\n            params,\n        }])\n        .save(path)"},"src/runtime/mod.rs#pub struct ExecutionContext":{"path":"src/runtime/mod.rs","line":38,"start":36,"verified":true,"hash":"e74878e25ca08409a65ed2cebb3f7a3abc2d6e6b69fb8140586d784ea8bb5bc2","code":"\n#[derive(Debug, Default)]\npub struct ExecutionContextBuilder {\n    providers_configured: bool,\n    initialization_seed: Option\u003cu64>,\n    model_seed: Option\u003cu64>,\n    storage: Option\u003cBox\u003cdyn TensorStore>>,\n    autograd: Option\u003cBox\u003cdyn AutogradEngine>>,\n    operations: Option\u003cBox\u003cdyn OperationProvider>>,\n}\n/// Selects an execution implementation, independently of graph execution mode.\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)]\npub enum ExecutionRoute {\n    #[default]\n    P1,\n    Legacy,\n}\n\n/// Fallible route selection preserves the existing infallible default builder.\n#[derive(Debug)]\npub struct RoutedContextBuilder {\n    builder: ExecutionContextBuilder,\n    route: ExecutionRoute,\n}"},"src/backend/cpu/storage.rs#pub struct CpuTensorStore":{"path":"src/backend/cpu/storage.rs","line":6,"start":4,"verified":true,"hash":"0c3776a48f279231d2d4961b3530336bbc074b50b701afbc28d86c8a346d750b","code":"\n#[derive(Debug, Default)]\npub struct CpuTensorStore {\n    buffers: HashMap\u003cTensorId, Rc\u003cRefCell\u003cTensorBuffer>>>,\n}\nimpl TensorStore for CpuTensorStore {\n    fn insert(&mut self, id: TensorId, buffer: TensorBuffer) -> MlResult\u003c()> {\n        if self.buffers.contains_key(&id) {\n            return Err(crate::TensorError::InvalidOperation {\n                op: \"insert\",\n                reason: \"duplicate tensor ID\".into(),\n            }\n            .into());\n        }\n        self.buffers.insert(id, Rc::new(RefCell::new(buffer)));\n        Ok(())\n    }\n    fn alias(&mut self, id: TensorId, source: TensorId) -> MlResult\u003c()> {\n        if self.buffers.contains_key(&id) {\n            return Err(crate::TensorError::InvalidOperation {\n                op: \"alias\",\n                reason: \"duplicate tensor ID\".into(),\n            }\n            .into());"},"src/runtime/legacy_session.rs#impl TensorStore for Store":{"path":"src/runtime/legacy_session.rs","line":42,"start":40,"verified":true,"hash":"5f04a758a41da718d05b535e4574d2e81317f8b0553bd223478c4f4c6e862c66","code":"#[derive(Debug)]\nstruct Store(Rc\u003cRefCell\u003cBridge>>);\nimpl TensorStore for Store {\n    fn insert(&mut self, id: TensorId, buffer: TensorBuffer) -> MlResult\u003c()> {\n        let mut bridge = self\n            .0\n            .try_borrow_mut()\n            .map_err(|_| ContextError::BorrowConflict)?;\n        let handle = bridge.session.tensor(buffer)?;\n        bridge.handles.insert(id, handle);\n        Ok(())\n    }\n    fn alias(&mut self, _id: TensorId, _source: TensorId) -> MlResult\u003c()> {\n        Err(unsupported(\"detach alias\"))\n    }\n    fn with_view(\n        &self,\n        id: TensorId,\n        visitor: &mut dyn FnMut(TensorView\u003c'_>) -> MlResult\u003c()>,\n    ) -> MlResult\u003c()> {\n        let bridge = self\n            .0\n            .try_borrow()\n            .map_err(|_| ContextError::BorrowConflict)?;"},"src/runtime/legacy_session.rs#fn execute_many":{"path":"src/runtime/legacy_session.rs","line":341,"start":339,"verified":true,"hash":"5f04a758a41da718d05b535e4574d2e81317f8b0553bd223478c4f4c6e862c66","code":"        Ok(self.execute_many(operation, handles)?.remove(0))\n    }\n    fn execute_many(\n        &mut self,\n        operation: &crate::contracts::Operation,\n        handles: &[NativeHandle],\n    ) -> MlResult\u003cVec\u003cNativeHandle>> {\n        use crate::contracts::{LossKind, Operation, Reduction};\n        if handles.is_empty() || operation.input_count().is_some_and(|n| n != handles.len()) {\n            return Err(TensorError::InvalidOperation {\n                op: \"legacy execution\",\n                reason: \"invalid input count\".into(),\n            }\n            .into());\n        }\n        let mut inputs = handles\n            .iter()\n            .map(|&h| self.get(h))\n            .collect::\u003cMlResult\u003cVec\u003c_>>>()?;\n        // Public loss targets are constants, including when pred/target are the\n        // same tracked handle. A separate native leaf severs that graph edge.\n        let loss_target = if matches!(operation, Operation::Loss { .. }) {\n            let target = inputs[1].tensor();\n            Some(Variable::new("},"src/backend/cpu/operations/kernels.rs#":{"path":"src/backend/cpu/operations/kernels.rs","line":1,"start":1,"verified":true,"hash":"dcf3110c6ccd597a5e224b98d5a67d3371235c49fd4544acb4fd7131d3b47758","code":"use super::*;\n\npub(crate) fn group_norm_spec(\n    input: &[usize],\n    gamma: &[usize],\n    beta: &[usize],\n    groups: usize,\n    epsilon: f32,\n) -> MlResult\u003c(usize, usize, usize, usize, usize, usize)> {\n    let channels = input.get(1).copied().unwrap_or_default();\n    let group_size = if groups == 0 { 0 } else { channels / groups };\n    let elements_per_group = input\n        .get(2)\n        .and_then(|height| height.checked_mul(*input.get(3)?))\n        .and_then(|spatial| spatial.checked_mul(group_size));\n    if input.len() != 4\n        || groups == 0\n        || channels == 0\n        || channels % groups != 0\n        || gamma != [channels]\n        || beta != [channels]\n        || !epsilon.is_finite()\n        || epsilon \u003c= 0.0\n        || elements_per_group.is_none_or(|elements| elements == 0)"},"src/tensor/operators/conv2d.rs#fn forward":{"path":"src/tensor/operators/conv2d.rs","line":182,"start":180,"verified":true,"hash":"42d2664e659c74d11f2bba608e6101f8184fe9818baa953f95e96b6b964517d9","code":"    }\n\n    fn forward(&self, targets: &[&dyn TensorBase]) -> MlResult\u003cVec\u003cGlobalTensor\u003cf32>>> {\n        if targets.len() \u003c 7 {\n            return Err(MlError::StringError(\n                \"Conv2dOp::forward: [input, weight, bias, stride_h, stride_w, pad_h, pad_w] 필요\".into(),\n            ));\n        }\n        let input  = targets[0];\n        let weight = targets[1];\n        let bias   = targets[2];\n        let stride_h = targets[3].data()[0] as usize;\n        let stride_w = targets[4].data()[0] as usize;\n        let pad_h    = targets[5].data()[0] as usize;\n        let pad_w    = targets[6].data()[0] as usize;\n\n        let in_shape = input.shape();\n        if in_shape.len() != 4 {\n            return Err(MlError::StringError(\n                format!(\"Conv2d: 입력은 4D [N, C_in, H, W] 이어야 합니다. 현재: {:?}\", in_shape)\n            ));\n        }\n        let w_shape = weight.shape();\n        if w_shape.len() != 4 {"},"src/runtime/dispatch.rs#fn commit":{"path":"src/runtime/dispatch.rs","line":105,"start":103,"verified":true,"hash":"543525a0786c4d8420f632a9174f85e3c6ac5a401e9ca84661257e69596abebd","code":"            .ok_or_else(|| TensorError::EmptyTensor.into())\n    }\n    fn commit(\n        &self,\n        name: &'static str,\n        inputs: &[&Tensor],\n        result: OperationOutput,\n        tracked: bool,\n    ) -> MlResult\u003cVec\u003cTensor>> {\n        if tracked && (result.backward.is_none() || result.outputs.len() != 1) {\n            return Err(AutogradError::BackwardNotSupported(name.into()).into());\n        }\n        if let Some(op) = &result.backward {\n            if op.input_count() != inputs.len() {\n                return Err(AutogradError::BackwardArityMismatch {\n                    expected: inputs.len(),\n                    got: op.input_count(),\n                }\n                .into());\n            }\n        }\n        let outputs = result\n            .outputs\n            .into_iter()"},"src/tensor/graph.rs#pub(crate) fn add_input":{"path":"src/tensor/graph.rs","line":60,"start":58,"verified":true,"hash":"16acb4f34c4fff8bf315fc82e7bbf06d532eeb45c88a51d13155485ede42754c","code":"    }\n\n    pub(crate) fn add_input(&mut self, variable: &Variable) -> NodeId {\n        let node_id = variable.node_id();\n        let node_idx = self.nodes.len();\n\n        #[cfg(feature = \"debugging\")]\n        tracing::debug!(\n            \"[Graph::add_input] id={:?} shape={:?} (total nodes after: {})\",\n            node_id,\n            variable.tensor().shape(),\n            node_idx + 1\n        );\n\n        #[cfg(feature = \"enableVisualization\")]\n        {\n            if crate::legacy::visualization::recording::is_active() {\n                let (label, role) = variable.visualization_metadata();\n                crate::legacy::visualization::recording::record_node(\n                    node_id,\n                    variable.tensor(),\n                    label,\n                    role,\n                    crate::legacy::visualization::NodeRole::Input,"},"src/runtime/backward.rs#for record in records.iter().rev()":{"path":"src/runtime/backward.rs","line":70,"start":68,"verified":true,"hash":"11ab9676de8c43881655bdc6604402cc5d1f37bf6b8d5e5a9a9196b4beb9ec8d","code":"        state.gradients.retain(|id, _| !seen.contains(id));\n        state.gradients.insert(root, seed);\n        for record in records.iter().rev() {\n            let Some(grad) = state.gradients.get(&record.output).cloned() else {\n                continue;\n            };\n            let inputs = record\n                .inputs\n                .iter()\n                .map(|&id| state.snapshot(id))\n                .collect::\u003cMlResult\u003cVec\u003c_>>>()?;\n            let saved = record\n                .saved\n                .iter()\n                .map(|&id| state.snapshot(id))\n                .collect::\u003cMlResult\u003cVec\u003c_>>>()?;\n            let views = inputs.iter().map(TensorBuffer::view).collect::\u003cVec\u003c_>>();\n            let saved_views = saved.iter().map(TensorBuffer::view).collect::\u003cVec\u003c_>>();\n            let incoming = record\n                .backward\n                .backward(&views, &saved_views, grad.view())?;\n            if incoming.len() != record.inputs.len() {\n                return Err(AutogradError::BackwardArityMismatch {\n                    expected: record.inputs.len(),"},"src/tensor/graph.rs#pub(crate) fn backward":{"path":"src/tensor/graph.rs","line":235,"start":233,"verified":true,"hash":"16acb4f34c4fff8bf315fc82e7bbf06d532eeb45c88a51d13155485ede42754c","code":"\n    #[cfg(feature = \"enableBackward\")]\n    pub(crate) fn backward(&mut self, output_id: NodeId) -> MlResult\u003c()> {\n        // ── 1. 전체 그래디언트 초기화 ────────────────────────────────────────\n        for node in &self.nodes {\n            Self::clear_node_grad(&node.grad);\n        }\n\n        // ── 2. 출력 노드 그래디언트 주입 ────────────────────────────────────\n        let output_idx = *self\n            .node_map\n            .get(&output_id)\n            .ok_or_else(|| MlError::StringError(\"Output node not found\".to_string()))?;\n        let output_node = &self.nodes[output_idx];\n\n        let mut ones = GlobalTensor::from_vec(\n            vec![1.0; output_node.tensor.shape().iter().product()],\n            output_node.tensor.shape(),\n        )?;\n        ones.dirty = true;\n        output_node.grad.replace(ones);\n\n        // ── 3. 역전파 루프 ───────────────────────────────────────────────────\n        #[cfg(feature = \"debugging\")]"},"src/runtime/legacy_session.rs#pub(super) fn backward_legacy":{"path":"src/runtime/legacy_session.rs","line":175,"start":173,"verified":true,"hash":"5f04a758a41da718d05b535e4574d2e81317f8b0553bd223478c4f4c6e862c66","code":"        Ok(outputs)\n    }\n    pub(super) fn backward_legacy(\n        &self,\n        output: &super::Variable,\n        options: super::BackwardOptions\u003c'_>,\n        observe: impl FnOnce(&super::State) -> MlResult\u003c()>,\n    ) -> MlResult\u003c()> {\n        if options.gradient.is_some() {\n            return Err(unsupported(\"explicit backward seed\"));\n        }\n        let mut state = self\n            .inner\n            .state\n            .try_borrow_mut()\n            .map_err(|_| ContextError::BorrowConflict)?;\n        let root = output.tensor().id();\n        if state.consumed.contains(&root) {\n            return Err(crate::AutogradError::GraphAlreadyFreed(root).into());\n        }\n        if !state.tracked.contains(&root) {\n            return Err(crate::AutogradError::NodeNotFound(root).into());\n        }\n        let bridge = state"},"src/optimizer/algorithms.rs#self.context.sub_assign":{"path":"src/optimizer/algorithms.rs","line":211,"start":209,"verified":true,"hash":"7e64b46dd5a47bc799a520eb35a227aa9ef92828ed3799a15343063e8ec78d3d","code":"                }\n            }\n            self.context.sub_assign(\n                entry.parameter.variable(),\n                &TensorBuffer::from_vec(delta, gradient.shape())?,\n            )?;\n        }\n        Ok(())\n    }\n\n    fn zero_grad(&self) -> MlResult\u003c()> {\n        for entry in &self.parameters {\n            self.context.clear_grad(entry.parameter.variable())?;\n        }\n        Ok(())\n    }\n}\n\nmacro_rules! context_optimizer {\n    ($name:ident) => {\n        #[derive(Debug)]\n        pub struct $name(OptimizerCore);\n        impl Optimizer for $name {\n            fn register(&mut self, parameter: &Parameter) -> MlResult\u003c()> {"},"src/runtime/training.rs#fn cleanup":{"path":"src/runtime/training.rs","line":38,"start":36,"verified":true,"hash":"38ea339c961a393db2518b8024b5f66d3ed0a22e335939e738b1facb01589dd7","code":"}\nimpl TrainingScope {\n    fn cleanup(&mut self) -> MlResult\u003c()> {\n        let mut state = self\n            .context\n            .inner\n            .state\n            .try_borrow_mut()\n            .map_err(|_| ContextError::BorrowConflict)?;\n        state.clear_graph()?;\n        state.gradients.clear();\n        state.collect()?;\n        self.context.inner.training_active.set(false);\n        self.finished = true;\n        Ok(())\n    }\n    pub(crate) fn finish\u003cT>(mut self, result: MlResult\u003cT>) -> MlResult\u003cT> {\n        match (result, self.cleanup()) {\n            (Ok(v), Ok(())) => Ok(v),\n            (Err(e), Ok(())) | (Ok(_), Err(e)) => Err(e),\n            (Err(primary), Err(cleanup)) => Err(MlError::CleanupError {\n                primary: Box::new(primary),\n                cleanup: Box::new(cleanup),\n            }),"},"src/nn/diffusion.rs#self.context.no_grad(||":{"path":"src/nn/diffusion.rs","line":382,"start":380,"verified":true,"hash":"14ca3d8800d801d0191542c38c2062902a87e7e5a1fb222e1a7936eca6ab2965","code":"    }\n    pub fn predict(&self, input: &Tensor, timesteps: &Tensor) -> MlResult\u003cTensor> {\n        self.context.no_grad(|| {\n            Ok(self\n                .forward(&input.as_variable()?, timesteps)?\n                .tensor()\n                .clone())\n        })\n    }\n}\nimpl TrainableModel for Unet {\n    fn context_id(&self) -> ContextId {\n        self.context.id()\n    }\n    fn parameters(&self) -> Vec\u003c&Parameter> {\n        let mut p = self.initial.parameters();\n        p.extend(self.time1.parameters());\n        p.extend(self.time2.parameters());\n        for stage in &self.down {\n            p.extend(stage.parameters());\n        }\n        p.extend(self.middle1.parameters());\n        p.extend(self.middle_attention.parameters());\n        p.extend(self.middle2.parameters());"},"src/nn/checkpoint.rs#":{"path":"src/nn/checkpoint.rs","line":1,"start":1,"verified":true,"hash":"9af297bde94588b373770c6a858697f4ca137ee55e7a76f2cb32a21ed8f35fda","code":"use crate::MlResult;\nuse serde::{Deserialize, Serialize};\nuse std::path::Path;\n\nconst MAGIC: &[u8; 8] = b\"TRNCHDP\\0\";\nconst FORMAT_VERSION: u32 = 1;\n\n// ── 직렬화 타입 ─────────────────────────────────────────────────────────────\n\n#[derive(Serialize, Deserialize, Debug, Clone)]\npub struct ModelState {\n    pub version: String,\n    pub layers: Vec\u003cLayerState>,\n}\n\n#[derive(Serialize, Deserialize, Debug, Clone)]\npub struct LayerState {\n    pub layer_type: String,\n    pub label: String,\n    pub config: serde_json::Value,\n    pub params: Vec\u003cParamState>,\n}\n\n#[derive(Serialize, Deserialize, Debug, Clone)]"},"src/nn/diffusion.rs#let frequencies =":{"path":"src/nn/diffusion.rs","line":338,"start":336,"verified":true,"hash":"14ca3d8800d801d0191542c38c2062902a87e7e5a1fb222e1a7936eca6ab2965","code":"            return Err(invalid(\"timesteps must have shape [batch, 1]\"));\n        }\n        let frequencies = self.context.tensor(\n            (0..self.dim / 2)\n                .map(|i| 10000f32.powf(-2.0 * i as f32 / self.dim as f32))\n                .collect(),\n            &[1, self.dim / 2],\n        )?;\n        let angles = timesteps.mul(&frequencies)?;\n        let embedding = self\n            .context\n            .concat(&[&angles.sin()?, &angles.cos()?], 1)?\n            .as_variable()?;\n        let time = self\n            .time2\n            .forward(&self.time1.forward(&embedding)?.silu()?)?;\n        let initial = self.initial.forward(input)?;\n        let mut h = initial.clone();\n        let mut skips = Vec::new();\n        for stage in &self.down {\n            h = stage.body(&h, &time)?;\n            skips.push(h.clone());\n            h = stage.resize.forward(&h)?;\n        }"},"src/nn/diffusion.rs#let initial = self.initial.forward":{"path":"src/nn/diffusion.rs","line":352,"start":350,"verified":true,"hash":"14ca3d8800d801d0191542c38c2062902a87e7e5a1fb222e1a7936eca6ab2965","code":"            .time2\n            .forward(&self.time1.forward(&embedding)?.silu()?)?;\n        let initial = self.initial.forward(input)?;\n        let mut h = initial.clone();\n        let mut skips = Vec::new();\n        for stage in &self.down {\n            h = stage.body(&h, &time)?;\n            skips.push(h.clone());\n            h = stage.resize.forward(&h)?;\n        }\n        h = self.middle2.forward(\n            &self\n                .middle_attention\n                .forward(&self.middle1.forward(&h, &time)?)?,\n            &time,\n        )?;\n        for (stage, skip) in self.up.iter().zip(skips.iter().rev()) {\n            h = stage.resize.forward(&h.nearest_upsample2d((2, 2))?)?;\n            h = self\n                .context\n                .concat(&[h.tensor(), skip.tensor()], 1)?\n                .as_variable()?;\n            h = stage.body(&h, &time)?;\n        }"},"src/nn/diffusion.rs#for stage in &self.down":{"path":"src/nn/diffusion.rs","line":355,"start":353,"verified":true,"hash":"14ca3d8800d801d0191542c38c2062902a87e7e5a1fb222e1a7936eca6ab2965","code":"        let mut h = initial.clone();\n        let mut skips = Vec::new();\n        for stage in &self.down {\n            h = stage.body(&h, &time)?;\n            skips.push(h.clone());\n            h = stage.resize.forward(&h)?;\n        }\n        h = self.middle2.forward(\n            &self\n                .middle_attention\n                .forward(&self.middle1.forward(&h, &time)?)?,\n            &time,\n        )?;\n        for (stage, skip) in self.up.iter().zip(skips.iter().rev()) {\n            h = stage.resize.forward(&h.nearest_upsample2d((2, 2))?)?;\n            h = self\n                .context\n                .concat(&[h.tensor(), skip.tensor()], 1)?\n                .as_variable()?;\n            h = stage.body(&h, &time)?;\n        }\n        h = self\n            .context\n            .concat(&[h.tensor(), initial.tensor()], 1)?"},"src/nn/diffusion.rs#h = stage.resize.forward(&h)?":{"path":"src/nn/diffusion.rs","line":358,"start":356,"verified":true,"hash":"14ca3d8800d801d0191542c38c2062902a87e7e5a1fb222e1a7936eca6ab2965","code":"            h = stage.body(&h, &time)?;\n            skips.push(h.clone());\n            h = stage.resize.forward(&h)?;\n        }\n        h = self.middle2.forward(\n            &self\n                .middle_attention\n                .forward(&self.middle1.forward(&h, &time)?)?,\n            &time,\n        )?;\n        for (stage, skip) in self.up.iter().zip(skips.iter().rev()) {\n            h = stage.resize.forward(&h.nearest_upsample2d((2, 2))?)?;\n            h = self\n                .context\n                .concat(&[h.tensor(), skip.tensor()], 1)?\n                .as_variable()?;\n            h = stage.body(&h, &time)?;\n        }\n        h = self\n            .context\n            .concat(&[h.tensor(), initial.tensor()], 1)?\n            .as_variable()?;\n        self.final_conv\n            .forward(&self.final_residual.forward(&h, &time)?)"},"src/nn/diffusion.rs#fn body(&self":{"path":"src/nn/diffusion.rs","line":163,"start":161,"verified":true,"hash":"14ca3d8800d801d0191542c38c2062902a87e7e5a1fb222e1a7936eca6ab2965","code":"}\nimpl Stage {\n    fn body(&self, input: &Variable, time: &Variable) -> MlResult\u003cVariable> {\n        let h = self\n            .second\n            .forward(&self.first.forward(input, time)?, time)?;\n        if let Some(a) = &self.attention {\n            a.forward(&h)\n        } else {\n            Ok(h)\n        }\n    }\n    fn parameters(&self) -> Vec\u003c&Parameter> {\n        let mut p = self.first.parameters();\n        p.extend(self.second.parameters());\n        if let Some(a) = &self.attention {\n            p.extend(a.parameters());\n        }\n        p.extend(self.resize.parameters());\n        p\n    }\n}\n\n#[derive(Debug)]"},"src/nn/diffusion.rs#h = self.middle2.forward":{"path":"src/nn/diffusion.rs","line":360,"start":358,"verified":true,"hash":"14ca3d8800d801d0191542c38c2062902a87e7e5a1fb222e1a7936eca6ab2965","code":"            h = stage.resize.forward(&h)?;\n        }\n        h = self.middle2.forward(\n            &self\n                .middle_attention\n                .forward(&self.middle1.forward(&h, &time)?)?,\n            &time,\n        )?;\n        for (stage, skip) in self.up.iter().zip(skips.iter().rev()) {\n            h = stage.resize.forward(&h.nearest_upsample2d((2, 2))?)?;\n            h = self\n                .context\n                .concat(&[h.tensor(), skip.tensor()], 1)?\n                .as_variable()?;\n            h = stage.body(&h, &time)?;\n        }\n        h = self\n            .context\n            .concat(&[h.tensor(), initial.tensor()], 1)?\n            .as_variable()?;\n        self.final_conv\n            .forward(&self.final_residual.forward(&h, &time)?)\n    }\n    pub fn predict(&self, input: &Tensor, timesteps: &Tensor) -> MlResult\u003cTensor> {"},"src/nn/diffusion.rs#for (stage, skip) in self.up":{"path":"src/nn/diffusion.rs","line":366,"start":364,"verified":true,"hash":"14ca3d8800d801d0191542c38c2062902a87e7e5a1fb222e1a7936eca6ab2965","code":"            &time,\n        )?;\n        for (stage, skip) in self.up.iter().zip(skips.iter().rev()) {\n            h = stage.resize.forward(&h.nearest_upsample2d((2, 2))?)?;\n            h = self\n                .context\n                .concat(&[h.tensor(), skip.tensor()], 1)?\n                .as_variable()?;\n            h = stage.body(&h, &time)?;\n        }\n        h = self\n            .context\n            .concat(&[h.tensor(), initial.tensor()], 1)?\n            .as_variable()?;\n        self.final_conv\n            .forward(&self.final_residual.forward(&h, &time)?)\n    }\n    pub fn predict(&self, input: &Tensor, timesteps: &Tensor) -> MlResult\u003cTensor> {\n        self.context.no_grad(|| {\n            Ok(self\n                .forward(&input.as_variable()?, timesteps)?\n                .tensor()\n                .clone())\n        })"},"src/nn/diffusion.rs#self.final_conv":{"path":"src/nn/diffusion.rs","line":378,"start":376,"verified":true,"hash":"14ca3d8800d801d0191542c38c2062902a87e7e5a1fb222e1a7936eca6ab2965","code":"            .concat(&[h.tensor(), initial.tensor()], 1)?\n            .as_variable()?;\n        self.final_conv\n            .forward(&self.final_residual.forward(&h, &time)?)\n    }\n    pub fn predict(&self, input: &Tensor, timesteps: &Tensor) -> MlResult\u003cTensor> {\n        self.context.no_grad(|| {\n            Ok(self\n                .forward(&input.as_variable()?, timesteps)?\n                .tensor()\n                .clone())\n        })\n    }\n}\nimpl TrainableModel for Unet {\n    fn context_id(&self) -> ContextId {\n        self.context.id()\n    }\n    fn parameters(&self) -> Vec\u003c&Parameter> {\n        let mut p = self.initial.parameters();\n        p.extend(self.time1.parameters());\n        p.extend(self.time2.parameters());\n        for stage in &self.down {\n            p.extend(stage.parameters());"},"src/nn/diffusion.rs#fn forward(&self, input: &Variable, time:":{"path":"src/nn/diffusion.rs","line":67,"start":65,"verified":true,"hash":"14ca3d8800d801d0191542c38c2062902a87e7e5a1fb222e1a7936eca6ab2965","code":"        })\n    }\n    fn forward(&self, input: &Variable, time: &Variable) -> MlResult\u003cVariable> {\n        let batch = input.tensor().shape()?[0];\n        let h = self.conv1.forward(&self.norm1.forward(input)?.silu()?)?;\n        let t = self\n            .time\n            .forward(time)?\n            .reshape(&[batch, self.channels, 1, 1])?;\n        let h = h.add(t.tensor())?;\n        let h = self.conv2.forward(&self.norm2.forward(&h)?.silu()?)?;\n        let skip = if let Some(layer) = &self.skip {\n            layer.forward(input)?\n        } else {\n            input.clone()\n        };\n        h.add(skip.tensor())\n    }\n    fn parameters(&self) -> Vec\u003c&Parameter> {\n        let mut p = self.norm1.parameters();\n        p.extend(self.conv1.parameters());\n        p.extend(self.norm2.parameters());\n        p.extend(self.conv2.parameters());\n        p.extend(self.time.parameters());"},"src/nn/diffusion.rs#let scores = q.matmul":{"path":"src/nn/diffusion.rs","line":136,"start":134,"verified":true,"hash":"14ca3d8800d801d0191542c38c2062902a87e7e5a1fb222e1a7936eca6ab2965","code":"            .execution_context()?\n            .scalar(1.0 / (self.channels as f32).sqrt())?;\n        let scores = q.matmul(k.tensor())?.mul(&scale)?.softmax(2)?;\n        let weighted = scores.matmul(v.tensor())?.reshape(&[n * h * w, c])?;\n        let output = self\n            .out\n            .forward(&weighted)?\n            .reshape(&[n, h * w, c])?\n            .transpose(&[0, 2, 1])?\n            .reshape(&shape)?;\n        output.add(input.tensor())\n    }\n    fn parameters(&self) -> Vec\u003c&Parameter> {\n        let mut p = self.norm.parameters();\n        for layer in [&self.q, &self.k, &self.v, &self.out] {\n            p.extend(layer.parameters());\n        }\n        p\n    }\n}\n\n#[derive(Debug)]\nstruct Stage {\n    first: Residual,"},"src/nn/diffusion.rs#let noisy = self":{"path":"src/nn/diffusion.rs","line":565,"start":563,"verified":true,"hash":"14ca3d8800d801d0191542c38c2062902a87e7e5a1fb222e1a7936eca6ab2965","code":"        t: usize,\n    ) -> MlResult\u003c(Variable, Variable)> {\n        let noisy = self\n            .scheduler\n            .q_sample(image.tensor(), noise, t)?\n            .as_variable()?;\n        let shape = image.tensor().shape()?;\n        if shape.len() != 4 {\n            return Err(invalid(\"expected NCHW image\"));\n        }\n        let times = self.context.tensor(\n            vec![t as f32 / self.scheduler.timesteps() as f32; shape[0]],\n            &[shape[0], 1],\n        )?;\n        let prediction = self.unet.forward(&noisy, &times)?;\n        let loss = prediction.mse_loss(noise, crate::Reduction::Mean)?;\n        Ok((prediction, loss))\n    }\n    pub fn sample_with_noise(&self, initial: &Tensor, step_noise: &[Tensor]) -> MlResult\u003cTensor> {\n        self.context.validate(initial)?;\n        if step_noise.len() != self.scheduler.timesteps() {\n            return Err(invalid(\"one noise tensor per timestep is required\"));\n        }\n        self.context.no_grad(|| {"},"src/nn/diffusion.rs#let mut skips =":{"path":"src/nn/diffusion.rs","line":354,"start":352,"verified":true,"hash":"14ca3d8800d801d0191542c38c2062902a87e7e5a1fb222e1a7936eca6ab2965","code":"        let initial = self.initial.forward(input)?;\n        let mut h = initial.clone();\n        let mut skips = Vec::new();\n        for stage in &self.down {\n            h = stage.body(&h, &time)?;\n            skips.push(h.clone());\n            h = stage.resize.forward(&h)?;\n        }\n        h = self.middle2.forward(\n            &self\n                .middle_attention\n                .forward(&self.middle1.forward(&h, &time)?)?,\n            &time,\n        )?;\n        for (stage, skip) in self.up.iter().zip(skips.iter().rev()) {\n            h = stage.resize.forward(&h.nearest_upsample2d((2, 2))?)?;\n            h = self\n                .context\n                .concat(&[h.tensor(), skip.tensor()], 1)?\n                .as_variable()?;\n            h = stage.body(&h, &time)?;\n        }\n        h = self\n            .context"},"src/nn/diffusion.rs#let prediction = self.unet.forward":{"path":"src/nn/diffusion.rs","line":577,"start":575,"verified":true,"hash":"14ca3d8800d801d0191542c38c2062902a87e7e5a1fb222e1a7936eca6ab2965","code":"            &[shape[0], 1],\n        )?;\n        let prediction = self.unet.forward(&noisy, &times)?;\n        let loss = prediction.mse_loss(noise, crate::Reduction::Mean)?;\n        Ok((prediction, loss))\n    }\n    pub fn sample_with_noise(&self, initial: &Tensor, step_noise: &[Tensor]) -> MlResult\u003cTensor> {\n        self.context.validate(initial)?;\n        if step_noise.len() != self.scheduler.timesteps() {\n            return Err(invalid(\"one noise tensor per timestep is required\"));\n        }\n        self.context.no_grad(|| {\n            let mut image = initial.clone();\n            let shape = image.shape()?;\n            if shape.len() != 4 {\n                return Err(invalid(\"expected NCHW image\"));\n            }\n            for t in (0..self.scheduler.timesteps()).rev() {\n                let times = self.context.tensor(\n                    vec![t as f32 / self.scheduler.timesteps() as f32; shape[0]],\n                    &[shape[0], 1],\n                )?;\n                let prediction = self.unet.predict(&image, &times)?;\n                image = self"},"src/runtime/backward.rs#let keep = state":{"path":"src/runtime/backward.rs","line":132,"start":130,"verified":true,"hash":"11ab9676de8c43881655bdc6604402cc5d1f37bf6b8d5e5a9a9196b4beb9ec8d","code":"            state.collect()?;\n        }\n        let keep = state\n            .leaves\n            .union(&state.retained)\n            .copied()\n            .collect::\u003cHashSet\u003c_>>();\n        state.gradients.retain(|id, _| keep.contains(id));\n        Ok(())\n    }\n}\n"},"src/optimizer/algorithms.rs#Algorithm::Sgd =>":{"path":"src/optimizer/algorithms.rs","line":155,"start":153,"verified":true,"hash":"7e64b46dd5a47bc799a520eb35a227aa9ef92828ed3799a15343063e8ec78d3d","code":"            let mut delta = vec![0.0; gradient.data().len()];\n            match self.algorithm {\n                Algorithm::Sgd => {\n                    for (output, &g) in delta.iter_mut().zip(gradient.data()) {\n                        *output = self.learning_rate * g;\n                    }\n                }\n                Algorithm::Momentum { momentum } => {\n                    for ((velocity, output), &g) in\n                        entry.first.iter_mut().zip(&mut delta).zip(gradient.data())\n                    {\n                        *velocity = momentum * *velocity + g;\n                        *output = self.learning_rate * *velocity;\n                    }\n                }\n                Algorithm::AdaGrad { epsilon } => {\n                    for ((accumulator, output), &g) in\n                        entry.first.iter_mut().zip(&mut delta).zip(gradient.data())\n                    {\n                        *accumulator += g * g;\n                        *output = self.learning_rate * g / (*accumulator + epsilon).sqrt();\n                    }\n                }\n                Algorithm::RmsProp { rho, epsilon } => {"},"tests/diffusion.rs#original.save_checkpoint":{"path":"tests/diffusion.rs","line":86,"start":84,"verified":true,"hash":"5c8550144cf9034e2f2048f3db665a1a342e1d38b2fa295d5a6a5e7ef4c0273b","code":"            .as_nanos()\n    ));\n    original.save_checkpoint(&path)?;\n    restored.load_checkpoint(&path)?;\n    std::fs::remove_file(&path)?;\n    let initial = ctx.tensor(vec![0.1; 16], &[1, 1, 4, 4])?;\n    let noises = vec![ctx.tensor(vec![0.0; 16], &[1, 1, 4, 4])?; 2];\n    assert_eq!(\n        original.sample_with_noise(&initial, &noises)?.to_vec()?,\n        restored.sample_with_noise(&initial, &noises)?.to_vec()?\n    );\n    Ok(())\n}\n"}},"modules":[{"path":"benches/context_diffusion.rs","line":4,"declaration":"mod allocation;","context":["#[path = \"support/counting_allocator.rs\"]"]},{"path":"benches/p1_compare.rs","line":2,"declaration":"mod diffusion;","context":["#[path = \"support/legacy_diffusion.rs\"]"]},{"path":"benches/p1_compare.rs","line":4,"declaration":"mod operation;","context":["#[path = \"support/legacy_diffusion.rs\"]","#[path = \"support/legacy_operation.rs\"]"]},{"path":"benches/p1_compare.rs","line":6,"declaration":"mod trainer;","context":["#[path = \"support/legacy_operation.rs\"]","#[path = \"support/legacy_trainer.rs\"]"]},{"path":"examples/replay_reference_diffusion.rs","line":4,"declaration":"pub mod mapping;","context":["#[path = \"support/reference_parameters.rs\"]"]},{"path":"src/backend/cpu/compute.rs","line":240,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/backend/cpu/core.rs","line":36,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/backend/cpu/legacy_compute.rs","line":231,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/backend/cpu/legacy_core.rs","line":44,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/backend/cpu/legacy_mod.rs","line":1,"declaration":"pub use compute::CpuCompute;","context":[]},{"path":"src/backend/cpu/legacy_mod.rs","line":2,"declaration":"pub use core::CpuCore;","context":[]},{"path":"src/backend/cpu/legacy_mod.rs","line":11,"declaration":"mod compute;","context":["#[path = \"legacy_compute.rs\"]"]},{"path":"src/backend/cpu/legacy_mod.rs","line":13,"declaration":"mod core;","context":["#[path = \"legacy_compute.rs\"]","#[path = \"legacy_core.rs\"]"]},{"path":"src/backend/cpu/legacy_mod.rs","line":15,"declaration":"mod parallel;","context":["#[path = \"legacy_core.rs\"]","#[path = \"legacy_parallel.rs\"]"]},{"path":"src/backend/cpu/legacy_mod.rs","line":123,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/backend/cpu/legacy_parallel.rs","line":90,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/backend/cpu/mod.rs","line":10,"declaration":"mod compute;","context":[]},{"path":"src/backend/cpu/mod.rs","line":11,"declaration":"mod core;","context":[]},{"path":"src/backend/cpu/mod.rs","line":12,"declaration":"pub(crate) mod operations;","context":[]},{"path":"src/backend/cpu/mod.rs","line":13,"declaration":"mod parallel;","context":[]},{"path":"src/backend/cpu/mod.rs","line":81,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/backend/cpu/operations/mod.rs","line":3,"declaration":"mod functional;","context":[]},{"path":"src/backend/cpu/operations/mod.rs","line":4,"declaration":"mod kernels;","context":[]},{"path":"src/backend/cpu/operations/mod.rs","line":509,"declaration":"mod forward;","context":[]},{"path":"src/backend/cpu/parallel.rs","line":125,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/backend/legacy_mod.rs","line":3,"declaration":"pub use cpu::CpuBackend;","context":[]},{"path":"src/backend/legacy_mod.rs","line":4,"declaration":"pub use device::{Device, DeviceType};","context":[]},{"path":"src/backend/legacy_mod.rs","line":9,"declaration":"mod device;","context":["#[path = \"legacy_device.rs\"]"]},{"path":"src/backend/legacy_mod.rs","line":11,"declaration":"mod feature;","context":["#[path = \"legacy_device.rs\"]","#[path = \"feature.rs\"]"]},{"path":"src/backend/legacy_mod.rs","line":13,"declaration":"mod cpu;","context":["#[path = \"feature.rs\"]","#[path = \"cpu/legacy_mod.rs\"]"]},{"path":"src/backend/mod.rs","line":4,"declaration":"pub use cpu::CpuBackend;","context":["#[cfg(feature = \"builtinKernels\")]"]},{"path":"src/backend/mod.rs","line":12,"declaration":"mod storage;","context":["#[cfg(feature = \"builtinStorage\")]","#[path = \"cpu/storage.rs\"]"]},{"path":"src/backend/mod.rs","line":13,"declaration":"pub use device::{Device, DeviceType};","context":["#[cfg(feature = \"builtinStorage\")]","#[path = \"cpu/storage.rs\"]"]},{"path":"src/backend/mod.rs","line":15,"declaration":"pub use storage::CpuTensorStore;","context":["#[cfg(feature = \"builtinStorage\")]"]},{"path":"src/backend/mod.rs","line":18,"declaration":"mod cpu;","context":["#[cfg(feature = \"builtinKernels\")]"]},{"path":"src/backend/mod.rs","line":19,"declaration":"mod device;","context":["#[cfg(feature = \"builtinKernels\")]"]},{"path":"src/backend/mod.rs","line":20,"declaration":"mod feature;","context":["#[cfg(feature = \"builtinKernels\")]"]},{"path":"src/comparison.rs","line":2,"declaration":"pub use crate::legacy::nn::models::diffusion::Diffusion as ReferenceDiffusion;","context":[]},{"path":"src/comparison.rs","line":8,"declaration":"pub use crate::legacy::nn::models::diffusion::encoder;","context":[]},{"path":"src/comparison.rs","line":9,"declaration":"pub use crate::legacy::nn::models::diffusion::embedding;","context":[]},{"path":"src/comparison.rs","line":10,"declaration":"pub use crate::legacy::nn::models::diffusion::unet;","context":[]},{"path":"src/comparison.rs","line":11,"declaration":"pub use crate::legacy::nn::models::diffusion::scheduler;","context":[]},{"path":"src/comparison.rs","line":14,"declaration":"pub use crate::legacy::nn::models::linear;","context":[]},{"path":"src/comparison.rs","line":16,"declaration":"pub use crate::legacy::nn::models::mlp;","context":[]},{"path":"src/comparison.rs","line":17,"declaration":"pub use crate::legacy::nn::models::autoregressive;","context":[]},{"path":"src/comparison.rs","line":18,"declaration":"pub use crate::legacy::nn::models::semi_supervised;","context":[]},{"path":"src/comparison.rs","line":19,"declaration":"pub use crate::legacy::nn::models::reinforcement;","context":[]},{"path":"src/contracts/mod.rs","line":7,"declaration":"mod errors;","context":[]},{"path":"src/contracts/mod.rs","line":8,"declaration":"pub use errors::*;","context":[]},{"path":"src/legacy.rs","line":3,"declaration":"pub mod backend;","context":["#[path = \"backend/legacy_mod.rs\"]"]},{"path":"src/legacy.rs","line":5,"declaration":"pub mod loss;","context":["#[path = \"backend/legacy_mod.rs\"]","#[path = \"loss/mod.rs\"]"]},{"path":"src/legacy.rs","line":7,"declaration":"pub mod nn;","context":["#[path = \"loss/mod.rs\"]","#[path = \"nn/legacy_mod.rs\"]"]},{"path":"src/legacy.rs","line":9,"declaration":"pub mod optimizer;","context":["#[path = \"nn/legacy_mod.rs\"]","#[path = \"optimizer/legacy_mod.rs\"]"]},{"path":"src/legacy.rs","line":11,"declaration":"pub mod tensor;","context":["#[path = \"optimizer/legacy_mod.rs\"]","#[path = \"tensor/mod.rs\"]"]},{"path":"src/legacy.rs","line":13,"declaration":"pub mod trainer;","context":["#[path = \"tensor/mod.rs\"]","#[path = \"trainer/legacy_mod.rs\"]"]},{"path":"src/legacy.rs","line":14,"declaration":"pub use loss::Reduction;","context":["#[path = \"trainer/legacy_mod.rs\"]"]},{"path":"src/legacy.rs","line":17,"declaration":"pub mod visualization;","context":["#[cfg(feature = \"enableVisualization\")]","#[path = \"visualization/legacy_mod.rs\"]"]},{"path":"src/legacy.rs","line":19,"declaration":"pub mod tests;","context":["#[path = \"visualization/legacy_mod.rs\"]","#[path = \"tests/mod.rs\"]"]},{"path":"src/legacy.rs","line":136,"declaration":"mod benchmark {","context":["#[cfg(test)]"]},{"path":"src/legacy.rs","line":400,"declaration":"pub mod comparison;","context":["#[path = \"comparison.rs\"]"]},{"path":"src/legacy.rs","line":401,"declaration":"pub use crate::{variable, tensor_ops, scalar_ops, scalar, register_operator, var_input, var_output, var_act, var_weight, var_bias, var_with_label};","context":["#[path = \"comparison.rs\"]"]},{"path":"src/lib.rs","line":2,"declaration":"pub mod backend;","context":[]},{"path":"src/lib.rs","line":3,"declaration":"pub mod contracts;","context":[]},{"path":"src/lib.rs","line":4,"declaration":"pub mod runtime;","context":[]},{"path":"src/lib.rs","line":5,"declaration":"pub use contracts::{","context":[]},{"path":"src/lib.rs","line":10,"declaration":"pub use runtime::{","context":[]},{"path":"src/lib.rs","line":15,"declaration":"pub mod tensor {","context":[]},{"path":"src/lib.rs","line":16,"declaration":"pub use crate::{Tensor, TensorBuffer, TensorView};","context":[]},{"path":"src/lib.rs","line":18,"declaration":"pub mod loss {","context":[]},{"path":"src/lib.rs","line":19,"declaration":"pub use crate::{LossError, Reduction};","context":[]},{"path":"src/lib.rs","line":22,"declaration":"pub mod legacy;","context":["#[cfg(feature = \"legacyBenchmark\")]"]},{"path":"src/lib.rs","line":23,"declaration":"pub mod nn;","context":["#[cfg(feature = \"legacyBenchmark\")]"]},{"path":"src/lib.rs","line":24,"declaration":"pub mod optimizer;","context":["#[cfg(feature = \"legacyBenchmark\")]"]},{"path":"src/lib.rs","line":25,"declaration":"pub mod trainer;","context":[]},{"path":"src/lib.rs","line":27,"declaration":"pub mod visualization;","context":[]},{"path":"src/loss/function.rs","line":470,"declaration":"mod softmax_ce_tests {","context":["#[cfg(test)]"]},{"path":"src/loss/mod.rs","line":2,"declaration":"mod display;","context":["#[path = \"display.rs\"]"]},{"path":"src/loss/mod.rs","line":4,"declaration":"mod function;","context":["#[path = \"display.rs\"]","#[path = \"function.rs\"]"]},{"path":"src/nn/activation/mod.rs","line":2,"declaration":"pub mod sigmoid;","context":["#[path = \"sigmoid.rs\"]"]},{"path":"src/nn/activation/mod.rs","line":4,"declaration":"pub mod tanh;","context":["#[path = \"sigmoid.rs\"]","#[path = \"tanh.rs\"]"]},{"path":"src/nn/activation/mod.rs","line":6,"declaration":"pub mod relu;","context":["#[path = \"tanh.rs\"]","#[path = \"relu.rs\"]"]},{"path":"src/nn/activation/mod.rs","line":8,"declaration":"pub mod softmax;","context":["#[path = \"relu.rs\"]","#[path = \"softmax.rs\"]"]},{"path":"src/nn/activation/mod.rs","line":10,"declaration":"pub mod identity;","context":["#[path = \"softmax.rs\"]","#[path = \"identity.rs\"]"]},{"path":"src/nn/activation/mod.rs","line":12,"declaration":"mod silu;","context":["#[path = \"identity.rs\"]","#[path = \"silu.rs\"]"]},{"path":"src/nn/diffusion.rs","line":3,"declaration":"mod named_parameters;","context":["#[path = \"diffusion_parameters.rs\"]"]},{"path":"src/nn/layers.rs","line":821,"declaration":"mod tests;","context":["#[cfg(test)]","#[path = \"../tests/nn/layers_tests.rs\"]"]},{"path":"src/nn/legacy_mod.rs","line":68,"declaration":"pub mod activation;","context":["#[path = \"activation/mod.rs\"]"]},{"path":"src/nn/legacy_mod.rs","line":70,"declaration":"pub mod conv2d;","context":["#[path = \"activation/mod.rs\"]","#[path = \"conv2d.rs\"]"]},{"path":"src/nn/legacy_mod.rs","line":72,"declaration":"pub mod pooling;","context":["#[path = \"conv2d.rs\"]","#[path = \"pooling.rs\"]"]},{"path":"src/nn/legacy_mod.rs","line":74,"declaration":"pub mod linear;","context":["#[path = \"pooling.rs\"]","#[path = \"linear.rs\"]"]},{"path":"src/nn/legacy_mod.rs","line":76,"declaration":"pub mod group_norm;","context":["#[path = \"linear.rs\"]","#[path = \"group_norm.rs\"]"]},{"path":"src/nn/legacy_mod.rs","line":78,"declaration":"mod parameter;","context":["#[path = \"group_norm.rs\"]","#[path = \"parameter.rs\"]"]},{"path":"src/nn/legacy_mod.rs","line":80,"declaration":"pub mod checkpoint;","context":["#[path = \"parameter.rs\"]","#[path = \"legacy_checkpoint.rs\"]"]},{"path":"src/nn/legacy_mod.rs","line":82,"declaration":"mod reshape;","context":["#[path = \"legacy_checkpoint.rs\"]","#[path = \"reshape.rs\"]"]},{"path":"src/nn/legacy_mod.rs","line":116,"declaration":"pub use checkpoint::{LayerState, ModelState, ParamState};","context":[]},{"path":"src/nn/legacy_mod.rs","line":505,"declaration":"pub mod models;","context":["#[path = \"native_models/mod.rs\"]"]},{"path":"src/nn/mod.rs","line":2,"declaration":"pub mod checkpoint;","context":[]},{"path":"src/nn/mod.rs","line":3,"declaration":"mod layers;","context":[]},{"path":"src/nn/mod.rs","line":4,"declaration":"pub mod pilots;","context":[]},{"path":"src/nn/mod.rs","line":5,"declaration":"pub use crate::{Parameter, Variable};","context":[]},{"path":"src/nn/mod.rs","line":6,"declaration":"pub use checkpoint::{LayerState, ModelState, ParamState};","context":[]},{"path":"src/nn/mod.rs","line":7,"declaration":"pub use layers::*;","context":[]},{"path":"src/nn/mod.rs","line":8,"declaration":"pub use pilots::*;","context":[]},{"path":"src/nn/mod.rs","line":10,"declaration":"mod diffusion;","context":[]},{"path":"src/nn/mod.rs","line":11,"declaration":"pub use diffusion::{Diffusion, DiffusionScheduler, Unet};","context":[]},{"path":"src/nn/native_models/autoregressive/mod.rs","line":130,"declaration":"mod tests;","context":["#[cfg(test)]","#[cfg(feature = \"enableBackward\")]","#[path = \"../../../tests/nn/native_models/autoregressive/mod_tests.rs\"]"]},{"path":"src/nn/native_models/diffusion/embedding.rs","line":134,"declaration":"mod tests;","context":["#[cfg(test)]","#[path = \"../../../tests/nn/native_models/diffusion/embedding_tests.rs\"]"]},{"path":"src/nn/native_models/diffusion/encoder.rs","line":77,"declaration":"mod tests;","context":["#[cfg(test)]","#[path = \"../../../tests/nn/native_models/diffusion/encoder_tests.rs\"]"]},{"path":"src/nn/native_models/diffusion/mod.rs","line":2,"declaration":"pub mod encoder;","context":["#[path = \"encoder.rs\"]"]},{"path":"src/nn/native_models/diffusion/mod.rs","line":4,"declaration":"mod decoder;","context":["#[path = \"encoder.rs\"]","#[path = \"decoder.rs\"]"]},{"path":"src/nn/native_models/diffusion/mod.rs","line":6,"declaration":"pub mod unet;","context":["#[path = \"decoder.rs\"]","#[path = \"unet.rs\"]"]},{"path":"src/nn/native_models/diffusion/mod.rs","line":8,"declaration":"pub mod scheduler;","context":["#[path = \"unet.rs\"]","#[path = \"scheduler.rs\"]"]},{"path":"src/nn/native_models/diffusion/mod.rs","line":10,"declaration":"pub mod embedding;","context":["#[path = \"scheduler.rs\"]","#[path = \"embedding.rs\"]"]},{"path":"src/nn/native_models/diffusion/mod.rs","line":321,"declaration":"mod tests;","context":["#[cfg(test)]","#[path = \"../../../tests/nn/native_models/diffusion/mod_tests.rs\"]"]},{"path":"src/nn/native_models/diffusion/scheduler.rs","line":236,"declaration":"mod tests;","context":["#[cfg(test)]","#[path = \"../../../tests/nn/native_models/diffusion/scheduler_tests.rs\"]"]},{"path":"src/nn/native_models/diffusion/unet.rs","line":1213,"declaration":"mod tests;","context":["#[cfg(test)]","#[path = \"../../../tests/nn/native_models/diffusion/unet_tests.rs\"]"]},{"path":"src/nn/native_models/linear/linear.rs","line":93,"declaration":"mod data_loader_tests;","context":["#[cfg(all(test, feature = \"enableBackward\"))]","#[path = \"../../../tests/nn/native_models/linear/linear_data_loader_tests.rs\"]"]},{"path":"src/nn/native_models/linear/mod.rs","line":6,"declaration":"pub mod linear;","context":["#[path = \"linear.rs\"]"]},{"path":"src/nn/native_models/mlp/mod.rs","line":4,"declaration":"pub mod mlp;","context":["#[path = \"mlp.rs\"]"]},{"path":"src/nn/native_models/mod.rs","line":28,"declaration":"pub mod linear;","context":["#[path = \"linear/mod.rs\"]"]},{"path":"src/nn/native_models/mod.rs","line":30,"declaration":"pub mod nonlinear;","context":["#[path = \"linear/mod.rs\"]","#[path = \"nonlinear/mod.rs\"]"]},{"path":"src/nn/native_models/mod.rs","line":32,"declaration":"pub mod mlp;","context":["#[path = \"nonlinear/mod.rs\"]","#[path = \"mlp/mod.rs\"]"]},{"path":"src/nn/native_models/mod.rs","line":34,"declaration":"pub mod diffusion;","context":["#[path = \"mlp/mod.rs\"]","#[path = \"diffusion/mod.rs\"]"]},{"path":"src/nn/native_models/mod.rs","line":36,"declaration":"pub mod transformer;","context":["#[path = \"diffusion/mod.rs\"]","#[path = \"transformer/mod.rs\"]"]},{"path":"src/nn/native_models/mod.rs","line":38,"declaration":"pub mod semi_supervised;","context":["#[path = \"transformer/mod.rs\"]","#[path = \"semi_supervised/mod.rs\"]"]},{"path":"src/nn/native_models/mod.rs","line":40,"declaration":"pub mod reinforcement;","context":["#[path = \"semi_supervised/mod.rs\"]","#[path = \"reinforcement/mod.rs\"]"]},{"path":"src/nn/native_models/mod.rs","line":42,"declaration":"pub mod autoregressive;","context":["#[path = \"reinforcement/mod.rs\"]","#[path = \"autoregressive/mod.rs\"]"]},{"path":"src/nn/native_models/mod.rs","line":44,"declaration":"pub use self::linear::LinearRegression;","context":["#[path = \"autoregressive/mod.rs\"]"]},{"path":"src/nn/native_models/mod.rs","line":45,"declaration":"pub use self::nonlinear::{LogisticRegression, SoftmaxRegression};","context":[]},{"path":"src/nn/native_models/mod.rs","line":46,"declaration":"pub use self::mlp::MLP;","context":[]},{"path":"src/nn/native_models/nonlinear/mod.rs","line":6,"declaration":"pub mod logistic;","context":["#[path = \"logistic.rs\"]"]},{"path":"src/nn/native_models/nonlinear/mod.rs","line":8,"declaration":"pub mod softmax;","context":["#[path = \"logistic.rs\"]","#[path = \"softmax.rs\"]"]},{"path":"src/nn/native_models/reinforcement/mod.rs","line":131,"declaration":"mod tests;","context":["#[cfg(test)]","#[cfg(feature = \"enableBackward\")]","#[path = \"../../../tests/nn/native_models/reinforcement/mod_tests.rs\"]"]},{"path":"src/nn/native_models/semi_supervised/mod.rs","line":139,"declaration":"mod tests;","context":["#[cfg(test)]","#[cfg(feature = \"enableBackward\")]","#[path = \"../../../tests/nn/native_models/semi_supervised/mod_tests.rs\"]"]},{"path":"src/nn/pilots.rs","line":3,"declaration":"mod autoregressive;","context":[]},{"path":"src/nn/pilots.rs","line":4,"declaration":"mod reinforcement;","context":[]},{"path":"src/nn/pilots.rs","line":5,"declaration":"mod semi_supervised;","context":[]},{"path":"src/nn/pilots.rs","line":7,"declaration":"pub use autoregressive::BigramLm;","context":[]},{"path":"src/nn/pilots.rs","line":8,"declaration":"pub use reinforcement::{LinearPolicy, TwoArmedBandit};","context":[]},{"path":"src/nn/pilots.rs","line":9,"declaration":"pub use semi_supervised::PiClassifier;","context":[]},{"path":"src/optimizer/algorithms.rs","line":399,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/optimizer/legacy_mod.rs","line":2,"declaration":"pub mod sgd;","context":["#[path = \"sgd.rs\"]"]},{"path":"src/optimizer/legacy_mod.rs","line":4,"declaration":"pub mod momentum;","context":["#[path = \"sgd.rs\"]","#[path = \"momentum.rs\"]"]},{"path":"src/optimizer/legacy_mod.rs","line":6,"declaration":"pub mod adagrad;","context":["#[path = \"momentum.rs\"]","#[path = \"adagrad.rs\"]"]},{"path":"src/optimizer/legacy_mod.rs","line":8,"declaration":"pub mod rmsprop;","context":["#[path = \"adagrad.rs\"]","#[path = \"rmsprop.rs\"]"]},{"path":"src/optimizer/legacy_mod.rs","line":10,"declaration":"pub mod adam;","context":["#[path = \"rmsprop.rs\"]","#[path = \"adam.rs\"]"]},{"path":"src/optimizer/legacy_mod.rs","line":12,"declaration":"pub mod adamw;","context":["#[path = \"adam.rs\"]","#[path = \"adamw.rs\"]"]},{"path":"src/optimizer/legacy_mod.rs","line":14,"declaration":"pub use sgd::SGD;","context":["#[path = \"adamw.rs\"]"]},{"path":"src/optimizer/legacy_mod.rs","line":15,"declaration":"pub use momentum::Momentum;","context":[]},{"path":"src/optimizer/legacy_mod.rs","line":16,"declaration":"pub use adagrad::AdaGrad;","context":[]},{"path":"src/optimizer/legacy_mod.rs","line":17,"declaration":"pub use rmsprop::RMSProp;","context":[]},{"path":"src/optimizer/legacy_mod.rs","line":18,"declaration":"pub use adam::Adam;","context":[]},{"path":"src/optimizer/legacy_mod.rs","line":19,"declaration":"pub use adamw::AdamW;","context":[]},{"path":"src/optimizer/mod.rs","line":2,"declaration":"mod algorithms;","context":[]},{"path":"src/optimizer/mod.rs","line":3,"declaration":"pub use crate::OptimError;","context":[]},{"path":"src/optimizer/mod.rs","line":4,"declaration":"pub use algorithms::*;","context":[]},{"path":"src/runtime/legacy_session.rs","line":871,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/runtime/mod.rs","line":16,"declaration":"mod builtin_autograd;","context":["#[cfg(feature = \"builtinStorage\")]","#[cfg(feature = \"enableBackward\")]"]},{"path":"src/runtime/mod.rs","line":18,"declaration":"pub use builtin_autograd::ReverseMode;","context":["#[cfg(feature = \"enableBackward\")]","#[cfg(feature = \"enableBackward\")]"]},{"path":"src/runtime/mod.rs","line":23,"declaration":"pub mod prepared;","context":[]},{"path":"src/runtime/mod.rs","line":801,"declaration":"mod backward;","context":[]},{"path":"src/runtime/mod.rs","line":802,"declaration":"mod dispatch;","context":[]},{"path":"src/runtime/mod.rs","line":804,"declaration":"mod legacy_session;","context":["#[cfg(feature = \"legacyBenchmark\")]"]},{"path":"src/runtime/mod.rs","line":805,"declaration":"mod receiver;","context":["#[cfg(feature = \"legacyBenchmark\")]"]},{"path":"src/runtime/mod.rs","line":806,"declaration":"mod training;","context":["#[cfg(feature = \"legacyBenchmark\")]"]},{"path":"src/runtime/mod.rs","line":814,"declaration":"mod tests;","context":[]},{"path":"src/runtime/mod.rs","line":817,"declaration":"mod visualization;","context":["#[cfg(feature = \"enableVisualization\")]"]},{"path":"src/runtime/mod.rs","line":820,"declaration":"mod visualization_disabled;","context":["#[cfg(not(feature = \"enableVisualization\"))]"]},{"path":"src/runtime/prepared/mod.rs","line":7,"declaration":"mod executor;","context":[]},{"path":"src/runtime/prepared/mod.rs","line":8,"declaration":"mod plan;","context":[]},{"path":"src/runtime/prepared/mod.rs","line":9,"declaration":"mod prepare;","context":[]},{"path":"src/runtime/prepared/mod.rs","line":10,"declaration":"pub use plan::{PreparedMode, PreparedPlan, PreparedProgram, TensorSlotId};","context":[]},{"path":"src/tensor/broadcast.rs","line":154,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/tensor/mod.rs","line":11,"declaration":"pub mod broadcast;","context":["#[path = \"broadcast.rs\"]"]},{"path":"src/tensor/mod.rs","line":13,"declaration":"pub mod creation;","context":["#[path = \"broadcast.rs\"]","#[path = \"creation.rs\"]"]},{"path":"src/tensor/mod.rs","line":15,"declaration":"pub mod display;","context":["#[path = \"creation.rs\"]","#[path = \"display.rs\"]"]},{"path":"src/tensor/mod.rs","line":17,"declaration":"pub mod graph;","context":["#[path = \"display.rs\"]","#[path = \"graph.rs\"]"]},{"path":"src/tensor/mod.rs","line":19,"declaration":"pub mod operators;","context":["#[path = \"graph.rs\"]","#[path = \"operators/mod.rs\"]"]},{"path":"src/tensor/mod.rs","line":616,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/tensor/operators/add.rs","line":279,"declaration":"mod broadcast_tests {","context":["#[cfg(test)]"]},{"path":"src/tensor/operators/concat.rs","line":151,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/tensor/operators/conv2d.rs","line":362,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/tensor/operators/group_norm.rs","line":255,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/tensor/operators/matmax.rs","line":131,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/tensor/operators/matmul.rs","line":363,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/tensor/operators/mod.rs","line":58,"declaration":"pub(crate) mod debug;","context":["#[cfg(feature = \"debugging\")]","#[path = \"debug.rs\"]"]},{"path":"src/tensor/operators/mod.rs","line":61,"declaration":"pub mod add;","context":["#[path = \"add.rs\"]"]},{"path":"src/tensor/operators/mod.rs","line":63,"declaration":"pub mod sub;","context":["#[path = \"add.rs\"]","#[path = \"sub.rs\"]"]},{"path":"src/tensor/operators/mod.rs","line":65,"declaration":"pub mod mul;","context":["#[path = \"sub.rs\"]","#[path = \"mul.rs\"]"]},{"path":"src/tensor/operators/mod.rs","line":67,"declaration":"pub mod div;","context":["#[path = \"mul.rs\"]","#[path = \"div.rs\"]"]},{"path":"src/tensor/operators/mod.rs","line":69,"declaration":"pub mod neg;","context":["#[path = \"div.rs\"]","#[path = \"neg.rs\"]"]},{"path":"src/tensor/operators/mod.rs","line":71,"declaration":"pub mod unary;","context":["#[path = \"neg.rs\"]","#[path = \"unary.rs\"]"]},{"path":"src/tensor/operators/mod.rs","line":73,"declaration":"pub mod matmul;","context":["#[path = \"unary.rs\"]","#[path = \"matmul.rs\"]"]},{"path":"src/tensor/operators/mod.rs","line":75,"declaration":"pub mod topk;","context":["#[path = \"matmul.rs\"]","#[path = \"topk.rs\"]"]},{"path":"src/tensor/operators/mod.rs","line":77,"declaration":"pub mod matmax;","context":["#[path = \"topk.rs\"]","#[path = \"matmax.rs\"]"]},{"path":"src/tensor/operators/mod.rs","line":79,"declaration":"pub mod sum;","context":["#[path = \"matmax.rs\"]","#[path = \"sum.rs\"]"]},{"path":"src/tensor/operators/mod.rs","line":81,"declaration":"pub mod trigonometric;","context":["#[path = \"sum.rs\"]","#[path = \"trigonometric.rs\"]"]},{"path":"src/tensor/operators/mod.rs","line":83,"declaration":"pub mod reshape;","context":["#[path = \"trigonometric.rs\"]","#[path = \"reshape.rs\"]"]},{"path":"src/tensor/operators/mod.rs","line":85,"declaration":"pub mod transpose;","context":["#[path = \"reshape.rs\"]","#[path = \"transpose.rs\"]"]},{"path":"src/tensor/operators/mod.rs","line":87,"declaration":"pub mod concat;","context":["#[path = \"transpose.rs\"]","#[path = \"concat.rs\"]"]},{"path":"src/tensor/operators/mod.rs","line":89,"declaration":"pub mod conv2d;","context":["#[path = \"concat.rs\"]","#[path = \"conv2d.rs\"]"]},{"path":"src/tensor/operators/mod.rs","line":91,"declaration":"pub mod pool2d;","context":["#[path = \"conv2d.rs\"]","#[path = \"pool2d.rs\"]"]},{"path":"src/tensor/operators/mod.rs","line":93,"declaration":"pub mod upsample;","context":["#[path = \"pool2d.rs\"]","#[path = \"upsample.rs\"]"]},{"path":"src/tensor/operators/mod.rs","line":95,"declaration":"pub mod group_norm;","context":["#[path = \"upsample.rs\"]","#[path = \"group_norm.rs\"]"]},{"path":"src/tensor/operators/mod.rs","line":250,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/tensor/operators/mul.rs","line":239,"declaration":"mod broadcast_tests {","context":["#[cfg(test)]"]},{"path":"src/tensor/operators/pool2d.rs","line":279,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/tensor/operators/topk.rs","line":115,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/tensor/operators/transpose.rs","line":153,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/tensor/operators/upsample.rs","line":131,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/tests/common/data/mod.rs","line":13,"declaration":"pub mod mnist;","context":["#[path = \"mnist.rs\"]"]},{"path":"src/tests/common/mod.rs","line":16,"declaration":"pub(crate) mod logging;","context":["#[path = \"logging.rs\"]"]},{"path":"src/tests/common/mod.rs","line":18,"declaration":"pub(crate) mod data;","context":["#[path = \"logging.rs\"]","#[path = \"data/mod.rs\"]"]},{"path":"src/tests/common/mod.rs","line":20,"declaration":"pub mod model;","context":["#[path = \"data/mod.rs\"]","#[path = \"model/mod.rs\"]"]},{"path":"src/tests/common/model/mod.rs","line":2,"declaration":"pub use crate::legacy::nn::models::*;","context":[]},{"path":"src/tests/mod.rs","line":2,"declaration":"pub mod common;","context":["#[path = \"common/mod.rs\"]"]},{"path":"src/tests/mod.rs","line":4,"declaration":"mod repro;","context":["#[path = \"common/mod.rs\"]","#[path = \"repro.rs\"]"]},{"path":"src/tests/mod.rs","line":8,"declaration":"mod mnist_test;","context":["#[cfg(test)]","#[path = \"mnist_test.rs\"]"]},{"path":"src/tests/mod.rs","line":12,"declaration":"mod checkpoint_test;","context":["#[cfg(test)]","#[path = \"checkpoint_test.rs\"]"]},{"path":"src/tests/repro.rs","line":2,"declaration":"mod reproduction_test {","context":["#[cfg(test)]"]},{"path":"src/trainer/autoregressive.rs","line":534,"declaration":"mod tests;","context":["#[cfg(test)]","#[path = \"../tests/trainer/autoregressive_tests.rs\"]"]},{"path":"src/trainer/core/convergence.rs","line":74,"declaration":"mod tests;","context":["#[cfg(test)]","#[path = \"../../tests/trainer/core/convergence_tests.rs\"]"]},{"path":"src/trainer/core/epoch_loop.rs","line":515,"declaration":"mod tests;","context":["#[cfg(test)]","#[path = \"../../tests/trainer/core/epoch_loop_tests.rs\"]"]},{"path":"src/trainer/core/legacy_convergence.rs","line":2,"declaration":"pub use crate::trainer::core::convergence::*;","context":[]},{"path":"src/trainer/core/legacy_metrics.rs","line":218,"declaration":"mod tests;","context":["#[cfg(test)]","#[path = \"../../tests/trainer/core/legacy_metrics_tests.rs\"]"]},{"path":"src/trainer/core/legacy_mod.rs","line":13,"declaration":"pub mod config;","context":["#[path = \"legacy_config.rs\"]"]},{"path":"src/trainer/core/legacy_mod.rs","line":15,"declaration":"pub mod metric_hook;","context":["#[path = \"legacy_config.rs\"]","#[path = \"legacy_metric_hook.rs\"]"]},{"path":"src/trainer/core/legacy_mod.rs","line":17,"declaration":"pub mod metrics;","context":["#[path = \"legacy_metric_hook.rs\"]","#[path = \"legacy_metrics.rs\"]"]},{"path":"src/trainer/core/legacy_mod.rs","line":19,"declaration":"pub mod convergence;","context":["#[path = \"legacy_metrics.rs\"]","#[path = \"legacy_convergence.rs\"]"]},{"path":"src/trainer/core/legacy_mod.rs","line":21,"declaration":"pub mod epoch_loop;","context":["#[path = \"legacy_convergence.rs\"]","#[path = \"epoch_loop.rs\"]"]},{"path":"src/trainer/core/legacy_mod.rs","line":23,"declaration":"pub mod runtime;","context":["#[path = \"epoch_loop.rs\"]","#[path = \"legacy_runtime.rs\"]"]},{"path":"src/trainer/core/legacy_mod.rs","line":25,"declaration":"pub mod observer;","context":["#[path = \"legacy_runtime.rs\"]","#[path = \"legacy_observer.rs\"]"]},{"path":"src/trainer/core/legacy_mod.rs","line":27,"declaration":"mod debug_trace;","context":["#[path = \"legacy_observer.rs\"]","#[path = \"debug_trace.rs\"]"]},{"path":"src/trainer/core/legacy_mod.rs","line":30,"declaration":"mod graph_capture;","context":["#[cfg(feature = \"enableVisualization\")]","#[path = \"graph_capture.rs\"]"]},{"path":"src/trainer/core/legacy_mod.rs","line":39,"declaration":"pub use config::{LogConfig, TrainerConfig, Metrics, TrainerBuilder};","context":[]},{"path":"src/trainer/core/legacy_mod.rs","line":40,"declaration":"pub use metric_hook::{MetricHook, BatchContext};","context":[]},{"path":"src/trainer/core/legacy_mod.rs","line":41,"declaration":"pub use metrics::{","context":[]},{"path":"src/trainer/core/legacy_mod.rs","line":45,"declaration":"pub use convergence::Convergence;","context":[]},{"path":"src/trainer/core/legacy_mod.rs","line":46,"declaration":"pub use epoch_loop::{EpochStep, StepOutput, StepDiagnostics, BatchObservations, EpochOutcome};","context":[]},{"path":"src/trainer/core/legacy_mod.rs","line":47,"declaration":"pub use runtime::TrainingRuntime;","context":[]},{"path":"src/trainer/core/legacy_mod.rs","line":48,"declaration":"pub use observer::{","context":[]},{"path":"src/trainer/core/legacy_mod.rs","line":53,"declaration":"pub use observer::{CaptureSelector, GraphVisualizationObserver, GraphVisualizationObserverBuilder};","context":["#[cfg(feature = \"enableVisualization\")]"]},{"path":"src/trainer/core/legacy_observer.rs","line":61,"declaration":"mod graph_observer {","context":["#[cfg(feature = \"enableVisualization\")]"]},{"path":"src/trainer/core/legacy_observer.rs","line":231,"declaration":"mod tests {","context":["    #[cfg(test)]"]},{"path":"src/trainer/core/legacy_observer.rs","line":235,"declaration":"pub use CaptureSelector as PublicCaptureSelector;","context":[]},{"path":"src/trainer/core/legacy_observer.rs","line":236,"declaration":"pub use GraphVisualizationObserver as PublicGraphVisualizationObserver;","context":[]},{"path":"src/trainer/core/legacy_observer.rs","line":237,"declaration":"pub use GraphVisualizationObserverBuilder as PublicGraphVisualizationObserverBuilder;","context":[]},{"path":"src/trainer/core/legacy_observer.rs","line":241,"declaration":"pub use graph_observer::{","context":["#[cfg(feature = \"enableVisualization\")]"]},{"path":"src/trainer/core/mod.rs","line":12,"declaration":"pub mod config;","context":[]},{"path":"src/trainer/core/mod.rs","line":13,"declaration":"pub mod convergence;","context":[]},{"path":"src/trainer/core/mod.rs","line":14,"declaration":"pub mod metric_hook;","context":[]},{"path":"src/trainer/core/mod.rs","line":15,"declaration":"pub mod metrics;","context":[]},{"path":"src/trainer/core/mod.rs","line":17,"declaration":"pub mod observer;","context":[]},{"path":"src/trainer/core/mod.rs","line":18,"declaration":"pub mod runtime;","context":[]},{"path":"src/trainer/core/mod.rs","line":27,"declaration":"pub use config::{LogConfig, Metrics, TrainerBuilder, TrainerConfig};","context":[]},{"path":"src/trainer/core/mod.rs","line":28,"declaration":"pub use convergence::Convergence;","context":[]},{"path":"src/trainer/core/mod.rs","line":29,"declaration":"pub use metric_hook::{BatchContext, MetricHook};","context":[]},{"path":"src/trainer/core/mod.rs","line":30,"declaration":"pub use metrics::{","context":[]},{"path":"src/trainer/core/mod.rs","line":35,"declaration":"pub use observer::{","context":[]},{"path":"src/trainer/core/mod.rs","line":39,"declaration":"pub use runtime::TrainingRuntime;","context":[]},{"path":"src/trainer/core/mod.rs","line":41,"declaration":"pub use observer::{","context":[]},{"path":"src/trainer/core/observer.rs","line":58,"declaration":"mod graph_observer {","context":[]},{"path":"src/trainer/core/observer.rs","line":228,"declaration":"mod tests {","context":["    #[cfg(test)]"]},{"path":"src/trainer/core/observer.rs","line":232,"declaration":"pub use CaptureSelector as PublicCaptureSelector;","context":[]},{"path":"src/trainer/core/observer.rs","line":233,"declaration":"pub use GraphVisualizationObserver as PublicGraphVisualizationObserver;","context":[]},{"path":"src/trainer/core/observer.rs","line":234,"declaration":"pub use GraphVisualizationObserverBuilder as PublicGraphVisualizationObserverBuilder;","context":[]},{"path":"src/trainer/core/observer.rs","line":237,"declaration":"pub use graph_observer::{","context":[]},{"path":"src/trainer/data/legacy_mod.rs","line":8,"declaration":"mod legacy;","context":["#[path = \"legacy.rs\"]"]},{"path":"src/trainer/data/legacy_mod.rs","line":10,"declaration":"mod loader;","context":["#[path = \"legacy.rs\"]","#[path = \"legacy_loader.rs\"]"]},{"path":"src/trainer/data/legacy_mod.rs","line":12,"declaration":"mod source;","context":["#[path = \"legacy_loader.rs\"]","#[path = \"legacy_source.rs\"]"]},{"path":"src/trainer/data/legacy_mod.rs","line":14,"declaration":"pub use loader::{","context":["#[path = \"legacy_source.rs\"]"]},{"path":"src/trainer/data/legacy_mod.rs","line":21,"declaration":"pub use source::{","context":[]},{"path":"src/trainer/data/legacy_mod.rs","line":61,"declaration":"mod tests;","context":["#[cfg(test)]","#[path = \"../../tests/trainer/data/legacy_mod_tests.rs\"]"]},{"path":"src/trainer/data/mod.rs","line":7,"declaration":"mod loader;","context":[]},{"path":"src/trainer/data/mod.rs","line":8,"declaration":"mod source;","context":[]},{"path":"src/trainer/data/mod.rs","line":10,"declaration":"pub use loader::{","context":[]},{"path":"src/trainer/data/mod.rs","line":17,"declaration":"pub use source::{","context":[]},{"path":"src/trainer/data/mod.rs","line":22,"declaration":"pub use crate::DataError;","context":[]},{"path":"src/trainer/data/mod.rs","line":23,"declaration":"mod prebatched;","context":[]},{"path":"src/trainer/data/mod.rs","line":24,"declaration":"pub use prebatched::*;","context":[]},{"path":"src/trainer/legacy_api.rs","line":143,"declaration":"mod tests;","context":["#[cfg(test)]","#[path = \"../tests/trainer/legacy_api_tests.rs\"]"]},{"path":"src/trainer/legacy_checkpoint.rs","line":291,"declaration":"mod tests;","context":["#[cfg(test)]","#[path = \"../tests/trainer/legacy_checkpoint_tests.rs\"]"]},{"path":"src/trainer/legacy_mod.rs","line":25,"declaration":"pub mod core;","context":["#[path = \"core/legacy_mod.rs\"]"]},{"path":"src/trainer/legacy_mod.rs","line":27,"declaration":"pub mod api;","context":["#[path = \"core/legacy_mod.rs\"]","#[path = \"legacy_api.rs\"]"]},{"path":"src/trainer/legacy_mod.rs","line":29,"declaration":"pub mod checkpoint;","context":["#[path = \"legacy_api.rs\"]","#[path = \"legacy_checkpoint.rs\"]"]},{"path":"src/trainer/legacy_mod.rs","line":31,"declaration":"pub mod supervised;","context":["#[path = \"legacy_checkpoint.rs\"]","#[path = \"supervised.rs\"]"]},{"path":"src/trainer/legacy_mod.rs","line":33,"declaration":"pub mod unsupervised;","context":["#[path = \"supervised.rs\"]","#[path = \"unsupervised.rs\"]"]},{"path":"src/trainer/legacy_mod.rs","line":35,"declaration":"pub mod semi_supervised;","context":["#[path = \"unsupervised.rs\"]","#[path = \"semi_supervised.rs\"]"]},{"path":"src/trainer/legacy_mod.rs","line":37,"declaration":"pub mod reinforcement;","context":["#[path = \"semi_supervised.rs\"]","#[path = \"legacy_reinforcement.rs\"]"]},{"path":"src/trainer/legacy_mod.rs","line":39,"declaration":"pub mod autoregressive;","context":["#[path = \"legacy_reinforcement.rs\"]","#[path = \"autoregressive.rs\"]"]},{"path":"src/trainer/legacy_mod.rs","line":41,"declaration":"pub mod data;","context":["#[path = \"autoregressive.rs\"]","#[path = \"data/legacy_mod.rs\"]"]},{"path":"src/trainer/legacy_mod.rs","line":43,"declaration":"pub(crate) mod progress;","context":["#[path = \"data/legacy_mod.rs\"]","#[path = \"legacy_progress.rs\"]"]},{"path":"src/trainer/legacy_mod.rs","line":45,"declaration":"pub use core::{","context":["#[path = \"legacy_progress.rs\"]"]},{"path":"src/trainer/legacy_mod.rs","line":55,"declaration":"pub use core::{CaptureSelector, GraphVisualizationObserver, GraphVisualizationObserverBuilder};","context":["#[cfg(feature = \"enableVisualization\")]"]},{"path":"src/trainer/legacy_mod.rs","line":56,"declaration":"pub use api::{TrainableModel, CheckpointableModel, StopReason, StepUnit, MetricValues,","context":["#[cfg(feature = \"enableVisualization\")]"]},{"path":"src/trainer/legacy_mod.rs","line":59,"declaration":"pub use api::{SupervisedOptions, SemiSupervisedOptions, AutoregressiveOptions, ReinforcementOptions};","context":[]},{"path":"src/trainer/legacy_mod.rs","line":60,"declaration":"pub use checkpoint::{TrainingCheckpoint, ParadigmTag, CheckpointManager, CHECKPOINT_SCHEMA_VERSION};","context":[]},{"path":"src/trainer/legacy_mod.rs","line":61,"declaration":"pub use data::{","context":[]},{"path":"src/trainer/legacy_mod.rs","line":72,"declaration":"pub use supervised::SupervisedModel;","context":["#[cfg(feature = \"enableBackward\")]"]},{"path":"src/trainer/legacy_mod.rs","line":73,"declaration":"pub use supervised::SupervisedTrainer;","context":["#[cfg(feature = \"enableBackward\")]"]},{"path":"src/trainer/legacy_mod.rs","line":77,"declaration":"pub use unsupervised::UnsupervisedModel;","context":["#[cfg(feature = \"enableBackward\")]"]},{"path":"src/trainer/legacy_mod.rs","line":78,"declaration":"pub use unsupervised::UnsupervisedTrainer;","context":["#[cfg(feature = \"enableBackward\")]"]},{"path":"src/trainer/legacy_mod.rs","line":82,"declaration":"pub use semi_supervised::SemiSupervisedModel;","context":["#[cfg(feature = \"enableBackward\")]"]},{"path":"src/trainer/legacy_mod.rs","line":83,"declaration":"pub use semi_supervised::{SemiSupervisedTrainer, ConsistencyRamp};","context":["#[cfg(feature = \"enableBackward\")]"]},{"path":"src/trainer/legacy_mod.rs","line":87,"declaration":"pub use reinforcement::RLModel;","context":["#[cfg(feature = \"enableBackward\")]"]},{"path":"src/trainer/legacy_mod.rs","line":88,"declaration":"pub use reinforcement::{Environment, StepResult, RLTrainer};","context":["#[cfg(feature = \"enableBackward\")]"]},{"path":"src/trainer/legacy_mod.rs","line":92,"declaration":"pub use autoregressive::AutoregressiveModel;","context":["#[cfg(feature = \"enableBackward\")]"]},{"path":"src/trainer/legacy_mod.rs","line":93,"declaration":"pub use autoregressive::AutoregressiveTrainer;","context":["#[cfg(feature = \"enableBackward\")]"]},{"path":"src/trainer/legacy_mod.rs","line":208,"declaration":"mod preset_tests;","context":["#[cfg(test)]","#[path = \"../tests/trainer/legacy_mod_preset_tests.rs\"]"]},{"path":"src/trainer/legacy_reinforcement.rs","line":497,"declaration":"mod tests;","context":["#[cfg(test)]","#[path = \"../tests/trainer/legacy_reinforcement_tests.rs\"]"]},{"path":"src/trainer/mod.rs","line":6,"declaration":"pub mod api;","context":[]},{"path":"src/trainer/mod.rs","line":7,"declaration":"pub mod checkpoint;","context":[]},{"path":"src/trainer/mod.rs","line":8,"declaration":"pub mod core;","context":[]},{"path":"src/trainer/mod.rs","line":9,"declaration":"pub mod data;","context":[]},{"path":"src/trainer/mod.rs","line":10,"declaration":"pub(crate) mod progress;","context":[]},{"path":"src/trainer/mod.rs","line":11,"declaration":"mod reinforcement;","context":[]},{"path":"src/trainer/mod.rs","line":12,"declaration":"mod runners;","context":[]},{"path":"src/trainer/mod.rs","line":13,"declaration":"mod service;","context":[]},{"path":"src/trainer/mod.rs","line":14,"declaration":"pub use api::*;","context":[]},{"path":"src/trainer/mod.rs","line":15,"declaration":"pub use core::*;","context":[]},{"path":"src/trainer/mod.rs","line":16,"declaration":"pub use data::*;","context":[]},{"path":"src/trainer/mod.rs","line":17,"declaration":"pub use reinforcement::*;","context":[]},{"path":"src/trainer/mod.rs","line":18,"declaration":"pub use runners::*;","context":[]},{"path":"src/trainer/semi_supervised.rs","line":622,"declaration":"mod tests;","context":["#[cfg(test)]","#[path = \"../tests/trainer/semi_supervised_tests.rs\"]"]},{"path":"src/trainer/supervised.rs","line":550,"declaration":"mod tests;","context":["#[cfg(test)]","#[path = \"../tests/trainer/supervised_tests.rs\"]"]},{"path":"src/visualization/dot.rs","line":140,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/visualization/graphviz.rs","line":66,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/visualization/legacy_dot.rs","line":140,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/visualization/legacy_mod.rs","line":4,"declaration":"mod capture;","context":["#[path = \"capture.rs\"]"]},{"path":"src/visualization/legacy_mod.rs","line":6,"declaration":"mod dot;","context":["#[path = \"capture.rs\"]","#[path = \"legacy_dot.rs\"]"]},{"path":"src/visualization/legacy_mod.rs","line":8,"declaration":"mod error;","context":["#[path = \"legacy_dot.rs\"]","#[path = \"error.rs\"]"]},{"path":"src/visualization/legacy_mod.rs","line":10,"declaration":"mod graphviz;","context":["#[path = \"error.rs\"]","#[path = \"graphviz.rs\"]"]},{"path":"src/visualization/legacy_mod.rs","line":12,"declaration":"pub(crate) mod recording;","context":["#[path = \"graphviz.rs\"]","#[path = \"recording.rs\"]"]},{"path":"src/visualization/legacy_mod.rs","line":14,"declaration":"mod snapshot;","context":["#[path = \"recording.rs\"]","#[path = \"legacy_snapshot.rs\"]"]},{"path":"src/visualization/legacy_mod.rs","line":16,"declaration":"mod statistics;","context":["#[path = \"legacy_snapshot.rs\"]","#[path = \"statistics.rs\"]"]},{"path":"src/visualization/legacy_mod.rs","line":18,"declaration":"mod writer;","context":["#[path = \"statistics.rs\"]","#[path = \"writer.rs\"]"]},{"path":"src/visualization/legacy_mod.rs","line":20,"declaration":"pub use capture::{VisualizationCapture, VisualizationCaptureBuilder};","context":["#[path = \"writer.rs\"]"]},{"path":"src/visualization/legacy_mod.rs","line":21,"declaration":"pub use dot::{DotEncoder, DotProfile};","context":[]},{"path":"src/visualization/legacy_mod.rs","line":22,"declaration":"pub use error::VisualizationError;","context":[]},{"path":"src/visualization/legacy_mod.rs","line":23,"declaration":"pub use graphviz::{GraphvizFailure, GraphvizFailureKind};","context":[]},{"path":"src/visualization/legacy_mod.rs","line":24,"declaration":"pub use snapshot::{","context":[]},{"path":"src/visualization/legacy_mod.rs","line":28,"declaration":"pub use writer::{","context":[]},{"path":"src/visualization/legacy_mod.rs","line":33,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"src/visualization/mod.rs","line":2,"declaration":"mod dot;","context":[]},{"path":"src/visualization/mod.rs","line":3,"declaration":"mod error;","context":[]},{"path":"src/visualization/mod.rs","line":4,"declaration":"mod graphviz;","context":[]},{"path":"src/visualization/mod.rs","line":5,"declaration":"mod snapshot;","context":[]},{"path":"src/visualization/mod.rs","line":6,"declaration":"pub(crate) mod statistics;","context":[]},{"path":"src/visualization/mod.rs","line":7,"declaration":"mod writer;","context":[]},{"path":"src/visualization/mod.rs","line":8,"declaration":"pub use dot::{DotEncoder, DotProfile};","context":[]},{"path":"src/visualization/mod.rs","line":9,"declaration":"pub use error::VisualizationError;","context":[]},{"path":"src/visualization/mod.rs","line":10,"declaration":"pub use graphviz::{GraphvizFailure, GraphvizFailureKind};","context":[]},{"path":"src/visualization/mod.rs","line":11,"declaration":"pub use snapshot::*;","context":[]},{"path":"src/visualization/mod.rs","line":12,"declaration":"pub use writer::{","context":[]},{"path":"src/visualization/statistics.rs","line":80,"declaration":"mod tests {","context":["#[cfg(test)]"]},{"path":"tests/cleanup.rs","line":1,"declaration":"mod support;","context":[]},{"path":"tests/counting_allocator.rs","line":3,"declaration":"mod allocation;","context":["#[path = \"../benches/support/counting_allocator.rs\"]"]},{"path":"tests/legacy_reference_diffusion.rs","line":19,"declaration":"mod replay;","context":["#[path = \"../examples/replay_reference_diffusion.rs\"]"]},{"path":"tests/models.rs","line":4,"declaration":"mod autoregressive;","context":["#[path = \"models/autoregressive.rs\"]"]},{"path":"tests/models.rs","line":6,"declaration":"mod reinforcement;","context":["#[path = \"models/autoregressive.rs\"]","#[path = \"models/reinforcement.rs\"]"]},{"path":"tests/models.rs","line":8,"declaration":"mod semi_supervised;","context":["#[path = \"models/reinforcement.rs\"]","#[path = \"models/semi_supervised.rs\"]"]},{"path":"tests/models.rs","line":10,"declaration":"mod supervised;","context":["#[path = \"models/semi_supervised.rs\"]","#[path = \"models/supervised.rs\"]"]},{"path":"tests/parameter_mapping.rs","line":2,"declaration":"mod mapping;","context":["#[path = \"../examples/support/reference_parameters.rs\"]"]},{"path":"tests/providers.rs","line":1,"declaration":"mod support;","context":[]}],"cargo":"[package]\r\nname = \"trench-deep\"\r\nversion = \"0.0.2\"\r\nedition = \"2024\"\r\ndescription = \"This framework is a project that I am independently designing/developing to study about deep learning. Basic levels of computation and automatic differential engines are implemented.\"\r\nlicense = \"Apache 2.0 License\"\r\nauthors = [\"2QRNpDwD@gmail.com\"]\r\n\r\n[features]\r\ndefault = [\"builtinStorage\", \"builtinKernels\"]\r\nbuiltinStorage = []\r\nbuiltinKernels = []\r\nlegacyBenchmark = [\"enableBackward\"]\nbenchmarkAlloc = [] # benchmark-only allocation instrumentation\nenableBackward = [] # 역전파 관련 기능들을 활성화 할경우 사용\r\nenableVisualization = [\"enableBackward\"] # 시각화 기능을 활성화 할경우 사용 (enableBackpropagation 필요)\r\ndebugging = [] # 디버깅이 필요할 경우 사용\r\n\r\n[dependencies]\r\nrand = \"0.9.1\"                      #테스트시 초깃값 생성용 랜덤 크레이트\r\nlog = \"0.4.27\"                      #테스트시 로깅용 크레이트\r\ntracing-subscriber = { version = \"0.3\", features = [\"fmt\", \"registry\", \"env-filter\", \"time\"] } # 위와 동일\r\ntracing-appender = \"0.2.3\"          # 위와 동일\r\nmnist = \"0.6.0\"                     # 테스트시 입력할 데이터셋 크레이트\r\nserde = { version = \"1.0.219\", features = [\"derive\"] } #테스트시 모델 저장용 크레이트\r\nserde_json = \"1.0.140\"                                 #테스트시 모델 저장용 크레이트\r\nindicatif = \"0.17.11\"             #테스트시 로깅용 크레이트\r\ntracing = \"0.1.41\"                 #테스트시 로깅용 크레이트\r\ntime = { version = \"0.3.41\", features = [\"macros\"] }\r\nthiserror = \"2.0.12\"               #오류 전파용 크레이트\r\nctrlc = \"3.4\"                      #학습 중단 시그널 처리용 크레이트\r\ncsv = \"1.3\"                        # eager CSV dataset source\r\n\r\n[profile.dev]\r\ndebug = true\r\n\r\n[profile.release]\nopt-level = 3\n\n[[bench]]\nname = \"p1_compare\"\nharness = false\nrequired-features = [\"legacyBenchmark\", \"builtinStorage\", \"builtinKernels\"]\n\n[[example]]\nname = \"replay_reference_diffusion\"\nrequired-features = [\"builtinStorage\", \"builtinKernels\", \"enableBackward\"]\n\n[[bench]]\nname = \"context_diffusion\"\nharness = false\nrequired-features = [\"legacyBenchmark\", \"builtinStorage\", \"builtinKernels\"]\n\r\n#This framework is a project that I am independently designing/developing to study about deep learning.\r\n#If you have any better ideas for the project, please contact us here at 2QRNpDwD@gmail.com or https://github.com/3QNRpDwD.\r\n\r\n","warnings":["src/runtime/receiver.rs#pub fn backward"]};
